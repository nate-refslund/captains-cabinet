# Layer-separation baseline (FINAL-C — three-layer dependency gate)
#
# Each line is `<path>:<rule_id>` — one known cross-layer violation that
# existed when the gate was first installed. CI fails if a new violation
# appears that is NOT on this list. The baseline can only shrink (ratchet
# down) via `bash cabinet/scripts/check-layer-separation.sh --update-baseline`
# after a cleanup PR. Growing it requires Captain approval.
#
# Rule ids:
#   FRAMEWORK_IMPORTS_INSTANCE — `import instance` / `from instance ...`
#   FRAMEWORK_IMPORTS_PRESETS  — `import presets`  / `from presets ...`
#   FRAMEWORK_PATH_INSTANCE    — runtime path coupling Path(...) / "instance"
#   FRAMEWORK_PATH_PRESETS     — runtime path coupling Path(...) / "presets"
#   PRESETS_IMPORTS_INSTANCE   — presets code imports instance/ (overlay
#                                path-string refs are allowed; code deps are not)
#
# Snapshot taken: 2026-05-26 (convergence-v2 / FINAL-C)
framework/events/emitter.py:FRAMEWORK_PATH_INSTANCE
framework/events/tests/test_emitter.py:FRAMEWORK_PATH_INSTANCE
framework/learning/self_improvement_loop.py:FRAMEWORK_PATH_INSTANCE
framework/learning/tests/test_self_improvement_loop.py:FRAMEWORK_PATH_INSTANCE
framework/measurement/role_evals/cos_compile_mission.py:FRAMEWORK_PATH_INSTANCE
framework/measurement/role_evals/cos_route_idempotent.py:FRAMEWORK_PATH_INSTANCE
framework/measurement/role_evals/cpo_decompose_criteria.py:FRAMEWORK_PATH_INSTANCE
framework/measurement/tests/test_role_evals.py:FRAMEWORK_PATH_INSTANCE
framework/measurement/tests/test_scenario_runner.py:FRAMEWORK_PATH_INSTANCE
framework/missions/session_bridge.py:FRAMEWORK_PATH_INSTANCE
framework/missions/tests/test_session_bridge.py:FRAMEWORK_PATH_INSTANCE
framework/missions/tests/test_supervisor.py:FRAMEWORK_PATH_INSTANCE
framework/roles/evolution.py:FRAMEWORK_PATH_INSTANCE
framework/roles/lifecycle.py:FRAMEWORK_PATH_INSTANCE
framework/roles/tests/test_lifecycle.py:FRAMEWORK_PATH_INSTANCE
# 2026-07-02 — Captain-ratified growth (F0.1 canonicalization of the June
# autonomous-operation work; ratification = the 11-question sit-down, Q1/Q3).
# RATCHET-DOWN DEBT: these 5 are instance-path couplings in framework/ that
# the PM-decoupling + Estate-Mapper work is expected to remove; revisit at
# plan B2/B4 and shrink via --update-baseline after the cleanup PR.
# (was 6 — test_trust_ladder.py entry ratcheted out 2026-07-04 when the
# trust-ladder module was deleted under the earn-demotion ruling.)
framework/learning/capability_gaps.py:FRAMEWORK_PATH_INSTANCE
framework/learning/tests/test_capability_gaps.py:FRAMEWORK_PATH_INSTANCE
framework/onboarding/onboard.py:FRAMEWORK_PATH_INSTANCE
framework/onboarding/tests/test_onboard.py:FRAMEWORK_PATH_INSTANCE
framework/triggers/registry.py:FRAMEWORK_PATH_INSTANCE
# 2026-07-04 — Captain-ratified growth (+3, lane/config-0705). The act-first
# lane reads Captain-owned runtime config that is instance-resident BY DESIGN
# (deployment-local, flippable/editable without a framework change;
# trust-inversion flip protocol, captain-decisions 2026-07-04). Per-file, the
# actual hits (adversarial review 2026-07-04 corrected the original "all three
# are the kill-flag read" wording — only one file reads the flag):
#   run_action_lane.py — instance/config/directions.yml AND the act-first
#                        kill-flag instance/config/act-first-enabled
#   action_exec.py     — instance/config/act-first-surfaces.yml (the Captain's
#                        act-first surface allow/deny config — itself a
#                        germline-lock.sh target)
#   actfirst_canary.py — instance/config/act-first-surfaces.yml
# All are config-file reads, not code deps. Not ratchet-down debt: the coupling
# is intentional as long as the act-first lane's Captain config (flag, surfaces,
# directions) lives in instance/. Removing the FLAG alone does NOT clear these
# entries — the surfaces/directions reads remain.
framework/acting/run_action_lane.py:FRAMEWORK_PATH_INSTANCE
framework/frontdoor/action_exec.py:FRAMEWORK_PATH_INSTANCE
framework/frontdoor/actfirst_canary.py:FRAMEWORK_PATH_INSTANCE

# 2026-07-05 — sovereign-posture merge growth (+14, feat/sovereign-posture →
# main reconciliation; RECONCILE 2026-07-05). PENDING Captain ratification at
# the merge window alongside the sovereign germline amendment
# (docs/proposals/germline-amendment-sovereign-posture-2026-07-05.md). All 14
# are the SAME class as the ratified 07-04 act-first entries: framework code
# reading Captain-owned, deployment-local runtime CONFIG that is
# instance-resident BY DESIGN — never code deps:
#   posture.py        — instance/config/posture.yml (Captain-locked posture
#                       ruling; absent/corrupt ⇒ guardian)
#   grants.py         — instance/config/grants/ (Captain-signed, schg-locked
#                       standing grants)
#   needs.py          — the needs wire flag + ledger location under the
#                       deployment root
#   standing_pull.py  — instance/config/outcomes.yml standing-mission pull
#   (+ their test files, which pin those exact paths)
framework/acting/tests/test_sov4_posture_routing.py:FRAMEWORK_PATH_INSTANCE
framework/authority/grants.py:FRAMEWORK_PATH_INSTANCE
framework/authority/needs.py:FRAMEWORK_PATH_INSTANCE
framework/authority/posture.py:FRAMEWORK_PATH_INSTANCE
framework/authority/tests/test_golden_evals_sovereign.py:FRAMEWORK_PATH_INSTANCE
framework/authority/tests/test_grants.py:FRAMEWORK_PATH_INSTANCE
framework/authority/tests/test_needs.py:FRAMEWORK_PATH_INSTANCE
framework/authority/tests/test_posture.py:FRAMEWORK_PATH_INSTANCE
framework/frontdoor/tests/test_sov6_binder_grant.py:FRAMEWORK_PATH_INSTANCE
framework/learning/tests/test_gate_apply_dark.py:FRAMEWORK_PATH_INSTANCE
framework/learning/tests/test_sov8_loop_and_gates.py:FRAMEWORK_PATH_INSTANCE
framework/missions/standing_pull.py:FRAMEWORK_PATH_INSTANCE
framework/missions/tests/test_standing_pull.py:FRAMEWORK_PATH_INSTANCE
framework/missions/tests/test_supervisor_standing.py:FRAMEWORK_PATH_INSTANCE

# 2026-07-06 — SRC-5 source-adapter SEED (+19 lines / 14 files). NOT new
# violations: this is the initial snapshot of the PRE-EXISTING screenpipe
# couplers for the two new rule classes (FRAMEWORK_IMPORTS_SCREENPIPE +
# FRAMEWORK_PATH_SCREENPIPE), frozen so the surface can only SHRINK as the
# source-adapter migration (docs/plans/source-adapter-boundary-2026-07-05.md §5)
# reroutes each caller through framework.sources.get_source() / re-homes it to
# instance/flavor-a. Every entry maps 1:1 to an entry in the strong sibling
# ratchet framework/tests/test_no_screenpipe_in_core.py::_ALLOWLISTED_FILES
# (14 files); each SRC-2..6 migration deletes the corresponding line here (via
# --update-baseline after the cleanup PR) AND the allowlist entry there. Target
# is EMPTY: framework/** names screenpipe nowhere. tests/ + the sources/ seam
# are excluded by the rule (the seam NAMES the Protocol; the adapter names
# screenpipe). GROWING this set is forbidden — a NEW screenpipe coupling is
# fixed by routing through get_source(), not by widening the baseline.
