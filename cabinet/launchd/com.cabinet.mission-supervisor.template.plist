<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
Mission-supervisor LaunchAgent template.
Event-sourced router: pushes newly-ready mission work-graph tasks to assigned
officers via Redis Streams. Idempotent — safe to re-run.
Fires every 5 minutes per Phase 1.2 of the convergence plan.
-->
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.cabinet.mission-supervisor</string>

  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>${REPO_ROOT}/cabinet/cron/mission-supervisor.sh</string>
  </array>

  <key>WorkingDirectory</key>
  <string>${REPO_ROOT}</string>

  <key>EnvironmentVariables</key>
  <dict>
    <key>HOME</key><string>${HOME}</string>
    <key>USER</key><string>${USER}</string>
    <key>CABINET_EVENT_LOG_DIR</key><string>${HOME}/Library/Application Support/cabinet/events</string>
    <key>REDIS_HOST</key><string>127.0.0.1</string>
    <key>REDIS_PORT</key><string>6379</string>
    <key>CABINET_ROOT</key><string>${REPO_ROOT}</string>
    <key>CABINET_SOURCE_REPO</key><string>${REPO_ROOT}</string>
    <key>OFFICER_NAME</key><string>mission_supervisor</string>
    <key>PATH</key><string>/opt/homebrew/bin:/opt/homebrew/sbin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
    <key>LANG</key><string>en_US.UTF-8</string>
    <key>LC_ALL</key><string>en_US.UTF-8</string>
  </dict>

  <key>StartInterval</key>
  <integer>300</integer>

  <key>StandardOutPath</key>
  <string>${HOME}/Library/Logs/cabinet/mission-supervisor.out.log</string>

  <key>StandardErrorPath</key>
  <string>${HOME}/Library/Logs/cabinet/mission-supervisor.err.log</string>

  <key>RunAtLoad</key>
  <false/>
</dict>
</plist>
