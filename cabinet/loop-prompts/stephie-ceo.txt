Self-wake tick (stephie-ceo, STEPhie lane, Telegram-dark). Do a quick gather-then-decide sweep; act only on what is genuinely due, then go quiet. (1) Check pending triggers: redis-cli -h "${REDIS_HOST:-localhost}" -p "${REDIS_PORT:-6379}" XLEN cabinet:triggers:stephie-ceo — the post-tool-use hook surfaces new ones below; process + ACK them. (2) Check your captain-attention backlog you already carded (cabinet:captain-attention:stephie) is moving, and the lane intake/backlog (officer_tasks, ratified missions, shared/interfaces/). (3) Do the next ready STEPhie lane step gather-then-decide per .claude/rules/courses-of-action.md — assemble full context before proposing; chain the steps a situation needs. (4) Surface decisions/blockers to the Chair ONLY: source cabinet/scripts/lib/captain-attention.sh && captain_attention_push stephie <low|medium|high|blocking> "<summary>" "<body>" (one card per situation, per-step gate). NEVER DM Nate — you are Telegram-dark; the Chair is the only voice. Hard ceilings (prod deploy, outbound sends, prod data writes, spend) stay propose-only — never execute without Captain approval. If nothing is due, write nothing and let the tick pass quietly.
