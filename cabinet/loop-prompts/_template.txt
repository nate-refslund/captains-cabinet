Self-wake tick ({{officer}}). Do a quick gather-then-decide sweep; act only on what is genuinely due, then go quiet. (1) Check pending triggers: redis-cli -h "${REDIS_HOST:-localhost}" -p "${REDIS_PORT:-6379}" XLEN cabinet:triggers:{{officer}} — the post-tool-use hook surfaces new ones below; process + ACK them. (2) PULL ratified outcomes — the wake mechanism delivers triggers, NOT missions, so an EMPTY trigger stream is NOT "nothing due": your ACTIVE ratified outcomes (instance/config/outcomes.yml — every node with owner_role: {{officer}}) are standing work you PULL every tick. List the ready set read-only: cd "$CABINET_ROOT" && python3 -m framework.missions.supervisor --json --dry-run; pick YOUR next ready node (depends_on satisfied) and progress it gather-then-decide per .claude/rules/courses-of-action.md — assemble full context before proposing; chain the steps a situation needs. (3) Record every node you finish so the work graph ADVANCES instead of re-injecting: bash cabinet/scripts/work-graph-complete.sh <node_id> --status done --evidence <file-or-text>; if completions just unblocked downstream nodes, run bash cabinet/cron/mission-supervisor.sh so the newly-ready tasks route to their owning officers. (4) Surface decisions/blockers needing the Captain to the Chair ONLY: source cabinet/scripts/lib/captain-attention.sh && captain_attention_push {{officer}} <low|medium|high|blocking> "<summary>" "<body>" (one card per situation, per-step gate). Unless YOU are the Chair (the deployment's single Telegram voice), NEVER DM the Captain directly. Authority follows the ONE posture-selected matrix: with no instance/config/posture.yml this deployment is guardian — everything below the ceilings proposes; hard ceilings (prod deploys, outbound sends, spend, secrets, network writes, credential grants) never execute without a Captain-signed grant or per-item approval; external sends per ACT-AND-DRAFT are ALWAYS per-item Captain-approved and queue_draft is the ONLY transport. (5) Reflection gate (event-triggered, never a clock): source cabinet/scripts/lib/reflection.sh && if [ "$(reflection_due {{officer}})" = "1" ]; then run the individual-reflection skill on YOUR domain — what shipped/stalled this cycle, what was harder than expected, what 3+ repeat pattern should become a draft skill, and the one change that would make the next cycle 10x better. Land any durable business/product/customer fact from this cycle as a Library record — library MCP library_create_record into the 'Business Brain' Space (library_search first: extend or supersede, never duplicate; shell fallback: source cabinet/scripts/lib/library.sh && library_create_record). Write the 3-level reflection to instance/memory/tier2/{{officer}}/reflections/ and END with reflection_stamp {{officer}} (stamps last-run + feeds the cross-officer-retro counter). fi  If nothing is due and no reflection is due, write nothing and let the tick pass quietly.
