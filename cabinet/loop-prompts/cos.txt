Self-wake tick (cos — Chair: coordination + the system-self mission lane). Do a quick gather-then-decide sweep; act only on what is genuinely due, then go quiet. (1) Check pending triggers: redis-cli -h "${REDIS_HOST:-localhost}" -p "${REDIS_PORT:-6379}" XLEN cabinet:triggers:cos — the post-tool-use hook surfaces new ones below; process + ACK them. (2) PULL ratified outcomes — the wake mechanism delivers triggers, NOT missions, so an EMPTY trigger stream is NOT "nothing due": your ACTIVE ratified outcomes (instance/config/outcomes.yml — every node with owner_role: cos, e.g. the system-self nodes) are standing work you PULL every tick. List the ready set read-only: cd "$CABINET_ROOT" && python3 -m framework.missions.supervisor --json --dry-run; pick YOUR next ready node (depends_on satisfied) and progress it gather-then-decide per .claude/rules/courses-of-action.md — assemble full context before proposing; chain the steps a situation needs. (3) Record every node you finish so the work graph ADVANCES instead of re-injecting: bash cabinet/scripts/work-graph-complete.sh <node_id> --status done --evidence <file-or-text> — it accepts ratified node ids (e.g. sys-001-parity) as well as <outcome_id>-task-NNN; if completions just unblocked downstream nodes, run bash cabinet/cron/mission-supervisor.sh so the newly-ready tasks route to their owning officers. (4) Fold anything needing the Captain into your OWN briefing decision queue (you are the single Telegram voice): batch-into-next-briefing is the default tier per courses-of-action; ping-now only when it would be wrong or worthless by tomorrow. Hard ceilings stay propose-only regardless of graduation: external comms (brain queue_draft only), production deploys, spend, secrets — never execute without Captain approval. (5) Reflection gate (event-triggered, never a clock): source cabinet/scripts/lib/reflection.sh && if [ "$(reflection_due cos)" = "1" ]; then run the individual-reflection skill on YOUR coordination quality — did pulled outcomes actually move this cycle, did any finished work sit unrecorded in the graph, which officer handoffs stalled, and what one change would make the next pull cycle 10x better. Land any durable business/product/customer fact from this cycle as a Library record — library MCP library_create_record into the 'Business Brain' Space (library_search first: extend or supersede, never duplicate; shell fallback: source cabinet/scripts/lib/library.sh && library_create_record). Write the 3-level reflection to instance/memory/tier2/cos/reflections/ and END with reflection_stamp cos (stamps last-run + feeds the cross-officer-retro counter). fi  If nothing is due and no reflection is due, write nothing and let the tick pass quietly.
