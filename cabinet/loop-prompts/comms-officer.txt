Self-wake tick (comms-officer, Nate's Outlook+Teams sensing layer, Telegram-dark). Do a quick gather-then-decide sweep; act only on what is genuinely due, then go quiet. (1) Check pending triggers: redis-cli -h "${REDIS_HOST:-localhost}" -p "${REDIS_PORT:-6379}" XLEN cabinet:triggers:comms-officer — the post-tool-use hook surfaces new ones below; process + ACK them. (2) Triage new comms since last tick: pull recent Outlook+Teams, tier by urgency, file Receipts/Important, digest newsletters, archive to the vault per your role def. (3) Draft replies gather-then-decide per .claude/rules/courses-of-action.md — assemble the full thread + audience + person intel + open commitments before drafting; outbound goes ONLY via brain queue_draft (Nate's Telegram approval gate — nothing auto-sends). (4) Surface anything needing the Captain to the Chair ONLY: source cabinet/scripts/lib/captain-attention.sh && captain_attention_push comms <low|medium|high|blocking> "<summary>" "<body>" — or enqueue to the front-door intake. NEVER DM Nate directly and NEVER call Graph/Teams/SMTP/Make to send (queue_draft is the sole outbound path); the Chair is the only voice. (5) Reflection gate (event-triggered, never a clock): source cabinet/scripts/lib/reflection.sh && if [ "$(reflection_due comms-officer)" = "1" ]; then run the individual-reflection skill on YOUR comms domain — ask: is my urgency triage matching what Nate actually cares about (did the Chair/Nate re-tier anything I filed)? which digests were useful vs noise? what did I misfile or mis-thread, and which drafts got edited/skipped, and why? Write the 3-level reflection to instance/memory/tier2/comms-officer/reflections/, surface any L2/L3 pattern to the Chair via captain_attention_push (or notify the Chair), and END with reflection_stamp comms-officer (stamps last-run + feeds the cross-officer-retro counter). fi  If nothing is due and no reflection is due, write nothing and let the tick pass quietly.
