# =============================================================================
# morphology.yml v2 — Cabinet World growth morphology (GRAMMAR AS LAW)
# =============================================================================
# world_at(T) = f_v(state_at(T)) — a PURE, VERSIONED function from censused
# state to structure. No LLM anywhere between ledger and pixel (ratified
# 2026-07-07: "the moment rendering can vary independently of state, a glance
# stops being evidence"). NO change to this file auto-merges — every base,
# anchor, and mapping change is a PR the Captain merges, gated by the
# binding-executor validator (cabinet/scripts/world-binding-validator.py:
# read-only allowlisted sandbox, errors/empty output = cannot merge).
#
# BINDINGS read the E0a census keyframe series (shared/interfaces/
# world-chronicle.jsonl via jq -s 'last') or fenced local files directly —
# the census writer already did the estate reads under its own PII fence,
# so the render path holds no DB creds and no Redis (§2 Stage 1).
# NB: leading-dash grep patterns use `-e` (grep eats them as options otherwise).
#
# LOCKED, FOREVER (law: block below): semantic assignments, tier bases,
# district compass anchors (no mirror/rotate), reserved salience palette +
# dual coding, honest-zero renders, rate-routing, the archetype set.
#
# v2 delta (world-alive direction 2026-07-08, Captain ratified "go all in"
# in-session; docs/plans/world-alive-direction-2026-07-08.md §5.2): appends
# the growth entries for the Street (Z1) and Island (Z0) scenes plus two
# Wardroom growth surfaces. The law: block and every v1 entry are
# byte-untouched. Rate-routed TEXTURE-class entries (noticeboard pins,
# street liveliness) are explicitly marked in their codex — volume cannot
# mint capability, ever.
#
# v3 delta (THE LIVING ISLAND, Captain-ratified 2026-07-09 "you may actually
# just finalize the plan and start implementing"; spec of record
# docs/plans/world-unified-spec-v2-2026-07-09.md §2–§9, §15 addenda + the
# 2026-07-09 rulings ledger): appends the ERA×RUNG growth-law entries (the
# rung source of truth is cabinet/world/growth-ladders.yml — Captain-tunable
# VALUES, hot-reloaded, previewed via world-preview), the staged+recomposed
# lighthouse, the lane reef-buoys, and the mailbox pending-flag surface.
# The law: block and every v1/v2 entry are byte-untouched. Behavior/overlay
# law (weather, commute, construction, fauna, apprentices, portrait rail,
# mailbox view, killswitch lever, roof cutaway) lives in show-grammar.yml v3
# per the live_overlay law line below.
version: 3

law:
  tiering: "tier_i = clamp(floor(log2(S_i / base_i + 1)), 0, 7); bases are cadence-justified per surface, never anchored to flatter today's cabinet; every base/curve change is a PR gated by day-3/30/365 fixture renders (E2)"
  hysteresis: "a tier must hold across 2 consecutive daily keyframes before its 24h construction scaffold completes"
  rate_routing: "any source >1 event/day is FORBIDDEN from creating morphology — textures only (path wear, smoke, sediment); sources <1/week are building-class; volume cannot mint capability, ever"
  district_anchors_fixed:
    note: "fixed polar anchors, never rotated or mirrored — Knowledge is always NE in every cabinet (E2 consumes; listed now as law)"
    anchors: { law: N, manor: N-beyond-law, memory: NE, works: E, fields: SE, harbor: S, signals: SW, residential: W, training: NW }
  layout_fold: "slot k = birth-order rank, total order pinned as (created_at, rowid); positions fix at birth and never move; land radius R = 24 + 6*floor(log10(total_events+1))"
  reserved_salience_palette:
    green: "verified ONLY"
    amber: "blocked/drift ONLY"
    red: "killswitch/frozen/WIP-violation ONLY"
    grey: "unmeasured ONLY"
    purple: "captain-gated ONLY"
    rule: "dual-coded with shape; WCAG-checked in the asset conformance gate; no variant pack can smuggle an alarm hue"
  honest_zeros: "zero renders prominent (dark lighthouse, empty racks) — absence of data must never read as absence of the surface"
  civic_anchor_exception: "N=3 civic anchors max, positions fixed here, each Captain-approved (Post Office placement class, §10.9) — none minted in v1"
  live_overlay: "Redis presence verbs + SSE drive ANIMATION only, never morphology (killswitch lever + officer motion are overlay surfaces, deliberately absent from this file)"

entries:
  - id: memory_store
    represents: "Library main wing height — the org's memory row mass"
    source_binding: "jq -r -s 'last | .memory_rows_total' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 80
    codex:
      represents: "Rows in cabinet_memory summed over source types, read from the daily census keyframe (which lifts them from the falsifier memory_ingestion block — no DB creds in the render path)."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "empty shelves, sealed genome case"
  - id: evolved_skills
    represents: "Forge tool wall — one hung tool per evolved skill"
    source_binding: "jq -r -s 'last | .evolved_skills' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 1
    codex:
      represents: "Files in memory/skills/evolved/ — skills the org grew beyond its shipped genome."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "bare wall, pegs drawn"
  - id: consequence_ledger
    represents: "Ledger lectern book thickness — acted/proposed consequence rows"
    source_binding: "jq -r -s 'last | .consequence_ledger_lines' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 70
    codex:
      represents: "Lines across consequence-events-*.jsonl — the act-with-undo economy's durable record."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "blank ledger"
  - id: commits
    represents: "Works district masonry — lifetime commits in the framework repo"
    source_binding: "jq -r -s 'last | .commits_total' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 120
    codex:
      represents: "git rev-list --count HEAD at census time."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "foundation stones only"
  - id: captain_rules
    represents: "Courthouse tablet wall — one tablet per indexed captain rule"
    source_binding: "grep -c -e '- id:' shared/interfaces/captain-rules-index.yaml"
    scope: org-global
    tier: T0
    replay: git
    base: 5
    codex:
      represents: "Indexed rows in the captain-rules index — standing law the hooks enforce."
      mechanism_path: "shared/interfaces/captain-rules-index.yaml"
      day0: "0 tablets, portcullis up"
  - id: work_completed
    represents: "Fields tilled — lifetime work_item_completed events"
    source_binding: "sqlite3 -readonly cabinet/cache/org-runtime.sqlite3 \"SELECT COUNT(*) FROM org_events WHERE event_type='work_item_completed'\""
    scope: org-global
    tier: T0
    replay: ledger
    base: 850
    codex:
      represents: "Completed work items in org_events — the org's shipped-work mass; E2 tiles these into ratified-outcome sectors (growing toward the Captain)."
      mechanism_path: "cabinet/cache/org-runtime.sqlite3"
      day0: "unplowed ground"
  - id: subagents_lifetime
    represents: "Residential yard cobblestones — lifetime crew completions"
    source_binding: "sqlite3 -readonly cabinet/cache/org-runtime.sqlite3 \"SELECT COUNT(*) FROM org_events WHERE event_type='subagent_completed'\""
    scope: org-global
    tier: T0
    replay: ledger
    base: 140
    codex:
      represents: "subagent_completed events — crew motes that lived and handed their folder back."
      mechanism_path: "cabinet/cache/org-runtime.sqlite3"
      day0: "no cobbles"
  - id: tier2_reflections
    represents: "Officer journal stacks — tier-2 working notes"
    source_binding: "jq -r -s 'last | .tier2_note_files' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 4
    codex:
      represents: "Markdown notes across instance/memory/tier2/ — the officers' own working memory."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "empty nightstands"
  - id: golden_evals_delta
    represents: "Dojo dummies grown beyond the shipped genome (delta vs 25-eval seed)"
    source_binding: "jq -r -s 'last | .golden_evals_delta_vs_seed' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 2
    codex:
      represents: "Golden evals authored HERE (count minus the 25-eval genome seed) — honest zero/negative renders as the shipped set only."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "0 grown — genome dummies only"
  - id: packs_inherited
    represents: "Harbor cranes — extension packs present (delta-vs-shipped lands E2)"
    source_binding: "jq -r -s 'last | .packs_dirs' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 1
    codex:
      represents: "Pack directories under packs/ — day0 ships 5 inherited cranes; authored-here delta becomes its own entry when the authorship marker exists (E2)."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "5 inherited cranes, tier 0 delta"
  - id: cells_graduated
    represents: "THE LIGHTHOUSE BEAM — graduated trust cells (dark = honest zero)"
    source_binding: "jq -r -s 'last | .cells_graduated' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 1
    codex:
      represents: "Graduation cells that reached act-first — the keystone maturity number; the beam lighting is the biggest visual event of the world's life."
      mechanism_path: "cabinet/scripts/falsifier-report.py"
      day0: "dark lighthouse (0 today — render it dark and prominent)"
  - id: hats_earned
    represents: "Hat racks in officer houses — earned role hats"
    source_binding: "ls instance/roles/hats"
    scope: dark
    tier: T0
    replay: none
    codex:
      represents: "role_hats organ exists but no hat has ever been earned (0 rows) — empty racks ARE the story; the live binding lands when the first hat does."
      mechanism_path: "instance/roles/hats"
      day0: "empty racks"
  - id: org_posture
    represents: "The Keep flag — autonomy posture (render-only forever; the world may display posture but never offer a widen control)"
    source_binding: "jq -r -s 'last | .org_posture' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    codex:
      represents: "Fail-closed posture census enum (guardian unless an attested ruling says otherwise) — forge-vector doctrine: display, never a control."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "guardian flag"

  # --------------------------------------------------------------------------
  # v2 entries — Street (Z1) + Island (Z0) + Wardroom growth surfaces
  # (world-alive direction 2026-07-08 §5.2; validator green required)
  # --------------------------------------------------------------------------
  - id: street_hq_floors
    represents: "HQ street building height — one modular floor per commits tier"
    source_binding: "jq -r -s 'last | .commits_total' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 120
    codex:
      represents: "Lifetime commits in the framework repo (census keyframe commits_total) — the Z1 street scene stacks one modular office floor per tier on the HQ facade; the same masonry mass the Works district renders on the island."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "ground floor only"
  - id: island_land_radius
    represents: "Island landmass — R = 24 + 6*floor(log10(events+1))"
    source_binding: "jq -r -s 'last | .org_events_total' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    codex:
      represents: "Total org_events rows at census time; the fold formula R = 24 + 6*floor(log10(total_events+1)) fixes the Z0 shoreline — land radius in tiles, water ring beyond. Positions fix at birth and never move (layout_fold law)."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "24-tile islet"
  - id: island_officer_houses
    represents: "One cottage per defined role, birth-order slots, west path"
    source_binding: "jq -r -s 'last | .ev_role_defined' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 1
    codex:
      represents: "role_defined events in org_events — each mints one Residential-W cottage at its birth-order slot; roof palette seeded per slug. Rendered UNIFORM size: per-officer census scope is still dark, so uniform is the honest render."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "no houses — an egg has no streets"
  - id: island_fields
    represents: "One tilled plot per ratified outcome; crop stage = work_completed tier (aggregate until E2 sector emitters)"
    source_binding: "jq -r -s 'last | .outcomes_total, .ev_work_item_completed' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 850
    codex:
      represents: "Two lines: ratified outcomes (plot count, Fields SE) and lifetime work_item_completed events (crop growth stage across all plots, base 850). Per-sector splits land with the E2 emitters — until then every plot renders the aggregate stage, and this codex says so."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "unplowed ground"
  - id: island_harbor_beacon
    represents: "THE dark beacon (interim silo sprite; bespoke lighthouse art tracked in the authored-art budget) — beam lights on first graduated cell"
    source_binding: "jq -r -s 'last | .cells_graduated' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 1
    codex:
      represents: "Graduated trust cells (falsifier keystone number), rendered at Harbor S as the farm silo with an UNLIT lamp overlay — the honest-zero lighthouse stand-in, prominent and slightly oversized. The beam lighting on the first graduation will be the biggest visual event in the world's life; it never gets a night light before then."
      mechanism_path: "cabinet/scripts/falsifier-report.py"
      day0: "dark beacon, loud about it"
  - id: island_harbor_crates
    represents: "Dock crates per extension pack"
    source_binding: "jq -r -s 'last | .packs_dirs' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 1
    codex:
      represents: "Pack directories under packs/ — one crate on the Harbor-S dock per pack; the Z0 face of the harbor-cranes surface (authored-here delta stays an E2 entry)."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "5 inherited crates"
  - id: island_services_mill_row
    represents: "Works-ridge mill/kiln row: total service rows, disabled rows render stopped"
    source_binding: "jq -r -s 'last | .services_rows_total, .services_rows_disabled' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 8
    codex:
      represents: "Two lines from the fleet manifest census: total service rows (mills/kilns on the Works E ridge) and disabled rows (rendered stopped/ghost-framed — a stopped mill is honest, not hidden)."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "9 spinning, rest ghost-framed"
  - id: wardroom_bookshelf_fill
    represents: "Wardroom bookshelf fill — tier-2 journal mass"
    source_binding: "jq -r -s 'last | .tier2_note_files' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 4
    codex:
      represents: "Markdown notes across instance/memory/tier2/ — fills the Z2 bookshelf station shelf-by-shelf (the Wardroom face of the officer-journal mass; the island Library remains the Memory-NE anchor)."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "empty shelf"
  - id: wardroom_noticeboard_pins
    represents: "Cork-board pin TEXTURE — today's org-event volume (rate-routed: >1/day source, texture only, never structure)"
    source_binding: "jq -r -s 'last | .events_today' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    codex:
      represents: "Org events observed today (census keyframe events_today) — pins = min(12, ceil(events_today/250)) tiny seeded squares on the noticeboard cork. TEXTURE class under rate-routing law: this >1/day source may never create morphology."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "bare cork"
  - id: street_liveliness
    represents: "Street prop unlock band by org age (bench/trees/planters) — TEXTURE class"
    source_binding: "jq -r -s 'first | .date' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    codex:
      represents: "Date of the FIRST census keyframe — org age bands unlock street set-dressing (<7d bare; 7-30d benches + trees; >30d second streetlight row + planters). Seeded positions fix once placed (fold law). TEXTURE class: age dresses the street, it never mints structure."
      mechanism_path: "cabinet/scripts/world-census.py"
      day0: "bare pavement"

  # --------------------------------------------------------------------------
  # v3 entries — THE LIVING ISLAND (Captain-ratified 2026-07-09; spec
  # docs/plans/world-unified-spec-v2-2026-07-09.md). growth-ladders.yml is the
  # RUNG SOURCE OF TRUTH for every growth surface: bases, rungs, era vocab and
  # hysteresis live THERE as Captain-tunable data (schema changes ride
  # grammar-law PRs like this one); entries here bind the STATE and cite their
  # ladder. Validator green required (read-only sandbox, live bindings).
  # --------------------------------------------------------------------------
  - id: era_vocabulary
    represents: "THE ERA AXIS — one org-maturity index selects the sprite vocabulary every element renders with (torches before streetlights, tent before house)"
    source_binding: "grep -c -e 'weight:' cabinet/world/growth-ladders.yml"
    scope: org-global
    tier: T0
    replay: ledger
    codex:
      represents: "The weighted era basket (5 real metrics: org age, total events, outcomes achieved, graduations, active lanes) in growth-ladders.yml — index = sum(weight * norm), era = highest threshold <= index, MANDATORY 2-keyframe hysteresis (no era flapping). ERA styles, RUNG measures: era never hides a count, only selects which sprite family renders it (spec §15.2; calibration winner 'balanced', WORLD-GROWTH-CALIBRATION gate). Recomputed per daily census keyframe — replay is ledger-true."
      mechanism_path: "cabinet/world/growth-ladders.yml"
      day0: "camp era — campfire/tent/torch/dirt vocabulary"
  - id: ladder_rungs
    represents: "THE RUNG AXIS — per-element magnitude ladders; growth-ladders.yml is the rung source of truth (no element spawns at final size)"
    source_binding: "grep -c -e 'rungs:' cabinet/world/growth-ladders.yml"
    scope: org-global
    tier: T0
    replay: ledger
    codex:
      represents: "Every bound element's visible growth ladder (29 ladders: tier/count/flag/per_lane modes over live-verified census/snapshot/ledger metrics, log2 bases, 2-eval hysteresis, era vocab per ladder). VALUE edits are Captain-editable directly — world-growth-validate.py refuses malformed/untruthful configs, the renderer HOT-RELOADS on mtime, and world-preview --maturity renders stills before committing (addendum 2 §3). Schema changes ride grammar-law PRs, never auto-merge."
      mechanism_path: "cabinet/scripts/world-growth-validate.py"
      day0: "every ladder at rung 0 — empty bucket, bare pole, dark cairn"
  - id: lighthouse_staged
    represents: "THE LIGHTHOUSE, staged and RECOMPOSED — tower grows by accumulating trust cells; the lamp lights only at the first graduation"
    source_binding: "jq -r -s 'last | .cells_accumulating' shared/interfaces/world-chronicle.jsonl"
    scope: org-global
    tier: T0
    replay: ledger
    base: 4
    codex:
      represents: "cells_accumulating (census keyframe) drives the tower stages dark_cairn -> stone_base -> tower_part -> tower_full (ladder: lighthouse; the lamp is ladder lighthouse_lamp, a flag on cells_graduated — 0 today, so DARK at every stage). RECOMPOSED per Captain ruling 2026-07-09: the lamp must fit the tower — proper tower tiles or a Room_Builder/exterior-piece composition, NEVER the water-tank/silo body; supersedes island_harbor_beacon's interim silo sprite (dual-view cross-link, D7/R8 registry). The beam lighting on the first graduation remains the biggest visual event of the world's life; no night light before then."
      mechanism_path: "cabinet/scripts/falsifier-report.py"
      day0: "dark cairn on the shore rock — ambition visible from birth"
  - id: lane_reef_buoys
    represents: "Reef buoys — retired and instance-test lanes render as honest anchored markers, never islands"
    source_binding: "grep -c -e 'status: retired' instance/config/outcomes.yml"
    scope: org-global
    tier: T0
    replay: git
    codex:
      represents: "Retired outcomes/lanes in outcomes.yml plus instance-only lanes (Captain rulings 2026-07-09): a retired or inactive lane keeps its fixed fan anchor (anchors never move — layout_fold; stepnetwork S at (120,168), spec §2.2) but renders a grey reef buoy whose inspect card says exactly retired/instance. The 'sensed' lane is an INSTANCE-only test app — never foundation, never an isle in egg defaults; stephie is an instance lane too (renders on this deployment, never in egg defaults). Buoys dual-code the grey-unmeasured horizon fog; grey stays the unmeasured hue, never an alarm."
      mechanism_path: "instance/config/outcomes.yml"
      day0: "open water — an egg has no retired lanes"
  - id: mailbox_pending
    represents: "Crossroads mailbox flag — raised while Captain decisions wait (P1 feed pending; renders flag-down until it lands)"
    source_binding: "jq -r -s 'last | .pending_captain_items' shared/interfaces/world-chronicle.jsonl"
    scope: dark
    tier: T0
    replay: ledger
    codex:
      represents: "pending_captain_items (Redis snapshot int, live-verified 0 on 2026-07-09) — flag up while > 0. DARK until P1 lands the field on the census keyframe/SSE snapshot AND the source-of-record call resolves (spec §14 open call #5: dashboard decision-queue vs Chair binder queue — one truth, one place). Click behavior is show-grammar mailbox_view law: READ-only render of the decision queue + deep-link, NO actuation ever (Captain ruling 2026-07-09; the ONE actuator is the killswitch lever)."
      mechanism_path: "cabinet/dashboard/src/app/api/world/stream/route.ts"
      day0: "flag down, empty box"
