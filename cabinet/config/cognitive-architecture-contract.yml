schema_version: cognitive-architecture-contract/v1
baseline_sha: 8f9c555d2064d55a159a53fcedd6df33434a9291
budgets:
  central_event_types:
    path: framework/events/emitter.py
    symbol: VALID_EVENT_TYPES
    maximum: 91
  central_action_types:
    path: framework/authority/classifier.py
    symbol: ACTION_TYPES
    maximum: 30
  services_total:
    path: cabinet/services.yml
    maximum: 57
  services_enabled:
    path: cabinet/services.yml
    maximum: 44
  layer_debt_entries:
    path: .layer-separation-baseline
    maximum: 24
  layer_allowlist_entries:
    path: .layer-separation-allowlist
    maximum: 19
  framework_production_modules:
    path: framework
    maximum: 206
  framework_production_noncomment_lines:
    path: framework
    maximum: 60067
  named_compiler_modules:
    path: framework
    pattern: "*compiler*.py"
    maximum: 1
  duplicate_event_writer_sinks:
    path: framework/events/emitter.py
    symbol: emit
    callee_prefix: _write_to_
    maximum: 3
temporary_allowances:
  - phase: COG-0
    budget: framework_production_modules
    additional: 2
    reason: import-inert trajectory contract package until structural compaction
    owner: cognitive-core-program
    sunset: '2027-01-19'
    deletion_gate: COG-7 brings the production-module count below the pinned baseline
  - phase: COG-0
    budget: framework_production_noncomment_lines
    additional: 1256
    reason: externally attested trajectory and holdout validation until structural compaction
    owner: cognitive-core-program
    sunset: '2027-01-19'
    deletion_gate: COG-7 brings production non-comment lines below the pinned baseline
  - phase: COG-1
    budget: framework_production_modules
    additional: 1
    reason: envelope-v2 dispatch and domain-local schema registry for the tasks outbox pilot
    owner: cognitive-core-program
    sunset: '2027-01-19'
    deletion_gate: COG-1 pilot retired or composed
  - phase: COG-1
    budget: framework_production_noncomment_lines
    additional: 942
    reason: envelope-v2 validator, table-drain relay extension, registry interface, and the F1 live-stream retarget seam (+26 measured, 2026-07-21, per the frozen review's binding conditions)
    owner: cognitive-core-program
    sunset: '2027-01-19'
    deletion_gate: COG-1 pilot retired or composed
  - phase: relaunch-killswitch
    budget: framework_production_noncomment_lines
    additional: 25
    reason: front-door killswitch fail-closed gate (channel.py _post_one + send_document reuse action_exec's SEC-3 reader; +25 measured 2026-07-21 via cognitive-architecture-census.py)
    owner: cognitive-core-program
    sunset: '2027-01-19'
    deletion_gate: killswitch gate folded or COG-7 compaction
  - phase: relaunch-ci-tolerance
    budget: framework_production_noncomment_lines
    additional: 7
    reason: public-CI artifact-anchored tolerance — fidelity/retro.py dunder-proxy AttributeError guard (stop _RetroUnavailable + PEP 562 module __getattr__ forwarding dunders) + frontdoor/actfirst_canary.py import-time→call-time board resolution (env.tasks_board() read at call, fail-closed on ''); both are latent-bug fixes that also let the fidelity/front-door suites pass on the artifact-stripped public tree; +7 measured 2026-07-22 via cognitive-architecture-census.py
    owner: cognitive-core-program
    sunset: '2027-01-19'
    deletion_gate: CI tolerance folded or COG-7 compaction
  - phase: COG-2
    budget: framework_production_modules
    additional: 5
    reason: COG-2 shadow temporal world model (Cortex) — the framework/cortex package (__init__/belief/engine/adapters + UNIT 2 query.py). UNIT 3 (consequence seam) added NO new module — it EXTENDS adapters.py (consequence adapter + tasks tombstone) and framework/fidelity/consequence.py (additive iter_ledger_rows), so the module count is unchanged. EXACT measured running total (+5 = unit-1's +4 plus unit-2's query.py), NOT the §10 planning ceiling of 6 — the structural-compaction mutant gate (test_cognitive_architecture_census.py) requires zero headroom (observed==max) so any growth is caught, so each COG-2 unit bumps this allowance to its exact new running total rather than pre-allocating the ceiling; tightened to phase actual at done-flip (shrink-only)
    owner: cognitive-core-program
    sunset: '2027-01-19'
    deletion_gate: COG-7 compaction folds the projection, or the phase is superseded
  - phase: COG-2
    budget: framework_production_noncomment_lines
    additional: 1189
    reason: COG-2 shadow temporal world model (Cortex) — unit 1 (belief record + pure fold + canonical hash + tasks adapter) plus UNIT 2 (envelope-file adapter §5.2a + as-of query API query.py + shared resolve_relationships/derive_status + belief_from_row) plus the unit-2 review fixes (F2 conflict-driven result status, F6 no-subject quarantine, cutoff hard-error) plus UNIT 3 (the consequence seam — additive iter_ledger_rows in framework/fidelity/consequence.py + the consequence adapter and tasks-adapter source-side tombstone path in framework/cortex/adapters.py) plus the unit-3 review fixes (F1 duplicate-consequence-row dedupe first-occurrence, F2 tombstone requires present-null new_status) plus UNIT 4 (§8 sim-5 serve-time store-hash binding in framework/cortex/query.py + fold-manifest records in framework/cortex/engine.py, +79 measured but left un-reconciled in the sim5 WIP base commit e6e5a959; §8 sim-7 cross-cabinet ingest fence — the classification=cross_cabinet refusal in framework/cortex/adapters.py _envelope_scope_reason, +14 measured). EXACT measured running total (+1103 = unit-1's +492 plus unit-2's +340 plus +24 unit-2-review lines plus unit-3's +135 plus +19 unit-3-review lines plus unit-4's +93) via cognitive-architecture-census.py, NOT the §10 planning ceiling of 1800 — the structural-compaction mutant gate requires zero headroom (observed==max), so each COG-2 unit bumps this allowance to its exact new running total; tightened to phase actual at the ledger done-flip (shrink-only)
    owner: cognitive-core-program
    sunset: '2027-01-19'
    deletion_gate: COG-7 compaction folds the projection, or the phase is superseded
  - phase: COG-3
    budget: framework_production_modules
    additional: 7
    reason: COG-3 wave-3 running total — U1 derivation core +5 (__init__ import-inert, model records/canonical-bytes/recorder-digests, states ordered-total derivation + BuildFailure, query vocab/scorecard/recommendation, ovi_view per-instrument) plus U2 +2 (graph.py pure build fold, counterfactual.py branch+prediction store). EXACT measured running total (221 vs 214), 2026-07-23, via cognitive-architecture-census.py; the wave-4 adapters package bumps this row to its new exact total; tightened to phase actual at done-flip (shrink-only)
    owner: cognitive-core-program
    sunset: '2027-01-19'
    deletion_gate: COG-7 compaction folds the graph, or the phase is superseded
  - phase: COG-3
    budget: framework_production_noncomment_lines
    additional: 1194
    reason: COG-3 running total — wave-1 D1 precursor +7 (consequence cabinet_id stamp in framework/cortex/adapters.py; belief.py ENGINE_VERSION bump net-zero) plus wave-3 U1 derivation core +495 (framework/objectives package incl. the u1-fix hardening — limb-i forged-pairing BuildFailure, vocabulary purity, empty-assumption strip, stray-view guard) plus wave-3 U2 +692 (graph.py pure fold with root auth/divergence/orphaning/C-F3 hashing, counterfactual.py, query serve surface with the REFUSE walls incl. the u2-fix graph_rows_hash limb + canonical-cutoff entry gate). EXACT measured (64680 vs 63486 base), 2026-07-23, via cognitive-architecture-census.py — the zero-headroom observed==max law means each COG-3 unit bumps this allowance to its exact new running total; tightened to phase actual at done-flip (shrink-only)
    owner: cognitive-core-program
    sunset: '2027-01-19'
    deletion_gate: COG-7 compaction folds the graph, or the phase is superseded
ownership:
  authoritative_state: domain_transaction
  cross_domain_delivery: transactional_outbox
  world_model: rebuildable_projection
  evidence: existing_evidence_plane
  promotion: existing_gate_only
declared_invariants:
  - independent_physical_safety_enforcers
  - no_global_database_bus_or_compiler
  - no_self_minted_graduation_credit
  - captain_rooted_objectives
  - shadow_before_authority
  - namespaced_operations_single_constitutional_effect
enduring_architecture_gates:
  - bash cabinet/scripts/verify-cognitive-architecture.sh
