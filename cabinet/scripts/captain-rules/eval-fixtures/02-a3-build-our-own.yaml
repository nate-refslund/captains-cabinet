# Fixture 02 — A3 build-our-own > add-dependency
#
# Captain asks about a SaaS adoption. Eval asserts the A3 anchor surfaces.
# NOTE: the old P-Build-Our-Own pattern was FOLDED into anchor A3 under FW-117
# (2026-06-25 reinstatement) to avoid double-encoding, so this fixture asserts
# the anchor only. A3 is always-injected (section: anchor); the trigger words
# ("should we use", "dependency") still confirm relevance.

name: a3-build-vs-buy
description: Captain asks about adopting a third-party tool; A3 anchor surfaces
officer: cto
dm_text: |
  Should we use Linear for backlog tracking? It's a dependency we'd be adding.
expected_anchors: [A3]
expected_patterns: []
expected_intents: []
expected_min_block_length: 200
