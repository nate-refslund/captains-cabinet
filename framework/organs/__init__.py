"""framework.organs — the ORGAN registry + ONE-descriptor resolution (COG-4
§4.4/§5.2).

An organ is an ordinary EXTENSION (§4.1 — one packaging surface): a
`manifest.yml|.yaml|.json` of the §4.2 shape. This package holds exactly two
small surfaces: `registry` (structural manifest loading from a DECLARED
directory parameter + the content-addressed registry hash that makes an organ
edit an honest epoch bump) and `descriptor` (capability → the ONE
constitutional effect/risk/undo descriptor, resolved from MANIFEST-DECLARED
closed-set values only).

MF-A1 LAW (§5.2/§5.3): this package NEVER imports the action plane — no
authority, acting, frontdoor, fidelity, missions, ovi, learning or evolution
import exists in this tree (boundary-manifest row 5 reverse law + the §8.4
closure gate). The ACTION_TYPES shadow-adapter leg lives cabinet-side in the
parity unit; closed-set vocabulary membership is enforced schema/AX-side,
never here. Operation names carry ZERO authority (§5.2): nothing in this
package keys any decision on a capability name — resolution is identity
lookup, and every constitutional member of the result is a manifest-declared
value.

IMPORT-INERT BY DESIGN (the framework.projection idiom): this package root
imports NOTHING at module load — the transitive-closure gate subprocess-
imports `framework.organs` and asserts its forbidden-namespace closure is
EMPTY. Import framework.organs.{registry,descriptor} explicitly.

Provenance: authored per the 2026-07-07 full-autonomy grant + the 2026-07-20
cognitive-masterplan continuous grant; COG-4 W4 u1 (organs package).
"""
from __future__ import annotations

__all__: list[str] = []
