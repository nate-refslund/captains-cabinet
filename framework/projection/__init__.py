"""framework.projection — the C3 projection kernel (COG-4 §6).

The EXTRACTED store disciplines shared by every rebuildable shadow projection
(cortex fold, objectives graph, scheduler snapshot/schedule): canonical bytes +
recorder-dialect digest, the content-excluded identity law, the parameterized
chained rows-hash, the manifest envelope, atomic writes, the verified
single-read serve with its REFUSE limbs, the canonical-cutoff validator, and
the cache-delete rollback grammar. Extraction, not invention — the two shipped
instantiations (framework/cortex, framework/objectives) remain byte-identical
under it (§6.4), and load-bearing differences stay domain-side parameters
(§6.2), never kernel policy.

IMPORT-INERT BY DESIGN (§8.3/§8.4): this package root imports NOTHING at module
load, and the whole tree imports STDLIB ONLY — the transitive-closure gate
subprocess-imports `framework.projection` and asserts its forbidden-namespace
closure is EMPTY. Import framework.projection.kernel explicitly. The kernel is
importable by cortex/objectives/scheduler internals + their CLIs only
(boundary-manifest row 6) — never a general utility grab-bag.

Provenance: authored per the 2026-07-07 full-autonomy grant + the 2026-07-20
cognitive-masterplan continuous grant; COG-4 W3 u1 (C3 kernel extraction).
"""
from __future__ import annotations

__all__: list[str] = []
