---
name: holistic-thinking
description: Universal L1/L2/L3 lens. Apply to every meaningful improvement: L1=immediate, L2=system, L3=org. Captain references this in retros, plans, and reviews — use it before recommending any non-trivial change.
# Background law (always-on lens, not a runbook) — model applies it on trigger;
# no meaningful user slash-invocation surface (audit #22).
user-invocable: false
---

<!-- single-source wrapper (egg R155, pairs R138): the canonical body of this
     skill lives at memory/skills/holistic-thinking.md (Captain-applied law; this
     wrapper carries only the trigger frontmatter). Do not add body content
     here — propose changes against the memory copy via the evolution loop. -->

Read `memory/skills/holistic-thinking.md` (from the repo root, `$CABINET_ROOT`) and
follow it as this skill's full instructions.
