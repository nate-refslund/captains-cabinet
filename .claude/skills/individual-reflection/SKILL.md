---
name: individual-reflection
description: Per-officer event-triggered reflection. Use after compaction, completion milestones, or CoS reflection trigger — never on a fixed clock. Skip if idle. Produces draft skills + value-maximization ideas.
---

<!-- single-source wrapper (egg R155, pairs R138): the canonical body of this
     skill lives at memory/skills/individual-reflection.md (Captain-applied law; this
     wrapper carries only the trigger frontmatter). Do not add body content
     here — propose changes against the memory copy via the evolution loop. -->

Read `memory/skills/individual-reflection.md` (from the repo root, `$CABINET_ROOT`) and
follow it as this skill's full instructions.
