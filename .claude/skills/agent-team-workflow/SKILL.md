---
name: agent-team-workflow
description: Coordinating parallel teammates and subagents from an officer session. Current reality (CLI ≥ v2.1.178) — TeamCreate is removed, teams are implicit, teammates spawn via the Agent tool's name param. Worker+reviewer patterns, worktree isolation, model routing per platform.yml, and the headless boundary (Agent Teams don't work headless — unattended lanes use subagents/workflows).
---

<!-- single-source wrapper (egg R155, pairs R138): the canonical body of this
     skill lives at memory/skills/agent-team-workflow.md (Captain-applied law; this
     wrapper carries only the trigger frontmatter). Do not add body content
     here — propose changes against the memory copy via the evolution loop. -->

Read `memory/skills/agent-team-workflow.md` (from the repo root, `$CABINET_ROOT`) and
follow it as this skill's full instructions.
