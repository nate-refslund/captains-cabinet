---
name: deploy-and-verify
description: Push → poll Vercel → validate live → close issue. The deploy validator + reviewer capability flow: detect git push, notify validators, verify, mark issue done.
---

<!-- single-source wrapper (egg R155, pairs R138): the canonical body of this
     skill lives at memory/skills/deploy-and-verify.md (Captain-applied law; this
     wrapper carries only the trigger frontmatter). Do not add body content
     here — propose changes against the memory copy via the evolution loop. -->

Read `memory/skills/deploy-and-verify.md` (from the repo root, `$CABINET_ROOT`) and
follow it as this skill's full instructions.
