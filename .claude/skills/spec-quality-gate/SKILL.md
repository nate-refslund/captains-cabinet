---
name: spec-quality-gate
description: Pre-commit checklist for product specs. Use before publishing any spec to Notion / specs/ dir: acceptance criteria measurable, dependencies named, rollback documented, captain attention budgeted.
# Background law (always-on gate, not a runbook) — model applies it on trigger;
# no meaningful user slash-invocation surface (audit #22).
user-invocable: false
---

<!-- single-source wrapper (egg R155, pairs R138): the canonical body of this
     skill lives at memory/skills/spec-quality-gate.md (Captain-applied law; this
     wrapper carries only the trigger frontmatter). Do not add body content
     here — propose changes against the memory copy via the evolution loop. -->

Read `memory/skills/spec-quality-gate.md` (from the repo root, `$CABINET_ROOT`) and
follow it as this skill's full instructions.
