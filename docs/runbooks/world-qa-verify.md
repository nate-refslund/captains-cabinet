# World QA / Verify — graduated to the skill library

**Moved (2026-07-17, vault wave):** this procedure is officer-EXECUTABLE —
world lanes run it after every world change — so it now lives at
[`memory/skills/world-qa-verify.md`](../../memory/skills/world-qa-verify.md),
content preserved verbatim (the clipped-page-screenshot aliveness method, the
`toDataURL()` false-dead WebGL gotcha, and the per-zoom post-merge
checklist).

Placement rule (see `vault/README.md`, "Runbooks vs skills"): officer-
executable procedures live in `memory/skills/`; Captain ceremonies stay
runbooks under `docs/runbooks/`. This stub stays so historical references
(ledger rows, plan docs, code comments) keep resolving.
