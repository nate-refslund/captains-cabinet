# Operative Egg Plan — the ONE merged, decided build order
**v1 · 2026-07-07 · branch `feat/fidelity-harness-design` @ 06818985 · status ledger: `docs/plans/operative-egg-ledger-2026-07-07.yml`**

This document discharges delta #1 of the compression verdict (`~/cabinet-egg-compression-2026-07-06.md` Part 2 §4.1): one
merge-and-decide pass collapsing exec §9 + Part III §5 (as amended by Part VI D2/D5/D13/D14 + Part V §3.6) + the 159-row
audit + the adversarial drop-verifications into a single operative plan. **Execute from THIS document and its ledger, not
from the egg doc.**

## 0. Authority & precedence (supersessions stamped)

Precedence, highest first — later documents do NOT need reader reconstruction anymore:
1. **THIS plan + its YAML ledger** (the only execution surface).
2. `~/cabinet-egg-compression-2026-07-06.md` Part 1 (compression table, egg spec, 11 challenged drops) — evidence base.
3. `~/egg-agent-analysis-2026-07-06.md` Part VI deltas D1–D15, then Part V rulings R1–R10 + Colony Contract, then Part III §5 migration spine.
4. **STAMPED SUPERSEDED in place** (void; do not execute from): egg-doc Part I §9 build-order v0 (fresh-seed — refuted by the
   "you already built the egg" verdict), Part II §3 ship order (pre-reconciliation), Part I §6 never-list (superseded by
   D13 sovereign hatch + D14 charter grantability). Also superseded: the egg doc's original Fix-#1 sequencing (replaced by D5).
5. Standing Captain rulings remain binding: earn-demotion (07-03/04), ACT-AND-DRAFT (07-04), field-test disturb-MAX (07-04),
   Decision B filesystem locks, access-inversion, HQ-Chair-only channel. External comms stays per-item Captain approval on
   THIS instance regardless of D14 (D14 is org-flavor architecture, not this deployment's policy — CG-9 records the distinction).

## 1. Current state — ALREADY-DONE and IN-FLIGHT (verified on tree 2026-07-07)

| Item | State | Evidence |
|---|---|---|
| Germline re-lock (Part III step 1 / D3 "lock now at current set") | **DONE 2026-07-06 19:58** | 116 schg paths verified now (`find . -flags schg`); enforcer triad itself locked. Residual: CG-9 ledger entry. |
| Flavor-A extraction behind the sources seam | **DONE** | E4 tier-1 `228f444f` + source-adapter PASS 2 `212fca39`; `test_no_screenpipe_in_core.py:92` allowlist EMPTY. R127 stamped done. |
| [D16] sovereign `verdict_gate` promotion fuel | **SHIPPED, UNRECONCILED** | `framework/fidelity/consequence.py:735-750` live. Needs CG-1 RULING, not build. No posture flip before CG-1. |
| Wave-1α T1–T4 (protocol widening/rename, apoptosis pack, scheduling, pack rail + de-Nate) | **IN-FLIGHT** (parallel session, this branch) | Verified NOT landed: `nate_replied_since` still at `run_draft_lane.py:208`; no apoptosis hits; self-improvement is template-plist-only; plugin.json still enumerates the live fleet. Rows W1A-T1..T4. |
| Judge calibration | **NOT scheduled** | `cabinet/services.yml:50` still a future-B5 comment → A2. |
| `cells_graduated` | **0** (unchanged) | falsifier series 07-06: acted_7d 0, approved_7d 3/33. The clock problem is real → §7. |

## 2. Lanes & sequencing

Four owner-lanes. **Hard sequencing rules:**
- **alpha-additive** — additive, live-runtime-safe, runs NOW under the 2026-07-07 full-autonomy grant. Nothing in alpha
  deletes, moves, or edits a scheduled organ's path; nothing touches schg paths.
- **W1A (in-flight)** — the four Wave-1α tasks currently being built; this plan folds them in, does not re-run them.
- **beta-compression** — every remove/compress/instance-split. **BLOCKED until CG-12 (Captain ratifies this plan).**
  Waves inside beta: **B1** dead-safe deletes (zero refs, verified) → **B2** CI-paired deletes (each carries its exact
  workflow/test edit in the SAME commit) → **B3** instance-splits (each with its pre-chore: fixtures, template homes,
  founding amendments) → **B4** content compress/dedupe/scrub → **B5** deferred (docker spawn chain after decouple;
  worktrees after merges; CI branch retarget at egg-packaging only). Before each wave: `git tag pre-egg-<wave>-YYYYMMDD`.
- **gamma-packs** — extract-pack relocations. **BLOCKED until W1A-T4 (pack rail) lands.** Live scheduled scripts move
  launchd-transparently (path update in same change, `verify-launchagents.sh` green).
- **captain-gated** — germline unlocks, rulings, proofs P2/P3. Never self-serve; batched windows (CG-13) where possible;
  relock same day (D3: never hold the boundary open while redesigning it).

Order of operations: `[now] alpha + W1A → CG-12 ratify → B1 → B2 → B3 → B4 → (G1 once T4 landed, may overlap B3/B4) →
P2 live re-hatch → B5 residue → P3 Mini hatch (freeze-new-organs keys on P3) → CG-10/CG-11 posture work per its own gates.`

## 3. Captain-gated register (every item, explicit)

All CG rows in the table (§6) — summary: **CG-1** D5/D16 reconcile (two options, BEFORE any posture flip) · **CG-2**
`run_action_lane.py` gather rewire (germline) · **CG-3** CLAUDE.md + brain-bridge.md split (germline docs) · **CG-4**
extension-manifest sunset field (germline schema) · **CG-5** compile-target decision · **CG-6** push decision · **CG-7**
employer-IP gate (blocks ALL commercialization) · **CG-8** mcp-scope host-grant removal · **CG-9** ledger supersession
entries incl. 07-06 relock · **CG-10** machine-confirm promotion-fuel amendment (after CG-1 + calibration ≥0.8) · **CG-11**
sovereign posture.yml attestation (after CG-1, A4, verdict-supply green) · **CG-12** ratify THIS plan (beta start gate) ·
**CG-13** cosmetic germline batch window · **CG-14** policy_engine.py pull-down · **CG-15** pre-tool-use constitution
entry · **CG-16** veto.py wiring · **P2/P3** proofs. Newly discovered locks folded in: `policy_engine.py` and
`act-first-surfaces.yml` are ALSO schg (verified 2026-07-07) → routed to CG-14/CG-13, not worked around.

### CG-1 in full — the two resolution options (decide before ANY posture flip)
- **Option A (gate the checklist):** keep `verdict_gate` fuel as shipped; add to the D13 sovereign activation checklist:
  "judge-calibration ≥0.8 proven (Wilson bound per A10) AND label-floor-met (A3) are preconditions"; sovereign cannot be
  attested until both hold. Cheapest; preserves D16 code.
- **Option B (scope the fuel):** rule that `verdict_gate` confirms count ONLY for deterministic-inverse machine evidence
  (`ttl_ok` restricted to label-floor-met periods per D5's guard), and the calibration precondition attaches to
  `verdict_judge` instead. Requires a comment/test pin on `consequence.py` semantics (CG window if wording lives in
  germline files).
Either way: silence from an absent Captain is never promotion fuel; D13's checklist gains the gate.

## 4. The 11 CI-coupled drops — each PAIRED with its exact decouple edit

| # | Drop | Exact pairing (SAME commit) | Row |
|---|---|---|---|
| 1 | `cabinet/host-agent/` | delete `.github/workflows/cabinet-ci.yml:326` host-agent test step | R063 |
| 2 | `cabinet/launchd/` concrete plists | KEEP `com.cabinet.gate-apply.plist` (+`INSTALL-flip.md`); then no CI edit needed — `test_gate_apply_dark.py:30-44` + lockstep `:453` stay green | R067 |
| 3 | Spec-039 ETL set | KEEP `etl-common.py` (`test_library_mcp_client.py:55`); delete the 3 ETL modules + their 3 test files together; CI `:286-289` keeps collecting the rest | R073 |
| 4 | `cabinet/scripts/cutover/` | delete CI `:299-302` cutover pytest step | R074 |
| 5 | one-time migrations | re-point `gates/gate-3-idempotency.sh:18` + `.py:89-95,119` default `--etl-script` off `migrate-sources-to-officer-tasks.sh` | R075 |
| 6 | docker-era `start-officer.sh` chain | decouple FIRST: CI `:187,:201` + `cabinet/tests/start-officer/*` + `cabinet-bootstrap.sh:789,857,891` spawn legs — **still live; deferred to B5** | R081 |
| 7 | `cabinet/sql/044` | **NOT A DROP — FIX**: wire into `load-preset.sh:251-260` + `cabinet-bootstrap.sh:973+` apply lists | R098 |
| 8 | `instance/config/` live values | re-point `framework/tests/test_env.py:108,198,254,342,432,496` at a shipped fixture instance first | R120 |
| 9 | `instance/config/contexts/` | retire/fixture `test-org-runtime.sh:43-49` + CI `:248` first; keep `_default.yml` | R124 |
| 10 | `instance/agents/` | give `cos/mcp.json` (sole cua-driver overlay, `start-officer-mac.sh:98-102`) a template home first — silent-loss class | R128 |
| 11 | `docs/proposals/` | `germline-amendment-*.md` ship as FOUNDING AMENDMENTS (or R031's merged lint gains a fixture) — `test_amendment_doc_lint.py:25-26,120-137` | R146 |
| +  | `cabinet/mcp-server` carve (adjacent finding) | adjust CI `:309,:319` when `host-tools.py` leaves | R089 |

## 5. Refuted drops honored (audit corrections baked into rows)

- **`cabinet/sql/044-library-embedding-hardening.sql` = FIX, not delete** — real latent bug; fresh-hatch DBs break today (R098, alpha, high priority).
- **Docker officer-spawn chain is STILL LIVE** via `cabinet-bootstrap.sh` peer-spawn + start-officer tests + CI — decouple before delete (R081, B5-deferred).
- **KEEP `com.cabinet.gate-apply.plist`** — hand-authored root LaunchDaemon, germline-enumerated, test-pinned Disabled (R067).
- **KEEP `etl-common.py`** — live library plumbing, not Linear-only ETL (R073).
- **KEEP `shared/force-push-log.md`** — enforced by the pre-push hook, entries already empty (R112).

## 6. The decided rows — every audit row + extensions

Classes: `germ-keep | compress | extract-pack | instance-split | remove`. Lane·Wave column shows owner-lane and beta wave /
gamma batch. Gate = literal verify-done command. Rollback default for tracked changes: `git revert` against the wave tag.
Convention: rows with **status done and wave `-`** are decided-retained (or already landed) — no work owed; they carry
lane `beta-compression` as the retention-verification owner. The suite green IS their gate. Rows with **status done and
a lettered wave** (R059, R088, R129, R130, R156, R159) are verified-already-correct or packaging-time-only — their gate
documents the invariant; no pre-packaging work owed.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| R001 | `framework/policies` | germ-keep (100%) | beta-compression · - | done | Retain 100%. NATE-DECISION comment scrub deferred to CG-13 batch (authority-matrix.yml is schg germline). _(note: cosmetic scrub via CG-13)_ | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R002 | `framework/authority` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R003 | `framework/acting (pure core: action_lane.py, loop.py, lane_dedup.py, voice_charset.py, product_health.py, tests/)` | compress (85%) | beta-compression · B4 | todo | Extend %%CAPTAIN%%/%%DIRECTIONS%% slot pattern with %%OFFICERS%% rendered from instance roster; make lane_default required config (drop "polads" literal at action_lane.py:404). | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R004 | `framework/acting/run_action_lane.py + run_draft_lane.py` | extract-pack (60%) | captain-gated · CG-2 | done | SPLIT: (a) run_action_lane.py gather rewire (vault-dir literals :739-755 -> PersonalSource.open_commitments/search + channel presentation) is GERMLINE-LOCKED -> CG-2 unlock-window amendment. (b) run_draft_lane.py side (source-method rename + :271 guard) rides W1A-T1 (in-flight). **[PAIR: CG-2 (germline unlock); W1A-T1]** | `grep -c '6-Commitments\\|2-Meetings\\|5-Reflections' framework/acting/run_action_lane.py  # 0 after rewire; pytest framework/acting -q` | germline amendment reversal doc + relock |
| R005 | `framework/learning` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R006 | `framework/measurement` | germ-keep (90%) | beta-compression · B4 | done | Retain runners/registries/pattern-detector; relocate concrete role_evals/ + scenarios/ seed content (five-officer archetype) to presets/work/. | `python3 -m pytest framework/measurement -q; git ls-files framework/measurement/role_evals \| wc -l  # 0` | git revert (wave tag pre-egg-B4) |
| R007 | `framework/fidelity (harness core)` | germ-keep (95%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R008 | `framework/fidelity (Flavor-A clone cells: benchmark.py, run_f1.py, measure_intent.py, decision_cell.py, retro.py, run_e2e_smoke.py, officer_prompt.py clone parts)` | extract-pack (45%) | gamma-packs · G1 | todo | Relocate behind the extension seam as an installable pack (manifest + validate-extension.sh). | `bash cabinet/scripts/validate-extension.sh <pack> && python3 -m pytest framework/ -q  # core green with pack absent` | git revert (wave tag pre-egg-G1) |
| R009 | `framework/fidelity/regression_corpus/cases/` | instance-split (10%) | beta-compression · B3 | done | Relocate regression_corpus/cases/ + manifest.json to instance layer; REGENERATE manifest in the same change (never ship stale). Mechanism (harvester/gate/FROZEN contract) stays in R007. | `git ls-files framework/fidelity/regression_corpus/cases \| wc -l  # 0; pytest framework/fidelity/tests/test_regression_gate.py -q  # no_verdict on empty corpus` | git revert (wave tag pre-egg-B3) |
| R010 | `framework/events` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R011 | `framework/triggers` | germ-keep (95%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R012 | `framework/missions` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. _(note: compiler.py:38 static packaging flag — cosmetic, fold into B4 sweep)_ | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R013 | `framework/roles` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R014 | `framework/sources` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. _(note: Protocol widening tracked as W1A-T1 (in-flight))_ | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R015 | `framework/schemas` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. _(note: sunset-field manifest amendment = CG-4 (schema is germline-locked))_ | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R016 | `framework/defaults` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R017 | `framework/watchdog` | compress (75%) | beta-compression · B4 | done | Keep check.py + registry machinery; move EXPECTATIONS rows, FULLTIME_OFFICERS roster, pipe-freshness table and briefing times to instance config (watchdog.yml). | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R018 | `framework/probes` | compress (70%) | beta-compression · B4 | done | Keep correlation/lib/verifier/runner + probe_github as the reference probe; vendor probe payloads leave per R045 (gamma); SP_ENV indirection per R070. | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R019 | `framework/outbox` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R020 | `framework/frontdoor (send-path core: intake, composer, channel, surface, run_frontdoor, reply_binder, run_briefing, attention_drain)` | germ-keep (90%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R021 | `framework/frontdoor (act-first/undo plane: action_exec, action_undo, actfirst_canary, binder_wire, veto_registry, tell_surface, tell_digest, action_lessons, action_reconcile)` | compress (85%) | beta-compression · B4 | todo | Move _DELEGATE_OFFICERS (action_exec.py:833) to roster config; extract per-kind forward/inverse executors (Monday GraphQL, Apple Reminders, calendar osascript) into extension packs (pairs G1); undo/veto/canary/binder plane stays verbatim. **[PAIR: executor packs ride G1]** | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R022 | `framework/frontdoor (calendar substrate: calendar_read.py, calendar_delete.py, calendar_template.py, calread_helper/)` | extract-pack (10%) | gamma-packs · G1 | todo | Move calendar substrate (calendar_read/delete/template + calread_helper/ + build script) to extensions/calendar-eventkit pack with manifest (undo_contract: delete_window); core keeps only the fail-closed wire contract. | `bash cabinet/scripts/validate-extension.sh <pack> && python3 -m pytest framework/ -q  # core green with pack absent` | git revert (wave tag pre-egg-G1) |
| R023 | `framework/frontdoor (recap/briefing composition: daily_recap.py, morning_synthesis.py, chair_drafts.py)` | compress (40%) | beta-compression · B4 | done | Keep run_briefing orchestration; replace composition with neutral synthesis skeleton enumerating get_source() surfaces; DELETE daily_recap Monday Activity/Reflections legs (boards archived 2026-07-05). | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R024 | `framework/channels (contract.py + mocks.py)` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R025 | `framework/channels (slack.py, teams.py, outlook.py, manifests/)` | extract-pack (25%) | gamma-packs · G1 | todo | Relocate behind the extension seam as an installable pack (manifest + validate-extension.sh). | `bash cabinet/scripts/validate-extension.sh <pack> && python3 -m pytest framework/ -q  # core green with pack absent` | git revert (wave tag pre-egg-G1) |
| R026 | `framework/autoreply` | remove (0%) | beta-compression · B1 | done | Delete framework/autoreply/ __pycache__ husk (untracked bytecode that still shadow-imports; living code moved to instance/flavor-a/ in 228f444f). | `test ! -d framework/autoreply` | none needed (untracked debris) |
| R027 | `framework/ovi` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R028 | `framework/onboarding` | compress (85%) | beta-compression · B4 | done | Lift _CABINET_DEFAULTS=['corridor','brain'] + default lane_mcps into preset config; generalize --board to opaque tracker-ref owned by the task-tracking extension. | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R029 | `framework/products` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R030 | `framework/tests` | germ-keep (90%) | alpha-additive · A | todo | Retain suite + EXTEND the hardcode-ratchet fence from framework/**/*.py to presets/ and .claude/rules/ (Part III EXTRACT item). | `python3 -m pytest framework/tests/test_no_launcher_hardcode.py -q  # green with widened fence` | git revert (additive test change) |
| R031 | `framework/tests/test_amendment_doc_lint.py + test_axes_amendment_doc_lint.py` | compress (40%) | beta-compression · B4 | done | Merge the two dated amendment-lint files into ONE generic amendment-package lint (token present, every germline edit covered, union-scan already at :120). PAIRED with R146: sovereign amendment doc stays as founding amendment (or lint gains a fixture). **[PAIR: R146 / test_amendment_doc_lint.py:25-26,120-137]** | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R032 | `conftest.py` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R033 | `pytest.ini` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R034 | `.layer-separation-baseline` | compress (40%) | beta-compression · B4 | done | Split baseline: BY-DESIGN entries (framework reading Captain-owned instance config: posture/grants/act-first) -> documented allowlist class; debt entries stay shrink-only; the FILE regenerates via the gate script. | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R035 | `framework/authority/tests` | germ-keep (95%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R036 | `framework/frontdoor/tests (minus calendar tests)` | compress (80%) | beta-compression · B4 | done | Neutralize golden fixtures (PolAds headline etc.); keep undo-plane/binder-wire/canary/sov contracts verbatim. | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R037 | `framework/frontdoor/tests/test_calendar_read.py + test_calendar_delete.py` | extract-pack (0%) | gamma-packs · G1 | todo | Calendar tests travel with the R022 calendar-eventkit pack. **[PAIR: R022]** | `bash cabinet/scripts/validate-extension.sh <pack> && python3 -m pytest framework/ -q  # core green with pack absent` | git revert (wave tag pre-egg-G1) |
| R038 | `framework/fidelity/tests (core)` | germ-keep (75%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R039 | `framework/fidelity/tests (F4 clone + retrodiction subset)` | extract-pack (15%) | gamma-packs · G1 | todo | F4 clone + retrodiction tests travel with the R008 flavor-a cells pack (1:1 map to adapter capability). **[PAIR: R008]** | `bash cabinet/scripts/validate-extension.sh <pack> && python3 -m pytest framework/ -q  # core green with pack absent` | git revert (wave tag pre-egg-G1) |
| R040 | `framework/acting/tests` | germ-keep (90%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R041 | `framework/learning/tests` | germ-keep (90%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R042 | `framework/events/tests` | germ-keep (95%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R043 | `framework/missions/tests` | germ-keep (90%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R044 | `framework/probes/tests (core)` | germ-keep (95%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R045 | `framework/probes/tests (vendor probes)` | extract-pack (0%) | gamma-packs · G1 | todo | Move each vendor probe test INTO its probe pack (installing the GitHub/Vercel/Sentry/CI/support probe installs its tests). **[PAIR: R018 keeps harness]** | `bash cabinet/scripts/validate-extension.sh <pack> && python3 -m pytest framework/ -q  # core green with pack absent` | git revert (wave tag pre-egg-G1) |
| R046 | `framework/channels/tests (contract core)` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R047 | `framework/channels/tests (vendor: slack/teams/outlook)` | extract-pack (0%) | gamma-packs · G1 | todo | Vendor channel tests (slack/teams/outlook) travel with R025 channel packs. **[PAIR: R025]** | `bash cabinet/scripts/validate-extension.sh <pack> && python3 -m pytest framework/ -q  # core green with pack absent` | git revert (wave tag pre-egg-G1) |
| R048 | `framework/watchdog/tests` | germ-keep (95%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R049 | `framework/ovi/tests` | germ-keep (90%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R050 | `framework/measurement/tests` | compress (50%) | beta-compression · B4 | todo | Keep test_scenario_runner.py (framework); concrete role-eval tests move with R006 seed relocation. **[PAIR: R006]** | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R051 | `framework/sources/tests` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R052 | `framework/outbox/tests` | germ-keep (95%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R053 | `framework/products/tests` | germ-keep (90%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R054 | `framework/roles/tests` | germ-keep (90%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R055 | `framework/triggers/tests` | germ-keep (95%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R056 | `framework/onboarding/tests` | germ-keep (85%) | beta-compression · B4 | done | Replace real PolAds board id 5092199368 + profile fixtures with neutral fixtures; reword "for Nate to apply" assertion captain-generic. | `python3 -m pytest framework/onboarding -q` | git revert (wave tag pre-egg-B4) |
| R057 | `framework/autoreply/tests` | remove (0%) | beta-compression · B1 | done | Delete framework/autoreply/tests __pycache__ residue together with R026. **[PAIR: R026]** | `test ! -d framework/autoreply` | none needed (untracked debris) |
| R058 | `SETUP.sh` | remove (0%) | beta-compression · B1 | done | Delete SETUP.sh (extinct Hetzner bootstrap; verified zero live refs — only frozen 05-26 analysis prose). | `test ! -f SETUP.sh && CI green` | git revert (wave tag pre-egg-B1) |
| R059 | `bin/` | remove (0%) | beta-compression · B1 | done | No repo change: bin/cabinet-calread is gitignored, rebuilt on target by build-calendar-helper.sh. Egg packaging excludes binaries; keep empty bin/ mount point. | `git ls-files bin/ \| wc -l  # 0` | n/a |
| R060 | `cabinet/env/_template.env` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R061 | `cabinet/env/sensed.env` | instance-split (0%) | beta-compression · B1 | done | Remove cabinet/env/sensed.env from the repo (git-TRACKED live chat id; loaded only slug-conditionally, Sensed project retired). Only _template.env ships. | `git ls-files cabinet/env/sensed.env \| wc -l  # 0` | git revert (wave tag pre-egg-B1) |
| R062 | `cabinet/migrations/` | compress (100%) | beta-compression · B4 | todo | Fold the 1-file cabinet/migrations/ into the consolidated sql home (rides R097). **[PAIR: R097]** | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R063 | `cabinet/host-agent/` | remove (0%) | beta-compression · B2 | done | Delete cabinet/host-agent/ AND remove the CI step in the SAME commit. **[PAIR: .github/workflows/cabinet-ci.yml:326 (host-agent test step) deleted same commit]** | `test ! -d cabinet/host-agent && ! grep -q 'host-agent' .github/workflows/cabinet-ci.yml && CI green` | git revert (wave tag pre-egg-B2) |
| R064 | `cabinet/adapters/` | germ-keep (90%) | beta-compression · B4 | done | Keep cabinet/adapters/README.md (the extract-adapter doctrine); strip stale Hetzner defaults + claims of nonexistent notion//linear//_template dirs. | `grep -c "founders-cabinet\\|Hetzner" cabinet/adapters/README.md  # 0` | git revert (wave tag pre-egg-B4) |
| R065 | `cabinet/deploy/` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R066 | `cabinet/launchd/ (templates + entitlements)` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. _(note: long-term: manifest renderer absorbs duplicate template rows (not blocking))_ | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R067 | `cabinet/launchd/ (concrete plists + generated/ + INSTALL-flip.md)` | instance-split (5%) | beta-compression · B3 | todo | Drop the 26 reproducible concrete plists (officers render from template+roster; services from services.yml). KEEP com.cabinet.gate-apply.plist (hand-authored root LaunchDaemon, germline-enumerated, pinned Disabled by test_gate_apply_dark.py:30-44) + INSTALL-flip.md (arming doc cited by probe_github.py:41, probe_sentry.py:71, probe_vercel.py:63, action_reconcile.py:5). REFUTED-DROP HONORED. **[PAIR: test_gate_apply_dark.py:30-44 + test_germline_lockstep_consistency.py:453 stay green (no CI edit needed if gate-apply kept)]** | `ls cabinet/launchd/*.plist \| grep -v template \| grep -vc gate-apply  # 0; python3 -m pytest framework/learning/tests/test_gate_apply_dark.py -q` | git revert (wave tag pre-egg-B3) |
| R068 | `cabinet/cron/` | compress (85%) | beta-compression · B4 | done | Strip /etc/environment.cabinet sourcing from 6 scripts + 'never DM Nate' literal (limit-reset-watchdog.sh:342) into config; keep the heartbeat/retro/self-improvement growth-loop scripts. | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R069 | `cabinet/scripts/ (materialize + lifecycle + governance core)` | germ-keep (90%) | beta-compression · - | done | Retain in egg as-is. _(note: policy_engine.py pull-down into framework/ (import-close germ layer) = CG-14 (file is schg-locked))_ | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R070 | `cabinet/scripts/ (evidence/verdict-plane runners)` | compress (90%) | beta-compression · B4 | done | Extract SP_ENV screenpipe sourcing from the 7 run-*.sh wrappers into PersonalSource adapter env indirection (clean-room binds NullPersonalSource). Behavior-preserving; verify launchd services still green after edit. | `bash cabinet/scripts/verify-launchagents.sh && grep -rc "\.screenpipe" cabinet/scripts/run-*.sh  # 0` | git revert (wave tag pre-egg-B4) |
| R071 | `cabinet/scripts/hooks/` | germ-keep (90%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R072 | `cabinet/scripts/lib/ (runtime core)` | germ-keep (85%) | beta-compression · B4 | done | Retain runtime core (org_runtime/policy consumption/work_graph/triggers); trim Notion-era affordances from library.sh + library-mcp-client.py. | `python3 -m pytest cabinet/scripts/lib/tests -q` | git revert (wave tag pre-egg-B4) |
| R073 | `cabinet/scripts/lib/ (Spec-039 ETL set)` | remove (0%) | beta-compression · B2 | done | Carve Spec-039 ETL: delete etl-linear.py, etl-github.py, run-etl.py, test_etl_fixtures.py, tests/test_etl_transforms.py, tests/test_run_etl.py TOGETHER. KEEP etl-common.py (live library plumbing — test_library_mcp_client.py:55 loads it). REFUTED-DROP HONORED. No CI edit needed: cabinet-ci.yml:286-289 keeps collecting the remaining 5 lib test files. **[PAIR: etl-common.py stays; ETL test files leave in the SAME change]** | `ls cabinet/scripts/lib/etl-*.py  # only etl-common.py; python3 -m pytest cabinet/scripts/lib/tests -q` | git revert (wave tag pre-egg-B2) |
| R074 | `cabinet/scripts/cutover/` | remove (0%) | beta-compression · B2 | done | Delete cabinet/scripts/cutover/ AND remove the CI step in the SAME commit. **[PAIR: .github/workflows/cabinet-ci.yml:299-302 (cutover pytest step) deleted same commit]** | `test ! -d cabinet/scripts/cutover && ! grep -q 'cutover' .github/workflows/cabinet-ci.yml && CI green` | git revert (wave tag pre-egg-B2) |
| R075 | `cabinet/scripts/ (one-time migrations & backfills, top-level)` | remove (0%) | beta-compression · B2 | done | Delete one-time migrations/backfills AND in the SAME commit re-point gates/gate-3-idempotency.sh:18 + gate-3-idempotency.py:89-95,119 (default --etl-script hardcodes migrate-sources-to-officer-tasks.sh) to a required arg / no-op fixture. **[PAIR: cabinet/scripts/gates/gate-3-idempotency.{sh,py} default-path edit same commit (CI :291-294 keeps gates tests green)]** | `python3 -m pytest cabinet/scripts/gates -q 2>/dev/null \|\| bash cabinet/scripts/gates/gate-3-idempotency.sh --help; ls cabinet/scripts/migrate-* backfill-* 2>/dev/null \| wc -l  # 0` | git revert (wave tag pre-egg-B2) |
| R076 | `cabinet/scripts/ (channel/vendor/OS glue)` | extract-pack (30%) | gamma-packs · G1 | todo | Move telegram-channel glue (officer-inbound-poller, run-frontdoor-briefing), voice-tts, calendar scripts, sentry/vercel glue behind the extension seam. CAUTION: these are LIVE scheduled scripts — move + update services.yml paths in the same change; verify launchctl set unchanged. | `bash cabinet/scripts/verify-launchagents.sh  # all services resolve post-move; launchctl list diff empty` | git revert (wave tag pre-egg-G1) |
| R077 | `cabinet/scripts/task_adapters/` | extract-pack (40%) | gamma-packs · G1 | todo | Keep base.py + task_sync_runner.py + claude-task-bridge.py (the seam); monday/jira/linear/asana concrete adapters become installable packs. | `bash cabinet/scripts/validate-extension.sh <pack> && python3 -m pytest framework/ -q  # core green with pack absent` | git revert (wave tag pre-egg-G1) |
| R078 | `cabinet/scripts/captain-rules/ + cabinet/scripts/meta-cognition/` | germ-keep (95%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R079 | `cabinet/scripts/gates/ + cabinet/scripts/git-hooks/` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R080 | `cabinet/scripts/ (test scaffolding: test-*.sh + tests/ + */tests/)` | compress (60%) | beta-compression · B2 | done | Drop tests whose subject is removed (cutover/tests, test_etl_fixtures.py, ledger-purge/sqlite-mirror tests IF their scripts leave) in the SAME commits as their subjects; fold overlapping test-*.sh. **[PAIR: rides R073/R074/R075 commits]** | `python3 -m pytest cabinet/scripts -q; bash cabinet/scripts/run-hook-regression.sh` | git revert (wave tag pre-egg-B2) |
| R081 | `cabinet/scripts/ (Docker-compose-era duplicates)` | remove (10%) | beta-compression · B5 | todo | DEFERRED delete — REFUTED as-is: docker officer-spawn chain still LIVE. PRE-CHORES first: (a) re-point or retire cabinet/tests/start-officer/{test-args,test-lane-export}.sh + cabinet-ci.yml:187,201 (they invoke start-officer.sh); (b) re-point cabinet-bootstrap.sh:789,857,891 peer-spawn (Dockerfile.officer -> entrypoint.sh:90-91 -> start-officer.sh) to the deploy/docker target or demote spawn to a pack. ONLY THEN delete start-officer.sh, entrypoint.sh, officer-supervisor.sh, Dockerfile.officer + friends. REFUTED-DROP HONORED. **[PAIR: cabinet-ci.yml:187,201 + cabinet/tests/start-officer/* + cabinet-bootstrap.sh:789,857,891 decoupled BEFORE deletion]** | `grep -rc 'start-officer.sh' cabinet/scripts/cabinet-bootstrap.sh cabinet/scripts/entrypoint.sh .github/workflows/cabinet-ci.yml 2>/dev/null  # 0 before delete; then CI green` | git revert (wave tag pre-egg-B5) |
| R082 | `cabinet/scripts/__pycache__ + lib/__pycache__ + lib/.pytest_cache + host-agent/__pycache__` | remove (0%) | beta-compression · B1 | done | Clean __pycache__/.pytest_cache debris (untracked, ~640KB). | `find cabinet -name '__pycache__' -not -path '*node_modules*' \| wc -l  # 0` | none needed (regenerated caches) |
| R083 | `cabinet/admin-bot` | extract-pack (20%) | gamma-packs · G1 | todo | Keep the out-of-band kill/pause PATTERN in core docs; the Telegram+docker/systemd implementation leaves as a pack (not wired into live Mac fleet — zero services.yml/launchd hits). | `bash cabinet/scripts/validate-extension.sh <pack> && python3 -m pytest framework/ -q  # core green with pack absent` | git revert (wave tag pre-egg-G1) |
| R084 | `cabinet/channels/library-mcp` | germ-keep (95%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R085 | `cabinet/channels/redis-trigger-channel` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R086 | `cabinet/dashboard (core console: killswitch/posture/governance/officers/tasks/library/costs/health/gaps/crons/integrations/settings/auth)` | compress (55%) | beta-compression · B4 | done | Re-ground 28 extinct /opt/founders-cabinet path defaults to CABINET_ROOT resolvers; strip Sensed/Nate mock config (src/lib/config.ts:13-16,357); keep the governance console (killswitch/posture/officers/memory). | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R087 | `cabinet/dashboard/src/{lib/provisioning, lib/botfather, app/api/telegram, app/api/cabinets, app/(authenticated)/cabinets, components/cabinets, components/consumer}` | extract-pack (30%) | gamma-packs · G1 | todo | Keep transport-agnostic provisioning core (state-machine.ts, guard.ts, worker.ts, audit.ts, /api/cabinets lifecycle); Telegram/BotFather dialog glue (flow.ts, lib/botfather, app/api/telegram) leaves as a pack. | `bash cabinet/scripts/validate-extension.sh <pack> && python3 -m pytest framework/ -q  # core green with pack absent` | git revert (wave tag pre-egg-G1) |
| R088 | `cabinet/dashboard/{node_modules,.next} + cabinet/channels/*/node_modules` | remove (0%) | beta-compression · B1 | done | No repo change: node_modules/.next untracked (verified git ls-files=0); egg packaging excludes; lockfiles regenerate. | `git ls-files cabinet/dashboard/node_modules cabinet/dashboard/.next \| wc -l  # 0` | n/a |
| R089 | `cabinet/mcp-server` | compress (70%) | beta-compression · B2 | done | Carve host-tools.py + test_host_tools.py (extinct host estate) out of mcp-server; fix CABINET_ROOT default (server.py:60 /opt path). PAIR: adjust cabinet-ci.yml:309/:319 mcp-server test steps in the same commit if host-tools tests are removed. **[PAIR: .github/workflows/cabinet-ci.yml:309,319 adjusted same commit]** | `python3 -m pytest cabinet/mcp-server -q && ! ls cabinet/mcp-server/host-tools.py 2>/dev/null; CI green` | git revert (wave tag pre-egg-B2) |
| R090 | `cabinet/officer-skills` | compress (50%) | beta-compression · B4 | done | Keep dir + 4 generic role files; move cos.txt/comms-officer.txt Nate content to instance overlays; FIX cos.txt:2 dangling pointer — memory/skills/chair-front-door-loop.md is now a TRACKED resource (promoted out of the gitignored evolved/ dir; fresh-hatch Wave A #13). | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R091 | `cabinet/loop-prompts` | compress (25%) | beta-compression · B4 | done | Ship ONE parameterized tick template ({{officer}} slots) carrying the pattern: pull-ratified-outcomes -> gather-then-decide -> work-graph-complete -> captain-attention -> reflection gate; 3 Nate lane prompts move to instance. | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R092 | `cabinet/runbooks` | instance-split (0%) | beta-compression · B1 | done | Move cabinet/runbooks/concierge-install-cabinet.md to the refslund.ai lane (zero inbound refs verified; do not confuse with docs/runbooks/). | `git ls-files cabinet/runbooks \| wc -l  # 0` | git revert (wave tag pre-egg-B1) |
| R093 | `cabinet/starter-spaces` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R094 | `cabinet/customer-templates` | instance-split (0%) | beta-compression · B1 | done | Move cabinet/customer-templates/ to the refslund.ai lane + fix presets/work/agents/coo.md:224,229 dangling pointers (they reference files that do not exist in the dir today). **[PAIR: coo.md pointer fix same commit]** | `git ls-files cabinet/customer-templates \| wc -l  # 0; grep -c customer-templates presets/work/agents/coo.md  # 0` | git revert (wave tag pre-egg-B1) |
| R095 | `cabinet/logs` | instance-split (1%) | beta-compression · B3 | done | Split to instance layer / egg ships slot only. | `git ls-files <path> \| grep -vc .gitkeep  # 0 tracked payload; CI green` | git revert (wave tag pre-egg-B3) |
| R096 | `cabinet/cache` | instance-split (0%) | beta-compression · B3 | done | Split to instance layer / egg ships slot only. | `git ls-files <path> \| grep -vc .gitkeep  # 0 tracked payload; CI green` | git revert (wave tag pre-egg-B3) |
| R097 | `cabinet/sql` | compress (85%) | beta-compression · B4 | todo | Consolidate 17 sql files + cabinet/migrations/ into ~5 idempotent baseline files applied by cabinet-bootstrap.sh + load-preset.sh; INCLUDES 044 content (run AFTER R098 wire-fix lands so the consolidation carries the column). **[PAIR: depends R098 (A-lane) landed first]** | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R098 | `cabinet/sql/044-library-embedding-hardening.sql` | germ-keep (5%) | alpha-additive · A | done | FIX, NOT DELETE (refuted drop — audit misread as unreferenced): wire 044-library-embedding-hardening.sql into load-preset.sh:251-260 AND cabinet-bootstrap.sh:973+ apply lists. Latent bug: dashboard/src/lib/library.ts:496-504,578-596 INSERTs embedded_at; base library.sql lacks the column; ONLY 044 adds it — fresh-hatch DBs break on record create/update today. **[PAIR: load-preset.sh + cabinet-bootstrap.sh apply-list edits (neither file is germline-locked; verified)]** | `grep -n '044-library-embedding-hardening' cabinet/scripts/load-preset.sh cabinet/scripts/cabinet-bootstrap.sh  # both hit; fresh-DB smoke: apply then check library_records.embedded_at exists` | git revert (single additive line per file) |
| R099 | `cabinet/tests/hook-regression` | compress (80%) | beta-compression · B4 | todo | Consolidate the 15 accreted fw-series harnesses into thematic suites; keep germline-readonly.sh (40 BLOCK/16 ALLOW) verbatim; neutralize 2 fixture strings (fw041 repo slug, fw076 org slug). | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R100 | `cabinet/tests/hook-regression/.last-run` | instance-split (0%) | beta-compression · B3 | done | Split to instance layer / egg ships slot only. | `git ls-files <path> \| grep -vc .gitkeep  # 0 tracked payload; CI green` | git revert (wave tag pre-egg-B3) |
| R101 | `cabinet/tests/meta-cognition` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R102 | `cabinet/tests/start-officer` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R103 | `cabinet/tests/watchdog` | extract-pack (0%) | gamma-packs · G1 | todo | Ship watchdog screenpipe probe test inside the flavor-a/screenpipe adapter pack (self-skips on other machines by design). **[PAIR: rides flavor-a pack (R008)]** | `bash cabinet/scripts/validate-extension.sh <pack> && python3 -m pytest framework/ -q  # core green with pack absent` | git revert (wave tag pre-egg-G1) |
| R104 | `constitution/` | compress (25%) | beta-compression · B4 | done | Compress constitution/ to the load-preset assembly base (framework/constitution-base.md + preset addendum is already the runtime source). Retirement TAIL is gated: pre-tool-use.sh:995 write-protect entry (germline-locked) -> CG-15; re-point create-officer.sh:62,139-140 + dashboard governance editor first. **[PAIR: CG-15 for the locked hook entry]** _(note: dir retirement blocked behind CG-15)_ | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R105 | `presets/README.md` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R106 | `presets/_template/` | germ-keep (100%) | beta-compression · B4 | done | Add validate.sh to presets/_template/ (work/portfolio/step-network each carry one; template does not). | `test -x presets/_template/validate.sh` | git revert (wave tag pre-egg-B4) |
| R107 | `presets/work/` | germ-keep (90%) | beta-compression · B4 | done | Scrub pass: 5 Nate literals -> captain_name resolver; mcp__screenpipe out of cos.md tools line; Docker mount refs updated; coo.md Sensed prompt + customer-template pointers fixed (pairs R094). | `grep -rc "Nate\\|screenpipe" presets/work/  # 0 (allowlisted resolver refs only); bash presets/work/validate.sh` | git revert (wave tag pre-egg-B4) |
| R108 | `presets/portfolio/` | germ-keep (88%) | beta-compression · B4 | done | Scrub agents/cos.md:62-64 (@NateHQChairBot, screenpipe intake) + 6 Nate lines to placeholder pattern (validate.sh 4b already fails on leaked deployment data). | `bash presets/portfolio/validate.sh` | git revert (wave tag pre-egg-B4) |
| R109 | `presets/personal/` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R110 | `presets/step-network/` | instance-split (0%) | beta-compression · B3 | done | Move presets/step-network/ to instance layer (superseded by portfolio preset). Residue edits same commit: instance/config/active-preset.example:8 stale slug line; run-frontdoor-briefing.sh:68 + sentry.sh:18,31 step-network env defaults. **[PAIR: active-preset.example + briefing/sentry default edits same commit]** | `git ls-files presets/step-network \| wc -l  # 0; CI green (bootstrap dry-run tests are fixture-based, verified)` | git revert (wave tag pre-egg-B3) |
| R111 | `officers/` | remove (0%) | beta-compression · B1 | done | Delete officers/ (1 tracked stale Hetzner .mcp.json + untracked symlinks; all references lazy-create or guarded — verified). | `git ls-files officers/ \| wc -l  # 0` | git revert (wave tag pre-egg-B1) |
| R112 | `shared/force-push-log.md` | germ-keep (100%) | beta-compression · - | done | REFUTED-DROP HONORED: KEEP shared/force-push-log.md verbatim — enforced mechanism (git-hooks/pre-push requires FORCE_PUSH_ANNOUNCED in it); entries already empty. | `test -f shared/force-push-log.md && grep -q FORCE_PUSH docs 2>/dev/null \|\| true; bash cabinet/scripts/git-hooks/pre-push --check 2>/dev/null \|\| true` | n/a (no change) |
| R113 | `shared/migration-ledger.md` | remove (0%) | beta-compression · B1 | done | Delete shared/migration-ledger.md stub (migrate-to-library.sh mints its own ledger; that script itself leaves in B2/R075 set). | `test ! -f shared/migration-ledger.md` | git revert (wave tag pre-egg-B1) |
| R114 | `shared/backlog.md` | instance-split (5%) | beta-compression · B3 | done | Reduce shared/backlog.md to an empty header stub (SLOT is interface DNA per ROLE_REGISTRY; content is month-stale instance payload). | `wc -l < shared/backlog.md  # header-only (<15 lines)` | git revert (wave tag pre-egg-B3) |
| R115 | `shared/cabinet-framework-backlog.md` | remove (0%) | beta-compression · B1 | done | Archive shared/cabinet-framework-backlog.md out of the repo (277KB container-era mirror; all consumers tolerate absence — drift audit prints not-found and exits 0). | `git ls-files shared/cabinet-framework-backlog.md \| wc -l  # 0; bash cabinet/scripts/audit-framework-backlog-drift.sh  # exit 0` | git revert (wave tag pre-egg-B1) |
| R116 | `shared/interfaces/` | germ-keep (15%) | beta-compression · - | done | Author the egg PACKAGING MANIFEST for shared/interfaces/: each ledger ships as header contract + EMPTY body (captain-vetoes.yml is the model). NEVER truncate live ledgers in-place in this checkout (falsifier-series.jsonl is live + protected-untracked; captain-decisions.md is the Captain pen). Add tracked .gitkeep to reviews/ (verification suggestion). | `packaging manifest exists listing every interface file -> header-only rule; test -f shared/interfaces/reviews/.gitkeep` | n/a (no change) |
| R117 | `shared/interfaces/product-specs/` | instance-split (0%) | beta-compression · B3 | done | git rm the 18 tracked product-specs (zero keep-surface refs verified); dir + .gitkeep stays. | `git ls-files shared/interfaces/product-specs \| grep -vc .gitkeep  # 0` | git revert (wave tag pre-egg-B3) |
| R118 | `shared/interfaces/reviews/ (+ dated evidence/activation files)` | instance-split (0%) | beta-compression · B3 | done | Already untracked (gitignored) — add tracked .gitkeep to shared/interfaces/reviews/ so the FW-019 checkpoint-review target dir ships. | `git ls-files shared/interfaces/reviews/.gitkeep  # tracked` | git revert (wave tag pre-egg-B3) |
| R119 | `shared/interfaces/{cos,cpo,cro,cto}-first-assignment.md` | compress (40%) | beta-compression · B4 | done | Rewrite 4 first-assignment files lane-config-relative (drop Docker-era /workspace/product + Notion/Linear refs), move into presets/work/, delete from shared/interfaces/. | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R120 | `instance/config/ (live values)` | instance-split (5%) | beta-compression · B3 | todo | CI-PAIRED (audit-refuted as staged): FIRST build a shipped fixture instance (e.g. framework/tests/fixtures/instance-testburg/) and re-point the 6 byte-identity guards test_env.py:108,198,254,342,432,496 at it. THEN the egg excludes live values at packaging (this checkout KEEPS its live values — it IS the instance). Mint missing .example twins (platform/roster/directions/probes/warrooms/peers/officer-emails). **[PAIR: framework/tests/test_env.py re-point commit precedes any exclusion; CI :403,:546 stay green]** | `python3 -m pytest framework/tests/test_env.py -q  # green against fixture, not live values` | git revert (wave tag pre-egg-B3) |
| R121 | `instance/config/*.example (8 files + active-preset.example)` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. 2026-07-10 Wave B leftovers: pre-existing twins carrying real personal/deployment values scrubbed to synthetic Testburg placeholders (account-flows / extensions / standing-grants by this agent; posture + trust-ladder re-cut clean by the parallel day-1-legibility slice); real-value grep audit clean; one germline-locked posture-presets comment routed to the next unlock window — see the yml note for full evidence. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R122 | `instance/config/posture-presets/` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R123 | `instance/config/policies/` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R124 | `instance/config/contexts/` | instance-split (12%) | beta-compression · B3 | done | CI-PAIRED: retire the test-org-runtime.sh CI step (cabinet-ci.yml:248) or fixture its two context reads (:43-49 hard-require contexts/captains-cabinet.yml + sensed.yml) FIRST; then lane context files leave at packaging. KEEP contexts/_default.yml (generic investigation_sources consumed via courses-of-action rule §1). **[PAIR: cabinet/scripts/test-org-runtime.sh:43-49 + cabinet-ci.yml:248 edit precedes exclusion]** | `CI green without lane contexts; test -f instance/config/contexts/_default.yml` | git revert (wave tag pre-egg-B3) |
| R125 | `instance/config/projects/` | instance-split (20%) | beta-compression · B3 | done | Keep + refresh _template.yml only (still says Docker mount_path /workspace/product); concrete lane files are instance payload. | `grep -c workspace/product instance/config/projects/_template.yml  # 0` | git revert (wave tag pre-egg-B3) |
| R126 | `instance/config/act-first-surfaces.yml` | compress (40%) | captain-gated · CG-13 | done | File is schg GERMLINE (verified): rewrite as act-first-surfaces.yml.example with empty denylist/cascade_gated + corruption-honesty contract; Nate ruling provenance stays in the Captain ledger. Requires unlock window. | `ls -lO instance/config/act-first-surfaces.yml  # schg re-applied post-edit; example twin exists` | germline amendment reversal + relock |
| R127 | `instance/flavor-a/` | extract-pack (0%) | gamma-packs · - | done | ALREADY DONE: flavor-a extraction IS the shipped seam proof (E4 tier-1 228f444f + source-adapter PASS 2 212fca39; screenpipe allowlist EMPTY, test_no_screenpipe_in_core.py:92). Egg excludes it at packaging; it travels as the model personal-source pack. | `python3 -m pytest framework/tests/test_no_screenpipe_in_core.py -q  # allowlist EMPTY holds` | n/a (landed) |
| R128 | `instance/agents/` | instance-split (0%) | beta-compression · B3 | done | CI-PAIRED (silent-loss class): FIRST give instance/agents/cos/mcp.json — the ONLY copy of the cua-driver MCP overlay merged by start-officer-mac.sh:98-102, no generator recreates it — a template home (cabinet/ or presets/ officer-mcp template). THEN dir leaves at packaging (.md files already regenerable). **[PAIR: template home lands BEFORE any drop; start-officer-mac.sh merge path verified against new location]** | `test -f <new-template-home>/cua-driver.mcp.json; bash cabinet/tests/start-officer/test-args.sh` | git revert (wave tag pre-egg-B3) |
| R129 | `instance/roles/` | instance-split (5%) | beta-compression · B3 | done | Already correct: only .gitkeeps tracked (active/archive/hats); seeded ymls gitignored + regenerated by bootstrap-roles.sh. | `git ls-files instance/roles \| grep -vc .gitkeep  # 0` | n/a |
| R130 | `instance/memory/` | instance-split (3%) | beta-compression · B3 | done | Already correct: tier2 skeleton .gitkeeps tracked, content gitignored. | `git ls-files instance/memory \| grep -vc .gitkeep  # 0` | n/a |
| R131 | `instance/activate-comms-scope.py` | remove (0%) | beta-compression · B1 | done | Archive instance/activate-comms-scope.py (spent one-shot 2026-06-24 amendment applier; zero refs verified after fixing the zsh grep bug). | `test ! -f instance/activate-comms-scope.py` | git revert (wave tag pre-egg-B1) |
| R132 | `CLAUDE.md` | germ-keep (80%) | captain-gated · CG-3 | captain-gated | CLAUDE.md -> framework-base + instance addendum assembled by load-preset.sh (exactly like the constitution). Tier-1/germline doc: Captain-gated split (Part III EXTRACT list). | `load-preset.sh assembles CLAUDE.md; officer boot smoke green` | germline amendment reversal + relock |
| R133 | `README.md` | germ-keep (90%) | beta-compression · B4 | done | Drop the mid-migration Status block (l.71-80) + Legacy Hetzner note (l.146-148) for the egg; quickstart/Safety sections stay verbatim. | `grep -c "Hetzner" README.md  # 0` | git revert (wave tag pre-egg-B4) |
| R134 | `captains-cabinet-guide.md` | germ-keep (95%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R135 | `cabinet-v2.md` | remove (0%) | beta-compression · B1 | done | Archive cabinet-v2.md (superseded by presets/) + RETARGET its 5 citations in the same commit: framework/README.md:38, cabinet/mcp-server/README.md:18, server.py:40, presets/personal/README.md:3, presets/personal/safety-addendum.md:37,56 (inline the normative rule text). **[PAIR: 5 citation retargets same commit]** | `test ! -f cabinet-v2.md && grep -rc "cabinet-v2.md" framework/ cabinet/ presets/  # 0` | git revert (wave tag pre-egg-B1) |
| R136 | `memory/golden-evals (eval-*.md + framework/)` | germ-keep (85%) | beta-compression · B4 | done | Ship eval set minus eval-021-brain-retrieval-quality.md (100% screenpipe — travels with flavor-a pack). CONSTRAINT: the eval-021 file is currently protected-untracked in this checkout (another session owns it) — do NOT touch until its owner commits; then relocate + resolve the numbering collision. **[PAIR: blocked on eval-021 owner-session commit; rides flavor-a pack]** | `git ls-files memory/golden-evals \| grep -c eval-021  # 0 after relocation; run-golden-evals.sh green` | n/a (no change) |
| R137 | `memory/golden-evals/phase-0 + phase-1 + phase-2 + library` | remove (0%) | beta-compression · B1 | done | Delete phase-0/1/2 + library eval scripts (all cd /opt/founders-cabinet — container-dead, zero callers verified). | `ls memory/golden-evals/phase-* memory/golden-evals/library 2>/dev/null \| wc -l  # 0` | git revert (wave tag pre-egg-B1) |
| R138 | `memory/skills (foundation *.md)` | compress (85%) | beta-compression · B4 | done | Deduplicate memory/skills foundation files against .claude/skills SKILL.md bodies (holistic-thinking verbatim twin etc.): single source + pointer; neutralize incidental Nate examples. | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R139 | `memory/skills/evolved` | instance-split (10%) | beta-compression · B3 | done | evolved/ ships EMPTY (.gitkeep). EXCEPTION first: promote captain-pattern-listening.md + captain-intent-inference.md to foundation (CLAUDE.md l.107-108 hard-references them as 4th/5th-loop mechanics). **[PAIR: promotion commit precedes emptying]** | `git ls-files <path> \| grep -vc .gitkeep  # 0 tracked payload; CI green` | git revert (wave tag pre-egg-B3) |
| R140 | `memory/logs` | instance-split (0%) | beta-compression · B3 | done | Split to instance layer / egg ships slot only. | `git ls-files <path> \| grep -vc .gitkeep  # 0 tracked payload; CI green` | git revert (wave tag pre-egg-B3) |
| R141 | `memory/tier3` | instance-split (0%) | beta-compression · B3 | done | Split to instance layer / egg ships slot only. | `git ls-files <path> \| grep -vc .gitkeep  # 0 tracked payload; CI green` | git revert (wave tag pre-egg-B3) |
| R142 | `.remember` | instance-split (0%) | beta-compression · B3 | done | Split to instance layer / egg ships slot only. | `git ls-files <path> \| grep -vc .gitkeep  # 0 tracked payload; CI green` | git revert (wave tag pre-egg-B3) |
| R143 | `docs (root: dated design/result records)` | remove (5%) | beta-compression · B4 | done | Archive dated root docs EXCEPT the 4 de-facto mechanism specs cited ~40x from germ docstrings: authority-matrix-design-2026-06-19, fidelity-harness-design-2026-06-18, cabinet-architecture-cohesive-2026-06-22, meta-cognition-direction-2026-06-25 -> move those into the living subset (R144) so no dangling docstring pointers. **[PAIR: 4-doc carve-out into R144 same commit]** | `grep -rl 'authority-matrix-design-2026-06-19' framework/ \| head -1  # germ docstring citations still resolve post-move` | git revert (wave tag pre-egg-B4) |
| R144 | `docs (living mechanism docs subset)` | germ-keep (80%) | beta-compression · B4 | done | Relocate living mechanism docs next to their organs (framework/docs/ or cabinet/docs/) per docs-track-code; absorb the 4 carved dated specs from R143 (same commit — R143 PAIR); drop hetzner-rollback.md. _(audit fix 07-07: was mis-stamped done — relocation verified NOT landed: no framework/docs/, hetzner-rollback.md still present)_ **[PAIR: R143]** | `test ! -f docs/hetzner-rollback.md && python3 -m pytest framework/ -q  # relocation landed; docstring citations resolve` | git revert (wave tag pre-egg-B4) |
| R145 | `docs/plans` | compress (25%) | beta-compression · B4 | done | Keep the 4 NORMATIVE specs pinned by germline artifacts (cabinet-axes-spec-2026-07-05 §6, source-adapter-boundary-2026-07-05, evolution-engine-spec, sovereign-build-spec); archive migration plans/EXECUTION-STATUS at packaging. NOTE: this plan + its ledger live here as instance coordination artifacts — excluded from egg at packaging. _(note: operative-egg-plan/ledger are instance artifacts, not egg content)_ _(done 2026-07-10: delivered as packaging transform `plans-archive` in `cabinet/scripts/egg-export-manifest.txt` — live tree untouched; yml note has the evidence)_ | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R146 | `docs/proposals` | instance-split (5%) | beta-compression · B3 | done | CI-PAIRED (Ring-0 lint chain): docs/proposals/germline-amendment-*.md MUST ship as the founding amendments (test_amendment_doc_lint.py:25-26 hardcodes the sovereign doc; :120-137 requires EVERY immutable-core entry in the union) — OR the lint gains a fixture via R031 merge. Non-amendment proposals (calendar runbooks etc.) archive. **[PAIR: R031 lint merge decides fixture-vs-founding-amendment; same commit]** | `python3 -m pytest framework/tests/test_amendment_doc_lint.py -q` | git revert (wave tag pre-egg-B3) |
| R147 | `docs/convergence-analysis-2026-05-26` | remove (0%) | beta-compression · B1 | done | Delete (git history preserves). | `test ! -e <path> && CI green` | git revert (wave tag pre-egg-B1) |
| R148 | `docs/patches` | remove (0%) | beta-compression · B1 | done | Delete docs/patches/ AFTER verifying calendar-fast-delete-action_undo.patch is merged/superseded in framework/frontdoor/action_undo.py (verification caution: 07-06 calendar wave left germline patches deferred as docs). **[PAIR: pre-check: diff patch vs action_undo.py current state]** | `test ! -d docs/patches  # only after pre-check recorded in commit message` | git revert (wave tag pre-egg-B1) |
| R149 | `docs/onboarding` | instance-split (0%) | beta-compression · B1 | done | Move docs/onboarding outputs (polads.md, stephie.md — generator products) to instance archive; generator recreates dir on first write. | `git ls-files docs/onboarding \| wc -l  # 0` | git revert (wave tag pre-egg-B1) |
| R150 | `docs/runbooks` | compress (50%) | beta-compression · B4 | todo | Split: gate-apply-runbook.md stays (documents a germ organ, pinned by test_gate_apply_dark); calendar-officer-grant.md rides the calendar pack (R022). **[PAIR: R022]** | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R151 | `.claude/settings.json + .claude/project-config.json.template` | germ-keep (95%) | beta-compression · - | done | Retain (germline-locked enforcement wiring; ${CLAUDE_PROJECT_DIR} portable). Permissions audit deferred to CG-13. _(note: settings.json is schg-locked; permissions-block audit for captain-profile denies folds into CG-13 window)_ | `ls -lO .claude/settings.json \| grep -q schg` | n/a |
| R152 | `.claude/rules (axes-contract, courses-of-action, org-runtime-native)` | germ-keep (100%) | beta-compression · - | done | Retain in egg as-is. | `python3 -m pytest framework/ -q  # suite pins this organ` | n/a (no change) |
| R153 | `.claude/rules/brain-bridge.md` | extract-pack (0%) | captain-gated · CG-3 | captain-gated | brain-bridge.md is schg GERMLINE: fold its PRINCIPLES (personal source read-first; single gated outbound; model-of-captain informs-never-leaks) into the source-adapter interface contract docs via CG-3 unlock; the FILE ships inside the screenpipe/flavor-a pack. | `core docs carry the 3 principles; pack carries the file; relock verified 116+ schg` | germline amendment reversal + relock |
| R154 | `.claude/commands` | germ-keep (95%) | beta-compression · B4 | done | Scrub the one Nate-term hit in cabinet-work-graph-complete.md; commands otherwise generic. | `grep -rc "Nate" .claude/commands/  # 0` | git revert (wave tag pre-egg-B4) |
| R155 | `.claude/skills` | compress (90%) | beta-compression · B4 | done | Dedupe skill bodies against memory/skills (R138 single-source rule); neutralize the 6 Nate-term skills (examples only). **[PAIR: R138]** | `python3 -m pytest framework/ -q && bash cabinet/scripts/run-hook-regression.sh  # green post-edit` | git revert (wave tag pre-egg-B4) |
| R156 | `.claude/agents` | instance-split (0%) | beta-compression · B3 | done | Already correct: derived layer, only .gitkeep tracked; load-preset/sync-agents repopulate. | `git ls-files .claude/agents \| grep -vc .gitkeep  # 0` | n/a |
| R157 | `.claude/worktrees` | remove (0%) | beta-compression · B5 | in-flight | CLEANUP 2026-07-07: 10 merged+clean worktrees removed (cabinet-axes, convergence-v2, de-nate, lane-closure-int, lane-integration, source-adapter-p2, sov-map, scratchpad base-check + germline-window-wt, codex b863) + 21 merged local branches deleted (`origin/feat/germline-window-2` KEPT as window record). Remaining: `convergence` (claude/convergence, unmerged 2026-05-26) — remove when merged or ruled dead. Untracked, excluded from git — packaging unaffected. | `git worktree list  # only main checkout remains (eventually)` | n/a (untracked) |
| R158 | `.claude-plugin` | compress (85%) | alpha-additive · W1A-T4 | done | RIDES W1A-T4: strip live-fleet enumeration from plugin.json description (roster is instance data); parametrize repo/ref in marketplace.json. | `grep -c "polads-ceo\\|stephie-ceo" .claude-plugin/plugin.json  # 0` | git revert (T4 commit) |
| R159 | `.github` | germ-keep (90%) | beta-compression · B5 | done | DEFERRED to egg-packaging: retarget CI branch triggers to the default branch ONLY at packaging time — doing it now kills live CI on feat/fidelity-harness-design. | `packaging-time: on.push.branches == [default]; until then CI keeps running on this branch` | git revert |
| W1A-T1 | `framework/sources protocol widening + rename` | germ-keep (—) | alpha-additive · W1A | done | IN-FLIGHT (being built now): widen PersonalSource Protocol to the off-protocol acting surface (find_threads, gather, draft_fn, still_awaiting, deploy_health, briefing_commitments) + rename nate_replied_since -> captain_replied_since (adapter shim keeps flavor-a working); give null.py honest-empty implementations; guard run_draft_lane.py:271; EXTEND the clean-room-source CI job to EXERCISE the acting surface against Null (imports are not enough — the draft-lane AttributeError proved it). Verified NOT yet landed on this branch (nate_replied_since still at run_draft_lane.py:208). | `grep -rn nate_replied_since framework/ --include="*.py" \| grep -v test \| wc -l  # 0 (shim aside); python3 -m pytest framework/sources framework/acting -q; clean-room CI job drives draft lane vs Null without AttributeError` | git revert (T1 commits; additive protocol change) |
| W1A-T2 | `self-improvement-loop + apoptosis scheduling` | germ-keep (—) | alpha-additive · W1A | in-flight | IN-FLIGHT: promote self-improvement-loop from .template.plist-only to a scheduled service (services.yml row + generate-plists) and schedule the apoptosis reaper cadence — each installed WITH a named trigger + sunset condition (Part III DEMOTE rule: no template plist without a named trigger). | `grep -n 'self-improvement' cabinet/services.yml  # enabled row with sunset:; launchctl list \| grep -c cabinet.self-improvement  # 1` | launchctl bootout + services.yml row revert |
| W1A-T3 | `apoptosis pack (seal-prereq 5)` | germ-keep (—) | alpha-additive · W1A | done | IN-FLIGHT: apoptosis as DATA + a reaper that PROPOSES: sunset: field on services.yml rows / organ manifests, scheduled reaper reads sunsets and emits deletion cards through the EXISTING binder gate — the org never deletes itself; the Captain taps (R10 ruling). services.yml sunset keys need no unlock (file not schg-locked, verified); the extension-manifest sunset FIELD needs CG-4 (schema is germline). **[PAIR: CG-4 for the manifest schema field]** | `reaper dry-run emits a deletion card for a synthetic expired organ; grep -c "auto_delete" framework/ cabinet/scripts/  # 0 (no self-delete path exists)` | git revert (additive); reaper service bootout |
| W1A-T4 | `additive extension-pack rail + marketplace de-Nate` | germ-keep (—) | alpha-additive · W1A | done | IN-FLIGHT: additive pack-loading rail for extensions/ (consumes validate-extension.sh AS-IS — that script is schg-locked, the rail may not edit it) + marketplace/plugin de-Nate (R158). Prerequisite for every gamma-packs move. **[PAIR: gamma lane blocked until this lands]** | `sample pack installs via the rail on a clean checkout; bash cabinet/scripts/validate-extension.sh <sample> passes; grep -c "polads-ceo" .claude-plugin/plugin.json  # 0` | git revert (rail is additive) |
| A2 | `judge-calibration scheduling (D5 step 1a)` | germ-keep (—) | alpha-additive · A | done | Promote the services.yml:50 "future B5" comment to a real enabled service row + rendered plist. Free; accumulation is passive; unscheduled-forever-False judge_verdicts_may_demote() is a wiring defect, not doctrine. Starts the ~7-week calibration clock (critical path). | `grep -n 'judge-calibration' cabinet/services.yml  # enabled row; launchctl list \| grep -c judge-calibration  # 1; test -f ~/Library/'Application Support'/cabinet/judge-calibration-status.json after first run` | launchctl bootout + services.yml row revert |
| A3 | `attention contract in cytoplasm (D6 + delta 6)` | germ-keep (—) | alpha-additive · A | todo | Committed FLOOR: N cards/day (<=5, the ratified sim commitment) the Captain answers at 100%, in instance config, enforced via composer per-tier caps; guaranteed-answer subset selected STRATIFIED-RANDOM across cells (never composer-EV-ranked — anti-curation, Break-5); + the seeded-flaw LABEL canary that exists nowhere today (actfirst-canary is an action canary). Governor ceiling ships too; floor coexists with field-test disturb-MAX. | `instance config carries floor+governor; composer test proves stratified-random selection; one seeded-flaw canary card traverses the loop and is caught` | git revert (config + composer additive) |
| A4 | `cooldown_days re-promotion anti-flap wire` | germ-keep (—) | alpha-additive · A | todo | Wire cooldown_days (schema-validated, consulted NOWHERE at runtime — DOCUMENTED-ONLY) into the graduation re-promotion path. D13 boundary 3: under sovereign, demote->re-promote is the MAIN dynamic; unwired cooldown = flapping cells. Hard precondition for CG-11. | `pytest: demoted cell cannot re-promote inside cooldown_days; framework/fidelity tests green` | git revert |
| A6 | `typed envelope v1 over the existing bus (V 3.6-1, D7)` | germ-keep (—) | alpha-additive · A | in-flight | Envelope schema + validator (~100 LOC) at the registry choke point over the EXISTING Redis/org_events traffic (N=4 officers already exchange untyped messages — retrofit-avoidance, not speculation): ulid id, from/to, six kinds (intent\|evidence\|verdict\|need\|grant_request\|heartbeat), reply_to, provenance, taint {tier,sources}, budget. Taint-join law (worst-of) + trust-non-inheritance land as germline VALUES via CG-9 ledger entry; enforcement checks ship here. | `validator rejects malformed/kind-seven envelopes; existing trigger traffic passes typed; property tests green` | git revert (validator additive; untyped path grandfathered until A12 gate) |
| A7 | `genome/api.py five-verb facade + AST ratchet (D4)` | germ-keep (—) | alpha-additive · A | todo | facade-as-linter, not wrapper: genome/api.py re-exporting sense() act() verify() learn() need() + an AST ratchet "cells import only genome.api" (same pattern as the axes linter). propose() is NOT a verb — the matrix resolves act(intent) to {executed\|proposed\|denied\|need_filed}; verify() requests plane-signed verdicts (append pen, never grade pen). | `python3 -m pytest framework/tests -q  # ratchet green; new cell code importing past the facade turns CI red` | git revert (linter additive) |
| A8 | `promote acted_7d to the watched dashboard (delta 9)` | germ-keep (—) | alpha-additive · A | todo | Surface acted_7d beside cells_graduated + approval-engagement in the falsifier series/dashboard headline — graduation math starves on ACTS as well as labels (07-06 line: acted_7d: 0, approved_7d: 3 on 33 cards, cells_graduated: 0). Also draws the 7-week calibration/label clock as the explicit critical path (delta 12). | `python3 cabinet/scripts/falsifier-report.py --dry-run \| grep -c acted_7d  # headline present` | git revert |
| A9 | `model_version + genome_version stamps (V 3.6-2, Break 10)` | germ-keep (—) | alpha-additive · A | todo | Stamp learned artifacts (skills, playbooks, style profiles, calibration proofs) with model_version + genome_version; mandatory re-proof through the replay gate on either swap. Applies Break 10 to the judge itself: judge-calibration proof carries model_version; core swap invalidates it until re-proven. | `artifact writers emit both stamps; calibration status file carries model_version; swap-simulation test forces re-proof` | git revert |
| A10 | `calibration as continuing control (delta 5)` | germ-keep (—) | alpha-additive · A | in-flight | Recalibration cadence; permanent ~10% stratified-random human re-label audit on machine-confirmed promotions; Wilson lower bound (or larger MIN_PAIRS) instead of point >=0.8 on 20 pairs. NOTE: if the bar FORMULA moves into authority-matrix.yml (germline) it rides CG-10/CG-13; the audit + cadence layers are additive here. **[PAIR: bar-formula changes escalate to CG-10 window]** | `calibration status shows cadence + Wilson bound; re-label audit queue emits ~10% of machine confirms` | git revert |
| A11 | `R8-priors measurement harness (delta 8)` | germ-keep (—) | alpha-additive · A | todo | Run the item-paired replay design on existing golden/regression corpora to MEASURE variance reduction + detectable-effect size before any L3/L4 promotion leans on the n~30-150 prior; re-forecast the month-6 band from ledger data at day-30. Retires the R8 design priors with measurements. | `harness report exists with measured variance-reduction; R8 priors marked calibrated-or-refuted in the plan ledger` | n/a (measurement only) |
| A12 | `bus-hardening acceptance gate before cell #2 (delta 7)` | germ-keep (—) | alpha-additive · A | todo | Before ANY new cell joins: stated delivery semantics (at-least-once + idempotent consumers + backpressure), envelope validator property tests, taint-join / forged-verdict / replay red-team cases in CI (extend the injection gauntlet to the typed bus, Break 3 standard). Blocks cell-division until green. **[PAIR: depends A6]** | `CI carries the red-team envelope suite; division proposals auto-blocked while gate red` | git revert |
| A13 | `execution-state ledger (THIS file pair) (delta 4)` | germ-keep (—) | alpha-additive · A | done | DONE at commit of this plan: docs/plans/operative-egg-ledger-2026-07-07.yml is the coordination surface; every future session updates status/last_update here instead of re-deriving state (step-1-was-half-executed-and-nothing-recorded-it cannot recur). | `python3 -c "import yaml,re;p=chr(124);ids={e['id'] for e in yaml.safe_load(open('docs/plans/operative-egg-ledger-2026-07-07.yml'))['entries']};pids=set(re.findall('^'+re.escape(p)+' ([A-Z][A-Z0-9-]*[0-9-][A-Z0-9-]*) ',open('docs/plans/operative-egg-plan-2026-07-07.md').read(),re.M));assert pids==ids,sorted(pids^ids)"  # exit 0 = ledger parses + 1:1 plan-row coverage  # md copy synced to the yml row's generalized regex 2026-07-11 (hardening loop C11 — the old narrow id-family regex here predated 89 newer id families; the yml row is authoritative)` | git revert |
| A14 | `autonomy-cell id hygiene (Part III step 2; V 3.6-3)` | germ-keep (—) | alpha-additive · A | done | Migrate the legacy officer:officer:cos graduation cell to a clean actor id (single cos lineage); re-attributes the stranded real negative label the gate never reads. State migration WITH backup copy of graduation state first. | `graduation state shows single cos lineage; stranded label visible to the gate; backup file exists` | restore graduation-state backup |
| A15 | `Proof-1 null-hatch single gate (D2)` | germ-keep (—) | alpha-additive · A | done | Compose the clean-room-source CI job + Testburg render into ONE named runnable gate: egg boots with no captain data, no screenpipe, no instance, adapters absent -> NullPersonalSource honest-empties everywhere. _(done 2026-07-07: `cabinet/scripts/null-hatch.sh` + `null-hatch` CI job; the poison probe caught + fixed a latent EACCES crash at `framework/fidelity/retro.py` import — unreadable ≡ absent)_ | `bash cabinet/scripts/null-hatch.sh  # exits 0 on a clean checkout; wired as a CI job` | git revert |
| CG-1 | `D5/D16 promotion-fuel reconcile — BEFORE any posture flip` | germ-keep (—) | captain-gated · CG | done | HIGHEST-STAKES ruling (delta 2). [D16] sovereign verdict_gate fuel is SHIPPED LIVE (consequence.py:735-750, commit d0548359) but unreconciled with D5 calibration-before-machine-fuel sequencing, and D13's sovereign checklist omits the gate. Choose ONE: (A) add "judge-calibration >=0.8 proven AND label-floor-met" to the D13 sovereign activation checklist (verdict_gate stays, gated); or (B) scope verdict_gate fuel to deterministic-inverse machine evidence only — ttl_ok counting ONLY in label-floor-met periods — and reserve the calibration precondition for verdict_judge. One paragraph; decides whether the headline safeguard is real. NO posture.yml (CG-11) until ruled. | `ruling recorded in shared/interfaces/captain-decisions.md; if (A): grep -q 'judge-calibration' <sovereign checklist doc>; if (B): consequence.py comment + test pin the scope` | ruling reversal entry in the same ledger |
| CG-2 | `run_action_lane.py gather rewire (germline unlock)` | extract-pack (—) | captain-gated · CG | done | Unlock-window amendment: replace vault-dir literals (run_action_lane.py:739-755, VAULT at :87) with PersonalSource.open_commitments/search + channel-contract presentation. Pairs R004. Live acting-lane organ: land dark behind a flag, canary one tick, then relock. | `grep -c '6-Commitments' framework/acting/run_action_lane.py  # 0; pytest framework/acting -q; germline-lock.sh verify  # boundary re-armed` | amendment reversal doc + relock; flag off |
| CG-3 | `CLAUDE.md + brain-bridge.md split (germline docs)` | germ-keep (—) | captain-gated · CG | done | Split root CLAUDE.md and .claude/rules/brain-bridge.md into framework-base + instance addendum assembled by load-preset.sh (constitution pattern). Covers R132 + R153. Both are Tier-1/germline docs. | `load-preset assembles both; officer boot smoke green; relock verified` | amendment reversal + relock |
| CG-4 | `extension-manifest sunset-field schema amendment` | germ-keep (—) | captain-gated · CG | done | Add sunset: field to extension-manifest.schema.json (schg-locked) so organ manifests carry death conditions (apoptosis as data). W1A-T3 reaper reads services.yml sunsets meanwhile. | `schema carries sunset field; validate-extension.sh accepts manifests with it; relock verified` | amendment reversal + relock |
| CG-5 | `compile-target decision (delta 11)` | germ-keep (—) | captain-gated · CG | captain-gated | The decision Part II §3 itself declares "decisive and unmade": what the egg compiles TO (Mac-native launchd applianceonly vs docker fleet-cron vs SDK/API-key substrate). Required for the world-facing ambition; currently a hole in the spine. Interacts with the commercialization substrate-PoC (CG-7 gate first). **RULED 2026-07-07 (Captain, this session): compile target = Mac-native launchd appliance on Claude Code CLI (subscription substrate); docker fleet-cron retained as supported axis; Anthropic-API/Agent-SDK substrate swap and SDK PoC DEFERRED until the cabinet is fully commercialization-ready. Packs must stay substrate-portable (no Max-seat assumptions baked in) but no swap work is scheduled. Pending: ledger entry in captain-decisions.md (batch with CG-9).** | `ruling in captain-decisions.md naming the target(s) + the deferred ones` | ruling amendment |
| CG-6 | `push decision for feat/fidelity-harness-design` | germ-keep (—) | captain-gated · CG | done | This branch is the live runtime and is committed-not-pushed by standing constraint. Captain decides when/where to push (off-box copy exists via backups; CRIT-4 exposure otherwise). | `gh pr view / git log origin..HEAD after Captain pushes` | n/a |
| CG-7 | `employer-IP written agreement (commercialization gate)` | germ-keep (—) | captain-gated · CG | captain-gated | NO commercial work (packs, pricing, LOIs, marketplace listing beyond de-Nate hygiene) until the employer-IP / STEP-as-first-customer structure is in writing (90-day action 1). **DEFERRED 2026-07-07 (Captain ruling): employer-IP/STEP structuring deliberately deferred until the cabinet is fully commercialization-ready — commercial lanes frozen BY CHOICE, not awaiting-legal; the day-90 legal-fail clock is SUSPENDED and restarts only when the Captain re-opens commercialization. Engineering hygiene (de-Nate, pack rail, marketplace mechanics) continues; pricing/LOIs/listing/IP work does not.** | `signed written agreement on file; ledger entry` | freeze commercial lanes |
| CG-8 | `mcp-scope.yml dangling host grant removal` | remove (—) | captain-gated · CG | done | Remove the cos `host` MCP grant that no live .mcp.json defines (dead grant = documented attack surface). File is schg germline — batch with the next unlock window (CG-13). | `grep -c 'host' cabinet/mcp-scope.yml  # grant gone; relock verified` | amendment reversal + relock |
| CG-9 | `constitutional-ledger supersession entries (delta 10)` | germ-keep (—) | captain-gated · CG | captain-gated | Captain records in captain-decisions.md: (a) 07-06 germline re-lock (ledger currently lags filesystem); (b) D14 external-comms-grantable-via-charter superseding the 07-04 "structurally non-grantable" clause (org flavor only; per-item approval stays THIS instance's policy); (c) R2 revocation of human-only co-sign on the reversible slice; (d) taint-join + trust-non-inheritance promoted to germline values (D7/D8). | `four dated entries present in shared/interfaces/captain-decisions.md` | ledger correction entry |
| CG-10 | `machine-confirm promotion-fuel amendment (D5 step 3)` | germ-keep (—) | captain-gated · CG | captain-gated | ONLY after CG-1 ruling + calibration proves >=0.8 (A2 running, A3 floor feeding pairs) + A10 controls live: germline amendment admitting calibrated machine confirms as promotion fuel — NARROWEST scope: reversible + undo-covered + deterministic-inverse cells; ttl_ok counts only in label-floor-met periods; calibration evidence in the amendment pack. | `amendment doc passes the lint; first cell graduates on evidence WHILE a deliberate demotion drill still fires (Part III step 7 gate)` | amendment reversal + relock |
| CG-11 | `sovereign posture.yml attestation (D13 activation)` | germ-keep (—) | captain-gated · CG | captain-gated | The D13 activation order, unchanged: germline re-lock DONE -> cooldown wire (A4) + CABINET_NEEDS_WIRED -> verdict-supply green -> CG-1 ruled -> posture.yml written as a Captain attestation ritual, per (task-family x irreversibility class), never one global bit. Teeth before territory. | `framework/authority posture resolution returns sovereign for the attested families; guardian-parity tests green; demotion drill fires` | delete posture.yml -> resolver fail-closes to guardian (proven live) |
| CG-12 | `RATIFY THIS PLAN (beta-compression start gate)` | germ-keep (—) | captain-gated · CG | done | Captain ratifies this operative plan. The ENTIRE beta-compression lane (B1-B5) is blocked until this flips to done. Alpha-additive + in-flight W1A proceed under the 2026-07-07 full-autonomy grant; gamma waits on W1A-T4 regardless. | `ledger entry CG-12 status=done with Captain ack reference` | n/a |
| CG-13 | `germline cosmetic batch (next unlock window)` | compress (—) | captain-gated · CG | done | Batch for the next scheduled unlock window: NATE-DECISION -> CAPTAIN-RULING comment scrubs (authority-matrix.yml, matrix.py); settings.json permissions-block audit (R151); act-first-surfaces.yml example-split (R126); CG-8 host-grant removal; CG-15 pre-tool-use write-protect entry. One window, one amendment doc, relock same day (never hold the boundary open — D3). | `single amendment doc covers the batch; germline-lock.sh verify green same day` | amendment reversal + relock |
| CG-14 | `policy_engine.py pull-down into framework/` | germ-keep (—) | captain-gated · CG | done | Egg-spec bullet 2: pull cabinet/scripts/lib/policy_engine.py (schg-locked, 1,930 LOC, 250-test corpus) down into framework/ so the germ layer is import-closed (gate.py:282 imports upward today). Move preserves bytes; hook path references update; relock at new path. | `python3 -m pytest cabinet/scripts/lib/tests framework/ -q; import-direction crosscut clean; relock verified at new path` | amendment reversal + relock at old path |
| CG-15 | `pre-tool-use.sh constitution write-protect entry edit` | compress (—) | captain-gated · CG | done | Tail of R104 constitution/ retirement: the :995 write-protect entry lives in schg-locked pre-tool-use.sh. Batch with CG-13 window. | `hook-regression suite green after edit; relock verified` | amendment reversal + relock |
| CG-16 | `veto.py wiring into the gate path` | germ-keep (—) | captain-gated · CG | captain-gated | veto.py is built/tested/standalone but NOT wired although the gate already assumes auto_with_veto_window (D14 flags this as a prereq bug for ANY veto-window rung, internal or external). Natural wiring point is the locked policy engine/gate — ride the CG-14 window or land an unlocked-seam wire if one exists (verify first; do NOT work around the lock). | `a queued send with veto window is vetoable by index before expiry; hook-regression + authority tests green` | unwire commit revert + relock |
| P1 | `Proof 1 — null hatch` | germ-keep (—) | alpha-additive · A | done | The egg runs with no captain data, no screenpipe, no instance, no adapters (D2). Built by A15; recorded here as the acceptance event — formal run green 2026-07-09, evidence `docs/plans/proofs/p1-null-hatch-2026-07-09.log` (local-hermetic accepted; CI re-proof on first master push). | `null-hatch gate command exits 0 in CI on clean checkout` | n/a (gate) |
| P2 | `Proof 2 — live re-hatch, no lost human verdicts` | germ-keep (—) | captain-gated · CG | captain-gated | Regenerate the CURRENT organism from egg + migrated evidence with NO lost human verdicts (the strongest falsifier; slots AFTER B3/B4 hygiene, BEFORE the Mini hatch). Captain schedules — touches the live runtime. | `ledger diff: verdict_human row count identical pre/post re-hatch; officers resume; killswitch drill passes` | restore from pre-rehatch backup + tag |
| P3 | `Proof 3 — clean-room Mac Mini hatch (the egg's first cry)` | germ-keep (—) | captain-gated · CG | captain-gated | cabinet-init from a fresh clone on the Mini: running Chair on NullPersonalSource honest-empties; killswitch revocation drill fail-closed; hatch <=1 concierge-day. FREEZE-NEW-ORGANS keys on this proof (D2). If it needs hand-surgery beyond documented activation steps, the egg is not yet an egg. | `timed hatch log <=1 day; Chair live on Null source; revocation drill fail-closed; zero hand-edits outside documented steps` | n/a (falsifiability event) |

## 7. The critical-path clock (delta 12)

The binding constraint is **labels, not code**: calibration needs ~20 judge-vs-human agreement pairs; at ~3 labels/week
that is **~7 weeks** — and it has NOT started (A2 unscheduled). Sequence decisions against this clock: A2 + A3 start the
clock NOW (alpha); everything in beta/gamma is off the critical path and must never preempt label-generating work.
Watch exactly the numbers the verdict named: `cells_graduated` (0), approval-engagement (3/33), and `acted_7d` (0 — A8
promotes it to the dashboard). Field-test disturb-MAX stays; the A3 floor guarantees the pairs accrue inside it (R7 exit
criterion per cell: 20 scored samples banked or explicit ruling).

## 8. Top-10 risk register (highest first)

1. **CG-1** D5/D16 reconcile — the headline safeguard's integrity; wrong order = trust-by-inattention promotions under a future sovereign flip.
2. **CG-2/R004** `run_action_lane.py` gather rewire — germline unlock on the LIVE acting lane; land dark + canary + relock same day.
3. **R081** docker spawn chain — premature delete breaks peer-spawn + CI on the live branch (refuted drop; B5 only after decouple).
4. **R120** instance live values — 6 byte-identity tests + 2 CI jobs go red if dropped unpaired; and this checkout must KEEP its values (it IS the instance).
5. **R098** sql/044 — the one place the audit's delete verdict was factually wrong; un-wired = every fresh hatch ships a broken library organ.
6. **R146** docs/proposals — Ring-0 lint chain; dropping the sovereign amendment doc reds every amendment-lint test.
7. **R067** launchd plists — blanket drop kills `test_gate_apply_dark` + germline lockstep; keep the one hand-authored plist.
8. **R128** `cos/mcp.json` — silent capability loss (no crash, no red test): computer-use wiring vanishes for `drives_computer` officers.
9. **R076/G1** vendor/OS glue pack moves — LIVE scheduled scripts (inbound poller, briefing); a path move without services.yml update strands the fleet's senses mid-flight.
10. **R116** shared/interfaces packaging — live Captain ledgers (`captain-decisions.md`, `falsifier-series.jsonl` — protected-untracked) must never be truncated in-place; empty-body rule applies at PACKAGING only.

## 9. Ledger contract

`docs/plans/operative-egg-ledger-2026-07-07.yml` is THE execution-state surface. Every session that moves a row: update
`status` + `last_update` in the SAME commit as the change; never delete rows (supersede with a note); statuses are
`todo | in-flight | done | captain-gated`. Beta rows may not leave `todo` while CG-12 is not `done`; gamma rows while
W1A-T4 is not `done`. Anything requiring an schg path goes through a CG row — recording a handback beats a workaround.
This file pair is instance coordination data (R145): excluded from the egg at packaging.


## 10. Addendum — Claude Code setup-audit rows (appended 2026-07-07, audit-P0 wave)

Source: `~/cabinet-claude-code-audit-2026-07-07.md` (80-agent audit; finding numbers below are its). Ledger rows AUD-1..AUD-12.
All alpha-additive except AUD-9 (settings.json hook wiring is schg-locked → captain-gated germline window). The audit's
germline-locked one-liners (#2, #4-hook-half, #8–#14, #20, #3b) do NOT get rows — they ride
`docs/proposals/germline-window-addendum-2026-07-07.md` into tonight's unlock window instead.
**Superseded finding recorded:** audit #6 (flip fleet to Fable-5) is SUPERSEDED by the Captain ruling 2026-07-07 pinning
`claude-opus-4-8[1m]`; AUD-2 keeps only the fallback-chain + loud-fallback half.
`AUD-12-R1` was appended 2026-07-16 as a second Captain-gated corrective row; this
supersedes only the row-range/class-count description above, not the original rows or their history.

| id | item | class | lane · wave | status | notes | gate_cmd (read-only) | rollback |
|----|------|-------|-------------|--------|-------|----------------------|----------|
| AUD-1 | `CLAUDE_CONFIG_DIR` officer isolation (audit #1) | additive | alpha-additive · AUD | done | Pilot ONE officer first — auth-store risk: a fresh config home carries no OAuth creds; prove auth + brain MCP before fleet rollout. Kills the 58KB personal-dossier import + 21-plugin contamination per session. | pilot launchctl env shows CLAUDE_CONFIG_DIR; boot green; personal CLAUDE.md/plugins absent from /context | revert launchd env; relaunch |
| AUD-2 | fallbackModel chain + Notification-hook page (audit #6 remainder) | additive | alpha-additive · AUD | in-flight | Pinned to `claude-opus-4-8[1m]` per Captain ruling 2026-07-07 — audit #6 flip-to-Fable is SUPERSEDED (recorded explicitly; do not flip the ~9 pin sites). Chain surfaces fallback loudly, never silently. | fallback chain on launch line; simulated unavailability pages via Notification hook | remove flag; relaunch |
| AUD-3 | hook overhead batch (audit #19) | additive | alpha-additive · AUD | todo | After the cost-ledger decision (#13, germline addendum) — batching depends on where the cost parse lands. | measured pre/post per-call overhead drop; hook-regression green | git revert |
| AUD-4 | skill invocation-control frontmatter sweep (audit #22) | additive | alpha-additive · AUD | done | `disable-model-invocation: true` on act-on-invoke skills; `user-invocable: false` on background law. | grep sweep over 21 SKILL.md files clean | git revert |
| AUD-5 | per-agent `memory:` frontmatter (audit #24b) | additive | alpha-additive · AUD | todo | Native durable role memory; retires officer-skills/*.txt refresh rot for the live roster. | roster agent defs carry memory:; forced-compaction refresh verified | git revert |
| AUD-6 | plugin ships hooks/rules payload (audit #25) | additive | alpha-additive · AUD | todo | Pack wave (rides the gamma pack rail W1A-T4): hooks/hooks.json with `${CLAUDE_PLUGIN_ROOT}`, rules/, count fixes, dup marketplace entry, name-leak scrub. | manifest ships hooks+rules; counts true; dup entry gone | git revert |
| AUD-7 | idle-wake RemoteTrigger spike (audit #26) | additive | alpha-additive · AUD | in-flight | Re-run the 2026-06-25 idle-wake test on 2.1.202 (RemoteTrigger/SendMessage/--bg). GATES the /loop stretch half of AUD-10 (#30). | documented spike verdict; trigger lands on idle session ⇒ scrape plane retire-eligible | n/a (read-only spike) |
| AUD-8 | sandbox pilot one officer (audit #27) | additive | alpha-additive · AUD | in-flight | `sandbox.enabled` + filesystem.denyWrite germline + network.allowedDomains + credentials scrub on ONE officer. | pilot green; germline write denied; off-list egress blocked | disable sandbox; relaunch |
| AUD-9 | StopFailure/TeammateIdle/PermissionRequest hooks (audit #28) | additive | captain-gated · CG | captain-gated | settings.json hook wiring is schg-locked → germline window only. | post-window: limit-hit stamp lands; hook-regression green; relock same day | amendment reversal + relock |
| AUD-10 | named-session continuity + CronCreate (audit #29+#30) | additive | alpha-additive · AUD | todo | `claude -n officer-<role>` / `--resume` per officer (NOT `--continue` — shared cwd bleeds sessions); session crons replace per-need /loop checks; /loop stretch gated on AUD-7. | relaunch resumes named session, no dialog scrape; session_crons readable in Stop hook | revert launch line |
| AUD-11 | `--json-schema` judges (audit #31) | additive | alpha-additive · AUD | done | Structured judge output; kills oauth_llm.py:102-117 parse-retry. Keep OAuth-only auth — NO `--bare` (refuted half). | judge path uses --json-schema; fidelity suite green | git revert |
| AUD-12 | consumer-side trigger ACK (audit #32) | additive | alpha-additive · AUD | done | ACK after processing, not on emit — crash between deliver and wake must not lose the trigger. `readOnlyHint` on library-mcp's 6 reads. | kill-between test keeps trigger; hints annotated | git revert |
| AUD-12-R1 | trigger retention corrective amendment; preserve stable `channel` consumer + unACKed PEL across officer restart | additive | captain-gated · CG | done | Corrective follow-up only; AUD-12 history stays done. Live read-only proof found 16 `cos` receipts owned by `channel` that the current immutable launcher's `XGROUP DELCONSUMER` would make unclaimable. Prepared per 2026-07-07 full-autonomy grant + Captain-approved 72-hour observe-only readiness goal; application still requires Captain unlock/relock. Exact diff and evidence: `docs/proposals/germline-amendment-trigger-retention-2026-07-16.md`. CEREMONY EXECUTED 2026-07-17 (batched w/ CG-31): launcher blob-verified + DELCONSUMER=0 live; 15/15+2/2 suites green post-relock; REAL receipt survived cos restart (same id, channel kept, re-claimed delivery 1→22, zero PEL loss over 50+ min); live ACK deferred by the ACTIVE observe-only soak (officer Bash closed — cos self-reported it), drains when the soak lifts; ACK semantics pinned by the isolated teeth test | two focused tests green; launcher has no channel DELCONSUMER; relock verified; same receipt ID survives live restart, re-delivers, and ACKs exactly once | stop officer with kill switch active; preserve Redis; source-revert only after PEL=0 and a new unlock/relock ceremony |

## 11. Addendum — GENESIS / migration rows (A13 parity, appended 2026-07-07)

One plan-doc row per ledger id, restoring the A13 1:1 yml↔plan invariant for the ids added straight to the yml
during the 2026-07-07 sessions (GENESIS seeds from the captain-decisions officer-notes; CHAIR-REPLY-WIRE from the
one-bot migration closure). Full per-row detail lives in the ledger yml. (ORG-SENSES-1/2 deliberately carried TWO
yml entries each — an AUD-wave additive spec and a GENESIS germ-keep seed — recorded as deliberate at the time;
UNIQUIFIED 2026-07-11 by LEDGER-HYGIENE-1: the GENESIS seeds now carry ids ORG-SENSES-1-GENESIS /
ORG-SENSES-2-GENESIS with supersession notes, and the AUD-wave rows keep the original ids — first-writer
stability. The id set is what A13 pins.) Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| EMBED-SEAM | EmbeddingProvider seam (provider/model/dims per instance; init ASKS for key, voyage recommended; keyless=BM25-degraded fail-soft; index stamped provider/dims + auto re-embed on switch + doctor probe) | germ-keep | alpha-additive · GENESIS | todo | Build the seam. _(2026-07-07 forward-guard already landed: keyless verified no-raise and hardened from silent-EMPTY to lexical-only degrade — `memory_search_lexical` in memory.sh; see row note.)_ | `per-row spec in captain-decisions officer-notes 2026-07-07` | git revert |
| ONBOARD-1 | cabinet-init genesis re-sequence: connect→focus→onboard→org PROPOSES outcomes (interview seed-outcomes superseded; Captain ruling 2026-07-07) | germ-keep | alpha-additive · GENESIS | in-flight | Re-sequence the genesis interview per the ruling. Org-PROPOSES organ + propose-only staging + local-first receipt shipped 2026-07-09 (PC-A); the interview re-sequence + focus-letter half rides WORLD-ONBOARDING-V1B. | `per-row spec in captain-decisions officer-notes 2026-07-07` | git revert |
| ONBOARD-2 | onboarding web-research: company/market/product brief → Library at genesis | germ-keep | alpha-additive · GENESIS | done | Genesis-time research brief lands in the Library. Done 2026-07-09 (PC-A): delivered lane proven with a real CLI brief; IOU lane proven end-to-end (honest queued note, never overwrites delivered). | `per-row spec in captain-decisions officer-notes 2026-07-07` | git revert |
| ORG-SENSES-1 | transcript-digest organ: officer JSONL → cabinet memory+embeddings, feeds skill_induction; flight-recorder output = seed ingest | germ-keep | alpha-additive · GENESIS | done | SHIPPED 2026-07-07: `cabinet/scripts/transcript-digest.py` + `.sh`, services.yml row (04:10 nightly, plist live), 13 tests; gate run = 427 digest rows + search hit + REPORT_ONLY would-propose + 0-taint audit — full evidence on the AUD-wave yml row. | `per-row spec in captain-decisions officer-notes 2026-07-07` | git revert |
| ORG-SENSES-2 | exhaust archival: Redis trigger-stream archive + Chair-DM archive → org memory | germ-keep | alpha-additive · GENESIS | in-flight | Durable org-memory landing for the message plane (capture hooks exist); dual yml entries (AUD spec + GENESIS seed; uniquified 2026-07-11 — the seed row now carries id ORG-SENSES-2-GENESIS). | `per-row spec in captain-decisions officer-notes 2026-07-07` | git revert |
| ORG-SENSES-1-GENESIS | GENESIS seed twin of ORG-SENSES-1 (transcript-digest organ) — re-id'd 2026-07-11 per LEDGER-HYGIENE-1; supersession note on the yml row, AUD-wave row keeps the original id | germ-keep | alpha-additive · GENESIS | done | Same organ, ships in-repo; flight-recorder seed-ingest lane per yml note. | `per-row spec in captain-decisions officer-notes 2026-07-07` | git revert |
| ORG-SENSES-2-GENESIS | GENESIS seed twin of ORG-SENSES-2 (exhaust archival) — re-id'd 2026-07-11 per LEDGER-HYGIENE-1; supersession note on the yml row, AUD-wave row keeps the original id | germ-keep | alpha-additive · GENESIS | in-flight | Trigger-stream half shipped 2026-07-10; Chair-DM half + aged-id spot-check remain per yml note. | `per-row spec in captain-decisions officer-notes 2026-07-07` | git revert |
| ORG-SENSES-3 | continuous coding-activity recording org-native (commits/PRs/Actions increments) | germ-keep | alpha-additive · GENESIS | todo | Org-native coding-activity sense. | `per-row spec in captain-decisions officer-notes 2026-07-07` | git revert |
| PACK-GENERALITY | every integration pack ships {connect, backfill, probes, evidence-classes} — industry-agnostic verdict supply (ad-ops/sales/finance/…) | germ-keep | alpha-additive · GENESIS | todo | Pack contract generalization. | `per-row spec in captain-decisions officer-notes 2026-07-07` | git revert |
| CHAIR-REPLY-WIRE | mechanical ⟦sp:prompt_id⟧ Chair-reply forwarding at the cos-inbound poller → XADD screenpipe:pipe-replies (idempotent by update_id; sp-bridge drains; binder skipped on marker) | germ-keep | alpha-additive · - | done | SHIPPED 2026-07-07: `framework/frontdoor/sp_reply_wire.py` + poller integration; default-ON, kill switch `CABINET_SP_REPLY_WIRE=0`; live-verified (sp-bridge group, self-test XADD). RE-HOMED 2026-07-15 (personal-clean wave): 100%-screenpipe-specific wire glue moved to `instance/flavor-a/flavor_a/screenpipe_reply_wire.py` (behavior byte-identical). | `python3 -m pytest instance/flavor-a/flavor_a/tests/test_screenpipe_reply_wire.py -q` | git revert |
| WORLD-E0A | Cabinet World E0a — census keyframe writer (`cabinet/scripts/world-census.py` → `shared/interfaces/world-chronicle.jsonl`; ratified kickoff `docs/plans/cabinet-world-build-kickoff-2026-07-07.md`) | additive | alpha-additive · WORLD | done | SHIPPED 2026-07-07: daily ~56-int census, falsifier-series discipline (append-only, flock, idempotent per date, closed int/enum schema refuses free text); fenced local reads only; services.yml row + `sunset:` + gui-domain plist + doctor probe #9 (replay-fog class); first live keyframe landed. | `python3.12 -m pytest cabinet/scripts/tests/test_world_census.py -q` | launchctl bootout + row delete + git revert (chronicle jsonl = untracked replay history — archive, never truncate) |
| WORLD-E0B | Cabinet World E0b — world-chronicle daemon (org_events rowid cursor + Redis presence reads + consequence/undo JSONL tail → verb-normalized `shared/interfaces/world/chronicle-*.jsonl` + XADD `cabinet:world:chronicle` + presence snapshot) | additive | alpha-additive · WORLD | done | SHIPPED 2026-07-07: observer-class read-model — plain reads, NEVER consumer groups; PII/secret scrub AT INGEST (field allowlist — payload free text never copied); writes fenced to `cabinet:world:*` + `shared/interfaces/world/`; live-verified (4010 records first minute, presence snapshot, heartbeat, doctor probe #9b); 20 tests. | `python3.12 -m pytest cabinet/scripts/tests/test_world_chronicle.py -q` | launchctl bootout + row delete + git revert |
| WORLD-E0B-MINI | Cabinet World follow-up — the Mini's stream once it hatches (E0b config is single-estate; growth §11 island=cabinet → natural shape is the Mini running its OWN census+chronicle organs, decided at hatch; carries the retroactive-seed check) | additive | alpha-additive · WORLD | todo | Checked 2026-07-07 ~19:15: Mini not yet hatched (no tailnet node, no hatch markers). | `at hatch: Mini events chronicled under its own cabinet_id` | n/a (not started) |
| WORLD-E1 | Cabinet World E1 — the Wardroom (`/world` PixiJS pure renderer + `/api/world/stream` SSE + deterministic director + quantized camera + WHAT/NOW/PROOF inspect + killswitch break-through + CI ratchets; morphology.yml v1 + show-grammar.yml v1 via Captain-merged PR — no grammar auto-merge, ever) | additive | alpha-additive · WORLD | captain-gated | Code SHIPPED 2026-07-07 (build green, 1105 dashboard tests, 7 CI ratchets, opaque `?sel=` handles, CSP pinned, fail-closed "grammar pending" mode); law-and-gate scripts + 23 tests. Captain gates: merge PR #122 (grammar law v1 — 13/13 bindings validated GREEN live; merge lights live motion = acceptance headline), LimeZu purchase (WORLD-E1-ASSETS), bake-off drills. | `cd cabinet/dashboard && npx vitest run src/lib/world && npx tsc --noEmit` | git revert (pure read-model — zero org-state writes) |
| WORLD-E1-ASSETS | Cabinet World E1 asset drop-in — LimeZu packs (Modern Interiors + Exteriors + Character Generator, ~$8; Captain purchase, §12 asset-order amendment: LimeZu day one) | additive | alpha-additive · WORLD | done | DONE 2026-07-08: packs purchased + dropped, gate GREEN (assets=380, all conformant); manifest committed 4b4a2214, binaries gitignored per commercial license; live serving verified (/world 200, manifest 200, sampled sprites 200) after a dashboard rebuild — Next snapshots public/ paths at build time, so rebuild after every asset drop; rendered world stays placeholder-marker until PR #122 merges (grammar fail-closed pending:true); sprite compositing = follow-on task. | `python3.12 cabinet/scripts/world-asset-gate.py` | n/a (render-layer only — removal returns placeholder mode) |

## 12. Addendum — world-alive rows (A13 parity, appended 2026-07-08)

One plan-doc row per WORLD-ALIVE-* ledger id — the world-alive wave (life, coziness, geography, grammar v2),
shipped as the PR #123 union merge `616eece4` citing the Captain's in-session "go all in" ratification 2026-07-08.
Direction doc: `docs/plans/world-alive-direction-2026-07-08.md`. Full per-row detail lives in the ledger yml.
Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| WORLD-ALIVE-LIFE | Behavior vocabulary + director v2 — five desk verbs, seeded idle wander, visible meetings, asleep, killswitch freeze; A* pathing; all variation fnv1a-seeded from (slug, logical tick) — zero wall clock/unseeded RNG (direction §1) | additive | alpha-additive · WORLD | done | SHIPPED 2026-07-08 in merge 616eece4; live aliveness verified per `docs/runbooks/world-qa-verify.md` (clipped page screenshots — never toDataURL). | `cd cabinet/dashboard && npx vitest run src/lib/world` | git revert 616eece4 -m 1 |
| WORLD-ALIVE-COZY | Wardroom interior warmth — LimeZu set-dressing + lighting lamp pools + dawn/day/dusk/night tint keyed to the snapshot clock (direction §2) | additive | alpha-additive · WORLD | done | SHIPPED 2026-07-08 in merge 616eece4; loud-failure badge contract extended to every new asset class. | `cd cabinet/dashboard && npx vitest run src/lib/world/lighting.test.ts && npx tsc --noEmit` | git revert 616eece4 -m 1 |
| WORLD-ALIVE-GEO | Z1 Street + Z0 island scenes + growth read-model (census keyframe → visible sprouts: HQ floors, land radius, fields, harbor beacon); staged-future farm/village packs promoted into the 414-row manifest (direction §3–4) | additive | alpha-additive · WORLD | done | SHIPPED 2026-07-08 in merge 616eece4; scene-swap stuck-black fade fixed 9cec794f; honest zero renders LOUD, never invented. | `python3.12 cabinet/scripts/world-asset-gate.py` | git revert 616eece4 -m 1 (+ manifest rows back out) |
| WORLD-ALIVE-GRAMMAR-V2 | The law delta itself — show-grammar.yml v2 + morphology.yml v2; pixels only via bindings; Captain merge of PR #123 = the ratifying act (direction §5) | additive | alpha-additive · WORLD | done | MERGED 2026-07-08 08:19Z (616eece4) citing the Captain's in-session "go all in"; validator GREEN; CSP byte-identical. | `python3.12 cabinet/scripts/world-binding-validator.py cabinet/world/morphology.yml` | git revert 616eece4 -m 1 |
| WORLD-AESTHETIC-GATE | ONE aesthetic gate for every future frame — `world-aesthetic-gate.py --mechanical\|--full\|--calibrate`: 6 stdlib ratchets + blinded pairwise vision judge (hidden-calibration D5 gate, >=0.90 or VOID) + taste accumulation (Captain rulings → corpus/goldens; licensed pixels gitignored, sha256 manifests tracked); runs per-change, never on cron (services.yml NOT-A-ROW note) | additive | alpha-additive · WORLD | done | SELF-TESTED ON HISTORY 2026-07-08: all 5 rejected-build reconstructions flagged hard (CLUSTER_FLAT_VOID ×5, + PALETTE_FOREIGN_MASS on street-void), 9/9 approved scenes pass; agent-as-judge calibration run 45/45 = 1.000 (floor 0.90), scatter candidate rejected 0/5 vs negatives. | `python3.12 -m pytest cabinet/scripts/world-aesthetic -q && python3.12 cabinet/scripts/world-aesthetic/calibrate.py prove` | git rm -r cabinet/scripts/world-aesthetic (pure gate tooling, zero org-state surface) |

## 13. Addendum — unified-island rows (A13 parity, appended 2026-07-08)

One plan-doc row per unified-island ledger id (2026-07-09: V1 re-staged into WORLD-V1A/WORLD-V1B per spec v2; V2/V3 extended — see §14) — the unified-island wave (one island, two districts:
Harvestholm village = cabinet self-work · Lantern Quay harbor = mission/product work), design ratified by the
Captain 2026-07-08. Direction doc: `docs/plans/world-unified-direction-2026-07-08.md` (spec v1 + Captain-ruling
addendum + feasibility run + gate evidence). Full per-row detail lives in the ledger yml. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| WORLD-V1A | Cabinet World v1a (was WORLD-UNIFIED-V1; re-staged 2026-07-09 per spec v2 `docs/plans/world-unified-spec-v2-2026-07-09.md` §12) — island + commute + mailbox-view + EGG-GROWTH CORE: the ratified v1 scope carries, + egg day-0 (t0 bucket per addendum 1, dark lantern-cairn) + three-class event routing + visible-work pipeline (P-SITES) + chunked unbounded map + `growth-ladders.yml` ERA×RUNG (Captain-tunable, addendum 2) + staged lighthouse + dark lantern posts; killswitch renders state only — ZERO actuators in v1a | additive | alpha-additive · WORLD | done | 07-08 design evidence carries (mockups gate ok:true 0/0, critic 7.5/10, classifier CONFIRMED on 18,114 events); triptych renders gate-GREEN 2026-07-09 (egg re-gated after the addendum-1 bucket). Interiors/inspect/UI-bar/lever split OUT to WORLD-V1B. Prereq: WORLD-GROWTH-CALIBRATION. Open calls carried: P1 source of record, sensor-fog P8, retired-lane render. | six-command bar (spec v2 §12 v1a) × 5 fixtures day AND dusk + determinism replay + commute no-ping-pong replay | git revert (pure read-model — zero org-state writes) |
| WORLD-V1B | Cabinet World v1b (split from WORLD-UNIFIED-V1 per spec v2 §12) — interiors + inspect + UI bar + THE LEVER: universal single-active roof-cutaway (needs D3 amendment — open call #1); 4 flagship truth-rooms; universal clickability + catch-all + coverage ratchet; portrait rail + pixel dialog frames (pack ON-DISK 07-09) + typography law; KILLSWITCH LEVER = the ONE actuator (P-KS, two-tap + confirm + cookie, Great-House wall, shells existing `kill-switch.sh`, chronicle-witnessed); economy readers P-ECO1; P-INT1/2 | additive | alpha-additive · WORLD | todo | Created 2026-07-09 per spec v2 + Captain rulings (lever = ONE actuator; STRICT cost-viz; everything else read-only incl. mailbox). BLOCKED by the D3 doctrine-amendment merge. | prereq WORLD-V1A + D3 merge; six-command bar on cutaway-open/night-card/lever-armed fixtures + lever end-to-end (arm → PULL → active → release; view-only without cookie; API-fail prints fallback; witness event) | git revert (lever is UI sugar over the proven script — revert returns shell-only actuation) |
| WORLD-UNIFIED-V2 | Unified island v2 — EXTENDED 2026-07-09 per spec v2 §12: archipelago canvas 240×192 + fixed bearing-fan anchors + LOD ×1/2+×1/4 + fog/buoys + reef-buoy; TWO-SIGNAL shipping on polads (P-A; depart on real milestone, at-anchor until probe `deploy_ready`, dock on verdict, turn back on rollback) + keyframe-confirm on stephie (P-B = Captain merge); isle rings r2 towns (P2+P3) + r3 work-graph lots (P5); salience config (P4); INFRA layer (P-INF: telegraph + infirmary + recovery walks); bestiary v2 subset | additive | alpha-additive · WORLD | todo | Supersedes the 07-08 scope note where conflicting; ships-never-render-cost; unprobed lanes degrade honestly. READ-ONLY doctrine unchanged. | prereq WORLD-V1B + P2 stamps; six-command bar on archipelago ×1+×1/4 day AND dusk + infra day-one truth render + ship-at-anchor night; ship-phase determinism; doctor-log parse contract-test; anchor spacing vitest | git revert (per-berth stages degrade honestly to V1 aggregate) |
| WORLD-UNIFIED-V3 | Unified island v3 — EXTENDED 2026-07-09 per spec v2 §12: library-with-real-queries (FOUR-library taxonomy, GET-only, corpora never blended) + ECONOMY FULL (P-ECO2 durable cost keyframes → counting-house per R1; P-ECO3 if Captain unlocks germline; P-ECO4 weekly notice) + BESTIARY FULL (P-LAWN, P-BEE, portrait faces from the ON-DISK pack) + P7/P8 as ratified | additive | alpha-additive · WORLD | todo | Feasibility verified: brain/product search live; harbor-record lives in the Harbormaster's hut card (spec v2 §5.2); CI ratchet extends observer-class ban to library surfaces. | prereq V2; GET-only proven in network trace; counting-house hysteresis on synthetic keyframes (no backfill); every $ card cites its exact key/field | git revert (read-only chrome over V2 geography) |
## 14. Addendum — world-next rows (A13 parity, appended 2026-07-09)

One plan-doc row per world-next ledger id — the world-next wave (egg → endless growth, interiors,
bestiary, infra, strict-cost economy, UI pack), synthesized in `docs/plans/world-unified-spec-v2-2026-07-09.md`
(THE ONE SPEC; supersessions §0, staged plan §12, Captain addenda §15). §13's V1 row was re-staged the same
day into WORLD-V1A/WORLD-V1B (rows above). Fresh Captain rulings 2026-07-09: killswitch lever = the ONE
in-world actuator; economy = STRICT cost-viz from the revived cost ledger; growth = VISIBLE WORK.
Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| WORLD-GROWTH-CALIBRATION | Growth calibration harness (Captain addendum 2 — REQUIRED before v1a growth ships): replay real history (137k+ org_events, keyframes, chronicle, outcomes) through candidate era-basket weights + rung curves from `cabinet/world/growth-ladders.yml`; week-by-week timelapse strip; judge on story readability, meaningful era transitions, egg pace, felt current-day maturity; era hysteresis mandatory | additive | alpha-additive · WORLD | done | Captain-tunable as DATA: value edits Captain-editable, schema via grammar PR; ships `world-preview --maturity` so tuning needs no orchestrator. Spec v2 §15.2–15.4. | harness run → timelapse artifact Captain-reviewed; validator refuses untruthful configs (every rung cites a real field); BLOCKS WORLD-V1A growth core | yml value revert returns prior curve; harness read-only over history |
| WORLD-NEXT-GROWTH | Growth grammar track (ships in WORLD-V1A): three-class routing (TEXTURE/QUICK/GREAT), visible-work pipeline (T0 = witness; CLEARING→RAISING→FINISHING→REVEAL pure f(T0, tick); false-start STRIKE), land expansion (trees fall on screen; quay reclamation; isle rings), chunked O(built) map, universal visible ladders (bucket→tank canon), ERA×RUNG | additive | alpha-additive · WORLD | done | Verdicts: 10 ADOPT · 1 ADOPT-STAGED (rings need P3) · 1 ADAPT (great-work T0 = keyframe ts per rate-routing). Triptych gate-GREEN 07-09. Track doc `world-next/growth-grammar.md` + `egg-tile-plan.md`; spec v2 §3 + §15.1–15.4. | rides WORLD-V1A acceptance; prereq WORLD-GROWTH-CALIBRATION | git revert (render + renderer-side world-sites.jsonl only) |
| WORLD-NEXT-INTERIORS | Interiors track (ships in WORLD-V1B): universal single-active cutaway in place (world ticks around the open room); truth-rooms (Library shelves = memory tier, Workshop pegboard = 9 skills vs 18-slot rack, Charter Hall 38 framed rulings + EMPTY veto plinth, cottages ×4 + v1.5 rooms); universal clickability (zero dead clicks) + coverage gauge + validator ratchet | additive | alpha-additive · WORLD | todo | Verdicts (pts 1/2/7): ADAPT (cutaway — needs D3 line; all-roofs-fade + scene swaps REJECTED) · ADOPT-STAGED (truth-rooms — 4 rooms on ZERO new plumbing; P-INT1/2 for depth) · ADOPT (clickability). Track doc `world-next/interiors.md`; spec v2 §5. | rides WORLD-V1B; BLOCKED by D3 amendment (open call #1) | git revert (per-building alpha + render layers) |
| WORLD-NEXT-BESTIARY | Bestiary track (v2 subset in WORLD-UNIFIED-V2, full in V3): water tank = embed_queue XLEN; subagents = transient APPRENTICE figures (addendum 1 supersedes chick mapping; chickens demote to fauna); cows = services herd w/ health via P-INF (DEAD animal walks to infirmary yard, R2); cat/dog decorative-honest; lawn = apoptosis state (P-LAWN); bees = comms.notified flights (P-BEE) | additive | alpha-additive · WORLD | todo | Verdicts (pt 9): 2 ADOPT · 2 ADAPT (tank re-bind; lawn state-not-performance) · 2 ADOPT-STAGED (cow health; point-to-point bees). Population law §15.5: human-shaped sprites = REAL ACTORS ONLY, fauna for joy. Track doc `world-next/bestiary.md`; spec v2 §6. | rides V2/V3 acceptance; manifest rows + asset gate + grammar PR per creature; vet flag AMBER, red killswitch-only | git revert + manifest rows back out |
| WORLD-NEXT-INFRA | Infra track (ships in WORLD-UNIFIED-V2): switchboard hut (doctor heartbeat, stale >26 h ⇒ all grey); 12 telegraph poles/wires, FIVE states dual-coded, humming INVERTED (only the broken wire animates); infirmary + exterior convalescent yard (real degradations only; empty renders prominently empty); wrench overlay via finding_map (nothing silently dropped); recovery = sprite walks home on real DEAD→OK diff | additive | alpha-additive · WORLD | todo | Verdicts (pts 4+11): 3 ADOPT · 1 ADAPT (last-use = honest absence) · 1 ADOPT-STAGED (Sentry/red-CI beds later). Handback: NEON_API_KEY + VERCEL_API_KEY genuinely unresolved — fixing them = first honest recovery-walk (call #9). Track doc `world-next/infra-viz.md`; spec v2 §7. | rides V2; P-INF ONE feed (R2) contract-tested vs live 344-line doctor log (unknown ⇒ grey); day-one truth render = acceptance fixture | git revert (wires = show-grammar procedural pass) |
| WORLD-NEXT-ECONOMY | Economy track — STRICT cost-viz (readers in WORLD-V1B via P-ECO1; full in V3): ledger `cabinet:cost:tokens:daily:<date>` + snapshots, writer `session-stop.sh`; surfaces = ONE home per truth (desk ledgers / Customs board / treasury pin / counting-house on CUMULATIVE post-P-ECO2, R1); "wages from the Captain's treasury" — rent/salary fiction REJECTED; 48 h TTL rendered around (grey, never $0-as-fact, never back-filled) | additive | alpha-additive · WORLD | todo | Verdicts (pt 10): 2 ADOPT · 1 ADOPT-STAGED (counting-house) · 2 ADAPT (per-shipment → aggregate until P-ECO3 Captain unlock, call #3; weekly → daily digest now). Captain's own sessions never draw from the ledger. Track doc `world-next/economy.md`; spec v2 §8. | P-ECO1 rides WORLD-V1B; P-ECO2/4 ride V3; P-ECO3 = germline unlock proposal; every $ card cites its exact key/field (vitest) | git revert (pure readers; keyframe fields additive) |
| WORLD-NEXT-UIPACK | UI-pack track (ships in WORLD-V1B; pack ON-DISK 2026-07-09 — purchase handback VOID, §15.6): portrait rail (composed portraits from Portrait Pieces, fnv1a(officer_id), buildable NOW; stale-verb dimming; unknown never attributes; red never); pixel dialog frames 32x32 dialogs / 16x16 micro-labels (never mixed); typography two-tier law (DOM mono for every numeral); buttons ONLY close/page/killswitch PULL-ABORT | additive | alpha-additive · WORLD | todo | Verdicts (pt 8): 3 ADOPT (frames; lever UI placement-adapted per R4; typography) · 1 ADOPT-STAGED→UNBLOCKED (rail — pack landed 07:05) · purchase RESOLVED-VOID. Linux Portrait app never executed — raw pieces via PIL. Track doc `world-next/ui-pack.md`; spec v2 §9 + §15.6. | rides WORLD-V1B (night-tint card golden; contrast ≥4.5:1); pack install = world-asset-install pattern, binaries gitignored; rail outside world framebuffer | git revert (DOM chrome; derived portraits regenerable) |

## 15. Addendum — Captain-call seal rows (A13 parity, appended 2026-07-09)

One plan-doc row per 2026-07-09 Captain-call ledger id. The call resolved ALL NINE spec-v2 §14 open captain
calls (`docs/plans/world-unified-spec-v2-2026-07-09.md`); verbatim-substance recorded in the
`shared/interfaces/captain-decisions.md` officer-note 2026-07-09 (gitignored runtime ledger). Resolutions #3
and #6 spawn the two governance/hygiene rows below; the build-start approval ("you may actually just finalize
the plan and start implementing") spawns the third. Neon MCP restored the same seal (resolution #9): entry live
in both config layers, doctor GREEN. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| GOV-UNLOCK-UX | Governance UX (resolution #3): germline unlock/lock must become non-Terminal for non-tech captains. CONSTRAINT: unlock is fail-DANGER — the in-world/dashboard flow must terminate in an OS-level auth ceremony (macOS Authorization Services / TouchID-gated signed helper), NEVER a web click; dashboard/world only RENDER lock state + LAUNCH the ceremony. Relock (fail-safe) may be one-tap | additive | alpha-additive · — | todo | Design task created at the seal; mirrors the axes-contract upgrade-ritual doctrine ("go unlock" button = forge vector) and the 2026-07-06 responsible-process-scoped EventKit-helper pattern; `germline-lock.sh` stays the single mutation path (helper shells it). | design doc Captain-reviewed; prototype proves no unlock from a bare HTTP request | n/a (design; shipped helper reverts to Terminal-only) |
| INSTANCE-SENSED-CLEANUP | Instance hygiene (resolution #6): `sensed` = INSTANCE-only test app, never foundation (renders as reef-buoy w/ honest instance-test card). Remove the lane from foundation surfaces/CI: `test-org-runtime.sh:43-60` (sensed.yml check :55-60 — CI-coupled to `instance/config/contexts/sensed.yml`, tolerates absence but binds while present), `test-post-tool-use-jsonl.sh` fixture slug, + test-cabinet-spawn / test-mac-dry-run / run-golden-evals / cabinet-bootstrap / send-to-warroom / send-voice / restart-all-officers-oneshot / `presets/work/agents/coo.md` | compress | alpha-additive · — | done | Decouple ships WITH the CI edit in the same change (never delete sensed.yml first); egg defaults must never ship a sensed lane. done 2026-07-10 (Wave B leftovers): CI check generalized to ANY instance lane (explicit `active: true|false` per present lane context, zero-lanes = pass-with-note egg path); fixtures → synthetic testburg slugs (EVAL-024 probes re-verified against the exact FW-076 regex 18/18 + 6/6); oneshot container list env-driven; residual = 2 comment example-args in germline-locked `captain-posture-warroom.sh:46-47` → routed to CG-18; sanitizer-side capital-S redaction kept deliberately (protective, rides the org-runtime kill list) — full evidence in the yml note. | `grep -rnw sensed framework/ cabinet/scripts/ presets/` → zero lane-coupled hits; test-org-runtime green on live AND sensed-less clean-room checkouts | git revert (foundation half only; instance sensed.yml untouched) |
| WORLD-V1A-BUILD | Cabinet World v1a BUILD in flight per Captain approval 2026-07-09 ("you may actually just finalize the plan and start implementing"): executes WORLD-V1A under spec v2 + §15 addenda; grammar bindings ride PR branch `feat/world-grammar-v3` (merged in Ship citing the approval); determinism ratchets absolute (no Date.now/Math.random in director/renderer, fnv1a(slug,tick), CSP byte-identical, loud-failure badges); every render through `world-aesthetic-gate.py` | additive | alpha-additive · WORLD | done | Build-start row; WORLD-V1A stays the acceptance surface. The nine §14 resolutions bind the build: lighthouse recomposed (lamp fits a proper TOWER, never the water-tank body), lever = ONE actuator (v1b), mailbox = READ-only decision-queue render + deep-link (P1 = dashboard decision-queue), weather bound to real signals w/ hysteresis (sun=doctor+probes green · rain=error/incident rate · fog=telemetry-degraded · storm=killswitch/red), reef-buoys for retired/instance-test lanes, stephie+sensed instance-only, strict cost-viz, visible-work growth, growth-ladders.yml hot-reload + world-preview. | rides WORLD-V1A acceptance (six-command bar × 5 fixtures day AND dusk + determinism replay + commute no-ping-pong; prereq WORLD-GROWTH-CALIBRATION) | git revert of build commits / grammar-v3 PR revert (pure read-model) |

## 16. Addendum — world-onboarding rows (A13 parity, appended 2026-07-09)

One plan-doc row per in-world onboarding ledger id. Captain-ratified direction 2026-07-09: onboarding
happens as much as possible INSIDE the world, joyfully — the sanctioned first write-surface. Design of record
`docs/plans/world-onboarding-hatching-2026-07-09.md` (arrival/interview/errands/first-letter/hatch/security/
staging + WORLD-WRITE-CLASS-1 ruling text §6.5). Honors the GENESIS re-sequence (ONBOARD-1: connect → focus →
onboard → org PROPOSES outcomes — interview seed-outcome questions removed) and frames external-by-nature
steps (BotFather, TCC, germline until GOV-UNLOCK-UX) as honest in-world errands. Captain amendment 2
(2026-07-09, doc §10) adds MULTI-CABINET: registry detection + island naming + per-install genesis gates in
v1b, full N-cabinet coexistence in v2 (spawning the readiness row below), colony-contract peers in v3, and
the shared archipelago (one renderer, N chronicle exhausts, riding WORLD-E0B-MINI — flagship demo: the Mini
hatch renders as a new island rising next to the MacBook island). Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| WORLD-ONBOARDING-V1B | THE HATCHING — in-world onboarding: **Hatch Cabinet.app double-click arrival** (Captain amendment 2026-07-09: ZERO Terminal on the stranger path — native launcher, bundled repo payload, silent dep bootstrap w/ native progress + Retry/Copy-details failure UX, dashboard bound 127.0.0.1, browser to `/world?hatching`; unsigned v1b = documented right-click→Open, signed+notarized = commercialization-lane; idempotent re-launch never re-hatches; `./hatch.sh` = documented Terminal fallback, same engine) → rowboat arrival + ISLAND NAMING at the signpost on landing (amendment 2: island name = cabinet name/slug; captain name → door lintel) → cabinet-init interview at the desk (every question → visible egg-era consequence; answers → the SAME `cabinet-init.answers.yml`; `generate-instance.py` at the Charter countersign; `--adopt` = first-class scene) → errand notes (bot token w/ live getMe + chat-id return leg, Voyage, TCC, germline scope lines, the Launching, healthchecks, germline lock) → FIRST LETTER at the mailbox = ONBOARD-1 focus → THE HATCH: officers arrive by boat on real launchd state incl. the lever Oath (P-d drill); genesis.stamp closes the window, hatching routes 410 forever. MULTI-CABINET (amendment 2, doc §10): `~/.cabinet/registry.json` + `com.cabinet.*` scan → existing world opens, "Hatch a new cabinet…" explicit; per-install genesis gates; shared archipelago = per-cabinet organs, ONE renderer over N chronicle exhausts (rides WORLD-E0B-MINI; Mini island rises next to the MacBook island) | additive | alpha-additive · WORLD | todo | WORLD-WRITE-CLASS-1 (doc §6.5): captain-identity (cookie + reauth OTU), config-plane only (answers/focus via the generator's existing jail + append-only .env backup-first never-echoed), NEVER org-runtime state; every write ledger-echoed; lever stays the ONE actuator; org activation = the launcher's native RAISE click (filesystem-watched .genesis-ready; fallback stdin ENTER), never HTTP. Seed-outcome questions REMOVED per ONBOARD-1; org PROPOSES outcomes as return letters (ratification deep-links out; in-world ratify = WORLD-WRITE-CLASS-2, NOT designed, open call). | prereq WORLD-V1B dialog layer; STRANGER-HATCH bar from the double-click (zip → right-click→Open → watchword → ONE native RAISE, ZERO Terminal, ≤1 admin prompt, ≤90 min, self-recorded, doctor GREEN; re-launch never re-hatches); security suite (410 post-genesis incl. stamp-deleted org-reality case, origin/Host/rate/OTU, zero-secret-echo, .env append-only+backup); byte-identical instance/ vs hand-run skill+generator; six-command world bar on hatching fixtures + determinism replay; cairn stays DARK; multi-cabinet v1b: second launch opens existing world + explicit hatch-new, naming refuses colliding/reserved slugs, registry = full bundle + zero secrets, A's genesis never gates B; A13 parity | git revert (design + row); shipped v1b reverts to skill-in-Terminal onboarding — cabinet-init + mini-hatch runbook unchanged underneath |
| MULTI-CABINET-READINESS | Audit + refactor of single-cabinet hardcodes so N cabinets coexist on one Mac (amendment 2; doc §10.3): Redis VERDICT = DB-index isolation via registry-allocated per-cabinet `REDIS_URL` (NOT key-prefixing — 164 files carry bare `cabinet:*` literals incl. law-cited keys; limits named: db = namespace not auth boundary, FLUSHALL crosses, Cluster forbids db>0; sweep the ~92 hardcoded 6379/bare redis-cli sites); launchd `com.cabinet.<slug>.<service>` (mother keeps bare labels until documented migration); tmux + `/tmp/cabinet-runtime` + `~/Library/Caches/cabinet` namespacing (the cce6e601 residual, promoted to a row); `CLAUDE_CONFIG_DIR` per cabinet (AUD-1 pattern); port + root registry-allocated; one bot per organism; per-clone germline lock | additive | alpha-additive · WORLD | todo | BLOCKS the v2 multi-run half of WORLD-ONBOARDING-V1B (v1b needs only detection+naming+registry, allocation recorded); mother single→multi conversion = documented Captain step, never silent; peers/federation stays DARK until v3 (typed envelopes, trust never crosses, ledgers never shared, propose-only). | mechanical sweep green (every Redis touch honors REDIS_URL; zero bare-label greps outside the mother exception) + LIVE two-cabinet side-by-side drill: both doctors GREEN, zero key/label/port/config-dir collisions, killswitch on A provably does not halt B | git revert per refactor slice; registry rows are data — delete row + bootout the drill cabinet's agents |

## 17. Addendum — improve-how-we-improve rows (A13 parity, appended 2026-07-09)

Captain-approved direction 2026-07-09: sharpen the BUILD PROGRAM itself. The meta-workspace, resume protocol, and
clean-room review live OUTSIDE the repo (`~/cabinet-meta/` — personal instance surface, never tracked or packaged
into the egg; the repo never moves, 30+ plists hold absolute paths into it); the orchestrator-session digest
extension rides the existing ORG-SENSES-1 organ in-repo. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| META-WORKSPACE | `~/cabinet-meta/` orchestration home: CLAUDE.md build-doctrine (standing grant + surviving limits, Fable-for-judgment / officers-on-opus routing, WORKFLOW-FIRST, ledger + A13 discipline, germline etiquette, dirty-guard/exact-path git discipline, aesthetic-harness gate, captain-decisions = write-never-commit), RESUME-BOARD.md (launch/resume convention + ScheduleWakeup-on-limit-death; backfilled: running world-v1a-build `wf_1b999318-08f` + agi-wires-validation `wf_b8f29965-4ba`, completed world-next-levels `wf_4a0b4fc7-029`), bin/cabinet-review.sh (scratch clone of origin/master + throwaway `CLAUDE_CONFIG_DIR`, AUD-1 isolation, cwd above the clone) | additive | alpha-additive · META | done | SHIPPED 2026-07-09; review script live-tested (clean detached clone @ 6c3bb5bf, `{}` config home, invocation printed, zero tokens burned). | `ls ~/cabinet-meta/CLAUDE.md ~/cabinet-meta/RESUME-BOARD.md ~/cabinet-meta/bin/cabinet-review.sh` | rm -rf ~/cabinet-meta (repo untouched) |
| ORCH-SESSION-DIGEST | ORG-SENSES-1 extension: orchestrator/captain-side session JSONLs (project dirs for $HOME + this repo, derived — never hardcoded; `CABINET_ORCH_TRANSCRIPT_DIRS` override) join transcript-digest with provenance `source_kind=orchestrator-session`; officer-boot sessions keep session-jsonl; per-path dedupe; identical taint/scrub/retention pipeline; heartbeat gains `orch=` | additive | alpha-additive · META | done | SHIPPED 2026-07-09 (organ files were CLEAN — not blocked-dirty); services.yml row notes updated in the same commit (docs-track-code). | `python3.12 -m pytest cabinet/scripts/tests/test_transcript_digest.py -q` | git revert (read-only organ; digests supersede-on-redigest) |
| ORG-BUILDS-ORG-PILOT | compile one real build wave — recommend v1b Hatching — as a mission through mission-compile + work-graph; officers execute, orchestrator reviews; acceptance = mission completes + experience/verdict rows minted as exhaust; the label-economy dogfood pilot | additive | alpha-additive · META | in-flight | 2026-07-10: routing half PROVEN — mission compiled + routed 2026-07-09T23:13Z, polads-ceo pickup confirmed zero-manual-nudge, first node (hatch-dialog 3b096340) landed on master via the officer's own lane; acceptance NOT met (mission incomplete, experience/verdict exhaust unminted, pull-plane-only pickup unproven); 4 findings in the ledger row. | ratified mission in outcomes.yml + work-graph tasks via org-status; completion mints experience/verdict rows | n/a (close mission via the work-graph event ledger) |
| NAMED-WORKFLOWS | promote proven orchestration patterns — harness-gated compose loop, 3-lens review, fix-loop — into `.claude/workflows/` named workflows invocable by any session/officer | additive | alpha-additive · META | todo | Created 2026-07-09; today's winning patterns are session-scoped scripts under `~/.claude/projects/*/workflows/scripts/` — invisible to officers and fresh sessions. | named workflow files exist under `.claude/workflows/` + fresh-session invocation works; per-phase review + resume semantics carried | git revert (additive files) |
| CABINET-BUILDS-CABINET | builder-cabinet: hatch a cabinet whose product island IS the captains-cabinet repo — officers ship framework PRs as cargo; prerequisite = ORG-BUILDS-ORG-PILOT success + MULTI-CABINET-READINESS; the §10 registry/archipelago design (`world-onboarding-hatching-2026-07-09.md`) is the substrate | additive | vision-deferred · META | todo | Created 2026-07-09; VISION row in the deferred lane — records the destination, NOT schedulable until both prerequisite rows are done; the org that builds the org: framework development becomes cargo through the same mission/work-graph/verdict economy as every other product island. | prereqs first (ORG-BUILDS-ORG-PILOT done + MULTI-CABINET-READINESS done), then: builder cabinet registered via the §10 registry (own namespace bundle), product island = this repo, ≥1 framework PR shipped as cargo through officer lanes + the normal review gates | n/a while deferred (supersede with a note); at execution: registry row is data — delete row + bootout the builder cabinet's agents; the repo itself untouched (PRs ride normal git review) |

## 18. Addendum — CI-audit rows (A13 parity restored, appended 2026-07-09)

The 2026-07-09 CI-audit wave appended three deferred-work rows to the ledger yml (@ dd6d837d) without their
plan-doc table rows, leaving the A13 gate red. This section restores 1:1 parity — the rows below are derived
mechanically from the committed yml entries (nothing invented here; the yml remains the richer record).
Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| CI-JOB-SPLIT | CI structure — split the monolith `ci` job into parallel jobs (syntax/lint · shell-unit · org-evals · python · dashboard · goldens/hooks) so each gate is a distinct PR check, shared setup via a composite action or reusable workflow; deferred from the 2026-07-09 CI-audit wave, which shipped the cheap 80% instead (`if: !cancelled()` on every independent verification step) | compress | alpha-additive · - | todo | Created 2026-07-09 (CI-audit); the monolith's real cost is visibility + retry granularity, not correctness. When executed: redis service only on jobs that need it; dashboard vitest chained after its npm ci. | PR checks show the split jobs individually red/green; total wall-clock ≤ the monolith's; no gate lost (step-name census before/after matches) | git revert (workflow-only change) |
| CI-MUTATION-GERMLINE | CI hardening — nightly mutmut mutation-testing job scoped to the SAFETY CORE: `framework/authority/` (posture.py resolver + matrix.py verdict tables) + the germline/captain-law checks in pre-tool-use.sh's python helpers; proves the safety tests would actually CATCH a flipped verdict | additive | alpha-additive · - | todo | Created 2026-07-09 (CI-audit item 9 — deliberately NOT wired that wave: mutmut needs scoping + budget tuning or it burns hours and cries wolf); rides the 03:17 UTC schedule event; start authority-only, widen to fence_lib/leak fences once the budget holds. | nightly mutmut run over the scoped modules < 30 min with a surviving-mutant budget; any NEW surviving mutant in authority posture/matrix verdict paths fails the nightly; baseline survivors documented per-mutant | git revert (additive nightly job; no effect on push/PR gates) |
| CI-FIXTURE-FREEZE | Test hygiene — freeze wall-clock-dependent fixtures so the nightly schedule run cannot rot (the audit's test_graduation.py date-drift claim RE-VERIFIED STALE — already frozen); remaining `datetime.now()`/`date.today()` users to review and freeze where they mix wall-clock with absolute dates: test_actfirst_gate · test_sov4_exec_posture · test_action_exec · triggers test_firing/test_registry · test_personal_source_protocol · test_ledger_purge_testrows:334,367,474 · test_world_chronicle:71,175 | compress | alpha-additive · - | todo | Created 2026-07-09 (CI-audit item 6); NOT executed that wave (framework/ files owned by live parallel waves; most listed usages look self-consistent) — the skewed-clock run is the honest discriminator, not eyeballing. | per file: frozen reference time (test_graduation.py pattern) OR a one-line self-consistency justification; proven by a clock-skewed run (`TZ=UTC faketime '+45 days' python -m pytest <files> -q` green, or equivalent freezegun sweep) | git revert per file (test-only edits) |
| CI-IMMUNE-SYSTEM | CI immune system — the 2026-07-09 CI-audit applied + verified END-TO-END on master: SHA-pinned actions + persist-credentials:false · master concurrency-cancel exemption · `if: !cancelled()` gate visibility · gitleaks secret-scanning job (fixture allowlist, nightly full-history) · zizmor workflow analysis (blocking at high) · nightly 03:17 UTC + pip caching · parse-fix 9939a6cf (duplicate cache keys killed every run since 8a3c1f3e at the parser — zero jobs, master pushes got no run) · gitleaks+zizmor added to master required status checks | additive | alpha-additive · - | done | done 2026-07-09; c4077063 ancestry verified before any push; parse defect root-cause-fixed (job never deleted); gitleaks range scan + zizmor high gate pre-verified locally; enforce_admins stays FALSE during the v1a/v1b storm (admin bypass is remote-logged) — flip tracked as CI-STRICT-ENFORCE. | protection GET → 7 required contexts incl. gitleaks+zizmor, strict:true; green master run 29036805182 @ 9939a6cf, all 7 jobs success (first real gitleaks+zizmor executions) | git revert the ci: commits; DELETE the two contexts via the protection API |
| CI-STRICT-ENFORCE | CI governance — flip master `enforce_admins:true` + move routine pushes to PR flow (direct FF master pushes become the exception) after the v1a/v1b build storm settles; until then admin pushes bypass the 7 required contexts with a logged remote notice | compress | alpha-additive · - | todo | Created 2026-07-09 (the deliberate other half of CI-IMMUNE-SYSTEM); deferred on purpose — flipping mid-storm dams the waves or breeds --no-verify habits; timing = Captain's call; pairs with CI-JOB-SPLIT (PR flow wants per-gate checks). | protection GET → enforce_admins.enabled:true; a master push without green required checks is REFUSED (not bypass-logged); routine work observed landing via PR | PATCH enforce_admins back to false (one API call) |
| WORLD-PRIMARY-DASHBOARD | /world = primary dashboard; classic stays as switchable view mode (Captain 2026-07-09) | germ-keep (—) | alpha-additive · WORLD | todo | Routing flip + persisted per-captain view toggle + inspect-card deep-links to classic counterparts; supersedes bake-off-gated takeover sequencing. | auth → / lands on world; toggle persists; classic ≤1 click | git revert |
| SURFACE-PARITY-LAW | every captain capability native on BOTH surfaces from one shared API (world=spatial, classic=panels; cross-deep-links) — Captain 2026-07-09 | germ-keep (—) | alpha-additive · WORLD | todo | Chat rail, decision queue, inspect data, killswitch, library queries, notifications: one data layer, two skins; parity test per capability. 2026-07-10 WARROOM-S1: decision-queue is the FIRST capability shipped under the law with its parity test (`framework/attention/tests/test_surface_parity.py`); world spatial render rides Stage 2. | parity tests green per capability | git revert |

## 19. Addendum — Perfect Cabinet rows (A13 parity, appended 2026-07-09)

The Perfect Cabinet program (HATCH · RECEIPTS · COMMONS), Captain-approved 2026-07-09. Direction doc:
`~/cabinet-meta/DIRECTION-perfect-cabinet-2026-07-09.md` (external orchestration workspace — never tracked in
this repo, never packaged into the egg); grounded in read-only recon `wf_b655f005-f45`. Metric ruling: **TTFR
(time-to-first-receipt) ≤ 5 min** is a NEW demo bar ALONGSIDE the ratified ≤ 90 min WORLD-ONBOARDING-V1B
stranger bar — not superseding it. First receipt = LOCAL-FIRST per the Captain ruling (doctor/proofs + first
briefing with org-proposed outcome cards; Telegram/BotFather becomes a post-receipt errand). Build runs
isolated on branch `feat/perfect-cabinet`; Wave C builds private-side only — publishing stays per-item
Captain-gated on CG-7. The yml rows remain the richer record. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| PC-A | Wave A — HATCH: `hatch.sh` v0 (thin Terminal-fallback orchestrator chaining the twice-rehearsed engine setup-mac.sh → cabinet-init → generate-instance --adopt → activation → P-a..P-d proofs → deploy cos → cabinet-doctor; flight-recorder built in; ORCHESTRATES, never reimplements — line-by-line spec = `docs/runbooks/mini-hatch-tonight-2026-07-07.md` + onboarding design §7.3; the engine of the ratified Hatch Cabinet.app) + timed script(1) clean-room baseline FIRST + critical-path boot diet on setup-mac.sh (fast proofs on path: null-hatch + P-b subset; full suite + dashboard build backgrounded; screenpipe/cua/Chrome behind an opt-in flag) + de-cloud the boot (local Postgres+pgvector default, NEON demoted from CRITICAL; local board default work store, GITHUB_PAT demoted; Telegram demoted to post-first-receipt) + FIRST RECEIPT at hatch end (run_briefing.py + one propose-first card at hatch completion; ONBOARD-1 org-proposed outcomes + ONBOARD-2 genesis brief) + cabinet-init defaults fast-lane (full interview stays for real captains) + fresh-hatch honesty rows (R098 verify/land by running its gate, R120 fixture re-point, INSTANCE-SENSED-CLEANUP, getMe validation loop, P1 formal null-hatch run) | additive | alpha-additive · PC | done | Captain approval 2026-07-09; direction doc at `~/cabinet-meta/DIRECTION-perfect-cabinet-2026-07-09.md` (never packaged); recon `wf_b655f005-f45`; TTFR ≤5 min NEW demo bar ALONGSIDE the ratified ≤90 min WORLD-ONBOARDING-V1B bar — not superseding it. Done 2026-07-09 — wave verified clean-room: TTFR 1s, total hatch 8s, evidence in verifier scratch dir `/tmp/pc-wavea-verify-LH2oom` (verdict pass at 8721d221; P-a..P-c PASS, P-d skip by design; launchd untouched 36→36; propose-only first receipt, 3 draft cards; genesis brief honest-IOU lane — AUD-1 + authenticated-CLI follow-up stays open). | `bash cabinet/scripts/hatch.sh --dry-run --defaults` exits 0; wave bar: timed clean-room stranger hatch (fresh macOS user, AUD-1 pattern, no captain-personal data) ends doctor GREEN + first briefing containing org-proposed outcome cards, TTFR measured and recorded | git revert per slice (hatch.sh orchestrates existing engine scripts, never reimplements them; boot-diet/de-cloud land as discrete revertable commits) |
| PC-B | Wave B — RECEIPTS: complete the receipt grammar (add `why:` + `cost:` to undo-journal rows and render both on the receipt — what / why / cost / undo becomes literal; both data sources exist today, they never reach the receipt) + plain-language action_type→phrase map + one-page how-your-Cabinet-is-governed (constitution → matrix → posture → veto → undo → retro) linked from /governance + /receipts read-only browser over the undo journal (decision-queue-card fetch-render-deep-link pattern) + kill the governance.ts mock-constitution fallback (read the real files, always in the checkout) + day-1 legibility (hatch materializes posture.yml + trust-ladder.yml from .example twins + one clearly-labeled seeded demo receipt in minute one) + feed-quality spec surfaced for Captain review with a build recommendation + hero-demo kit on the synthetic Testburg cabinet (seeded chronicle, tier2 notes, one queued draft, one acted card — no captain-personal or employer data in any public demo) | additive | alpha-additive · PC | done | Captain approval 2026-07-09; direction doc at `~/cabinet-meta/DIRECTION-perfect-cabinet-2026-07-09.md` (never packaged); recon `wf_b655f005-f45`; TTFR ≤5 min NEW demo bar ALONGSIDE the ratified ≤90 min WORLD-ONBOARDING-V1B bar — not superseding it. Integrated 2026-07-10 (Wave-B integrator, five reviewed areas; suites green, leak audit clean; germline handback subset + contract corrections + remaining scope recorded in the yml row note). DONE 2026-07-10 — wave verified clean-room: verdict pass at 193bb00a, TTFR 2s, total hatch 8s, HATCH VERDICT GREEN, evidence in verifier scratch dir `/tmp/pc-waveb-verify-u8bwsP` (receipt grammar live: human phrases + why on all rendered rows + write-time-attributed cost with honest unattributed fallback + 48h undo + trusted ·pid· marker; /receipts 200 with Testburg why/costs + DEMO badge; /governance 200 on the REAL constitution — mock heading dead; day-1 files guardian-default, twins byte-identical, propose-only first briefing with 3 draft cards, demo receipt labeled + never journaled; dashboard vitest 1484 passed, wave pytest 53 passed; leak audit PASS with adjudication — zero real values in generated day-1 files; launchd 36==36 + live repo untouched). Follow-ups recorded in the yml note: hatch --clean-room CABINET_RUNTIME_DIR containment seam (pre-existing, also Wave A), feed-quality spec item (6), Telegram-side germline receipt render handback. | TBD at wave launch — bar: on a fresh hatch the FULL receipt (what/why/cost/undo) renders in the Telegram receipt AND on /receipts; demo kit produces licensing-safe screenshots from synthetic Testburg data only; mock constitution dead | git revert (receipt fields are additive on undo-journal rows; /receipts is read-only; mock-constitution fix reverts to the mock; demo kit is synthetic data + scripts) |
| PC-C | Wave C — COMMONS: the egg as a FRESH-CUT EXPORT, never a repo flip (tracked personal/employer data contaminates git history; exclusion rows R120/R124/R127/R128/R145 = the pre-written scrub list) + publish gate (gitleaks + null-hatch + no-launcher-hardcode + clean-room review of the EXPORT) + un-break the install rail (retarget .claude-plugin/plugin.json + all 7 marketplace.json entries off the dead marketplace source) + public-repo hygiene (SECURITY.md, CONTRIBUTING.md, pre-release banner, fix the README instance/-largely-gitignored overclaim, regenerate captain-rules-index empty + extend the interface gitignore) + licensing-safe visuals (receipt card, dashboard, terminal fleet — NOT LimeZu world art until licensing confirmed) + launch kit as DRAFTS only (Show HN draft, quickstart GIF script, category narrative, comparison page, business-model proposal — BSL 1.1 already reserves the hosted offering) + one exemplar third-party-style pack + authoring walkthrough; publishing captain-gated on CG-7 | additive | alpha-additive · PC | done | Captain approval 2026-07-09; direction doc at `~/cabinet-meta/DIRECTION-perfect-cabinet-2026-07-09.md` (never packaged); recon `wf_b655f005-f45`; TTFR ≤5 min NEW demo bar ALONGSIDE the ratified ≤90 min WORLD-ONBOARDING-V1B bar — not superseding it; build PRIVATE-SIDE, publishing waits on CG-7; Captain-only gates SURFACED never bypassed (CG-7 employer-IP, CG-5 distribution vehicle, colleague-name consent/redaction, LimeZu license). Integrated 2026-07-10 (Wave-C integrator, todo→in-flight; five reviewed areas in one set — export tool + publish gate, install rail + hygiene, launch-kit drafts, pack exemplar, hatch hardening; export verify PASS, publish gate honestly RED per the runbook's expect-RED worklist, suites green, leak audit clean — full evidence in the yml row note; verifier flips to done). DONE 2026-07-10 — wave verified clean-room: verdict pass at 116d12b1 (all 5 PC-C commits), evidence in verifier scratch dir `/tmp/pc-wavec-verify-XjAHIL` — export PASS (egg-export.sh exit 0: 1450 files + egg-manifest.json, 24 deletes + 9 transforms, all 8 procedure assertions hold); publish gate honestly RED 1/4: (a) gitleaks 0 findings, (b) null-hatch inside the export, (c) no-launcher-hardcode all PASS; gate (d) 1927 adjudicated genuine hits = the runbook expect-RED Captain scrub/consent worklist (zero scrub-misses, zero gate bugs; 36 CG-19 `[adj]`-masked coordinate hits — first real firing of the masking path; fail-closed re-proven with gitleaks/pytest off PATH → RED, never skip-pass); clean-room hatch GREEN exit 0, TTFR 1s / total 6s; containment fix proven — live /tmp/cabinet-runtime byte-untouched (sha256 + stat identical), zero launchctl operations, scratch runtime routed into the flight-log area; launch drafts CG-7-headed + export-excluded; pack validate OK with exactly one canonical repo coordinate. The egg is NOT publishable today and the gate correctly blocks it — publishing stays per-item CG-7 Captain-gated regardless of gate verdict. · LICENSING PREMISE SUPERSEDED 2026-07-10 (Wave E relicense fix-pass): the scope parenthetical "BSL 1.1 already reserves the hosted offering" earlier in this row records the BSL-era premise — the Captain's relicensing ruling 2026-07-10 moved the repo to pure MIT (LICENSE + README License/Trademark sections; every BSL-referencing launch draft rewritten), so nothing is reserved and hosted = canonical-not-exclusive (`docs/launch/business-model-proposal.md` §1). The BSL text stays per never-rewrite — this sentence is the correction; the yml PC-C note carries the richer same-date record, including the verified export-fencing of the remaining historical BSL records. | TBD at wave launch — bar: an export artifact passes the publish gate in clean-room review; launch kit complete as DRAFTS; NOTHING goes public without per-item Captain approval | git revert (export tool + hygiene + drafts are additive and in-repo; nothing published — no external state to unwind; the publish flip itself stays CG-7 Captain-gated) |

## 20. Addendum — Wave B leftovers CG rows (A13 parity, appended 2026-07-10)

Two captain-gated germline rows recorded by the Perfect Cabinet Wave B leftovers agent. Both are
unlock-window edits to schg paths (`cabinet/scripts/hooks/` is a germline-locked dir) — never editable from a
build lane, so the exact fixes are recorded here and applied at the next window. The yml rows remain the
richer record. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| CG-17 | germline `cabinet/scripts/hooks/post-tool-use.sh:639` — quote-naive captain_name parse: `awk '{print $2}'` keeps the leading double-quote of a quoted YAML value and truncates multi-word names to the first token (a quoted name renders with a stray leading quote in officer-facing text today) | compress | captain-gated · CG | captain-gated | Next unlock window: append the `tr -d` double-quote strip after the awk, per the `assemble-config.sh:60` idiom (which additionally strips single quotes — mirror both); yields the clean first name; full multi-word capture (a `cut -d: -f2-` style parse) deliberately NOT part of this minimal edit; hook file untouched until the window. | post-window: the :518 pipeline carries the quote-strip; a quoted multi-word captain_name fixture parses with no stray quote; hook-regression green; relock same day | git revert (single-line pipeline change) |
| CG-18 | germline `cabinet/scripts/hooks/captain-posture-warroom.sh:46-47` — comment usage-examples name the retired instance lane as the example context slug (INSTANCE-SENSED-CLEANUP residual; the only remaining whole-word hits of that row's gate grep, both inside the schg hooks dir) | compress | captain-gated · CG | captain-gated | Next unlock window: swap the example arg in both comment lines to the synthetic `testburg` slug, matching the send-to-warroom.sh header examples updated 2026-07-10. | post-window: `grep -rnw sensed framework/ cabinet/scripts/ presets/` → zero hits; comment-only, hook-regression green; relock same day | git revert (comment-only) |

Scoping note (2026-07-10 fix-pass, additive): the sibling demo wave's same-integration doctrine-reference
comment in `cabinet/scripts/demo-dashboard.sh` ("sensed-decouple direction"; :171 at fix-pass time — new
demo-wave-owned file, line may drift) is a third whole-word hit of
the INSTANCE-SENSED-CLEANUP gate grep — a reference to the doctrine, not a lane default, so that row's
zero-lane-coupled gate stands green as written. CG-18's post-window gate reads scoped the same way: zero hits
under `cabinet/scripts/hooks/` plus zero lane-coupled hits tree-wide; the literal tree-wide zero recorded in
the row above is reachable only if the demo area also rewords its comment (optional — not required for
green). The yml notes on both rows carry the authoritative wording.

## 21. Addendum — Wave C ledger-hygiene row (A13 parity, appended 2026-07-10)

One row recorded by the Perfect Cabinet Wave C hatch-hardening agent, closing a follow-up flagged in the
PC-B verifier note: the ledger carries duplicate row ids — ORG-SENSES-1 and ORG-SENSES-2 each appear twice
(an AUD-wave additive spec + a GENESIS germ-keep seed; 260 entries / 258 unique ids when the verifier
flagged it, before this addendum's own row). The §11
header prose records the dual entries as deliberate at the time, but duplicate ids make row addressing
ambiguous and are invisible to the set-based A13 parity gate. The yml row carries the authoritative dedup
procedure (never-delete honored via re-id + supersession notes) and the reasoning for why the row is NOT
captain-gated. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| LEDGER-HYGIENE-1 | `docs/plans/operative-egg-ledger-2026-07-07.yml` — duplicate row ids ORG-SENSES-1 + ORG-SENSES-2 (each twice: AUD wave + GENESIS seed; invisible to the set-based A13 gate; ambiguous row addressing) | compress | alpha-additive · - | done | EXECUTED 2026-07-11 (hardening loop cycle 1) per the sanctioned procedure; both gates exit 0 at commit. Sanctioned dedup per the header's never-delete rule: keep all four rows; re-id the later, thinner GENESIS-seed pair to ORG-SENSES-1-GENESIS / ORG-SENSES-2-GENESIS with supersession notes recording the original ids (the AUD-wave pair keeps them — first-writer stability); same commit adds `\| <ID> \|` plan-doc table rows for the two new ids, updates the §11 dual-entry prose sentence, and re-runs the A13 gate + this row's uniqueness gate. NOT captain-gated: the header contract gates only schg germline paths and Captain rulings/unlocks; supersession notes are its own documented remedy; A14 id-hygiene precedent ran alpha-additive (full reasoning in the yml note). | this row's `gate_cmd` counts ids vs unique ids and fails on any mismatch — RED BY DESIGN until the dedup executes (duplicates today: ORG-SENSES-1 + ORG-SENSES-2); A13 stays green throughout | git revert (id renames + supersession notes + parity lines in one commit; no runtime surface) |

## 22. Addendum — Wave C install-rail adjudication row (A13 parity, appended 2026-07-10)

One captain-gated row recorded by the Perfect Cabinet Wave C install-rail fix-pass agent, resolving the
wave-review must-fix: the PC-C-mandated retarget writes the canonical public install coordinate (the
Captain's own GitHub handle) into the manifests that SHIP in the egg — they are the install rail — while
the publish gate's zero-tolerance real-value scan bans that surname everywhere, with no reconciliation
mechanism: structurally RED, unpublishable even after every genuine residue sweep. The reconciliation is a
RECORDED adjudication — an exact-string allowlist in `cabinet/scripts/egg-publish-gate.sh` whose masked
occurrences always print and persist in the gate report — never a pattern relax. The entry itself is the
captain-gated item: the Captain ratifies or strikes it at the CG-7 publish review, and publishing stays
blocked on CG-7 regardless. The yml row remains the richer record. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| CG-19 | `cabinet/scripts/egg-publish-gate.sh` GATE (d) — adjudicated exact-string allowlist, single entry: the canonical public install coordinate carried by the shipping install-rail manifests (`.claude-plugin/plugin.json` 3 URLs + `marketplace.json` 8 source blocks); masked occurrences always surface ([adj] console lines + grouped report section + PASS-line count), any other banned token on the same line still fails, patterns never relaxed | additive | captain-gated · CG | done | Entry SURFACED (not self-ratified) by the Wave C install-rail fix-pass; the Captain ratifies or strikes it at CG-7 — publishing stays CG-7-blocked either way. Same edit also pins the gate's grep engine (default `/usr/bin/grep`; `EGG_GATE_GREP` override) and adds a planted-sample self-test that fails the whole gate closed on engine mismatch (closes the ugrep/busybox word-pattern fail-open should-fix). Neither the gate script nor this ledger ships in the egg. | `bash -n` green + exactly one allowlist literal in the gate script (exact command in the yml row's `gate_cmd`); the engine self-test runs on every gate invocation | delete the one allowlist entry line — gate (d) returns to zero-tolerance RED on the shipping manifests until they are re-pointed |
| CG-20 | `cabinet/scripts/egg-publish-gate.sh` GATE (d) — pending adjudication for the 4 intended-value surname hits the MIT relicense (Captain ruling 2026-07-10) makes canonical: the `LICENSE:3` copyright line + 3 `captains-cabinet-guide.md` authorship bylines (lines 5/443/445). Supersedes the Wave-E build report's handback (add the verbatim copyright line to `ADJUDICATED_ALLOWLIST`), which is mechanically UNEXECUTABLE: the gate's literal-masking charset contract (line 263, case pattern `*[!A-Za-z0-9./_-]*`) fail-closes on the line's spaces/parentheses, so the verbatim entry aborts every gate run at startup before any scan — verified against the actual case pattern | additive | captain-gated · CG | done | Captain picks an EXECUTABLE path at the CG-7 publish review: (a) allowlist the glob-inert exact token `Refslund` (charset-verified; honest breadth: exact-string masking also covers the 3 intended guide bylines — same adjudication class — and any future token occurrence, with every masked hit still printing as an `[adj]` line in the grouped report, never silent, and any other banned token on the same line still failing); or (b) amend the literal-masking charset contract to admit spaces/parentheses so the full exact line can be the entry (narrowest masking) — a GATE EDIT that rides this same adjudication mechanism with its own adversarial review, never a quiet relax. Until ruled: gate (d) stays honestly RED on these 4 hits (fail-closed by design) and publishing stays CG-7-blocked regardless of verdict | post-ruling verify per the yml row's `gate_cmd`: option (a) = `bash -n` green + the one new entry/rows pair present + a gate run passing the charset guard with the 4 hits as `[adj]` lines; option (b) = reviewed amendment artifact recorded before any gate edit lands | nothing to unwind until ruled; (a) = delete the entry + its `ADJUDICATED_ROWS` twin (CG-19 pattern); (b) = revert the reviewed amendment commit |

*(CG-20 row appended 2026-07-10 by the Wave-E relicense fix pass — same column format; the yml row remains the richer record.)*

## 23. Addendum — Wave D rows (A13 parity, appended 2026-07-10)

Perfect Cabinet Wave D (APP EXPERIENCE), Captain-approved 2026-07-10; spec =
`~/cabinet-meta/designs/DESIGN-app-feel-2026-07-10.md` REV 2 (external orchestration workspace — never
tracked here, never packaged). From the app-feel area: one build row (hatch tail + dashboard PWA + serve-bind
plumbing) and one captain-gated row (CC-LOOP, the consolidated dashboard-bind-default call — the
CABINET_DASHBOARD_HOST mechanism shipped default-preserving at 0.0.0.0; only the default FLIP is gated).
Parallel Wave-D areas append their own rows to this table (attribution in the row footnotes).
The yml rows remain the richer record. Column format matches §6.
*(Update 2026-07-12: the CC-LOOP ruling landed — "flip it", option (i)+(iii) hybrid — and the flip
shipped as the `dashboard(OC-LOOPBACK)` integration commit on `feat/loopback-limezu` (locate:
`git log --grep OC-LOOPBACK`); the CC-LOOP row below is flipped to done with
a supersede note. The "captain-gated" framing in this intro is the 2026-07-10 historical record.)*

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| PC-D-APPFEEL | Wave D app-feel area — hatch.sh app-feel tail (plan step 15: hoisted `http://127.0.0.1:<port>/` dashboard URL + Add-to-Dock/Install hints in WHERE-THINGS-LIVE; `--with-launchd`: `/api/health`-probed auto-open + validated `~/Applications` `.webloc` (tmp → `plutil -lint` → mv), SSH/`HATCH_NO_OPEN` escapes, clean-room skip, set-e-safe non-fatal tail) + dashboard PWA readiness (`manifest.ts` metadata route; ORIGINAL compass-seal `icon.svg` — pure geometry, zero LimeZu — + `gen-icons.mjs` on in-tree sharp + committed PNGs 192/512/512-maskable/apple-180; `layout.tsx` viewport+appleWebApp; middleware matcher excludes EXACTLY the five install surfaces; unauthenticated `GET /api/health` liveness making the pre-existing `services.yml` expectation true) + `start-dashboard.sh` D4a PORT-precedence fix (explicit env > `cabinet/.env` > 3100, captured before the `set -a` sourcing) + `CABINET_DASHBOARD_HOST` plumbing with DEFAULT 0.0.0.0 PRESERVED (flip = CC-LOOP) + egg-manifest expect-present rows + docs-track-code (hatch-v0 runbook, mac-mini runbook, deploy-mac.sh comment) | additive | alpha-additive · PC | done | Built 2026-07-10 in worktree `feat/perfect-cabinet` (integrator commits). SPEC DEVIATION recorded in the yml note: the spec's captain-gated D4b landed as mechanism-only with the current 0.0.0.0 default per the Wave-D launch standing constraint (OC-LOOPBACK pending); only the default flip stays gated. Honesty: probe-gated open, AMBER on timeout, forced-failure exit-0 tests, favicon.ico 404 documented, Safari Add-to-Dock cookie-copy = manual v0.5 test item, weekly re-login stated plainly, no zero-Terminal claim. | `python3.12 -m pytest cabinet/scripts/tests/test_hatch_app_feel.py cabinet/scripts/tests/test_dashboard_pwa_static.py cabinet/scripts/tests/test_start_dashboard_bind.py -q` + dashboard `npx vitest run src/app/pwa.test.ts` + dead-name grep clean (exact command in the yml row) | git revert (all additive; the hatch-generated `.webloc` is instance-side and never ships) |
| CC-LOOP | dashboard bind DEFAULT — the consolidated Captain call (replaces COMPANION OC-1 / app-feel rev-1 call 1 / HATCHAPP mini check; a.k.a. OC-LOOPBACK). Mechanism (`CABINET_DASHBOARD_HOST`) shipped in PC-D-APPFEEL with default KEPT at 0.0.0.0 (live box reached over Tailscale); THIS row gates only the default flip. Options: (i) loopback default + mini opt-out `CABINET_DASHBOARD_HOST=0.0.0.0` with the runbook migration step run before deploy; (ii) keep 0.0.0.0 default, loopback opt-in (= shipped state); (iii) loopback default + `tailscale serve` fronting (unlocks remote PWA install later). Prepared flip = one composition-default line + deploy-mac.sh sentence + runbook migration step + bind-test assertion, ONE commit when ruled | additive | captain-gated · CG | done | Recorded 2026-07-10 by the Wave-D app-feel area — SURFACED, not self-ratified. Until ruled the bind default is unchanged; nothing else in Wave D depends on it (a 0.0.0.0-bound server still serves every printed/bookmarked/manifest 127.0.0.1 URL). Retired bind-var spellings are gate-checked to appear nowhere (bind-test dead-name gate + PC-D-APPFEEL grep). SUPERSEDED 2026-07-12 — Captain ruled "flip it": option (i)+(iii) hybrid (loopback default 127.0.0.1; `tailscale serve` = the blessed remote path; `CABINET_DASHBOARD_HOST=0.0.0.0` opt-out in `cabinet/.env` for plain tailnet/LAN reach). Flip landed as one revertable `dashboard(OC-LOOPBACK)` integration commit on `feat/loopback-limezu` (code + repinned bind tests + docs + this row's flip, same commit; the `world(LIMEZU)` credits commit rides the same integration set, separate ratification; locate both: `git log --grep OC-LOOPBACK` / `--grep LIMEZU`). Code-side gate verified 2026-07-12: default = 127.0.0.1 at `start-dashboard.sh:55`, bind suite 8 passed. Mini-side gate items stay OPEN as deploy-time obligations: `lsof` bind check on the mini; migration step (0.0.0.0 opt-out into the mini's `cabinet/.env`, or `tailscale serve` fronting) BEFORE this deploys or plain-tailnet reach is lost. The "until ruled" clauses here and the 2026-07-10 standing lines in PC-D-APPFEEL / PC-D / the §23 intro are historical records superseded by this note. The yml row remains the richer record. | post-ruling read-only verify: HOST composition default matches the ruling; `lsof -iTCP:3100 -sTCP:LISTEN` on the mini shows the ratified bind; migration step preceded the flip if reach must persist | nothing to unwind until ruled; the flip lands as one revertable commit — landed 2026-07-12: revert the `dashboard(OC-LOOPBACK)` integration commit to restore the 0.0.0.0 default |
| PC-D-COMPANION | Wave D companion area (D1) — menu-bar companion, the Captain's hand: `cabinet/companion/main.swift` (single-file AppKit `NSStatusItem` accessory; `--smoke`/`--version` argv dispatch; five honest SHAPE-encoded template-icon states from cheap loopback `redis-cli` reads with 2s kill-timeouts — doctor click-only, never on the poll loop; boots AMBER "no data yet", 120s staleness renders AMBER; officers submenu; 1s-probe-gated Open Dashboard/World; background Run Doctor → companion log + notification; Services submenu via static root-stamped wrappers, every Terminal-opening item labeled "(opens Terminal)"; hatch offer runtime-guarded on `hatch.sh` existing; kill-switch lever = explicit verbs + fresh pre-read refusal + typed STOP/RESUME confirm + shell of the locked `kill-switch.sh` with loopback `REDIS_URL` pinned + mandatory GET post-verify; `SMAppService` login item DEFAULT OFF; notification degrade to menu line; App Nap activity token held for lifetime; anchored `CABINET_DASHBOARD_PORT=` line-scan of `cabinet/.env`, never sourced; BIND-AGNOSTIC — reads no dashboard bind variable, grep-pinned) + `Info.plist` (LSUIElement, stable `com.cabinet.companion`) + `README.md` (degradation table, opens-Terminal honesty, NOT-V1B disclaimer) + `cabinet/scripts/build-companion.sh` (swiftc on-target, `plutil -lint`, ad-hoc codesign; bundle gitignored — calread precedent) + `cabinet/scripts/tests/test_build_companion.py` (always-runnable doctrine greps incl. dead-bind-name concatenation checks; build/signature/quarantine/smoke skipif no swiftc) + `.gitignore` bundle entry + 3 egg-manifest `expect-present` rows | additive | alpha-additive · PC | done | Built 2026-07-10 in worktree `feat/perfect-cabinet` (integrator commits; FW-019 review artifact required at commit — >300 lines). Spec v0.6: D3 SUPERSEDED per F1 (loopback fix owned by app-feel under the canonical var), D4 hatch hint handed off to the WHERE-THINGS-LIVE writer. Deviations + fix-pass corrections 2026-07-10 in the yml note (template icon = shape-encoded per launch instruction; third honest "Actuation UNVERIFIED" outcome; Deploy Dry-Run wrapper corrected to `--all --dry-run` — the spec §4 table's bare `--dry-run` is a no-selector usage error exit 64, runtime-verified; presence killswitch parsed with NO default — a missing field routes to the fallback GET + AMBER instead of green-by-default). Acceptance gate GREEN on the build box (re-run post-fix): ad-hoc signed, no quarantine, `--smoke` STATE=GREEN via live read-only Redis, suite 6 passed | `bash cabinet/scripts/build-companion.sh && "${CABINET_ROOT:-/Users/nate/captains-cabinet}/bin/Cabinet Companion.app/Contents/MacOS/cabinet-companion" --smoke && python3.12 -m pytest cabinet/scripts/tests/test_build_companion.py -q` | git revert (all additive: `cabinet/companion/`, build script, tests, `.gitignore` entry, 3 manifest rows; the built bundle is gitignored — never in HEAD, never in the egg) |
| APPSHELL-V05 | Wave D shell area — Hatch Cabinet.app v0.5 thin double-clickable installer face: `cabinet/scripts/appshell/main.swift` (single-file AppKit stub; first-launch dialog Hatch / Hatch + move-in behind a SECOND explicit `--with-launchd` confirm with Back as default / Cancel; honest re-launch dialog, never re-unpacks; `HATCH_APP_SMOKE=1` headless mode; `CABINET_HATCH_PREFIX` override; `ditto` unpack with quarantine stripped on the EXTRACTED PAYLOAD ONLY — no Gatekeeper evasion; Launch Services Terminal handoff, zero Apple-events automation) + `hatch-run.command.in` (script(1) transcript + flight log; allowlisted single-line request file deleted on read) + `build-hatch-app.sh` (pristine HEAD-clone egg cut; payload provenance + sha256; explicit ad-hoc bundle seal) + `claims-lint.sh` (THE shared 11-pattern forbidden-claims lint, fence fail-closed, blank-not-delete) + `appshell-gate.sh` + `test_appshell_build.py` (incl. the manifest-exclusion tripwire arming at the tracking commit) + runbook with the Gatekeeper 2026-reality transport matrix | additive | alpha-additive · PC | done | Built 2026-07-10 in worktree `feat/perfect-cabinet` (area review verdict: approve); integrator landed the docs-agent dependencies — this row, egg-manifest `delete`/`expect-absent` ×3 (dev tooling builds the vehicle, never rides it), world-onboarding §11 Gatekeeper staleness addendum. Deviations in the yml note (in-Terminal end-of-run notice — osascript banned; HEAD-clone cut; ad-hoc bundle seal; lint blanks not deletes; 14 fixtures; 205-line stub; builder named per spec file map). Manual once-per-target checks open: Terminal-handoff TCC prompt, AirDrop/Gatekeeper on 26.2 (honest-empty runbook slots) | `bash cabinet/scripts/appshell/appshell-gate.sh` | git revert (all additive, dev-side only; manifest rows + pytest tripwire keep appshell out of every egg) |
| PC-D | Wave D — APP EXPERIENCE (the wave row over PC-D-COMPANION + PC-D-APPFEEL + APPSHELL-V05): menu-bar companion + dashboard PWA/app-feel + Hatch Cabinet.app shell v0.5; the wave's one captain call consolidated in CC-LOOP (bind default, a.k.a. OC-LOOPBACK — ruling pending, default preserved at 0.0.0.0) | additive | alpha-additive · PC | done | Captain approval 2026-07-10; specs in `~/cabinet-meta/designs/` (never packaged); design run `wf_ef62780e-b28`. Integrated 2026-07-10 (Wave-D integrator; verifier flips to done): wiring verified end-to-end (hatch dry-run exit 0 with the new tail steps, both apps built + ad-hoc signed + plutil OK, companion `--smoke` STATE=GREEN read-only, bind default unchanged, dead bind spellings zero, D2 `--stop` confirmed in-tree); suites framework 4069p/18s + scripts 463p with the predicted pre-commit egg-export RED (the 7 expect-present rows, resolves at the commits) + dashboard tsc clean/vitest 1489p; leak + overclaim audit adjudicated clean (one integrator README rephrase; canonical-root hits = existing CG-7 scrub-worklist class); FW-019 artifact at `shared/interfaces/reviews/feat-perfect-cabinet-wave-d-2026-07-10.md`. Done 2026-07-10 — clean-room verifier PASS @ HEAD `c99478f9` (scratch clone + throwaway HOME; orchestrator flip): hatch `--defaults --clean-room` GREEN exit 0 in 6s, TTFR 1s (proofs 523p/36p, P-d skipped by design; `/tmp/cabinet-runtime` byte-untouched, zero launchctl, no browser); companion + Hatch shell built ad-hoc-signed via the spec-pinned build scripts (companion `--smoke` STATE=GREEN read-only, doctrine greps clean; shell payload 1469 files sha-verified — corrects the stale 1451 count, claims-lint planted-violation proof red-then-green); PWA install surfaces + `/api/health` served unauthenticated with auth intact elsewhere (`/receipts` 307 → login; vitest 1489p, tsc clean); bind default 0.0.0.0 preserved, loopback opt-in only (8 bind tests, dead spellings zero). Evidence `/tmp/pc-waved-verify-NYZc1F`. Standing: OC-LOOPBACK ruling pending (default unchanged), CG-5 vehicle ruling pending, nothing published (CG-7 per-item approval stands). Area rows + CC-LOOP left as-recorded (own gates govern) | `bash cabinet/scripts/hatch.sh --dry-run --defaults` exits 0 printing the new tail steps as plan items (per-area gates on the three area rows) | git revert per area commit (each additive and revertable; nothing shipped depends on the CC-LOOP ruling) |
| PC-E-P1CORPUS | Wave E scrub follow-up — dedicated P1 replay-corpus rewrite (`framework/attention/tests/test_p1_acceptance_replay.py`): replace the remaining observed-feed residue (real first names, real commit hashes, real cmt-* ids and due dates) with a fully synthetic Testburg corpus that preserves the dedup-relationship topology (shared-ref groups, owed_by/owed_to bridge, `ref=` drift, 23-cards→10-presentations invariants) | additive | alpha-additive · PC | todo | Filed 2026-07-10 by the Wave-E scrub-fixtures fix pass (review should_fix #3): the corpus' evidence strings are semantically load-bearing for dedup grouping, so a mechanical name swap risks silently changing the tested topology — the rewrite gets its own row; interim convergence landed in the same pass (partner-slug rename, docstring de-realed, real-place/real-event vocabulary unified with the P1 story pseudonyms) | `python3.12 -m pytest framework/attention/tests/test_p1_acceptance_replay.py -q` (both replay invariants hold on the synthetic corpus) | git revert (test-fixture-only rewrite) |

*(PC-D-COMPANION row appended 2026-07-10 by the Wave-D companion build area; spec = `~/cabinet-meta/designs/DESIGN-companion-2026-07-10.md` v0.6 — same column format as above. APPSHELL-V05 + PC-D rows appended 2026-07-10 by the Wave-D integrator — APPSHELL-V05 discharges the shell area's docs-agent row contract; PC-D is the wave row.)* *(PC-E-P1CORPUS row appended 2026-07-10 by the Wave-E scrub-fixtures fix pass — same column format; the yml row remains the richer record.)*

## 24. Addendum — cozy-density + truth-sweep rows (A13 parity, appended 2026-07-09)

*(Integrator note, 2026-07-10 feat/perfect-cabinet merge: this section landed on master numbered §19 in parallel with the branch's §19–§23 appends; renumbered §24 here — the lower numbers were already taken and ledger notes cite the branch's §23 by number. Content unchanged.)*

One plan-doc row per cozy-density / v1a-truth-sweep ledger id. Captain feedback 2026-07-09: live /world
real but less detailed/decorative/cozy/alive than the approved 7.5 mockups → cozy-density pass shipped
(mockups promoted to harness goldens; density/dressing as data/config + grammar-lawful bindings, seeded).
Truth-sweep audits (scratchpad `v1a-truth/`: gap-inventory, fog-spec-audit, estate-sweep) spawned the
three residual-finding rows. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| WORLD-COZY-DENSITY | Cozy-density fix pass — live render lifted to the approved 7.5 mockup bar: mockups → harness GOLDEN positives (recalibrated, separation proved); outdoor DECOR table (~35 props) + meadow 1-in-3 + 11 tuft decals + 2-wide road + quay wharf band + real pen; mockup-lineage three-pass grass; palette-lawful opaque dither VEIL (replaces alpha washes that were 15–61% palette-foreign); drop-shadow pass; engine growth-fog horizon band (§2.4 'mist beyond' shipped early); first fauna unstaged (dog/chickens/fish) in the same commit that binds the cuts; world-preview --hour | germ-keep (—) | alpha-additive · WORLD | done | Shipped 2026-07-09 @ e9fc8e7a (same-day Captain cozy feedback). Judge on live day capture: PROMOTE — calibration 24/24, vs-neg 5/5, vs-pos 8/11 incl. a WIN vs pos-mockup-unified-world (7.95 ≥ 7.5 bar). Officer name chips close-tier-only. cat/birds/butterflies stay staged. | world-aesthetic --mechanical GREEN on preview-egg + preview-today day/night + live day/night canvas; vitest 1424 + tsc + build green | git revert; corpus rows via build_corpus.py REGISTRY + --calibrate refit |
| WORLD-FENCE-RUNS | Post-and-rail fence RUN autotiling (corners/gates) for pasture + garden framing — mockups carry ~5 runs/60 tiles; per-piece hedge framing shipped as interim; pairs with future livestock pens | compress | alpha-additive · WORLD | todo | Created 2026-07-09 (cozy residual). ~150 LOC + farm 2_Fences cut verification. | fence runs render with correct corner/gate pieces; mechanical gate + determinism suite GREEN | git revert |
| WORLD-CROSSROADS-KIOSK | Crossroads noticeboard + post-kiosk props (v1-carried geography, spec §2.1) — engine renders the mailbox only; treasury pin stays v1b | compress | alpha-additive · WORLD | todo | Created 2026-07-09 (cozy residual; needs outdoor noticeboard art choice — interior cork board is wrong-object). ~60 LOC once art chosen. | noticeboard + kiosk render with inspect cards; mechanical gate GREEN | git revert |
| WORLD-NIGHT-CORPUS | Night/dusk positive corpus class for the aesthetic judge — all-day corpus means night candidates can only be mechanically gated; record 1–2 Captain-approved night positives + night-aware rubric note | compress | alpha-additive · WORLD | todo | Created 2026-07-09 (cozy pass: live night capture mechanical-GREEN with the lawful veil + glows; pairwise deferred until night positives exist). | manifest carries ≥1 night positive; --calibrate separation proof stays green; a night candidate gets a calibrated pairwise verdict | REGISTRY row removal + --calibrate refit |
| WORLD-CEREMONIES | Ceremonies 1/2/5/7 renderers (spec v2 §12 v1a scope; defs direction doc §5): berth chalk + cleat · crate stack-settle on real stage delta · retirement tarp → cart → composter · approvals packet boat + courier + mailbox flag (flag P1-blocked; boat + courier are not) | compress | alpha-additive · WORLD | todo | Created 2026-07-09 (truth-sweep finding 1): ZERO ceremony renderers exist (grep-verified) — largest previously-un-ledgered v1a gap; ceremony-1 chalk partially rides the P-SITES quick-work kind. | each ceremony driven ONLY by real events/keyframes; six-command bar on one fixture per shipped ceremony; determinism replay; mechanical gate GREEN | git revert (render passes over existing feeds) |
| WORLD-FIXTURE-DEBT | v1a acceptance fixture debt — committed --map/--render/--labels triples for egg-day0 · day-3 mid-RAISING (obligation carried onto P-SITES landing) · day-30 · today · dusk money shot, day AND dusk + dusk gate runs; shipped evidence was render-only egg/today | compress | alpha-additive · WORLD | todo | Created 2026-07-09 (truth-sweep finding 2). world-preview --hour makes dusk variants pure render-input data. | fixture triples committed in-repo, mechanical gate GREEN on every triple; day-3 triple lands WITH P-SITES | git rm fixture dir (evidence artifacts) |
| WORLD-BERTH-TRUTH | Berths with aggregate cargo in the BROWSER engine (carried v1 scope) — PIL twin draws outcome-bound chalked berth outlines; engine has zero berth entities bound to outcomes.yml; static truth surface distinct from ceremony-1 | compress | alpha-additive · WORLD | todo | Created 2026-07-09 (truth-sweep finding 3). Port the PIL twin berth pass as a grammar-lawful seeded binding. | one berth outline per ratified outcome lane + inspect card citing the outcomes.yml row; binding-validator entry GREEN; mechanical gate GREEN | git revert (read-model pass) |
| WARROOM-S1 | command center Stage 1 (command-center-proposal 2026-07-10 §6): situations.py §4.2 view + queue.py one-attention-census (admission law DARK pending C3, ranker, blast/decay, N=7 cap + overflow need) + H1/H2/H4/H5/H6 hygiene + `GET /api/attention/queue` + SSE `pending_captain_items` + mailbox alias + classic `⚑ N need you` strip + read-only `/queue` page + pinned Telegram card + briefing section + C7 seed applied | additive | alpha-additive · WORLD | done | DONE 2026-07-10; C4/C6/C7 self-ratified under the standing grant; room v1 read-only + deep-link per Captain C2 NO; Stage 2 (war-table/gantry/chip) rides WORLD-V1B; H0 arming rides germline Window 3. | `pytest framework/attention -q` (incl. test_surface_parity.py) + dashboard vitest/tsc green | git revert (census artifact gitignored; unpin the Telegram card) |
| CANDOR-FOUNDATION | CANDOR LAW non-germline half: eval-024 dissent-before-compliance harness (`cabinet/evals/candor/`, fail-closed self-test, 16/16) + run-golden-evals EVAL-024-CANDOR section + telegram-communication 'candor before comfort' law + hatching charter covenant line; germline half (constitution §Values + role clauses + eval body) staged DARK on feat/germline-window-3 | additive | alpha-additive · META | done | DONE 2026-07-10 (Captain ruling: candor = germline VALUE; dissent-then-obey; vetoes bind; agreement-as-target banned; tone configurable, truthfulness not); apply contract `docs/proposals/germline-amendment-candor-2026-07-10.md`, token `apply candor law`, rides Window 3. | `bash cabinet/scripts/run-golden-evals.sh` EVAL-024-CANDOR green + `pytest cabinet/scripts/tests/test_candor_eval_harness.py -q` | git revert (non-germline; staged half inert without the window) |
| SCREENPIPE-GATE-TRIAGE | screenpipe estate Telegram gate one-time triage sweep (161 unanswered cards, median 18.6d) — C5: gate EXCLUDED from room v1 census (no silent double-count); visit each card once, close/park/promote with a disposition | additive | alpha-additive · META | done | DONE 2026-07-11 (PERSONAL-AGENT RESET executes the sweep): 158 unanswered registry rows visited once, disposition expired-to-digest (snapshot-first backups-onevoice-20260711.tar.gz); close=158 into 0-Inbox/legacy-backlog-digest-2026-07-11.md, park=43 fresh/live commitments stay with the organ, promote=1 (EC DG JUST Jul-14 re-filed via the admission law, ADMITTED); census double-count stayed 0; Stage-3 adapter RETIRED (organ-backed commitments already reach the room via intake). | every gate card carries a disposition; room census double-count stays 0 | restore telegram_pending.json from backups-onevoice-20260711.tar.gz (rows kept, flags only) |
| WRITE-CLASS-2-ROOM | in-world card-answering (WORLD-WRITE-CLASS-2) — may the world room ever carry verdict verbs? C2 future call filed as a decision-card row; room v1 shipped read-only + 100% deep-link (Captain C2 NO, orchestrator conceded) | additive | captain-gated · WORLD | done | MERGED+LIVE 2026-07-10 via PR #135 merge 230b647c + live origin fix 41b1964d; door live-verified on the deployed dashboard (ARM 200 → confirm face → cancel, zero fires). RULED YES 2026-07-10 (Ruling A, captain-decisions): WRITE-CLASS-2 = YES under the equal-authority-door law — implemented on feat/captain-surface-v2 as `POST /api/attention/verdict` (same gate + binder grammar + feed journal as Telegram; HMAC session + CSRF + two-tap + rate limit + pid freshness), PR in review. Dashboard first; the world wardroom rides the SAME API later — lever exclusivity holds in-world until then. Original o1/o2/o3 filing superseded (kept in the yml row for provenance). | merge of feat/captain-surface-v2; dashboard verdict + verdict-attacks suites green; in-world room stays read-only pre-wardroom-adoption | git revert the branch commits (door is additive; world room untouched) |

## 25. Addendum — Germline Window 3 rows (A13 parity, appended 2026-07-10)

*(Integrator note, 2026-07-10 feat/perfect-cabinet merge: renumbered from master's parallel §20 — same collision as §24 above. Content unchanged.)*

One plan-doc row per Window-3 germline ledger id. The Captain opened the boundary 2026-07-10 (token
`apply candor law` + the ONE-window bundle "sure", captain-decisions 2026-07-10); the orchestrator
executed WINDOW-RUNBOOK.md: H0 pull (already at origin tip d06423b6) + merge `feat/germline-window-3`
@ a18c1f15 → merge commit `84da8606`, full gates green (framework 4224p/19s, cabinet/scripts 349p,
hook-regression 15/15, layer-sep new=0, lockstep+lint+attention 469p, golden evals 26/26 incl.
EVAL-024-CANDOR, A13 exit 0), deploy chain (13 plist rows, GATHER canary green, CABINET_ADMISSION_LAW
ON per C3, dashboard /queue + ⚑ strip + SSE verified, doctor GREEN), rolling officer restarts.
Relock is the Captain's sudo (READY-TO-RELOCK reported). Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| W4-GERMLINE | W4 lessons-splice germline half (dead-wire #4): `framework/acting/action_lane.py` %%LESSONS%% slot + render_lessons() + lessons= param; `run_action_lane.py` loads the SIE-1 correction ledger best-effort into PROPOSER_SYSTEM — Captain corrections become standing proposer instructions | additive | alpha-additive · META | done | DONE 2026-07-10 Window 3 (staged f9090add, landed via merge 84da8606); dry-run canary W4-OK (lessons block or no-lessons note, never the raw token). | `pytest framework/acting/tests/test_lessons_splice.py -q` green; dry-run emits no literal %%LESSONS%% | R2 mini-window `git revert -m 1 84da8606` (atomic with both acting files) |
| W8-GERMLINE | W8 head→tail germline half (dead-wire #8): `cabinet/scripts/hooks/session-start.sh` captain-patterns/intents boot injection `head -100` → `tail -40` — the freshest encoded law finally boots (append-only ledgers put the newest at the tail) | additive | alpha-additive · META | done | DONE 2026-07-10 Window 3 (staged 657cf1be, landed via 84da8606); boot canary: 3 "last 40 lines" tail sections in a fresh cos boot; officers picked it up via the same-window rolling restart. | `bash -n` clean; `grep -c 'tail -40'` = 3; boot context carries 3 tail sections | R2 mini-window `git revert -m 1 84da8606` |
| CANDOR-GERMLINE | CANDOR LAW germline half: `framework/constitution-base.md` §Values (evidence-cited dissent before compliance; dissent-then-obey — vetoes bind; agreement-as-target banned; flatter no one; candor genome D15c) + Chair clause (`cos.md`) + lane-CEO template clause + eval body `memory/golden-evals/eval-024-candor.md` + apply contract; completes CANDOR-FOUNDATION | additive | alpha-additive · META | done | DONE 2026-07-10 Window 3 (staged d244a5cf..a18c1f15, landed via 84da8606); amendment lint green; EVAL-024-CANDOR 16/16; same window armed the war-room census germline plane (875416fd, per its amendment doc) — arms at next relock. | amendment lint green + `run-golden-evals.sh` EVAL-024-CANDOR green + constitution carries CANDOR | R2 mini-window `git revert -m 1 84da8606` (eval-body removal alone never disarms candor — harness is non-germline) |
| LIVING-DASHBOARD | Connections + intents + chat rail + custom components + theming (alignment doc 2026-07-10) | germ-keep (—) | alpha-additive · SURFACE | todo | Staged S1-S4 per alignment doc; behind captain-surface-v2. | S1 gate per doc | git revert |
| WRITE-CLASS-3-CREDENTIALS | credentials through the surface — captain-gated ruling | germ-keep (—) | captain-gated · SURFACE | captain-gated | Write-only fields, mission-requested only, journaled. | ruling in captain-decisions | reversal |

## 21. Addendum — Captain Surface v2 row (A13 parity, appended 2026-07-10)

One plan-doc row for the ledger id added at ship of the captain-surface-v2 wave (Chair master
prompt, Captain-forwarded 2026-07-10; scope + Rulings A/B ratified in captain-decisions the same
day). Shipped as a PR from `feat/captain-surface-v2` → master — NOTHING auto-merges; Captain
review required. Same commit also flips WRITE-CLASS-2-ROOM (Ruling A) above. Column format
matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| CAPTAIN-SURFACE-V2 | Captain Surface v2 — plain-language deck + equal-authority verdict door (WRITE-CLASS-2) + comms-native TG engine: `framework/attention/plain.py` copy tables + jargon linter (pytest+vitest teeth, plain.json drift-pinned) + `verdicts.py`; `POST /api/attention/verdict` two-tap door (HMAC session + double-submit CSRF + rate limit + pid-freshness-at-fire + fixed-argv bridge into the SAME binder wire); `framework/comms/surface/` one-card-one-decision + pacing (ask-first, cap 3–5) + pin lifecycle (adopt-standing-card) + briefing-as-card; verify-at-fire + draft withdraw/supersede/reconcile + tiered-escalation gate (dark) | additive | alpha-additive · META | done | DONE 2026-07-10 (Captain in-session authorization): merged to master 230b647c after real review + merge-race resolution (CI 7/7 green on 3e91dbdd); live origin defect found+fixed at deploy 41b1964d (0.0.0.0-bind same-origin — Host-header-derived origin + 2 regression tests); dashboard rebuilt+kickstarted, /queue live-verified authed, two-tap ARM→confirm→cancel zero fires; CABINET_QUEUE_CARD=0 applied to frontdoor-briefing + intake-surface (reloaded); TG engine dark per PR, comms path proven to briefing intake (lawful H5 demotion); doctor GREEN 124; A13 parity 282 ids. Original record: PR opened 2026-07-10 (commits 72c74e3c..ea4012fd + ledger commit); zero schg-locked files edited; window-4 locked-file diffs (queue_card.py / action_lane.py render_card / tell_surface.py digests) DEFERRED per `docs/proposals/germline-window-4-deferral-2026-07-10.md`; knobs awaiting Captain: pin design (adopt-standing-card recommended), ask-first vs auto-push, cap default 5 in the 3–5 band. | framework pytest 4390p/19s + dashboard vitest 1507p + tsc + build + layer-sep new=0 (all green at ship) | git revert the branch commits (additive; engine dark until armed) |

## 26. Addendum — Wave E rows (A13 parity, appended 2026-07-10)

One plan-doc row per Wave-E integrator ledger id (the relicense fix pass already added CG-20 in §22's
adjudication lineage and the scrub-fixtures fix pass added PC-E-P1CORPUS in §19's Perfect Cabinet
lineage — this section carries the integration-time rows). Provenance for the set: Captain rulings
2026-07-10 (MIT relicense + contribution flow + scrub-not-consent), per the 2026-07-07 full-autonomy
grant; LimeZu brief attached to the wave result. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| CG-21 | adjudicated publish-gate allowlist — git's canonical null object id (40 zeros), the intended PUBLIC CONSTANT in cabinet/scripts/git-hooks/pre-push + test-pre-push-hook.sh (both ship; trip GATE (d) id:[0-9]{9,}); masked per the CG-19 mechanism, every occurrence still prints [adj] + report + PASS-line annotation; fixed-value mask can never hide a real id | additive | captain-gated · CG | done | Recorded + EXECUTED 2026-07-10 (Wave-E integrator, per the wave instruction citing Captain scrub-not-consent + MIT rulings 2026-07-10); Captain ratifies or strikes at CG-7. | bash -n egg-publish-gate.sh + exactly ONE array occurrence of the constant (row gate_cmd) | delete the entry line + its ADJUDICATED_ROWS twin — zero-tolerance returns |
| R160 | egg-export transform launchd-portable-only (PC-E scrub-paths; AMENDED same-day) — egg ships .template.plist twins + officer-entitlements.plist + INSTALL-flip.md + gate-apply as the ONE portable-dark exception (paths sed-rewritten to envsubst placeholders in the export copy, DARK flags + ritual comment preserved, source never touched — the export's germline-lockstep suite requires the wired FILES entry on disk, deleting it bricked publish-gate (b)); every other rendered com.cabinet.*.plist excluded; fresh targets render from services.yml + templates | instance-split | alpha-additive · PC | done | Built by the scrub-paths area; gate-apply amendment by the integrator after the first post-commit gate run (fail-closed content verify: no /Users/, placeholder present, Disabled intact). | pytest test_egg_export.py::test_launchd_ships_portable_templates_only | remove transform line + exporter function — gate (d) sub:/users/nate goes RED on rendered plists |
| R161 | egg-export transform claude-egg-swap (PC-E scrub-paths) — docs/templates/CLAUDE-egg.md ships AS the egg's CLAUDE.md; live repo-root CLAUDE.md is officer-loaded (live-coupling: never scrub-edited) and excluded; pre-tracking cuts print a loud UNSWAPPED note (appshell precedent), publish gate = fail-closed backstop | instance-split | alpha-additive · PC | done | Built by the scrub-paths area; ACTIVATES at the integrator commit tracking the template (paired pytest skips until then, then enforces). | pytest test_egg_export.py::test_claude_egg_swap_once_template_tracked | remove transform + untrack template — egg falls back to live CLAUDE.md, gate (d) RED |
| PC-E-LOCKSTEP | Wave E scrub follow-up — SOURCE-LOCKSTEP flips: five test-retained token sets pinned to non-test source values (action_exec.py delegate officers, world-geo.ts ISLE_ANCHORS lanes, seed-war-room.py seed slug + board ref, docker.ts mock TELEGRAM placeholders, action_lane.py lane_default + prompt spec); each pair flips in the SAME commit, never the test alone | compress | alpha-additive · PC | in-flight | Filed 2026-07-10 from the scrub-fixtures rows_to_flip; the sibling rewrite.py polads-ceo map is the recorded coordination hazard. In-flight 2026-07-12 (Wave-G): (b) landed bf64afc0; (a)+(e) staged dark on CG-25; (c) leave-with-reason per recon (both sides retained); (d) open. Integrated 2026-07-14: branch merged master + pushed — CG-25 merge-before-apply prereq MET, (a)+(e) unblocked for the ceremony; merged-tree suites + three-twin live proof green (evidence in the yml row). | post-flip: gate (d) hits for the five pairs drop to 0; per-pair suites green | n/a until flipped (each flip lands as one revertable commit) |
| PC-E | Perfect Cabinet Wave E — scrub (fixtures ~150 test files -> synthetic vocabulary; paths/docs + transforms R160/R161) + MIT relicense (LICENSE, DCO, plugin.json fields, drafts) + contribution kit (.github templates, FUNDING placeholder, cabinet-feedback.sh, TRANSPARENCY.md, genesis card) + adjudications CG-20 executed / CG-21 | additive | alpha-additive · PC | done | INTEGRATED 2026-07-10 (four adversarially-reviewed areas, one integration set); wave metric = gate (d) hit count on a fresh HEAD cut (two-digit target); deliberately-retained real values recorded in the row note; verifier flips to done. DONE 2026-07-10 — independent verifier verdict pass @ 8f2fba99 (evidence /tmp/pc-wavee-verify-m5t0rn): export PASS 1530 files; publish gate a/b/c PASS, gate (d) 529 (wave metric 1927 -> 529, 61 [adj]-masked under CG-19/20/21/22) -> honestly RED 1/4 fail-closed pending CG-7; MIT relicense verified (verbatim LICENSE + DCO 1.1 + 7 plugin manifests MIT, zero business-source residue); contribution kit verified (feedback dry-run redacted all 5 planted leaks, gh never invoked; OC slug still a documented placeholder); hatch regression GREEN exit 0, TTFR 1s; suites framework 4331p/19s + scripts 598p/1s + dashboard tsc clean/vitest 1544 green. New latent verifier finding recorded in the yml row note (order-dependent __pycache__ flake in test_gate_apply_dark; follow-up row recommended). Publish stays per-item CG-7 Captain-gated; LimeZu brief delivered to the Captain. | fresh egg-export + egg-publish-gate run (row gate_cmd); framework + scripts + dashboard suites green | git revert the wave commit set (adjudication/transform rollbacks on their own rows) |
| CG-22 | adjudicated publish-gate allowlist — nil-UUID placeholder prefix (34 chars) carried by the 19 all-zero sequential placeholder ids in cabinet/dashboard/src/lib/config.ts; same all-zero-constant class as CG-21; dashes make the mask impossible inside a real digit-run id | additive | captain-gated · CG | done | Recorded + EXECUTED 2026-07-10 (integrator gate-d recount fix pass, CG-19 mechanism); values are shape-load-bearing default-config exemplars; Captain ratifies or strikes at CG-7. | bash -n gate script + exactly the entry + comment carry the prefix (row gate_cmd) | delete the entry + rows twin — zero-tolerance returns |
| CG-23 | germline amendment (staged dark) — consumption-aware trim guard in `framework/attention/hygiene.py` `trim_forwarded_streams` (gate XDEL on `_safe_to_trim`; closes the sweeper half of the 2026-07-11 H2-XDEL-vs-CEO-plane finding; drainer half guarded on master) | additive | captain-gated · CG | captain-gated | Verified staged diff in `docs/proposals/germline-hygiene-trim-guard-addendum-2026-07-11.md`; rides the HANDBACK #10 unlock window with the kill-switch fix + posture/trust-ladder relock. | post-ceremony: patch applied + attention/frontdoor suites green + same-day relock | do not apply at ceremony / revert before relock |
| CG-24 | germline amendment (staged) — 3 verified C17 hook-security patches: (A) kill-switch halts `mcp__*`+`Task` on unreachable Redis, (C) germline gate resolves `..`-traversal per-token, (D) `post-tool-use.sh` redacts secrets + logs 0600 | additive | captain-gated · CG | captain-gated | 3 verified diffs in `designs/hook-patches/` (provenance sha in README); each applies to the current schg hook + `bash -n` clean. Rides the germline unlock window. B+E deferred; rotate already-leaked tokens separately. | at ceremony: `patch -p1` each + `bash -n` + relock same session | do not apply / revert before relock |
| CG-25 | germline amendment — PC-E-LOCKSTEP pairs (a)+(e), lane/officer instance-split in the acting organ: `action_exec.py` `_DELEGATE_OFFICERS` literal → call-time `frozenset(env.officers())` at both whitelist sites (fail-closed to empty = every officer rejected loudly), `action_lane.py` prompt officer-enum literal → `%%OFFICERS%%` slot rendered from `env.officers()` + `lane_default="polads"` → `""` resolving `env.lane_default()` (the PolAds-first ruling now instance data: platform.yml `lane_default`, landed with Wave G) | instance-split | captain-gated · CG | captain-gated | LANDED ON MASTER 2026-07-15 (commits b71fe796/3abe4876, germline-4file-prep worktree) after independent re-verification (fresh, read-only against the live tree): wave-g-lockstep prereq confirmed merged live, officer-capabilities.conf/platform.yml data confirmed byte-identical live-vs-patch, `env.officers()`/`env.lane_default()` re-executed in-process resolve exactly to the retired literals, suites re-run green post a concurrent origin/master merge. Original verification: two diffs carried verbatim in `docs/proposals/germline-lockstep-lane-resolver-addendum-2026-07-12.md` (provenance sha 960d4c4d), shadow suites 1296 passed/0 failed. Only the Captain-sudo live-tree ceremony remains. | at ceremony: Captain unlock → `git checkout <landed-ref> -- action_lane.py action_exec.py` (supersedes the earlier patch-p1 step, now an ordinary landed commit) → `py_compile` + `pytest framework/frontdoor framework/acting -q` green → Captain relock same session | do not apply / revert before relock — branch-side resolvers + fixture stay inert (proven green pre-ceremony) |
| CG-26 | germline 4-file prep, half 2 — `cabinet/mcp-scope.yml` + `cabinet/officer-capabilities.conf` comment-only provenance headers (zero data-row change, diff-with-comments-stripped empty) + new `framework/tests/test_roster_conf_lockstep.py` (roster.yml ↔ conf/scope officer-SET coverage, skips cleanly absent roster.yml) + a shrink-only `test_no_launcher_hardcode.py` ratchet update dropping the needles CG-25 made dead | instance-split | captain-gated · CG | captain-gated | CANDOR FINDING: the brief asked for "the same treatment" as CG-25 — investigated in `docs/proposals/germline-4file-prep-mcp-officer-conf-2026-07-15.md` and found NOT to apply: `env.officers()` already reads `officer-capabilities.conf` directly/generically (the exact function CG-25 calls), `world-compose-portraits.py` reads it the same way for the world-manifest portraits, and `generate-instance.py`'s hatch flow directs hand-editing both files per lane CEO — this is instance data doing its job, not a framework literal. Making them roster.yml-GENERATED was considered and REJECTED (would touch the live security hook `pre-tool-use.sh` + `load-preset.sh`, unprovable from a worktree alone — the cardinal fleet-safety flag, routed here). FINDING 1 (flagged not fixed): real roster.yml↔conf capability-LIST drift exists today (conf over-grants vs roster.yml per officer) — the new test checks officer-SET coverage only, deliberately not list equality, pending a Captain/CoS ruling on which file is stale. FINDING 2 (flagged not resolved): the schg-lock-vs-hand-customization tension is a product/classification call, not engineering. Verified: yaml parse structurally identical, lockstep test 6 passed/1 skipped, test_env 72 passed, ratchet 19 passed (baseline 7→5), frontdoor+acting 1705/22 skipped. | at ceremony (same window as CG-25): apply comment-only commits + copy in the new test file + `pytest test_roster_conf_lockstep.py test_env.py -q` green (7 passed on the live tree once roster.yml is read) + grep hit-count unchanged + relock same session | git revert the comment-only commits / drop the new test file — zero runtime behavior depends on either, no ceremony dependency |
| R162 | egg-export transform framework-docs-archive — DATED framework/docs design snapshots (*-2026-*.md, 4 files, ~86 gate hits: instance history citing live paths/lanes/Captain; rewording would falsify) archive out with an ARCHIVED-NOTE.md stub; LIVING contract docs (work-model.md guide-referenced, consequence-ledger.md, outcome-watchdog.md) stay, scrubbed at source (examples -> generic) | instance-split | alpha-additive · PC | done | Built 2026-07-10 by the integrator (biggest mechanical residual class; founding-amendment docs ship by R031/R146 and were NOT touched). | pytest test_egg_export.py::test_framework_docs_dated_snapshots_archived | remove transform + expect rows + exporter function — snapshots ship again, gate (d) RED |
| R164 | instance_test_lanes instance-split (Wave G follow-up) — instance-lanes.ts INSTANCE_TEST_LANES = {'sensed'} (Captain 2026-07-09 instance-test ruling; verbatim engine-route move) still bakes an instance lane into the shared dashboard module and forces the token into its vitest fixtures; flip to an instance-config field (instance_test_lanes list, platform.yml sibling of lane_default) read fail-honest to the EMPTY set + free the fixtures in the SAME commit (lockstep) | instance-split | alpha-additive · PC | done | Filed 2026-07-12 by the Wave-G resolver-core fix pass — the module's tracked-for-a-ledger-row promise had no row (a deferral without an execution handle). Module + fixture comments cite this id. Renumbered from R163 at Wave-G integration 2026-07-14 (id raced with master's captain-PII-scrub row, which keeps R163). BUILT 2026-07-16 by final-residual-scrub: instanceTestLanes(root) reads instance_test_lanes from instance/config/platform.yml (js-yaml JSON_SCHEMA, fail-honest to empty Set); fixtures flipped to Testburg vocabulary ('orchard') in the same commit; 55/55 vitest green. | post-flip: no `sensed` token in the module or its vitest fixtures; instance-lanes suite green testburg-only | revert the one commit |
| R165 | pytest one-invocation collection conflict — framework/ + cabinet/scripts/lib/tests are both packages named 'tests' (framework/__init__.py absent): ModuleNotFoundError 'tests.test_env' when collected together; pre-existing at the Wave-G base, CI unaffected (separate invocations) but tree-wide pytest + future CI consolidation trip it | additive | alpha-additive · - | todo | Filed 2026-07-14 by the Wave-G integrator from the resolver-core concern (reproduced at base with pre-wave files). Fix directions in the yml row (importmode=importlib / unique package names / init namespacing); sibling __pycache__ dark-grep flake recorded by the PC-E verifier — integration suites ran with bytecode disabled. | one-invocation collect-only exit 0 (row gate_cmd) | n/a (records a pre-existing defect) |

## 27. Addendum — public-repo captain-PII scrub row (A13 parity, appended 2026-07-12)

One plan-doc row for the emergency master-tip scrub executed the day the repository was confirmed
PUBLIC. Values are referenced by class only, per the real-value doctrine. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| R163 | master-tip captain-PII scrub — instance/config seeds neutralized in place (platform.yml chat-id -> all-zero placeholder + captain_name default; officer-emails.yml captain rows -> example.invalid; outcomes/directions/probes/contexts/projects/hq-draft/sources/authority-enforcing -> generic example wording, lane/product ids kept per pending rulings 5b-a/b; test_generate_instance.py FORBIDDEN chat-id token -> the tracked placeholder) + SECURITY.md public-source honesty rewrite + .gitignore instance/cache/ line; schg instance/config/act-first-surfaces.yml blocked-handback; residuals recorded in the yml row note | instance-split | alpha-additive · - | done | Executed 2026-07-12 (emergency wave; repo PUBLIC): zero tracked hits for captain chat id + captain emails at HEAD; scoped gate-(d)-style count 314 -> 183 over the 16 instance-seed/test files with every survivor adjudicable by class — correction 2026-07-12 (adversarial review): the original 18-touched-files wording overstated scope; the touched ledger/plan pair itself retains historical name/path/id tokens in append-only row notes, recorded as residual in the yml row note together with the review's residual additions (chair-bot-handle carriage in 7 more tracked files; colleague work-email carriage in a plan doc + policy-shadow test fixtures; colleague-name fixture vocabulary across framework/ tests+docs; a private-property detail in a design doc; act-first-surfaces unlock row widened to org-structure names as well as ids; detector ban-list self-carriage flagged for the Captain); generate-instance suite 58 green; docs sweep green; touched YAML all safe_load-clean, outcomes structure verified against the schema with the same tolerance the pre-scrub master copy needs. | row gate_cmd (pytest + placeholder greps + docs sweep) | git revert the scrub commits (do NOT revert while the repo is public); runtime re-seeds the untracked overlay |

## 28. Addendum — publish-gate pattern-externalization row (A13 parity, appended 2026-07-14)

One plan-doc row for the R166 detector-ban-list externalization (the R163 residual flag (e)
executed with Captain approval — exfil red-team item 1 on the 2026-07-12 pre-prod audit map).
Values are referenced by class only, per the real-value doctrine. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| R166 | publish-gate pattern-source externalization — `egg-publish-gate.sh` GREP_PATTERNS + name-bearing ADJUDICATED_ALLOWLIST entries stop hardcoding captain/colleague values: tracked gate keeps GENERIC classes only (9+-digit id runs; the CG-21/CG-22 all-zero constants); the captain-specific suite + CG-19/CG-20 allow entries load from UNTRACKED `instance/config/publish-scan-patterns.local` (tracked synthetic `.local.example` twin; gitignored; git-archive cuts cannot carry it); FAIL-CLOSED: gate aborts exit 2 on a missing/pattern-less file (never a vacuous GREEN), cabinet-feedback.sh refuses an under-scrubbed bundle on a private-side checkout without it; EGG_GATE_PATTERNS_FILE = test-only seam; engine self-test probes now synthetic | additive | alpha-additive · - | done | Executed 2026-07-14 (full-autonomy grant + Captain-approved audit finding): data-only directive parse (kind whitelist, charset guards, file:LINE-NUMBER-only errors); hermetic synthetic tests both suites (masking semantics, fail-closed paths, generic-id teeth, captain-machine self-audit that live values appear nowhere tracked — skips on CI); docs/manifest/runbook + CG-19/CG-20 gate_cmd supersessions same commit; named deployment handback: seed the live patterns file before the next real publish-gate run; residual: test_testburg_fixture.py BANNED_PATTERNS still self-carries (separate row-worthy decision) | row gate_cmd (bash -n both consumers + exact-equality pin on the generic GREP_PATTERNS line + template/gitignore greps) | git revert the R166 commit (do NOT revert while the repo is public — re-lands the PII leak; re-scrub first); the untracked patterns file is unaffected |

## 29. Addendum — Personal-agent reset rows (A13 parity, appended 2026-07-11)

*(Integrator note, 2026-07-14 fidelity-wip-snapshot merge: this section landed on the branch numbered §22 in parallel with master’s §26–§28 appends; renumbered §29 here — §22 was already taken by the Wave C install-rail section. Content unchanged.)*

Two plan-doc rows for the ledger ids added by the PERSONAL-AGENT RESET (Captain interview
2026-07-11, binding: voice + queues depth; briefing end-state = cabinet-only briefing-as-card;
old backlog = expire + digest; commitments backlog + Telegram pins NOT sacred; vault/brain/CRM +
voice/nate-model SACRED). The same run flips SCREENPIPE-GATE-TRIAGE above to done (it executes
that sweep). Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| ONE-VOICE-RESET | Personal-agent reset, voice arm: briefing-as-card ARMED (`instance/config/comms-surface.yml briefing_card:true` + `framework/frontdoor/run_briefing.py` card mode — archive-then-ACK to `instance/memory/briefings/`, ONE edit-in-place card, wrapper-grep-honest `sent`) + pacing/triage engine ARMED (`cabinet/cron/surface-pin-tick.sh` → `engine --tick`; ask-first/cap-5; escalation DARK) + chunk-emitter retired (pre-v2 briefing wall no longer sends; channel.py germline untouched) + screenpipe retired-as-sender verified (both .env gates, content folds as card DATA; zero pipe.md edits) + 2 live defects fixed (quiet-hours-in-UTC via CABINET_CAPTAIN_TZ in both wrappers; demoted-card 288/day retry spam via pacing deferred-until-next-briefing) | additive | alpha-additive · META | done | DONE 2026-07-11: live evidence feed 2026-07-11 — last wall mid 1060 len 5701 (2 chunks) vs card mid 1061 len 200, re-run EDITED same message (same situation_key, zero (1/N)); HANDBACK window-4: verdict-vocabulary defect (approved/edited/rejected vs approve/edit/skip) also holds the H5 action-card streak, so per-item decision cards fold into the briefing count until the germline wave. | `python3.12 -m pytest framework/frontdoor/tests/test_run_briefing_card_mode.py framework/comms/surface/tests/ -q` | delete the `briefing_card` yml key (classic path byte-identical) + revert `--tick` to `--pin-tick` |
| LEGACY-BACKLOG-DIGEST | Personal-agent reset, queue arm: cabinet pile 152 situations closed via H2 closure rows (census 162 → 10; keepers = decisions room + `thread:seed/*` + surface cards) + screenpipe registry 158 expired + 47 stale open commitments archived (43 stay open, 9 live-due ⚑) → ONE digest (vault `0-Inbox/legacy-backlog-digest-2026-07-11.md` + `shared/interfaces/` copy); ajour-faktura closed FIRST by Captain word (SIE-1 lesson-004 + H2 closure, pin 1055 ticked 2→1); EC DG JUST Jul-14 re-filed via admission law (ADMITTED → pin lawfully 1→2); pins reset (HQ = exactly the overview card 1055; screenpipe bot chat = zero) | additive | alpha-additive · META | done | DONE 2026-07-11, snapshot-first `~/.screenpipe/state/backups-onevoice-20260711.tar.gz` (394 paths); registry rows kept in place with expired flags; commitment archive = frontmatter `status: archived` + provenance; organ keeps running fresh on the 43 open rows. | `python3.12 -c "import json;c=json.load(open('shared/interfaces/attention-queue.json'));assert c['pending_total']<=12 and c['pending_captain_items']==2"` | restore from the tarball (screenpipe+vault); cabinet feed is append-only — re-file via admission law |

## 30. Addendum — Product/captain-agnostic P1 manifest handback row (A13 parity, appended 2026-07-15)

One plan-doc row for the ledger id filed by the P1 landing-review fix pass (Captain
directive 2026-07-14, BACKLOG.md "Product/captain-agnostic foundation" P1 entry — make
the framework/cabinet layer never hardcode a product or captain). Values are referenced
by class only, per the real-value doctrine. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| PCA-P1-MANIFEST | Product/captain-agnostic P1 — NAMED HANDBACK: `cabinet/dashboard/public/world-assets/manifest.json` still carries 4 product-officer portrait rows (`portrait_polads-ceo[_sheet]`, `portrait_stephie-ceo[_sheet]`, each an id/path/`fnv1a`-seed triple) after the P1 sweep instance-generated `services.yml`/plists/`seed-war-room.py`/`import-linear-to-library.sh` and decoupled `decision_cell`'s default git corpus — the one loop-ownable BACKLOG P1 scope item left with no recorded disposition | instance-split | alpha-additive · PCA | todo | Root cause: `world-compose-portraits.py`'s `default_roster()` already reads officer slugs from `cabinet/officer-capabilities.conf` (roster-driven, not a Python literal) — the manifest is the committed OUTPUT of running that generator against a roster source that is itself germline/schg-locked and out of this pass's scope. Real fix needs (a) prefer `instance/config/roster.yml` via `lib_roster.load_roster()` in the generator, then split officer-portrait rows into a separate gitignored/instance-generated partial manifest merged at dashboard load time (touches the Cabinet World render pipeline — requires the calibrated aesthetic-harness judge gate per doctrine, out of scope for a side-effect fix), or (b) a Captain-ratified adjudication that the committed artifact may name the live roster. Precedent: same treatment as CG-25's `action_exec.py` deferral in this same P1 body of work. Re-verified zero regressions this pass: framework/ 4875p/22s/1 pre-existing fail, capstone ratchet 18/18, roster+seed-war-room 14/14, env+decision_cell+surface_parity 92/92, cabinet/scripts 709p/3s, dashboard vitest 1696/1696. | `python3 -c "import re;t=open('cabinet/dashboard/public/world-assets/manifest.json').read();print(len(re.findall('polads-ceo',t))+len(re.findall('stephie-ceo',t)))"` — target 0 once instance-generated, or a captain-ratified adjudication row if the artifact stays as-is | n/a — documentation-only row; the eventual code fix rolls back with a normal git revert of that commit |

## 31. Addendum — Product/captain-agnostic P1 ratchet-capstone row (A13 parity, appended 2026-07-15)

One plan-doc row for the ledger id filed by the P1 ratchet-capstone pass (Captain
directive 2026-07-14, BACKLOG.md "Product/captain-agnostic foundation" P1 entry).
Values are referenced by class only, per the real-value doctrine. Column format
matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| PCA-P1-RATCHET | Product/captain-agnostic P1 — RATCHET CAPSTONE: `framework/tests/test_no_launcher_hardcode.py` extended to catch hardcoded product/lane tokens (`polads`/`stephie` and their `-ceo` officer forms), sequenced AFTER genericizing the 8 loop-ownable `framework/` files that still cited them in doc/comment examples | additive | alpha-additive · PCA | done | Removed refs first (8 non-germline files reworded to the repo's synthetic `acme` vocabulary — zero runtime-logic change), then extended `_CHECKS` with a `polads`/`stephie` word-bounded check mirroring the existing org-domain check's shape. Full framework/ grep 20 hits/13 files → 9 hits/5 files, and every remaining hit sits in a file on `germline-lock.sh`'s schg `FILES[]` (verified live-tree `schg` via `ls -lO`): `action_lane.py`/`action_exec.py` per staged-dark ledger row CG-25, `run_action_lane.py` per CG-25's own leave-with-reason disposition, `situation.py`+`graduation.py` newly dispositioned by this row. None edited live — each is a tracked, needle-scoped, forcing-function-backed entry in the ratchet's own `_TEMPORARY_LINE_RESIDUALS` tier (baseline raised 1→7), the same shrink-only mechanism already carrying the pre-existing board-id residual, generalized to a 5th literal family rather than replaced. Added a hermetic positive test + a lookalike false-positive guard. | `python3.12 -m pytest framework/tests/test_no_launcher_hardcode.py -q` — 19/19 green (was 18); zero regressions on full framework/ suite (4895 passed/21 skipped/1 pre-existing unrelated fail, same as PCA-P1-MANIFEST already documented) | git revert this commit — the 8 rewords are pure doc/comment text and the ratchet extension is additive; reverting restores the pre-PCA-P1 18-test ratchet |
| R167 | `docs/proposals` archive (fills the R146 gap) + `source-adapter-boundary-2026-07-05.md` dropped from the R145 keep-list | compress | beta-compression · - | done | NEW transform `proposals-archive`: the current 20 `docs/proposals/germline-amendment-*.md` FOUNDING AMENDMENTS (the original 18 plus the 2026-07-15 evidence-recorder and officer-security amendments; R031/R146 — ratified, frozen; still walked by `test_amendment_doc_lint.py`'s live-tree union glob even though that lint no longer ships) stay verbatim; the other 8 non-amendment addenda/activation runbooks archive with an ARCHIVED-NOTE.md stub, same shape as R145/R162. R145 AMENDMENT: `source-adapter-boundary-2026-07-05.md` dropped from the plans-archive keep-list (3 specs remain) — re-audited first (0 raw `/Users/nate` hits; ~20 of its flagged hits are its own necessary Tier-1/Tier-2 screenpipe-coupling CENSUS subject matter, several are protected `nate_model` mentions, nothing shipped requires its presence) — a dated design census is the same "instance history, reword-would-falsify" class R145/R162 already archive rather than reword, so it now falls through to that same treatment instead of shipping 20+ vault-path literals. | `python3 -m pytest cabinet/scripts/tests/test_egg_export.py -q -k 'proposals or plans_archived'` | git revert this commit — restores un-archived docs/proposals and the 4-spec plans-archive keep-list; packaging-manifest-only |

## 32. Addendum — confirmed-gaps closure + gate (d) bucket-arithmetic row (A13 parity, appended 2026-07-16)

One plan-doc row for the ledger id filed by the confirmed-gaps closure pass (a
prior code-review of this same egg-export effort). Values are referenced by
class only, per the real-value doctrine. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| R168 | Confirmed-gaps closure pass (Husqvarna/step-network/Nate sweeps, brain-audit exclusion, synthetic retrieval-eval seed, screenpipe mac-setup split) + NAMED HANDBACK for germline-blocked residuals + full gate (d) bucket arithmetic — see the yml row's note for the complete accounting | compress | beta-compression · - | captain-gated | Closed every fixable-by-me bucket named in the 6 confirmed gaps (7 commits, each independently re-tested green — see yml note for the full list). Named handback (germline-blocked, no cached sudo, never worked around): `memory/golden-evals/{eval-008,eval-009,eval-011,framework/fw-002-spending-limits.sh}` + `cabinet/scripts/hooks/{post-tool-use.sh,captain-escalation-precheck.sh}` + `instance/config/posture-presets/personal-macbook.yml` — all under schg-locked `germline-lock.sh` FILES/DIRS entries. | Fresh export + gate re-measurement (pinned real `/usr/bin/grep`, confirmed non-front-end): 1021 total unallowlisted hits (down from 1143/1026), bucketed 34 golden-evals-germline + 26 other-germline + 231 self-referential-detector-fixture (allowlisted) + 128 ratified-frozen-amendment-docs (judgment call, flagged) + 4 LICENSE-attribution (allowlisted) + 271 product-name polads/stephie/sensed (report-only per instruction, done) + 229 screenpipe-as-architecture-example (judgment call, flagged) + 98 genuinely out-of-scope (pre-existing id-shaped test literals across 40 files + step-network mentions in files this task did not name). GATE (d) stays RED — NOT a false-GREEN claim; see yml note "BOTTOM LINE" for exactly what still needs a Captain decision vs. a germline ceremony vs. a separate dedicated pass. | git revert the 7 commits (each independent, zero runtime-logic change, all string/comment/manifest/fixture edits) |

## 33. Addendum — review-item coverage-check + gate (d) re-measurement row (A13 parity, appended 2026-07-16)

One plan-doc row for the ledger id filed by the review-item coverage-check pass
(verifying the 5 named review items from `designs/PREFLIP-REVIEW-2026-07-15.md`
against the actual export tree). Values are referenced by class only, per the
real-value doctrine. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| R169 | Review-item coverage-check pass (5 items, `designs/PREFLIP-REVIEW-2026-07-15.md`) — all CLOSED (4 already covered by prior transforms: interfaces-header-only, Vercel-team-id env-var indirection + plans-archive, R167's source-adapter-boundary drop, R162's framework-docs-archive; 1 fixed this pass: `cabinet/mcp-server/test_server.py`'s `sensed`→`acme` test-fixture rename) — see the yml row's note for the complete per-item evidence | compress | beta-compression · - | done | Verified each of the 5 review items directly against a fresh export tree (not just the source tree) — see yml note for the exact file listings/greps per item. The one genuine gap (mcp-server test fixtures using the real product codename as fake project-slug data) is fixed in commit 51ee0ca1, 61/61 tests still green. | `python3.12 cabinet/mcp-server/test_server.py` — 61 passed, 0 failed | git revert commit 51ee0ca1 — pure test-data rename, zero logic change |

## 34. Addendum — memory-wave3 landing rows (A13 parity, appended 2026-07-16)

Plan-doc rows for the ledger ids filed by the memory-wave3 integrator (lanes
BC / D / E landed on master from reviewed lane diffs, base `405abed3` applied
`--3way` onto `0baad320`; lane A-supersession was NOT landed in that pass —
not review-cleared at integration time. Superseded 2026-07-16, same day: lane
A review-cleared — rebased onto `aa56f43e`, all three recheck findings closed
— and landed by the memory-w3a integrator; row MEMORY-W3-A below). CG-27 is
the Captain-gated
ceremony row staged by the BC boot-pack; the schg hook edits ride ONLY the
`docs/proposals/` patch artifact + amendment doc, never a tree edit. Column
format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| MEMORY-W3-A | Memory supersession closure (§4.2 APPLY half): `cabinet/scripts/memory-supersede-apply.py` consumes the detector's proposals file behind a 14-day soak gate — near-dup class re-validated against the LIVE store (recomputed Jaccard≥0.75 via the detector's imported normalization, same source_type, both rows live via a windowless by-id probe with unreachable-probe = OPEN blocked-db, order intact) then one guarded parameterized UPDATE sets `superseded_by` (raced = no-op; ≤50/run; in-run touched-id deferral; reversible `--undo`, reversal re-blocks arming until `--resolve-reversals`); contradiction-cue pairs file Captain one-tap cards (binder grant → `approved_pending_apply` executes via the same guarded path; need closed with granted/superseded receipt); half-soak Captain card whose veto BINDS; config `instance/config/memory-supersession.yml` (missing = ratified `soak`, malformed/unknown = `hold`, fail-closed); soak ledger gitignored (ids + hashes, never content); own services row Sun 05:45; egg ships only the `.example` twin | additive | alpha-additive · MEMORY-W3 | done | Landed 2026-07-16 by the memory-w3a integrator from the lane's reviewed diff (rebased onto `aa56f43e`, three recheck findings closed upstream, `--3way` clean; organ + tests byte-identical to the reviewed FIXED copies). Companion ci-collect lane landed same batch: stale FW-023 CI step renamed (lib/tests IS collected — the census claim was false), task_adapters + world-aesthetic suites added as separate CI steps, sov6 denied-need parametrized negative test pins the grant authority layer. Launchd install deliberately NOT done from the worktree (generate-plists + bootstrap path). | `python3.12 -m pytest cabinet/scripts/tests/test_memory_supersede_apply.py -q` — 74 passed | git revert; bootout + row delete + regenerate if installed meanwhile; per-pair `--undo` re-nulls exactly the pointer the organ set |
| MEMORY-W3-BC | Consolidation boot-pack: reflection/retro loops terminate in consolidation (`consolidated_belief` / `trust: reflection`; skills + doctrine-pack copies byte-parity) + `cabinet/scripts/memory-distill.py` review-then-promote captain-law digest organ (proposal-only default; `--apply` refuses unless the on-disk proposal byte-matches a fresh render, then promotes `captain-law-digest.md` + queues rows; `--check` read-only staleness probe → doctor N2 WARN/AMBER + retro Part 5 step 13) + staged germline boot-pack artifact `docs/proposals/germline-session-start-digest-2026-07-15.patch` (ceremony = CG-27) | additive | alpha-additive · MEMORY-W3 | done | Landed 2026-07-16 from the lane's reviewed diff (9/9 findings closed upstream); egg-safe (.patch is a plain file under docs/proposals; manifest expect-absent rows; test_egg_export gone-tuple); teeth: suite fails 9 tests on the pre-fix build, mutant/tamper/pristine-allows controls embedded; post-ceremony simulation on a fresh clone green with harness verdicts byte-identical to pristine base. | `python3.12 -m pytest cabinet/scripts/tests/test_memory_distill.py cabinet/scripts/tests/test_session_start_digest_patch.py -q` — 45 passed | git revert (tree side); the staged patch never applies outside the CG-27 ceremony — pre-ceremony the digest is inert |
| MEMORY-W3-D | Retrieval eval standing gate at four layers: `CABINET_MEMORY_RERANK` off-seam + `retrieval-eval.sh --no-rerank` arm; nightly `retrieval-eval` services row (03:50, both arms, floors recall≥0.60 / MRR≥0.50 per arm, one verdict JSONL line/night to `cabinet/logs/retrieval-eval-history.jsonl`, credless = clean skip); cabinet-doctor check 11 via `--probe` (file+env only, WARN/AMBER, wake-grace); RANKING-BLOCK sha256 fingerprint fixture regenerable only by a passing `--stamp` | additive | alpha-additive · MEMORY-W3 | done | Landed 2026-07-16; doctor/services conflicts vs lane BC resolved both-kept, master's de-Nate'd comment preserved. Upstream: inverted-order mutant collapses MRR to 0.10 (blended arm exits 1, rerank arm passes); live arms diverge (rerank MRR 0.958 vs blended 0.736); fingerprint stamped via a real passing run; launchd install of the new row deliberately NOT done from the worktree (normal generate-plists + bootstrap path). | `python3.12 -m pytest cabinet/scripts/tests/test_retrieval_eval_gate.py -q` and `python3.12 -m pytest cabinet/scripts/lib/tests/test_memory_rerank_toggle.py -q` — 24 + 5 passed | git revert; bootout + row delete + regenerate if installed meanwhile |
| MEMORY-W3-E | Embedding-provenance stamp hardening: `memory_embedding_stamp --if-absent` (INSERT … ON CONFLICT DO NOTHING) is the only mode deploy paths call (load-preset Neon arm + bootstrap step_apply_schemas) so redeploys can never overwrite stamped provenance (doctor DIMS-DRIFT WARN latches); bare mode = reserved re-stamp upsert; unknown args exit 2 pre-psql; bootstrap stamp subshell pins the hatched estate; EMBED_* resolve process-env > cabinet/.env grep-extract > default identically in memory.sh and doctor ES_*; `.env.example` documents the seam | additive | alpha-additive · MEMORY-W3 | done | Landed 2026-07-16 incl. the EMBED-SEAM row-note update (mechanism detail lives there). Upstream: stateful stub-psql end-to-end (stamp OK → flip WARN → if-absent redeploy preserves row, WARN latches → bare re-stamp OK); mutant reproduces the pre-fix false-green; parameterized psql -v only, DSN never printed, no DELETE. | `python3.12 -m pytest cabinet/scripts/lib/tests/test_embed_seam_wiring.py -q` — 32 passed | git revert |
| CG-27 | germline session-start boot-pack (staged dark) — captain-law digest boot injection + write plane, ONE patch ONE ceremony over THREE schg files: `hooks/session-start.sh` (digest injected in full, first), `hooks/pre-tool-use.sh` (§5 Write/Edit arm + §5c CAPLAW_PATH_RE gain the digest AND the distiller as doorway protection), `lib/officer-sandbox.sh` (kernel deny on the digest vnode); artifact + amendment doc under `docs/proposals/` | germ-keep | captain-gated · CG | done | CEREMONY EXECUTED 2026-07-16 (unlock → live ff-pull to d9f09bb8 [patch master-landed @32a26c6d per C17 precedent, superseding the in-window patch step] → in-window distiller re-run + re-promote sha 85fed17b → relock, 78 locked ARMED; post-ceremony state-aware suite green on LIVE copies; boot spot-check digest-first). Originally filed by the memory-wave3 integrator — the row is the NAMED HANDBACK (ceremony is Captain-sudo, non-grantable). No locked file edited anywhere; patch built against pristine copies, verified by the state-aware CI suite without touching locks; boundary re-verified at filing (78 locked / 0 unlocked). Same ceremony that starts boot-injecting the digest write-guards it (hook + bash screen + kernel) — all-or-none pinned. Captain-side kill switch: delete the digest, boot output byte-identical to today. | at ceremony: Captain unlock → `patch -p1 < docs/proposals/germline-session-start-digest-2026-07-15.patch` → `bash -n` ×3 + state-aware suite + `run-hook-regression.sh` green → commit with this row's status flip → Captain relock + verify same session | do not apply; or `patch -p1 -R` in a new window / revert the ceremony commit — three files together, never partial; runtime kill switch needs no unlock |

## 35. Addendum — Evidence Recorder v1 hardening row (A13 parity, appended 2026-07-16)

| CG-28 | evidence recorder hardening — 27-finding fix batch for the deep review of PR #140 Evidence Recorder v1 (the onboarding audit plane). CONTENT change over the ALREADY-schg evidence/hook/onboarding set (no boundary extension): `hooks/pre-tool-use.sh` (§5a path-normalize raw-read boundary + reject multi-line doorways + destructive-verb store-wipe arm + interpreter-import screen — 3 P0 same-UID bypasses #1/#2/#3 + #4-hook), `framework/evidence/{recorder,verifier,redaction,policy,__main__}.py` + tests, `framework/onboarding/journey.py`, dashboard `onboarding/evidence/route.ts` + `telegram/provisioning-webhook/route.ts`; amendment doc `docs/proposals/germline-amendment-evidence-recorder-hardening-2026-07-15.md` | germ-keep | captain-gated · CG | captain-gated | LANDED ON MASTER 2026-07-16 @298d8d23 via PR #149, 7/7 CI green (run 29502781132), off review commit e5ec86e. The plane shipped as its own schg-locked audit surface in PR #140, so its own reviewed defects (incl. 3 P0 same-UID hook bypasses falsifying the 'Captain-only evidence boundary': §5a unnormalized → reads the HMAC .signing-key via `..`; `rm -rf x/../evidence/v1` wipes the append-only store; line-oriented safe-doorway grep → newline command-smuggle) could only be fixed as a germline content change. 7 file-disjoint groups off master, every finding a teeth-test proven to fail pre-fix (reviewer reverted hunks); redaction 2nd round caught a ReDoS in its own r1 keyword relax; hook builder REJECTED the review's GERM_PATH_RE edit (would break the lockstep meta-test) → normalizer+rm-arm keeps germline-lock.sh + immutable-core.yml + §5 case-list BYTE-IDENTICAL (no boundary extension; 371 lockstep tests green). Verified merged tree: evidence/onboarding/policies 249p/1s, golden-evals 26/26, layer-sep new=0, docs-track GREEN, null-hatch PASS. | at ceremony (Captain-sudo, same day): from clean master @ merged tip, `sudo germline-lock.sh unlock` → `git checkout origin/master -- <14 germline files: pre-tool-use.sh, framework/evidence/{__main__,policy,recorder,redaction,verifier}.py + the 4 evidence tests, journey.py, onboarding/evidence/route.ts+.test.ts, provisioning-webhook/route.ts>` → `sudo germline-lock.sh lock` → `status` (BOUNDARY ARMED) + `verify` (write-refused) SAME session | `git revert -m 1 298d8d23` reverts the whole batch together (files stay branch-side inert until the ceremony re-materializes them under schg); never partially apply |

## 36. Addendum — final-residual-scrub continuation row (A13 parity, appended 2026-07-16)

Plan-doc row for the ledger id filed by the final-residual-scrub continuation
pass — verifying the 7 named residual items (products/golden-evals/Husqvarna/
step-network-bootstrap/brain-audit/captain-vetoes/9-digit-literals) plus the
`_PRODUCT_TOKEN` ratchet against a fresh export, fixing the one genuine gap
found. Values are referenced by class only, per the real-value doctrine.
Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| R170 | final-residual-scrub continuation — verified all 7 named residual items + the `_PRODUCT_TOKEN` ratchet CLOSED or AT PRACTICAL CEILING (germline-blocked); found+fixed 1 straggler comment (`compose_unified.py`'s missed `stepnetwork`→`example-org` twin of the sibling-file fix); fresh gate (d) re-measurement (699, down from 700) — see the yml row's note for the complete per-item evidence and full bucket accounting | compress | beta-compression · - | done | Merged current origin/master first (19 commits behind, clean). Verified each of the 7 items directly against a fresh commit-based export (not source-tree grep): all either already done by the prior same-day pass on this branch, or at their germline-blocked practical ceiling (cross-referenced against the FULL germline-lock.sh FILES[]+DIRS[] arrays, not FILES[] alone). Fixed the one straggler found. Re-ran full suites post-merge: framework/ 5170p/24s (env-only skips), cabinet/scripts/tests 946p/2s/1 pre-existing-confirmed-unrelated fail (predates this branch on origin/master, local-only, CI-skipped). | `python3.12 -m pytest cabinet/scripts/tests/test_world_validator_gate.py cabinet/scripts/tests/test_world_census.py cabinet/scripts/tests/test_world_chronicle.py -q` — 64 passed (unchanged from baseline); `bash cabinet/scripts/check-layer-separation.sh` — new=0 | git revert commit d51f9fc9 (the only tree change this pass made) — restores the single missed comment; comment-only, zero logic change |

## 37. Addendum — captain-reminders landing row (A13 parity, appended 2026-07-16)

Plan-doc row for the ledger id filed by the captain-reminders integrator:
Spec 041's due-at reminder worker was shipped but scheduled nowhere (and its
trigger lib sourced from a dead convergence-era /opt path — a silent dead
limb); the tick-live lane armed it on the fleet manifest and the captain-arm
lane added the Captain delivery surface (needs-ledger one-tap cards + verdict
reconcile) plus the create path. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| CAPTAIN-REMINDERS-1 | captain-reminders — Spec 041 completion: tick-live arming (`cabinet/services.yml` `due-at-reminder-tick` cron row 300s + portable `cabinet/scripts/due-at-reminder-tick.sh`) + Captain-arm delivery (due captain rows file needs-ledger one-tap cards; grant/later/deny reconcile every tick) + create path (`cabinet/scripts/remind-captain.sh` + `cabinet/sql/042-tasks-reminder-kind.sql` + `framework.env.captain_slug()`); P2 closed en route (claim RETURNING is machine-fields-only, title re-read by id — a newline/tab title can no longer forge an officer-routing row) | additive | alpha-additive · - | done | Landed 2026-07-16 from two reviewed lane diffs on one clean worktree; sole overlap (`cabinet/scripts/due-at-reminder-tick.sh`) hand-merged: tick-live plumbing (SCRIPT_DIR root, cabinet/.env sourcing, loud degrade, quiet-tick heartbeat) + captain-arm routing/reconcile; triggers lib sourced SCRIPT-relative; tick-live psql stub upgraded to the merged claim shape; services.yml + runbook prose updated to match. Deliberately NOT installed from the worktree — the plist rides the normal generate-plists + deploy path. Runbook: `docs/runbooks/captain-reminders.md`. | `python3.12 -m pytest cabinet/scripts/tests/test_due_at_reminder_tick.py cabinet/scripts/tests/test_captain_reminder_arm.py -q` + `python3.12 -m pytest framework/tests/test_env.py -q` — 7 + 56, then 77 passed (incl. live ephemeral-Postgres claim/bump/re-arm/no-forgery suite + e2e of the shipped tick) | git revert the landing commit; if installed meanwhile: `launchctl bootout gui/$UID/com.cabinet.due-at-reminder-tick` + row delete + regenerate; migration 042 reverts by reclassifying `type='reminder'` rows then re-adding the two-value CHECK |

## 38. Addendum — platform-radar triage-gating row (A13 parity, appended 2026-07-17)

Plan-doc row for the ledger id filed by the platform-radar integrator when
the triage+gating lane landed: the self-updating-cabinet v1 foundation —
workarounds registry + sandboxed propose-only retest runner + adoption-gates
doctrine + CTO-shaped triage skill. v1 posture is observe/triage/propose
ONLY, with Ring-0 changes Captain-carded always. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| RADAR-1 | platform-radar triage+gating — `cabinet/config/workarounds.yml` registry (3 seed rows from the 2026-07-16 fleet-down incident) + sandboxed propose-only retest runner `cabinet/scripts/workaround-retest.{sh,py}` (verdicts still_needed/fix_confirmed/inconclusive; fingerprint-deduped `WPROP-` retirement proposals to `shared/interfaces/workaround-retire-proposals.jsonl`) + probe `cabinet/scripts/workaround-probes/egress-apply-lock-timing.py` + doctrine `docs/runbooks/platform-adoption-gating.md` (GATE 0..3) + skill `memory/skills/platform-radar-triage.md` (+ R155 wrapper `.claude/skills/platform-radar-triage/SKILL.md`; ADOPTION-GATES block verbatim-twin, test-pinned) | additive | alpha-additive · RADAR | done | Landed 2026-07-17 from the lane's reviewed diff (all 4 review findings closed + a sibling P2#2 path-qualified-verb instance; safety claims rewritten to the honest 3-layer model — reviewed-PR config as first trust boundary, drift/typo screen explicitly NOT a security boundary, macOS sandbox-exec deny file-write/network/signal as the real containment, `sandboxed:` stamped per verdict). v1 = observe/triage/propose-only (GATE 0), auto-apply only per Captain-pre-ratified class (GATE 1), adoptions ride the staged deploy path (GATE 2), Ring-0 Captain-carded every time with eval evidence (GATE 3 — the Ring-0 Captain-card law). Delta files are untrusted data end to end (json-parsed, string-compared only, never interpolated; hostile-delta injection control with canaries). No services.yml row / plist in this lane; the first live radar pass is the orchestrator's after install. | `python3.12 -m pytest cabinet/scripts/tests/test_workaround_registry.py cabinet/scripts/tests/test_workaround_retest.py cabinet/scripts/tests/test_platform_radar_triage_skill.py -q` — 50 passed (macOS incl. the obfuscated-verb sandbox-containment proof; skips honestly where sandbox-exec is absent) | git revert the landing commit (pure additive; gitignored runtime ledgers inert after revert) |

## 39. Addendum — /tasks event-stream dashboard emit-parity row (A13 parity, appended 2026-07-17)

Plan-doc row for the ledger id filed by the /tasks event-stream lane
(`cabinet/docs/tasks-board.md`): the durable stream `cabinet:tasks:events`
is produced by `my-tasks.sh` only — the dashboard write path mutates
`officer_tasks` without emitting, so stream consumers miss dashboard-driven
transitions until parity lands. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| R171 | `/tasks` event-stream dashboard emit parity — `cabinet/dashboard/src/lib/tasks.ts` mutation paths (`startTask`/`queueTask`/`doneTask`/`setBlocked`/`cancelTask`/`updateTask`, live via the `/api/tasks` + `/api/tasks/[id]` routes) emit nothing on `cabinet:tasks:events`; consumers see only `my-tasks.sh` transitions (a dashboard-blocked task files no Captain card) | additive | alpha-additive · - | todo | Emit the equivalent A6-enveloped entry from the dashboard mutation paths (same flat fields, envelope `enforce()` gate before the XADD, best-effort) or route dashboard mutations through the emitting CLI; keep the `cabinet:tasks:updated` pub/sub payload byte-stable; `cabinet/docs/tasks-board.md` carries the known-gap note until this row lands (remove it in the closing commit) | `grep -c 'cabinet:tasks:events' cabinet/dashboard/src/lib/tasks.ts` — 0 today (this row's gap); ≥1 + an emit-parity test when landed | additive row — revert the emit commit; the scoped doc claim stays true either way |

## 40. Addendum — tasks-canonical wave landing row (A13 parity, appended 2026-07-17)

Plan-doc row for the ledger id filed by the tasks-canonical integrator:
three reviewed lanes (context-doctrine, adapter-kit, event-stream) landed
as one unit — canonical `/tasks` doctrine + preset-aware context resolver
twins, the task-adapter kit with its conformance CI gate and nightly drift
falsifier, and the durable `cabinet:tasks:events` stream. Column format
matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| TASKS-CANON-1 | tasks-canonical wave — canonical `/tasks` doctrine (constitution "Canonical task surface" clause + `CLAUDE.md`/`docs/templates/CLAUDE-egg.md` twins; amendment doc `docs/proposals/germline-amendment-context-resolver-2026-07-17.md`); preset-aware context resolver twins (`cabinet_resolve_context` in `cabinet/scripts/lib/lanes.sh` → `cabinet/scripts/my-tasks.sh` + `cabinet/scripts/task_sync_runner.py`; dashboard `cabinet/dashboard/src/lib/active-context.ts` behind `/api/tasks`; CI ratchet `framework/tests/test_canonical_tasks_ratchet.py`); task-adapter kit (`cabinet/scripts/task_adapters/` base seam + auto-discovered conformance gate + in-memory reference + `_template` scaffold + gh-CLI `github_issues` + `cabinet/runbooks/task-adapter-authoring.md`); task-sync scheduling truth (`cabinet/services.yml` `task-sync` 900s row; orphaned `com.cabinet.task-sync.template.plist` deleted) + nightly `task-sync-drift` falsifier (verdict JSONL + doctor check 12 `--probe`); durable `cabinet:tasks:events` stream (A6-enveloped `my-tasks.sh` emits + `cabinet/scripts/task-events-watch.py` + `instance/config/task-watch.yml`; dashboard emit gap filed as R171) | additive | alpha-additive · - | done | Landed 2026-07-17 from three reviewed lane diffs on one clean worktree off origin/master @e90d9d2b; sole cross-lane overlaps (`my-tasks.sh`, `task_sync_runner.py`) 3-way merged clean; ledger append-append races resolved keep-both TWICE (CAPTAIN-REMINDERS-1 §37 kept; platform-radar §38 raced in on master mid-landing, kept; event-stream addendum renumbered §39, this §40 appended); constitution edit verified non-germline (not schg, not in `germline-lock.sh` FILES[]) and rides the amendment doc | `python3.12 -m pytest` over the six new cabinet test files + `cabinet/scripts/lib/tests/test_resolve_context_sh.py` + `cabinet/scripts/task_adapters/tests` + `framework/tests/test_canonical_tasks_ratchet.py` + `framework/tests/test_env.py` — green on the landed tree; full gate evidence in `shared/interfaces/reviews/feat-tasks-canonical-cp1.md` | additive wave — git revert the landing commit; service rows uninstall via the normal generate-plists regenerate path; no schema change rides this wave |

## 41. Addendum — vault-consolidation wave rows (A13 parity, appended 2026-07-17)

Rows filed by the vault-consolidation integrator: the Captain-ratified
2026-07-16 direction lands as two lanes off one clean worktree —
`product-brain/` → `vault/` rename + resolver/docs (VAULT-1, with its
germline hook-watch ceremony handback CG-30), then the Library retirement
(LIB-RETIRE-1, with its germline danglers ceremony handback CG-29).
Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| VAULT-1 | vault consolidation — `product-brain/` → `vault/` (git mv; README rewritten as the doc-placement one-pager: what-belongs, `[[wikilink]]` conventions, Obsidian-compatible-never-required); `framework.env.org_vault_dir()` resolver (env override → legacy alias → new key → legacy key, fail-closed) with `product_brain_dir()` deprecated alias; `memory-reconcile.sh` + `backfill-memory.sh` walk `vault/` + legacy + `docs/**/*.md`; egg manifest ships `vault/README.md`; rename ratchet `cabinet/scripts/tests/test_vault_rename_ratchet.py` | additive | alpha-additive · - | done | Landed 2026-07-17 from the reviewed vault-rename-docs lane diff, clean worktree off origin/master @e90d9d2b; zero schg paths touched — the hook watch list rides the staged ceremony patch (CG-30 handback); interim coverage via nightly reconcile + backfill of both trees | `python3.12 -m pytest cabinet/scripts/tests/test_vault_rename_ratchet.py cabinet/scripts/tests/test_bootstrap_memory_chain.py -q && python3.12 -m pytest framework/tests/test_env.py -q && bash cabinet/scripts/docs-track-code-sweep.sh` | git revert the landing commit; alias chain keeps legacy configs working either side |
| CG-30 | germline vault hook-watch ceremony — `cabinet/scripts/hooks/post-file-write-memory.sh` (schg DIR) gains `*/vault/*.md` → product_brain + rooted `docs/*.md` → framework_doc, keeps legacy arm; staged patch `patches/germline-vault-hook-watch-2026-07-17.patch` + addendum `docs/proposals/germline-vault-hook-watch-addendum-2026-07-17.md` | germ-keep | captain-gated · CG | done | Filed 2026-07-17; CONTENT LANDED TO MASTER 2026-07-17 (hook watch-arms + their test locks, one commit; routing revised to MASTER-FIRST per the CG-27/CG-31 checkout-from-master precedent — the staged patch stays as ceremony reference/proof); still a NAMED HANDBACK (Captain sudo, non-grantable): live-tree activation pending the Captain checkout window — ceremony = content-on-master + checkout-activate, never edit-in-live-tree; only live delta until then is instant vs nightly embedding of vault/docs writes | content landed on master; at ceremony (Captain sudo, relock SAME day): fetch → unlock `cabinet/scripts/hooks` → `git checkout origin/master --` the hook → blob-verify vs origin/master → `bash -n` + `test_bootstrap_memory_chain.py` green → relock → `status`+`verify` SAME session | git revert the landing commit — legacy watch arm + nightly reconcile keep working; a synced live box re-syncs from master in a new window; or do not run the live sync |
| CG-31 | kill-switch flip-event audit trail ceremony — `cabinet/scripts/kill-switch.sh` (schg) gains `emit_flip_event`: verified activate/deactivate emit the registered `kill_switch_activated`/`_deactivated` org events, fail-quiet so the ledger can never block the emergency surface; amendment `docs/proposals/germline-amendment-killswitch-events-2026-07-17.md`; tests `cabinet/scripts/tests/test_kill_switch_events.py` (7 behavioral, disposable-redis) | germ-keep | captain-gated · CG | done | Filed 2026-07-17 — Wave-1 item (e) second half (captain-message-effect §2.3): switch flips left no ledger trace, so the 07-15 lockdown reading INACTIVE on 07-16 is unattributable; first half (eval redis sandbox) landed @1cd84459. Emit-only: SET/DEL + read-back byte-unchanged; unverified flips emit NOTHING; raw redis-cli bypasses by nature (officer-side raw DELs stay hook-blocked, EVAL-001b) CEREMONY EXECUTED 2026-07-17: unlock → checkout (blob-verified) → relock (78/0, write-refused); live acceptance green on a disposable redis (both flips read-back-verified + both audit rows landed); live switch untouched | at ceremony (Captain sudo, same day): fetch → `sudo germline-lock.sh unlock` → `git checkout origin/master -- cabinet/scripts/kill-switch.sh` → blob-verify vs origin/master → `sudo germline-lock.sh lock` → `status`+`verify` SAME session → live acceptance vs DISPOSABLE redis + tmp CABINET_EVENT_LOG_DIR only | revert the amendment commit on master + a new unlock→checkout→relock ceremony; no state migration (emit-only) |
| CG-32 | adjudicated publish-gate allowlist — SYNTHETIC/constant numeric literals in shipped test fixtures + schemas that trip GATE (d) `id:[0-9]{9,}` but are NOT captain board/chat ids (JS max-int `9007199254740991`; synthetic chat/bot id `8123456789`/`987654321`; epoch stamps `1786000000`/`1721464800`; redis stream ids `1700000000000`/`1700000000001`; CI run `29687917451`; date fragment `202607140`) — each masked by its EXACT value per the CG-19 mechanism; a fixed synthetic mask can never hide a real id, and any other banned token on the same line still fails | additive | captain-gated · CG | done | Recorded + EXECUTED 2026-07-21 (relaunch scrub-2 residual pass, 2026-07-07 full-autonomy grant + scrub-not-consent ruling 2026-07-10); same class as CG-21/CG-22 all-zero constants extended to non-zero synthetic fixture ids. The DOGFOOD checksum-sealed bundle archives out of the egg instead (mask-uncoverable all-zero genesis hash); the two bare ten-zero prose hits were reworded at source (a ten-zero substring of the CG-21 40-zero constant would trip the gate self-audit), and the two ascending fakes `123456789`/`1234567890` were reworded to `8123456789` (they are substrings of the gate's own planted self-test sample `501234567890`). Captain ratifies or strikes at CG-7 | `bash -n` gate script + `allow CG-32 <digits>` directives present in the untracked patterns file (gate self-audit test covers non-leakage on CI) | delete the `allow CG-32` directive lines from the untracked patterns file — id-run RED returns on those fixtures |
| LIB-RETIRE-1 | Library retirement — second vector store retired (closes memory-study Q4/C7): MCP server deregistered from BOTH `.mcp.json` layers; `/library` dashboard surfaces → retirement stubs (vault archive + `memory_search`); `lib/library.sh` writes vector-free (schema stays in place, dormant — no destructive DDL); READ-ONLY archive export `cabinet/scripts/retire-library-export.py` → `<root>/vault/library-archive/` (vault first — the rename landed first in this wave; legacy fallback for un-migrated roots); resurrection ratchet + staged-ceremony-patch checks in `cabinet/scripts/tests/test_library_retirement_ratchet.py` | compress | beta-compression · - | done | Landed 2026-07-17 after VAULT-1; ruling: Captain-ratified 2026-07-16 (runbook `docs/runbooks/library-retirement-2026-07-16.md`); transitional copy finished to `vault/`; dual-root seams declared in the rename ratchet; EXPORT NOT RUN here (orchestrator executes on the box); supersedes library-organ content of R084/R098/AUD-12/R086 (notes updated, rows preserved); germline danglers ride CG-29 | `python3.12 -m pytest cabinet/scripts/tests/test_library_retirement_ratchet.py cabinet/scripts/tests/test_retire_library_export.py -q && python3.12 -m pytest cabinet/scripts/lib/tests/test_library_sh_retirement.py -q && (cd cabinet/dashboard && npm run test)` | git revert the landing commit — DB never written (export read-only, unexecuted), schema never dropped |
| CG-29 | germline library-retirement danglers — git-side content MASTER-LANDED (drop `library` from every `cabinet/mcp-scope.yml` officer/scaffold grant + `universal:`, drop `mcp__library` from `.claude/settings.json` allow, land the `cabinet/scripts/start-officer-mac.sh` comment-only merge-note fix); the THREE schg live inodes sync via checkout-from-master (CG-27/CG-31 precedent). Addendum `docs/proposals/germline-library-retirement-addendum-2026-07-16.md`; reference patch `docs/proposals/germline-library-retirement-2026-07-16.patch` | germ-keep | captain-gated · CG | done | CONTENT LANDED TO MASTER 2026-07-17; live-tree activation pending the Captain checkout window (ceremony = content-on-master + checkout-activate, never edit-in-live-tree). Filed 2026-07-17; routing revised 2026-07-17 to MASTER-FIRST (per 2026-07-07 grant; CG-27 (32a26c6d) / CG-31 checkout-from-master precedent) — git-side content lands on master, no schg inode touched; still a NAMED HANDBACK (sudo unlock+relock is interactive). Landing inert (server already deregistered from both `.mcp.json` layers); post-land the grant surfaces are ratcheted clean by `test_germline_grant_surfaces_stay_library_free` | content lands on master; at ceremony (Captain sudo, relock SAME day): fetch → unlock → `git checkout origin/master --` the three files → blob-verify vs origin/master → relock → `status`+`verify` SAME session → ratchet suite green (staged-patch test skips on the landed mark) | git revert the landing commit (removal-only config + additive tests/docs; no schg inode touched) — a checked-out live box re-syncs from master in a new window; or do not run the live sync |
| CG-33 | COG-4 organ packaging — extension gate pair germline amendment: `framework/schemas/extension-manifest.schema.json` gains `kind: organ` + the fourteen organ-contract fields (incl. `domain_operations` namespaced ids, `descriptor`, `freshness_needs`, `starvation_bound`) + the `journal:<id>` undo grammar, schema STAYS draft-07 with `additionalProperties:false`; `cabinet/scripts/validate-extension.sh` hand-rolled interpreter gains an integer branch + the required-when-organ ORGAN BLOCK on both validator paths; proposal `docs/proposals/germline-amendment-extension-manifest-organ-2026-07-23.md` carries the complete proposed edit text | germ-keep (—) | captain-gated · CG | captain-gated | Filed 2026-07-23 at COG-4 contract landing (contract §4.5, MF-G1 — maximum lead time; NAMED Captain sudo-unlock handback, non-grantable). W4+ units build against the proposed text; window not open when W4 lands ⇒ dependent organ-validation units PARK with dated markers — schg never worked around | at ceremony (Captain sudo, relock SAME day): lock state re-verified fresh (`germline-lock.sh status` + `ls -lO`) → unlock → apply the proposal edit pair → proposal §4 battery green (organ fixture OK on both paths, named mutants RED, channel manifests unchanged-valid, AX suite + amendment lint green) → relock → `status`+`verify` SAME session | one-revert restore of the two files from pre-amendment bytes — itself Captain-windowed (contract §16 code inverse); until applied nothing depends on the edit (dependent units PARK) |
| CG-34 | COG-5 holdout freeze — immutable-core.yml Ring-0 listing (framework/policies dir-cover): EXACTLY ONE `files`-class entry `framework/evolution/holdout_gen.py` + `pending: [germline-lock, hook-s5, hook-s5b, base-safety]` (SOV-C8 lockstep, the axes-amendment precedent) added to `framework/policies/immutable-core.yml`; gate-S0 refusal (`touches_ring0`, gate.py:347) binds from the LISTING alone — Stage B of the COG-5 contract §7.4 STAGED freeze truth ((iii)+(iv) true from the window onward; the Stage-A interim posture is honestly NOT Ring-0); proposal `docs/proposals/germline-amendment-immutable-core-holdout-2026-07-24.md` carries the complete proposed edit text | germ-keep (—) | captain-gated · CG | captain-gated | Filed 2026-07-24 at COG-5 contract landing (contract §7.5.1, MR1 — maximum lead time; NAMED Captain sudo-unlock handback, non-grantable; BATCHABLE with the CG-33 window — HANDBACKS item 19 — one window, the reviewed edits together, same-day relock). W5+ freeze-verification units build against the proposed text; window not open when they land ⇒ each PARKS with a dated marker and the arming record carries `holdout_freeze: pending-captain-window` — schg never worked around | at ceremony (Captain sudo, relock SAME day): lock state re-verified fresh (`germline-lock.sh status` + `ls -lO` on `framework/policies/`) → unlock → apply the proposal's one-entry listing → proposal §7 gates A-D green (yaml parse + lockstep meta-test with the four pending xfails, gate-S0 refusal probe + non-listed-sibling negative control, amendment-doc lint) → relock → `status`+`verify` SAME session | one-revert restore of `framework/policies/immutable-core.yml` from pre-amendment bytes — itself Captain-windowed (a germline edit both ways — contract §16); until applied nothing depends on the listing (freeze-verification units PARK; the league-open checklist's Stage-B requirement stays unmet) |
| CG-35 | drafting to act-then-tell — `framework/policies/authority-matrix.yml` `draft_only` row moves from the earn-up ladder (`graduated: auto`, everything else `propose_only`) to the trust-first act-and-tell ladder (`notify_after` at graduated/eligible/propose_only/unmeasured, `demote: propose_only`), mirroring the `read_only_dispatch` row; the sovereign posture table's `draft_only` row moves with it (leaving it would make sovereign NARROWER than guardian at `unmeasured`); `earn_up` stays `propose_only` (narrowing is always legal) and every hard-ceiling row is untouched — **sending still requires the Captain**; proposal `docs/proposals/germline-amendment-draft-only-act-then-tell-2026-07-26.md` carries the complete proposed edit text, the validator analysis and the exact test pins to move | germ-keep (—) | captain-gated · CG | captain-gated | Filed 2026-07-26 at the arm-the-cabinet landing (NAMED Captain sudo-unlock handback, non-grantable; BATCHABLE with the CG-33 + CG-34 windows). Captain ruling 2026-07-26 overrides his own 2026-07-04 draft_only earn-up ruling — reason of record: with the trust ladder unable to climb, earn-up meant asking forever. Until the window opens the ruling is NOT in effect and no setting claims otherwise (a config naming `act_then_tell` would resolve to `guardian` — a setting that lies). LANDED 2026-07-26 on `feat/draft-only-act-then-tell` (landed-then-ceremonied — a germline CONTENT fix is built in a clone, landed to master like any change, and the window re-materialises the landed bytes): both edits + the §5 test-pin moves + comment-only corrections in the three germline modules that still claimed `draft_only` keeps earn-up + a plain-language fix to `docs/how-your-cabinet-is-governed.md` + new CI arms verified to FAIL against pre-change code are ON MASTER. The §3 optional `_TRUST_FIRST_UNMEASURED` tightening was NOT taken (it costs a framework production line against a census budget at its ceiling, and its merge channel is already closed twice over) — still the Captain's call at the window. A PRE-EXISTING validator gap found while writing the mutation sensors (action_types are pinned to map exactly once but not to WHICH class, so a send kind could be relocated off the ceiling row) was recorded in proposal §9 and is now CLOSED in the validator (2026-07-27, `fix/matrix-class-mapping-pin`): matrix.py invariant #6 `_validate_ceiling_class_mapping` pins the whole ceiling mapping against one declared source, closing both that variant and a second one found by executing it (re-pointing which ROW carries the ceiling, which reached a full ALLOW at the gate); the +65 census lines were paid as the `matrix-class-mapping-pin` temporary allowance. Status stays `captain-gated`: the sudo window is still owed, so the DEPLOYMENT's locked tree still reads as the 2026-07-04 ruling | at ceremony (Captain sudo, relock SAME day): lock state re-verified fresh (`germline-lock.sh status` + `ls -lO` on `framework/policies/authority-matrix.yml`) → unlock → apply the two edits → proposal §6 gates green (yaml parse; `python3.12 -m pytest framework/authority -q` with the §5 test pins moved in the same commit; `run-golden-evals.sh`; `cognitive-architecture-census.py`) → relock → `status`+`verify` SAME session | one revert of `framework/policies/authority-matrix.yml` to the pre-amendment bytes — itself Captain-windowed; until applied, drafting keeps asking, which is the pre-ruling behaviour, not a broken state |

## 42. Addendum — dependency-radar observe row (A13 parity, appended 2026-07-17)

Plan-doc row for the ledger id filed when the radar's deterministic,
observe-only half landed (built in the radarwave observe lane): nightly
hash-diff sweep over a tracked registry of upstream surfaces plus the
claude binary/PATH probe (the 2026-07-16 officer-boot incident class),
reporting deltas to runtime logs and the per-day triage-seam mirror the
gated platform-radar triage lane (RADAR-1, landed first) consumes. Fetched
text is UNTRUSTED provenance-fenced data end to end. The 2026-07-17 fix
pass closed the lane's five review findings (quadratic normalize → linear;
stale PATH pin → generator cross-pin; dead triage seam → mirror +
version-change deltas; ledger/A13 parity), and the landing pass closed the
one open cross-lane P2: sha-shaped `new_version` mirror stamps false-fired
comparator `version_condition` rows in the landed matcher — comparator
matching is now gated on version-shaped values, repro-pinned in both
lanes' suites. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| RADAR-OBSERVE | dependency radar, observe half — `cabinet/scripts/dependency-radar.py` + tracked registry `cabinet/config/dependency-radar.yml` (9 surfaces incl. the claude binary/PATH probe cross-pinned to `generate-plists.py` PATH_ENV), services row `dependency-radar` (nightly 04:20), doctor check #14 (raced renumber at landing: PR #155's evidence-plane probe took #12 and shifted task-sync-drift to #13), runbook `docs/runbooks/dependency-radar.md`, runtime outputs under `~/.cabinet/` + the per-day triage mirror `cabinet/logs/platform-radar/delta-<date>.json` (gitignored; the gated triage lane's intake) + the comparator version-shape gate in `cabinet/scripts/workaround-retest.py` `condition_matches_delta()` | additive | alpha-additive · - | done | Built in the radarwave observe lane; 2026-07-17 fix pass closed all 5 review findings: linear-time normalize (5MB flood 0.04s, was hours — corpus-pinned byte-equivalence), PATH cross-pin (registry/remedy/runbook = PATH_ENV, test parses the generator), triage seam alive (mirror in the triage lane's intake shape; probe version-bump deltas make version_condition retests reachable; hostile content inert in every surface incl. the mirror), ledger+A13 parity. Landing pass (this row) closed the open P2 in the LANDED matcher: sha256:<hash> content stamps (and probe status words) satisfied every numeric `>` pin via compare_versions' non-numeric fallback — comparator version_conditions now match only version-shaped new_version (120-char cap before the anchored regex; sha/status strings never match; fixed-in stays never-auto-matched), reviewer's repro pinned in test_workaround_retest.py (unit + e2e --from-delta) and through the REAL matcher in test_dependency_radar.py; deduped the docs-sweep-allowlist glob both lanes shipped. Remaining: generate-plists + deploy (installs com.cabinet.dependency-radar; refreshes stale-PATH fleet plists). | `python3.12 -m pytest cabinet/scripts/tests/test_dependency_radar.py cabinet/scripts/tests/test_workaround_retest.py -q` + `python3.12 cabinet/scripts/dependency-radar.py --validate-registry` — radar suite incl. ReDoS-flood, hostile-changelog, seam-contract + cross-pin controls; retest suite incl. the sha-vs-comparator repro | git revert; if installed: `launchctl bootout gui/$UID/com.cabinet.dependency-radar` + drop the services row + regenerate; runtime outputs gitignored/inert; reverting the matcher gate restores only the sha-false-fire defect |

## 43. Addendum — reminders-mini wave row (A13 parity, appended 2026-07-17)

Row filed by the reminders-mini integrator: the Captain-ruled instant
at-fire-time reminder push with one-tap buttons, the `remind-captain.sh`
dead-root fix, and the quiet-hours discoverability question in the Phase-2
cabinet-init interview. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| REMIND-2 | reminders mini-wave — captain-reminder INSTANT at-fire-time push + one-tap buttons (Captain ruling 2026-07-17 "the time of day is set by the captain → push instantly"): `cabinet/scripts/captain-reminder-arm.py` `push_card` submits ONE `kind=captain-reminder` attention-gate item per fire; `framework/attention/charter-default.yml` kind-matched `captain-reminder` FLOOR class (Captain provenance, §4.10.4) + `deadline_iso=due_at`/ping-now belt; inline ✓ Done / ⏰ Later 7d / ✗ Drop buttons — `decision_card` CB_VERBS `ndg`/`ndl`/`ndd` + `tap_wire` needs-verb arm composing the CANONICAL typed binder line through `binder_wire.handle_captain_update` (equal-authority door; 8-hex fullmatch re-validated pre-compose); `remind-captain.sh` dead `/opt/founders-cabinet` root default → script-relative resolution; Phase-2 interview quiet-hours question (`framework/onboarding/quiet_hours.py` render-from-live-default + keep/change/disable through `charter.amend`, floor carried unchanged; `.claude/skills/cabinet-init/SKILL.md` step; deployment comms-charter override + amendments ledger gitignored); runbook `docs/runbooks/captain-reminders.md` Known-residual (instant push) closed | additive | alpha-additive · - | done | Landed 2026-07-17 from two reviewed lane diffs on one clean worktree off origin/master @ed5b64d8; schg guard ls -lO clean on every touched live path (zero locked hunks — no ceremony stage); untrusted title stays card DATA (never rides a button); per-fire uuid5 situation identity (snooze-bump re-pushes, crash re-file dedups); push failure degrades to one stderr line with the briefing digest as durable fallback; keep/change/disable fail-closed with floor never widened | lane suites 21 + 53; full `cabinet/scripts/tests` 1392 passed/4 skipped; framework attention+comms+onboarding 644 passed/1 skipped; `bash -n` clean; docs-track-code-sweep GREEN (files=44 findings=0); check-layer-separation new=0; A13 + id-uniqueness + ledger-status-parity green pre+post; full gate evidence in `shared/interfaces/reviews/feat-reminders-mini-cp1.md` | git revert the landing commit (both lanes additive: new modules/tests, appended charter floor entry + classes row, tap-wire verb arm, verb-enum widening, gitignore entries; no service rows, no DB schema change; deployment comms-charter overrides are gitignored runtime files) |
| PRESET-DEV-1 | developer preset v1 — OPTIONAL software product-kind kit (Captain-ratified 2026-07-17; first of a family under the general Flavor-B product org): `presets/developer/` flat-copied from work + ratified deltas (onboarding `lane_mcps [library, telegram, github, playwright, neon-ro]`, `cabinet_default_plugins []`; First Mate display title with machine id `cos` frozen; CTO `+mcp__github`; CPO pairing paragraph; `coordinator_title` terminology key; safety-addendum connector rows + trifecta legs + declared-OFF Stripe/PostHog/Resend; validate.sh neutrality assert + residue battery; measurement test renamed `test_org_scenarios_developer.py` — basename-collision fix) + day-1 README runbook + 8 connector one-pagers (vercel = REST+probes NOT MCP; env split `VERCEL_TOKEN` walked vs `VERCEL_API_KEY`+`VERCEL_TEAM_ID` probe-read) + `starter-spaces/product-journal.yml` (Space renamed off the vault-ratcheted product-compound token) + `starter/probes.yml`; root `.mcp.json` ADDITIVE `github`/`neon-ro`/`playwright`; `plan.py` `_lane_mcps` github-when-repo; `generate-instance.py` optional `cabinet.preset` (opt-in print only — work stays the functional default); cabinet-init SKILL.md §3b optional preset question; `probes.yml.example` stale TEAM_ID caveat fixed; `packs/preset-developer-pack` (referenced payload, flavor `[personal, org]`) + marketplace row + core-description pin; twin-drift pin `cabinet/scripts/tests/test_preset_developer_parity.py` | additive | alpha-additive · - | done | Landed 2026-07-17 from a clean worktree off origin/master @2e04642e (per 2026-07-07 full-autonomy grant). Declarations-only: env NAMES + URLs + markdown — zero glue code, zero secrets, zero scope auto-grants (mcp-scope/capabilities stay declare→Captain-applies). Known gaps deliberate: dashboard onboarding API question excluded (schg-locked — future ceremony); pinned third-party vercel-mcp retirement filed separately (live fleet references it); services.yml probe-vercel prose note still names the old TEAM_ID constant; flat-only composition revisit trigger (4th preset) noted in the orchestrator BACKLOG | `bash presets/developer/validate.sh && python3.12 -m pytest presets/ cabinet/scripts/tests/test_preset_developer_parity.py cabinet/scripts/tests/test_generate_instance.py -q && python3.12 -m pytest framework/onboarding/tests/test_plan.py -q && python3.12 -m json.tool .mcp.json > /dev/null && bash cabinet/scripts/validate-extension.sh packs/preset-developer-pack` — 93 + 13 at landing; layer-sep new=0; seed-library dry-run green | git revert the landing commit(s) — all additive; no service rows, no schema change, no live-runtime surface touched |

## 44. Addendum — vault-browse landing row (A13 parity, appended 2026-07-17)

Row filed by the vault-browse integrator: the read-only `/vault` dashboard
browser over the org vault corpus — the onward reading surface the retired
`/library` notice (LIB-RETIRE-1) now points at. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| VAULT-BROWSE-1 | read-only vault-browse dashboard view — `/vault` `[[...path]]` catch-all server component over the ORG vault FILESYSTEM: `cabinet/dashboard/src/lib/vault.ts` (faithful TS mirror of `framework/env.py` `org_vault_dir()` that NEVER reads the personal `vault_dir` key; realpath-under-root confinement — NUL/absolute/`../`-traversal/symlink-escape all deny to a generic 404; listings lstat and never follow symlinks; basename+slug+frontmatter-title note index, bounded walk), `src/lib/vault-wikilinks.ts` (pure parsers COPIED from `lib/wikilinks.ts`, never imported — that module pulls `@/lib/db`; internal-only code-aware `[[wikilink]]` rewrite, unresolved → inert sentinel), `src/components/vault/VaultMarkdown.tsx` (react-markdown + remark-gfm + rehype-sanitize defaultSchema minus `<img>`; no rehype-raw, no dangerouslySetInnerHTML; hardened anchors), Vault in `ADVANCED_NAV` only, `/library` retirement notice links onward to `/vault`; deps react-markdown@10.1.0 + remark-gfm@4.0.1 + rehype-sanitize@6.0.0; runbook `docs/runbooks/vault-browser-2026-07-17.md` | additive | alpha-additive · - | done | Landed 2026-07-17 from the reviewed lane diff on one clean worktree off origin/master @db1ddd0d; read-only vault-browse view repointing `/library` over the vault filesystem — auth-gated ((authenticated) route group, HMAC `cabinet_session` middleware; `/vault` + `/api/vault/*` 307 → `/login` unauthenticated), DB-free (library vector store stays retired; LIB-RETIRE-1 ratchet unaffected), zero mutation surface (source-contract-pinned); root resolution fail-closed, personal `vault_dir` never read (test-pinned); world/graph surfaces + Tailscale phone exposure = documented Phase-2 (runbook "Not in this wave") | `cd cabinet/dashboard && npm ci && npm test && npm run typecheck` — vitest incl. traversal/symlink/NUL/absolute negative controls, XSS negative controls, internal-only wikilink rewrite, read-only+DB-free source contract, `/vault` 307 auth gate | git revert the landing commit (purely additive dashboard view + 3 sanitizing-render deps; no DB schema change, no service rows, no germline paths) |

## 45. Addendum — Library identity/graph landing row (A13 parity, appended 2026-07-17)

Row filed by the identity-graph fix lane: `/library` becomes the reader
itself (Captain naming ruling 2026-07-17), the filesystem graph + backlinks
return, and the fix pass closes the review's XSS/ReDoS/ratchet/nav findings.
SUPERSEDES the `/vault`-addressing + nav facts pinned in VAULT-BROWSE-1
(row preserved above per ledger doctrine). Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| LIB-IDENTITY-GRAPH-1 | Library identity + graph/backlinks — `/library` IS the vault reader: the `[[...path]]` catch-all browser MOVED `/vault`→`/library` (retirement notice page deleted), `/vault/[[...path]]` = redirect alias (encoding parity, no open redirect), separate Vault nav entry DROPPED (ONE Library entry in both navs; `ADVANCED_NAV`=19 after the Evidence-wave union); graph resurrected at `/library/graph` on the FILESYSTEM (`src/lib/vault-graph.ts`: confined `listDir` walk + `parseWikilinksBounded` + `resolveNoteTarget`; depth 8 / 20k files / 32KB per-note parse cap / 8MB corpus budget / ~30s TTL; serialized props into `components/library/GraphCanvas.tsx` — zero API routes, zero DB; hover tooltip HTML-escaped in `components/library/graph-tooltip.ts`), backlinks under each note (`components/library/BacklinksPanel.tsx`, fail-closed); ReDoS guards in `src/lib/vault-wikilinks.ts` `parseWikilinksBounded`; vault-rename ratchet ALLOWED entry for `vault-graph.test.ts`; runbook `docs/runbooks/vault-browser-2026-07-17.md` updated same change | additive | alpha-additive · - | done | SUPERSEDES VAULT-BROWSE-1's `/vault` addressing, `Vault in ADVANCED_NAV only`, and notice-links-onward facts (that row stays as the confinement/sanitize substrate record). Fix pass closed the 2026-07-17 review findings: tooltip stored-XSS (float-tooltip innerHTML string branch) escaped + negative-controlled; corpus-amplified quadratic wikilink parse (~16.5s on a planted 200KB `[` note pre-fix) linear-guarded + capped + budgeted, timing-pinned; vault-rename ratchet ALLOWED entry (red only once the test file is TRACKED); nav 3-way seam vs Evidence union hand-resolved to 19. RESIDUAL → SEARCH lane (owner): pre-existing DB-backed `/api/library/*` routes stay live beside the zero-DB graph — `/api/library/graph` page-unreferenced, `search`+`typeahead` still consumed by `CommandPalette` in the authenticated layout; repoint-or-stub pending there | `cd cabinet/dashboard && npm ci && npm test && npm run typecheck` — graph confinement + edge-harvest-bounds suite, graph-tooltip XSS controls, `parseWikilinksBounded` ReDoS timing guards, route contracts, nav pin 19; plus `python3.12 -m pytest cabinet/scripts/tests/test_vault_rename_ratchet.py cabinet/scripts/tests/test_library_retirement_ratchet.py -q` with the lane files TRACKED | git revert the landing commit — route moves + additive modules/tests, nav entry drop, ratchet allowlist line, runbook edits; no DB schema change, no service rows, no germline paths; revert restores the VAULT-BROWSE-1 state |

## 46. Addendum — Library phase-2 wave landing row (A13 parity, appended 2026-07-17)

Wave row filed by the library-phase2 integrator: the Captain's 3-question
ratification landed — `/library` is the reader (identity + graph/backlinks,
per-lane contract row LIB-IDENTITY-GRAPH-1 in §45), the existing
cabinet_memory search is exposed on the Library root, and the world-library
lane is HELD un-landed (no review verdict on record — lens agents
limit-died; lane diff + aesthetic artifacts preserved in the wave
scratchpad). The retired STORE stays retired. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| LIBRARY-P2-1 | Library phase 2 — TWO reviewed lane diffs landed from one clean worktree off origin/master `@f75648df`: IDENTITY (`/library` = the `[[...path]]` vault reader, `/vault` = redirect alias, History note, ONE Library nav entry) + GRAPH/BACKLINKS (filesystem wikilink graph at `/library/graph`, zero API/DB) + SEARCH (GET `/api/library/search` over cabinet_memory — parameterized binds, org-knowledge classes only, rate-limited, cookie-gated; `LibrarySearch` box on the Library root; consumer card rewired). Integration deltas: search's notice-page hunk superseded by the root mount; `VAULT_NOTE_BASE` flipped `/vault`→`/library` + test pins (the search lane's own prescribed flip); middleware.test.ts both-lanes merge; ratchet ALLOWED entries for the 6 `product_brain` source_type-ENUM files (DB row data, not a path); docs refreshed to the landed link target. WORLD-LIBRARY lane NOT landed — no review verdict on record (lens+fix agents limit-died; artifacts preserved); `/api/world/library/*` ships nothing here | additive | alpha-additive · - | done | Landed 2026-07-17. Gates: vitest 117 files / 2153 passed + 1 env-gated skip; tsc clean; npm ci 0 vuln; docs-sweep GREEN 50/0; layer-sep new=0; python suite 1532 passed / 5 skipped / 2 failed → ratchet failure closed by the ALLOWED entries (12/12 targeted), evidence-seam failure reproduced on PRISTINE `@f75648df` (pre-existing local-env, VAULT-BROWSE-1 residual, CI authoritative); A13 + uniqueness + status-parity green pre (330) + post (331) | `cd cabinet/dashboard && npm ci && npm test && npm run typecheck` + `python3.12 -m pytest cabinet/scripts/tests/test_vault_rename_ratchet.py cabinet/scripts/tests/test_library_retirement_ratchet.py -q` (lane files TRACKED) | git revert the landing commit — identity/graph revert restores the VAULT-BROWSE-1 `/vault` state; search additions additive; no DB schema change, no service rows, no germline paths |

## 47. Addendum — world-Library card landing row (A13 parity, appended 2026-07-17)

Row filed by the world-library integrator: the lane §46 recorded as HELD
un-landed now lands — the verdict-bearing review (2 Fable lenses → fix
pass → re-verify at the current master base) is on record with all
findings closed. The harbor Library building opens the Library in-world:
a READ-ONLY browse/read/search card over the org vault, at parity with
the `/library` reader and its cabinet_memory search contract. Mechanical
aesthetic gates pass 0-error with the card open; the calibrated judge
verdict is recorded honestly (iterate — every why-line cites
world-baseline composition the lane never touched, none cites the card;
the asset-forge library-art residual is the path back above the promote
bar). Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| WORLD-LIBRARY-1 | world Library card — the harbor Library building opens the Library IN-WORLD: READ-ONLY browse/read/search card as chrome over the live canvas — GET `/api/world/library/{browse,note,search}` (GET-only + `cabinet_session`-gated, world-ratchet-pinned); browse/note EXCLUSIVELY through the confined `lib/vault.ts` resolvers (denial = generic 404 byte-identical to a miss, no existence oracle) + the exact `/library` `rewriteWikilinks` pipeline; search = server-side adapter over the LIBRARY-P2-1 `GET /api/library/search` cabinet_memory contract (own-cookie passthrough, q as bound search-param DATA, never logged, upstream bodies never relayed, honest degraded/429 surfacing, `normalizeLane2Response` type-checks + caps, `safeRelPath`-only paths, note route re-confines on open); React-text-node/`VaultMarkdown` rendering only, in-note internal links open IN-CARD (unmappable internal-shaped hrefs INERT — never a same-tab exit off `/world`), the one deep-link opens `/library` in a NEW tab; no DB imports — the retired store stays retired; `engine-client.tsx` wiring +17 lines; runbook `docs/runbooks/world-library-card-2026-07-17.md` | additive | alpha-additive · - | done | Landed 2026-07-17 from the reviewed lane diff (builder → 2-lens review → fix pass → re-verify `@d203c462`, all findings closed — closes the §46 HELD residual) on a clean worktree off origin/master `@d203c462`. Aesthetic recorded honestly: mechanical gates 0-err card-open (foreign palette 0.86% vs 5% limit; storm control card-open 14.7% vs card-closed 16.0% — the card LOWERS foreign mass), re-reproduced at landing; calibrated judge 4 VALID runs (cal 55/55 = 1.000) — 5/5 vs negatives, 0-2/11 vs positives → ITERATE on world-baseline factors (meadow patchwork, staged worksite marker, storm dither — never the card); path back above the promote bar = asset-forge library art + ground-variation pass, then re-judge. Landing gates: npm ci 0 vuln; vitest 120 files / 2204 tests green (+1 skip each); tsc clean; docs-sweep GREEN 51/0; layer-sep new=0; world+aesthetic pytest 164 passed / 5 skipped; A13 + uniqueness + status-parity green pre (331) + post (332). Post-push fix same day: landing-push CI red on the vault-rename ratchet — ALLOWED entries added for the two lane test files (declared legacy `CABINET_PRODUCT_BRAIN_DIR` alias + live `product_brain` source_type enum value; the LIBRARY-P2-1 unstaged-state-artifact class; targeted ratchets 12 passed / 1 skipped), follow-up push CI authoritative. Review artifact: `shared/interfaces/reviews/feat-world-library-cp1.md` (FW-019) | `cd cabinet/dashboard && npm ci && npm test && npm run typecheck` — library-panel (contract normalize/caps), world-library-routes (GET-only exports, 401 gate, 404-parity no-oracle, cookie passthrough, no upstream-body relay), library-card (canvas never gated on the card, no navigation API, XSS negative controls, inert unmappable hrefs); plus `python3.12 cabinet/scripts/world-aesthetic/world-aesthetic-gate.py --mechanical` on a card-open frame | git revert the landing commit — additive routes/card/panel-lib/tests + 17-line engine-client wiring + runbook; no DB schema change, no service rows, no germline paths, no map/asset change (aesthetic baseline unaffected) |

## 48. Addendum — autonomy-graded action seam landing row (A13 parity, appended 2026-07-18)

Landing row for the autonomy-seam wave: the Captain's 2026-07-17
graded-action doctrine ("every autonomous mutation's mode is a FUNCTION of
the posture level; Ring-0 ALWAYS Captain regardless") becomes THE machine
law — one seam every mutating organ consults, two organ retrofits that can
only tighten, a golden eval whose runnable half lands non-germline and
whose body is staged for the Captain's germline window. Ledger pair of
this table row: `docs/plans/operative-egg-ledger-2026-07-07.yml` id
`AUTONOMY-SEAM-1`. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| AUTONOMY-SEAM-1 | autonomy-graded action seam — Captain law 2026-07-17 as machine law: `framework/authority/action_mode.py` (`action_decision({ring, reversibility, category[, undo_handle]}, posture)` → propose / act_tell / go; `RING0_CATEGORIES` = {constitution, germline, officer-model-routing, claude-binary, spend-caps} pinned by EQUALITY — a category match forces ring 0 against a claimed ring 2; act_then_tell grants act_tell ONLY with reversible + a registered undo handle; every unknown fails closed to propose; pure — `file_needs=False`, never writes; `ring_for_repo_path` off the immutable-core enumeration, unknown ⇒ None ⇒ propose) + retrofits `cabinet/scripts/workaround-retest.py` (disposition asked of the seam, stamped `action_mode`+`captain_card` on verdicts + proposal rows; rows stay `propose_only: true` in EVERY mode; `_PRERATIFIED_AUTO_CLASSES` pinned EMPTY; Ring-0 components map via `_RING0_COMPONENT_CATEGORY`) and `cabinet/scripts/memory-supersede-apply.py` (seam = OUTER gate: an ARMED soak additionally requires an act-mode answer else `held-by-action-seam` — recorded, never executed; go never bypasses soak/hold/veto) + golden eval EVAL-026-ACTION-MODE (`cabinet/evals/action-mode/` deterministic harness + pinned fixtures wired into `cabinet/scripts/run-golden-evals.sh` fail-closed; eval BODY staged for the schg-locked `memory/golden-evals/` via `docs/proposals/germline-amendment-action-mode-eval-2026-07-17.md`) + doctrine: constitution section "Autonomy — GRADED ACTION LAW", adoption-gates GATE 0/1/3 rewritten in the verbatim twins `docs/runbooks/platform-adoption-gating.md` + `memory/skills/platform-radar-triage.md` | additive | alpha-additive · - | done | Landed 2026-07-18 from the reviewed lane diff on a clean worktree off origin/master @c117de12 (per 2026-07-07 full-autonomy grant; Captain ruling RECORDED to captain-decisions.md at landing from the orchestrator-backlog capture). Tighten-only by construction: guardian/earn_up = byte-identical v1 propose-only (test-pinned both directions: sovereign+unarmed soak still refuses, guardian+armed soak still refuses, broken seam holds); zero schg hunks (ls -lO clean on every touched live path); eval body = Captain germline-window ceremony, nothing else waits on it | `python3.12 -m pytest framework/authority/tests/test_action_mode.py cabinet/scripts/tests/test_memory_supersede_apply.py cabinet/scripts/tests/test_workaround_retest.py cabinet/scripts/tests/test_platform_radar_triage_skill.py -q` + `python3.12 cabinet/evals/action-mode/harness.py --self-test` — 137 + 132 at landing, harness 32/32 arms; framework/authority 934 passed / 5 skipped; docs-sweep GREEN 54/0; layer-sep new=0 | git revert the landing commit — additive seam module/tests/harness/fixtures/amendment doc; retrofits revert to the v1 hardcoded propose-only constants; no germline paths (the staged eval body simply goes unapplied), no service rows, no DB schema change |

## 49. Addendum — config-rot landing row (A13 parity, appended 2026-07-18)

Landing row for the config-rot wave (fresh-instance-first: sources fixed,
not symptoms — the three silent-defaults audit findings become one key, one
resolver and one real edit path). Ledger pair of this table row:
`docs/plans/operative-egg-ledger-2026-07-07.yml` id `CONFIG-ROT-1`. Column
format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| CONFIG-ROT-1 | config-rot lanes — briefing-time SOURCE OF TRUTH (`instance/config/platform.yml` `briefing_times` → new `framework.env.briefing_times()` resolver with YAML-sexagesimal rescue + bounds + dedup, fleet default 07:30/19:30; `generate-plists.py` stamps the frontdoor-briefing StartCalendarInterval from the key with printed provenance; `cabinet/services.yml` calendar row + `instance/config/watchdog.yml` `briefing:` block demoted to parity-pinned mirrors; 07:00/19:00 prose rot fixed in preset+instance cos.md / `/cabinet-briefing` / deploy runbook / coo quarter-close, pinned by `cabinet/scripts/tests/test_briefing_time_parity.py`) + TZ unification (`framework.env.captain_timezone()` = THE one resolver — silent Europe/Berlin default flipped to LOUD one-warn UTC; gate + comms engine fall through an unloadable `CABINET_CAPTAIN_TZ` to the resolver, never a silent UTC; watchdog check.py rides env-or-resolver; the 5 wrapper one-line greps quote-strip `tr -d` + warn-on-missing with UTC final fallback; out-of-range `CABINET_BRIEFING_TIMES` env slots dropped by the shared normalizer instead of crashing `_next_briefing`; `cabinet/scripts/tests/test_briefing_tz_hardening.py` executes each wrapper's REAL extraction line — a reverted `tr -d` reddens it; `generate-plists.py` machine-tz≠captain-tz warning compares CURRENT UTC OFFSETS so Copenhagen==Berlin stays silent) + comms-charter edit path (tracked `instance/config/comms-charter.yml.example` twin value-identical to `framework/attention/charter-default.yml`, test-pinned both directions; `framework/frontdoor/charter_amend.py` propose-only one-sentence amend verbs `charter: <sentence>` / `charter grant\|drop CHM-<hex8>` — anchored bounded grammar, ENTIRE merged result schema-validated fail-closed before any write, §4.10.4 conservative asymmetry: quieten auto-applies under chair trust, louder applies ONLY with the grant reply's own Telegram message id as citable Captain provenance (poller passes mid or None — missing id refuses); matched verbs TERMINAL incl. refusals, collision corpus vs every binder verb family; runbook `docs/runbooks/comms-charter-amend.md`) | additive | alpha-additive · - | done | Landed 2026-07-18 from the two reviewed config-rot lane diffs on a clean worktree off origin/master @b8d49277 (per 2026-07-07 full-autonomy grant); closes the silent-defaults audit findings (2026-07-17 reminders mini-wave: A/META charter twin+verb, C briefing-cadence divergence, captain_timezone fallback split); SCHG `ls -lO` clean on every touched live path — the locked `memory/golden-evals/eval-006-briefings-on-schedule.md` fix rides the STAGED `patches/germline-eval-006-briefing-times-2026-07-18.patch` (RESIDUAL: Captain unlock-window ceremony, then add the eval to PROSE_FILES); integrator delta: 3 docs-sweep-allowlist entries for the charter runtime ledgers | `python3.12 -m pytest cabinet/scripts/tests/test_briefing_time_parity.py cabinet/scripts/tests/test_briefing_tz_hardening.py framework/tests/test_env.py framework/attention/tests/test_gate.py framework/attention/tests/test_charter.py framework/comms/surface/tests/test_config.py framework/frontdoor/tests/test_reply_binder_charter.py -q` + full-fleet `generate-plists.py` render printing the briefing_times provenance line — at landing: lane suites 33+138+58; render exit 0 (46 plists, 07:30/19:30); frontdoor 1088p/21s; attention 291p; comms 199p; watchdog 55p; framework/tests 1067p/1s; cabinet/scripts 1686p/5s (2 non-lane fails root-caused: docs-sweep calibration mid-run churn, green serial; evidence-access replay pre-existing live-box soak state, green in master CI 29646100273 at the same SHA); docs-sweep GREEN 58/0; layer-sep new=0 | git revert the landing commit — additive modules/tests/twin/runbook/staged patch artifact; consumers revert to env-only reads; no germline path edited (the staged eval patch simply goes unapplied), no service rows, no DB schema change |

## 50. Addendum — fresh-relaunch launch-machinery Wave-1 landing row (A13 parity, appended 2026-07-18)

Landing row for the fresh-relaunch Wave-1 merge: the launch machinery
(deploy/provision/seed tooling + runbooks + the KEEP/DROP seed manifest)
lands as the MINIMAL CI-GREEN subset — union allowlist resolution + the
clean-room review's exactly-two A/B-verified CI-ratchet fixes, everything
else deferred to Wave 2 per the Captain's 100%-SCRATCH ruling. Ledger pair
of this table row: `docs/plans/operative-egg-ledger-2026-07-07.yml` id
`RELAUNCH-LAND-W1`. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| RELAUNCH-LAND-W1 | fresh-relaunch launch machinery Wave 1 — merge of `feat/fresh-relaunch-prep` onto master: `cabinet/scripts/cabinet-deploy.sh` + `cabinet/scripts/runtime-provision.sh` + `cabinet/scripts/relaunch-seed.sh` (new scripts), `cabinet-doctor.sh` check addition, runbooks `docs/runbooks/dev-runtime-split-cutover.md` + `docs/runbooks/fresh-instance-relaunch.md`, seed manifest `docs/plans/fresh-instance-relaunch-manifest-2026-07-15.md`, the branch's 4 FW-019 review artifacts; `docs-sweep-allowlist.txt` resolved as the UNION (every master entry incl. `instance/config/evidence-signing.yml` KEPT + the branch's `shared/cabinet.env` block with its WHY comment) | additive | alpha-additive · - | done | Landed 2026-07-18 by the verified integrator on a clean worktree off origin/master @a4056f82 (per 2026-07-07 full-autonomy grant) after a clean-room review vs CURRENT master; Wave 1 = union resolution + the review's exactly-two CI-blocker fixes ONLY: (1) `relaunch-seed.sh` header prose reworded to drop the bare product-brain token (zero seed behavior change); (2) two honest sanctioned-consumer entries in the never-a-score fixture (`runtime-provision.sh` persistence-symlink plumbing; the manifest as KEEP/DROP inventory); the landing's own FW-019 artifact initially tripped the scalar scan and was reworded, not allowlisted; core safety review-verified (redis confinement, dry-run-no-kickstart, rollback, no-secrets-echoed, fleet-safe, 0 germline among the 12 files); Wave 2 (deferred per Captain 100%-SCRATCH ruling) = archive-only seeder rewrite + 2 high / 2 medium / low hardening findings | `python3.12 -m pytest cabinet/scripts/tests/test_vault_rename_ratchet.py cabinet/scripts/tests/test_never_a_score_eval.py -q` + `bash cabinet/scripts/docs-track-code-sweep.sh` — at landing: ratchet pair 17 passed; docs-sweep GREEN 60/0; layer-sep new=0; full cabinet/scripts 1687p/5s/1f = the pre-existing `test_evidence_seam_bypass_replay[evidence-access.sh]` live-box condition, reproduced identically on pristine @a4056f82 (CI on the landing push is the authority) | git revert -m 1 the merge commit — additive scripts/runbooks/manifest/review artifacts + allowlist union + the two ratchet-fix edits; no germline paths, no service rows, no DB schema change, nothing wired into launchd by this land |

## 51. Addendum — fresh-relaunch launch-machinery Wave-2 landing row (A13 parity, appended 2026-07-18)

Landing row for the fresh-relaunch Wave-2 hardening — the deferred
remainder of `RELAUNCH-LAND-W1` per the Captain's 100%-SCRATCH ruling: the
archive-only rewrite of `relaunch-seed.sh` (it seeds nothing — the fresh
instance inherits nothing and is produced by `hatch.sh --defaults`) plus
the clean-room review's containment / governance-persistence / fleet-safety
hardening on `runtime-provision.sh` + `cabinet-deploy.sh`, a new 14-test
contract suite, the `.gitignore` germline trio, and the Docs-Must-Track-Code
doc edits. Ledger pair of this table row:
`docs/plans/operative-egg-ledger-2026-07-07.yml` id `RELAUNCH-LAND-W2`.
Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| RELAUNCH-LAND-W2 | fresh-relaunch launch machinery Wave 2 — archive-only rewrite of `cabinet/scripts/relaunch-seed.sh` (seeds NOTHING; all six `seed_*` fns + `PRODUCT_SPECS` DELETED, not flag-gated) + hardening on `cabinet/scripts/runtime-provision.sh` (10 governance leaves + 3 dirs persisted across deploys) + `cabinet/scripts/cabinet-deploy.sh` (deploy-side consultant restart-skip, `lib_roster` label allowlist via `re.fullmatch`, non-canonical `--runtime-root` kickstart confinement) + the `--archive-path` absolutize-before-nesting-guard containment fix; new `cabinet/scripts/tests/test_relaunch_seed_archive_only.py` (14 tests); `.gitignore` germline trio (posture/trust-ladder/standing-grants); Docs-Must-Track-Code on `docs/runbooks/fresh-instance-relaunch.md` + `docs/plans/fresh-instance-relaunch-manifest-2026-07-15.md` | additive | alpha-additive · - | done | Landed 2026-07-18 by the verified integrator (Opus 4.8 per the Captain 2026-07-18 Fable-exhausted exception) FF-only onto origin/master @2f121dbe — the branch's single content commit 54751b10 is a direct child of the master tip so no rebase was needed; the deferred remainder of `RELAUNCH-LAND-W1` per the Captain 100%-SCRATCH ruling; the load-bearing safety property is that the seeder never reads a live-content path — its one job is a restorable pre-cutover archive holding the real mode-600 `cabinet/.env` rollback net and never echoing a secret value; integrator-verified end-to-end on a fixture: empty-seed (0 files under the runtime-root) + vault-archived (org-brain memory tier2 + governance ledger + `cabinet/.env` present, `__pycache__`/`.pyc` excluded, mode 600, no secret leak); 0 germline (schg) paths among the 8 files | `python3.12 -m pytest cabinet/scripts/tests/test_relaunch_seed_archive_only.py -q` (14 passed) + `bash cabinet/scripts/docs-track-code-sweep.sh` (GREEN 60/0) + `bash cabinet/scripts/check-layer-separation.sh` (new=0) — at landing full cabinet/scripts 1701p/5s/1f = the pre-existing `test_evidence_seam_bypass_replay[evidence-access.sh]` live-box condition, reproduced byte-identical on a pristine @2f121dbe worktree, master CI run 29656063094 green on all 7 jobs (CI is the authority) | git revert 54751b10 + the ledger commit — `relaunch-seed.sh` reverts to the Wave-1 form and the deploy/provision hardening + new suite + `.gitignore` trio + doc edits back out; no germline path edited, no service rows, no DB schema change, nothing installed on the fleet |
| FRESH-HATCH-A | fresh-hatch Wave A — the 6 first-boot blockers from prelaunch-audit D5 so a fresh clean-room cabinet hatches clean and green (unlocked files only, no schg path edited): #56 `cabinet/scripts/setup-mac.sh` PEP-668 `--break-system-packages` fallback + import-verify + LOUD fail surfacing pip stderr; #58 `cabinet/scripts/cabinet-doctor.sh` roster-gated scope grants (non-deployed product lanes dropped, unregistered-but-granted -> WARN) + base-vs-overlay MCP-env DEAD -> WARN downgrade (NAMES-only contract preserved, `mcp-scope.yml` only READ); #59 `cabinet/scripts/lib_roster.py` on_demand from roster type + doctor not-loaded consultant -> skip not DEAD; #57 `cabinet/scripts/load-preset.sh` derives the work-store conn from `cabinet/.env` on the Mac-native path; #60 `cabinet/scripts/generate-plists.py` officer WRAPPER prepends `$HOME/.local/bin` (PATH_ENV kept the captain-agnostic absolute literal to preserve the dependency-radar cross-pin); #13 `memory/skills/chair-front-door-loop.md` promoted to a TRACKED path + 6 referrers repointed + chair-preflight brain MCP made optional; 5 new pytest files + on_demand cases in `test_lib_roster.py` | additive | alpha-additive · - | done | Landed 2026-07-19 by the verified integrator (Opus 4.8 per the Captain 2026-07-18 Fable-exhausted exception) FF-only onto origin/master @8dced97b — the two content commits 96876f5b + 3954fb44 fast-forward the master tip, no rebase needed; CANDOR DEVIATION on #60 (in the review artifact): the design's `PATH_ENV = ~/.local/bin` literal would break the dependency-radar cross-pin, so PATH_ENV stays the absolute literal and `$HOME/.local/bin` is prepended in the officer WRAPPER (bash expands it at boot) so `test_dependency_radar.py` stays green; cp2 self-review caught + fixed a real teeth gap where the new guard's `git grep` matched its own literal + the review artifact (would have reddened master CI), fixed with git exclude pathspecs; 0 schg (germline) paths (ls -lO clean); `mcp-scope.yml` collision READ-only; deeper product-lane MCP prune stays a Captain HANDBACK | `python3.12 -m pytest cabinet/scripts/tests/test_setup_mac_pep668_fallback.py cabinet/scripts/tests/test_doctor_fresh_hatch_green.py cabinet/scripts/tests/test_load_preset_workstore_schema.py cabinet/scripts/tests/test_plist_path_local_bin.py cabinet/scripts/tests/test_portfolio_chair_resources.py cabinet/scripts/tests/test_lib_roster.py -q` + `bash cabinet/scripts/null-hatch.sh` + `bash cabinet/scripts/check-layer-separation.sh` — at landing framework/ 6013p/29s; clean-room-source 1106p/1s; full cabinet/scripts 1721p/5s/1f = the pre-existing `test_shipped_catalog_harness_still_green[evidence-access.sh]` live-box condition reproduced byte-identical on a pristine @8dced97b worktree, master CI green 7/7 at 8dced97b run 29659629231 (CI is the authority); null-hatch PASS; golden 28/28; layer-sep new=0; A13 + ledger-status-parity green pre+post | git revert 3954fb44 96876f5b + the ledger commit — the 6 blocker fixes + 5 new test files back out; the original gitignored copy under `memory/skills/evolved/` is untouched (relocation was a tracked byte-identical copy); no germline path edited, no service rows, no DB schema change, nothing installed on the fleet |
| FRESH-HATCH-B | fresh-hatch Wave B — 6 LANDABLE silent-dead/security fixes (unlocked files only, no schg path edited) + 2 schg-locked constitutional fixes STAGED as apply-clean handback patches for a Captain unlock window: #27 measurement corpus never seeded (`framework/measurement/{role_eval_runner,scenario_runner}.py` discover `instance/measurement/<kind>` + `cabinet/scripts/load-preset.sh` copies the preset seed there, so per-role evals + org scenarios run instead of returning empty and passing the safety-gate vacuously); #21 `framework/learning/self_improvement_loop.py` validation gate FAILS CLOSED on shells_run=0 + runs the golden shells after CABINET_ROOT is restored; #51 `cabinet/scripts/generate-plists.py` officer wrapper decouples the best-effort `cabinet/.env` source from the exec chain (semicolons not and-and, a readable-file guard, cd-or-exit still aborts) so a missing/malformed .env cannot silently kill a service before it can report; #52 `cabinet/services.yml` disables two screenpipe-hardwired personal-source monitors (healthchecks-drill + check-brain-health) so they stop crying wolf on a box with no personal source; #50/#33 `framework/frontdoor/intake.py` ack() replaces blind XTRIM MAXLEN with a MINID-safe _safe_trim so a Telegram-dark backlog cannot evict undelivered captain items; #32 `cabinet/scripts/lib/triggers.sh` clamps the ack-trim boundary to the exhaust-archive cursor so the trim never erases audit entries the daily archive has not read. 6 new pytest files + handback hygiene (.gitignore ignores the real `trusted-mcps.json`, tracked `trusted-mcps.json.example`). HANDBACKS (NOT landed, staged under `designs/hook-patches/`): #9 `gen-officer-mcp-config.py` cross-officer RCE via the MCP scope gate; #7 `classifier.py` curl -XVERB + scheme-less-host mutations routed to the network_write ceiling | additive | alpha-additive · - | done | Landed 2026-07-19 by the verified integrator (Opus 4.8 per the Captain 2026-07-18 Fable-exhausted exception) via a PR-GATE squash-merge onto master — PR CI verified green PER-JOB BEFORE merge so master never went transiently red (branch was 2 clean commits be2159d0 + d346dc7a FF-able from origin/master @3e9038bc, PR-gated deliberately); all 6 fixes fail-closed/conservative, argv-not-shell-interpolated, zero product/captain tokens; NO schg (germline) path edited (ls -lO clean on all 18 landed files); the two constitutional-file fixes (#9 + #7, both on germline-lock.sh FILES) STAGED as git-apply-clean patches with a handback note documenting the Captain unlock->apply->relock window + the #9 one-time trusted-mcps.json provisioning; both bare-worktree reds (test_actfirst_gate covered-evidence + test_evidence_seam_bypass_replay evidence-access.sh) reproduced byte-identical on a pristine origin/master @3e9038bc worktree, master CI green 7/7 at 3e9038bc run 29665452179 (CI is the authority) | `REDIS_HOST=localhost python3.12 -m pytest framework/measurement/tests/test_measurement_seed.py cabinet/scripts/tests/test_load_preset_measurement_seed.py framework/learning/tests/test_validation_gate_fail_closed.py cabinet/scripts/tests/test_wrapper_spof_and_monitor_gating.py framework/frontdoor/tests/test_intake_safe_trim.py cabinet/scripts/tests/test_triggers_archive_clamp.py -q` (6 Wave-B fix suites) + (cd cabinet/scripts/lib && pytest tests/) embed-seam guard + `bash cabinet/scripts/check-layer-separation.sh` (new=0) — at landing framework/ 6030p/1f-preexisting/31s; cabinet/scripts/lib/tests 236p; cabinet/scripts/tests 1735p/5s/1f-preexisting; sources 1106p/1s; intake safe-trim 8p; null-hatch PASS 1105p/2s; golden 28/28; layer-sep new=0; A13 + id-uniqueness green pre+post | git revert the FRESH-HATCH-B squash-merge commit — the 6 fixes + 6 new test files + handback hygiene back out; NO germline path edited (the #9/#7 patches were never applied), two DISABLED monitor rows re-enable, no DB schema change, nothing installed on the fleet |

## 52. Addendum — germline authority-security landing rows (#7 curl ceiling + #9 MCP-command RCE) (A13 parity, appended 2026-07-19)

Landing rows for the two schg-locked Wave-B security fixes that `FRESH-HATCH-B`
staged as apply-clean handback patches (`designs/hook-patches/`). Captain
authorized folding both into the framework on 2026-07-19 ("go with your
recommendation") so the imminent fresh relaunch inherits them by hatching from
`master` — this SUPERSEDES the handback's Captain-unlock/relock ceremony (a
worktree off `origin/master` is not schg-locked; the relaunch's own
`germline-lock.sh lock` re-applies schg to the live copies later). No live-tree
edit, no sudo, no current-fleet sync ceremony, and no `trusted-mcps.json`
companion manifest (the brain/perplexity over-block is a current-fleet-only
concern deliberately out of scope). Germline CONTENT changed, not the germline
FILES list, so `test_germline_lockstep_consistency` holds. Ledger pair:
`docs/plans/operative-egg-ledger-2026-07-07.yml` ids `FRESH-HATCH-B-SCHG-7` +
`FRESH-HATCH-B-SCHG-9`. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| FRESH-HATCH-B-SCHG-7 | LAND the #7 curl-ceiling germline fix `framework/authority/classifier.py` (on `germline-lock.sh` FILES): bundled `-XVERB` / `--request=VERB` and scheme-less-host curl mutations now route to the network_write ceiling instead of falling through to auto-approvable `local_edit` under enforcing/sovereign posture (the `-X`/`--request` verb regex widened to catch the bundled and equals forms; a conservative curl-remote check fails to the ceiling when a curl mutation carries no localhost marker — only reached for curl, so non-curl and genuine local edits are untouched and localhost curl mutations stay `local_edit`). Applied byte-identical to the reviewed+verified staged patch (reverse-apply clean); staged patch kept as historical record | germ-keep | captain-gated · - | done | Landed 2026-07-19 by the verified integrator (Opus 4.8 per the Captain 2026-07-18 Fable-exhausted exception) at code commit 030dc3f6, via a PR-GATE merge onto master (PR CI verified green PER-JOB before merge so master never went transiently red); Captain-authorized 2026-07-19 "go with your recommendation" under the 2026-07-07 full-autonomy grant; germline CONTENT changed, FILES list unchanged so `test_germline_lockstep_consistency` stays green; NO live schg file edited, NO current-fleet ceremony, NO `trusted-mcps.json` companion; amendment `docs/proposals/germline-amendment-authority-security-2026-07-19.md` | `python3.12 -m pytest framework/authority/tests/test_classifier.py framework/tests/test_germline_lockstep_consistency.py -q` (123 + 371 pass) — at landing framework/ 6042p/31s/0f; sources 1106p; golden 28/28; null-hatch PASS; layer-sep new=0; the only red is the pre-existing `test_evidence_seam_bypass_replay[evidence-access.sh]` bare-worktree condition reproduced byte-identical on pristine `origin/master` @3c579e31, master CI is the authority; A13 + ledger-status-parity green pre+post | git revert the PR-gate merge (or code commit 030dc3f6) — `classifier.py` reverts to the pre-#7 form and the paired test additions back out; the staged `designs/hook-patches/*.patch` stays; no live schg file edited, no service rows, no DB schema change, nothing installed on the fleet |
| FRESH-HATCH-B-SCHG-9 | LAND the #9 MCP-command-RCE germline fix `cabinet/scripts/gen-officer-mcp-config.py` (on `germline-lock.sh` FILES): the officer MCP scope gate matched server NAME only while the boot COMMAND came from the jq-merged officer-writable `instance/config/extra-mcps.json`, so a rogue overlay could inject a scoped server's command (cross-officer RCE). `filter_config` now ALWAYS emits the TRUSTED committed spec for a kept server (committed base + `cabinet/mcp-overlays/*.mcp.json` + an OPTIONAL write-protected `instance/config/trusted-mcps.json`), so an overlay command override is NEUTRALIZED (trusted boots, injected never does, no DoS of a legit server) and a scoped server no trusted layer defines is REFUSED fail-closed; `extra-mcps.json` is deliberately never a trusted layer. Applied byte-identical to the reviewed+verified staged patch (reverse-apply clean); staged patch kept as historical record | germ-keep | captain-gated · - | done | Landed 2026-07-19 by the verified integrator (Opus 4.8 per the Captain 2026-07-18 Fable-exhausted exception) at code commit 030dc3f6, via a PR-GATE merge onto master (PR CI verified green PER-JOB before merge); Captain-authorized 2026-07-19 "go with your recommendation" under the 2026-07-07 full-autonomy grant; CAPTAIN-AGNOSTIC on master — NO `instance/config/trusted-mcps.json` companion and NO brain/perplexity provisioning added (a fresh captain has no officer-only servers so nothing is dropped; every committed server incl. cabinet-comms boots its trusted spec; the fail-closed refusal only affects an overlay server no trusted layer defines); the brain/perplexity over-block is a current-fleet-only concern out of scope; germline CONTENT changed, FILES list unchanged so `test_germline_lockstep_consistency` stays green; NO live schg file edited; amendment `docs/proposals/germline-amendment-authority-security-2026-07-19.md` | `python3.12 -m pytest cabinet/scripts/tests/test_gen_officer_mcp_config.py framework/tests/test_germline_lockstep_consistency.py -q` (41 + 371 pass; RCE closed on every vector) — at landing full cabinet/scripts/tests 1749p/5s/1f = the pre-existing `evidence-access.sh` bare-worktree condition reproduced byte-identical on pristine `origin/master` @3c579e31; lib/tests 236p; null-hatch PASS; layer-sep new=0; master CI is the authority; A13 + ledger-status-parity green pre+post | git revert the PR-gate merge (or code commit 030dc3f6) — `gen-officer-mcp-config.py` reverts to the pre-#9 form and the paired test additions back out; no `trusted-mcps.json` manifest was created; the staged `designs/hook-patches/*.patch` stays; no live schg file edited, no service rows, no DB schema change, nothing installed on the fleet |

## 53. Cognitive Core & Evolutionary Foundry program (A13 parity, appended 2026-07-19)

Captain-authorized implementation program governed by the living masterplan
`docs/cognitive-core-foundry.md`. Every phase requires its own detailed,
iteratively reviewed implementation plan, tests/negative controls, independent
post-build review, fixes, measured gates, integration, and green master CI
before the next row may advance. Phase 0 is import-inert; later rows are `todo`,
not claims of implementation. Ledger pair: `docs/plans/operative-egg-ledger-2026-07-07.yml`
ids `COG-0` through `COG-8`. Column format matches §6.

| ID | Path | Class (keep) | Lane · Wave | Status | Action | Gate | Rollback |
|---|---|---|---|---|---|---|---|
| COG-0 | executable architecture/trajectory contracts, shrink-only census, clean-hatch/export survival, and inverse manifest | additive | alpha-additive · - | done | Import-inert Phase 0 under the 2026-07-07 full-autonomy grant; no service, event, store, authority, or runtime behavior change; independent frozen review and real gates required; Captain-authorized Opus 4.8 exception while Fable is exhausted | focused contract/census suite + complete compatibility floor + census + layer/docs/golden/export/null-hatch + ledger parity + every CI job | apply `cognitive-core-phase-0-rollback-manifest-2026-07-19.yml`; retain append-only COG rows with matching rollback notes |
| COG-1 | envelope v2 + one domain-owned transactional outbox pilot | additive | alpha-additive · - | in-flight | Compare at least two domains, select one bounded pilot, preserve v1, migrate no second domain | reviewed phase plan + crash/replay/idempotency/parity/latency/cutover/rollback proof | no-op until started; future adapter/pointer rollback required |
| COG-2 | temporal epistemic world model as rebuildable projection | additive | alpha-additive · - | done | Project claims/provenance/uncertainty without becoming authoritative business state | reviewed phase plan + rebuild/fence/contradiction/provenance/corruption/shadow proof | delete/rebuild projection and restore read pointer; domain truth untouched |
| COG-3 | Captain-rooted objective/causal model | additive | alpha-additive · - | done | Explicit observed/hypothesized/tested/refuted/unknown causal states; no manufactured certainty | reviewed phase plan + conflict/staleness+verdict-honesty/counterfactual/Goodhart/root-integrity/no-scalar proof (veto sim deferred to the read-pointer-flip amendment, contract §6.7) — SHIPPED 2026-07-23 @de625eab, frozen review PASS digest-bound, battery 361 green | remove shadow projection; no read-pointer file exists this phase; no live authority |
| COG-4 | composable cognitive organs + deterministic shadow scheduler | additive | alpha-additive · - | done | Pure planner inside fixed wakes; dispatch rechecks authority/budget/freshness; namespaced operations resolve into one smaller constitutional effect/risk/undo descriptor while legacy `ACTION_TYPES` becomes a compatibility adapter; versioned trajectories retain granular operation identity separately without granting it authority — IN-FLIGHT 2026-07-23: contract of record `docs/plans/cognitive-core-phase-4-contract-2026-07-23.md` (premise-check wf_8625da64-a2a READY-TO-PLAN + attack UPHOLD; four-lens panel + rev-1 + independent MF-verify LAND); germline extension-gate-pair amendment handback filed (CG-33) — SHIPPED 2026-07-24: wave merges de5d16c4/0d8a74d4(WR)/cee6741e(W1)/3ed25b11(W2)/71171c16(W3)/75da084f(W4)/fc51fd59(W5)/dfb1a00e(W6) all per-job CI green; battery 690 armed green post-surgery; census actuals 236 modules / 66548 lines / services 52/40 (N7: 57→52, 44→40) / 30 action_types; N9 parity 33/33 zero divergence; frozen review PASS digest e2c35aa9303a9dac…; rollback manifest born-closed; germline micro-unit parked (CG-33 window) | reviewed phase plan + determinism/starvation/forgery/budget/authority/latency/cost/retirement + three heterogeneous non-software fixtures without central-enum growth | reversible pointer/adapter; compose or retire permanent services |
| COG-5 | Foundry arming, trajectories, arenas, benchmarks, league, frozen holdout oracle | additive | alpha-additive · - | in-flight | Real corpus required; observation-only trajectories; no credentials/live writes; aggregate-only holdout — IN-FLIGHT 2026-07-24: contract of record `docs/plans/cognitive-core-phase-5-contract-2026-07-24.md` (premise-check wf_fc493c16-a6a READY-TO-PLAN + attack UPHOLD-with-corrections; four-lens panel + rev-1 + independent MF-verify LAND); germline holdout-freeze amendment handback filed (CG-34). PARKED 2026-07-28 (deliberate, per the 2026-07-25 launch-readiness direction gate where both arms refused more consumerless cognitive substrate, plus the 2026-07-20 one-phase-at-a-time law): status stays in-flight and last_update deliberately stays 2026-07-24 — marker only, nothing claimed done; full rationale and citations in the ledger note | reviewed phase plan + required real corpus and 11 adversarial simulations | no live champion mutation; independently rehearsed archive/pointer rollback |
| COG-6 | atomic champion membrane, canary, breaker, earned promotion | additive | alpha-additive · - | todo | Existing Gate only; reversible text-genome first; Ring-0 permanently excluded | reviewed phase plan + 10 champion/canary/concurrency/rollback simulations | incumbent stays runnable; first regression returns traffic automatically |
| COG-7 | mandatory structural compaction and proven deletion | compress | beta-compression · - | todo | Remove COG-0 allowances and shrink central vocabularies, enabled services, coupling, production modules/lines, named compilers, and duplicate event-writer sinks below base maxima without deleting safety boundaries | reviewed phase plan + zero-reader/fallback/parity/restore/writer/adapter/compiler/service/layer/clean-hatch simulations | retained archive + rehearsed restore per deletion |
| COG-8 | optional capability-safe multi-Cabinet federation after local compaction | additive | alpha-additive · - | todo | Optional distribution, not required for intelligence; share bounded capabilities/evidence, never Captain authority, grants, posture, private memory, or holdout cases | reviewed phase plan + forged-peer/replay/compromise/skew/partition/conflict/transfer/rollback/hatch simulations | revoke each peer independently; local authority/runtime remain intact |
