# Cognitive Core Phase 4 — Composable Cognitive Organs + Deterministic Shadow Scheduler inside Fixed Wakes — CONTRACT (rev 1 — panel dispositions applied)

**Status:** rev 1 (2026-07-23) — the four-lens plan-attack panel is COMPLETE; every MF/SF/note finding was orchestrator-adjudicated and is applied IN PLACE (appendix "rev-1 2026-07-23 — panel dispositions of record"). The **premise-check-of-record is COMPLETE** (workflow `wf_8625da64-a2a`: 7 ground readers + synthesis verdict **READY-TO-PLAN** + adversarial attack **UPHOLD**). The attack's 3 serious + 4 minor mandatory items (MR1–MR7 below) each carry a named in-text resolution (§19 register). Next: per the Captain's 2026-07-20 cognitive-masterplan grant the premise-check is orchestrator-run and is COMPLETE with ZERO genuine forks (§19; the COG-3 precedent) — no Captain gate; the §4.5 germline-window handback is FILED at contract landing for Captain action at convenience (it gates ONLY the W4 germline micro-unit; the PARK fallback stands). Then S0 ground re-pin, then tests-first build per §14. No COG-4 code exists at contract-landing time (verified: no `docs/plans/*phase-4*` file, no organ/scheduler code, ledger row `status: todo`).
**Ledger row:** `docs/plans/operative-egg-ledger-2026-07-07.yml:3363-3372` (COG-4, todo, last_update 2026-07-19); plan-doc twin `docs/plans/operative-egg-plan-2026-07-07.md:1050` — A13 parity holds (one benign phrasing shift: the no-central-enum-growth constraint anchors to the fixtures gate in the plan doc, to the namespaced-ops note in the ledger; same constraint). Every `gate_cmd` family, note-law, and rollback clause of that row is a binding obligation restated mechanically in §12/§16.
**Parent:** `docs/cognitive-core-foundry.md` — Phase-4 charter L182-188 (charter sentence L184, **15** required simulations L186 — byte-counted, a prior reader's "16" is corrected; exit L188); §4.5 L112-118 (organ contract fields L114, six forbidden powers + separate dispatcher L116, launchd/floors law L118); §2 baseline table L30-39; invariants §3 L48-61 (esp. L56 effects-precede-tools / no second effect algebra, L57 domain-operations-are-not-constitutional-vocabulary — names Phase 4 explicitly, L60 shadow-before-authority, L61 deletion-requires-evidence); §5 protocol L130-148; §7 scorecard L224-239; §8 migration law L241-247; §9 stop conditions L249-263; **Phase-7 reservation L210** — ACTION_TYPES retire/compress is Phase 7; COG-4 only shadows + adapts ("demonstrably only a compatibility adapter", exit L188 — never removal).
**Prior contract (structural template):** `docs/plans/cognitive-core-phase-3-contract-2026-07-22.md` — section discipline mirrored; its build-time adjudications of record BIND package style here: schemas are FULL Draft-2020-12 documents validated in callers/tests only (jsonschema import-pin-forbidden inside new framework packages); cache-dir-first signatures; two-limb verified-join precedent; vacuity guards carry retirement conditions; R-A non-empty-assumptions and R-B direction-inert idioms inform every "evidence" surface here.
**Grounding:** premise-check corpus at ground-clone tip `e7f95d5a` (fetched from the canonical remote, tip SHA asserted — LESSONS.md L1113 discipline). All `:line` cites below were byte-verified this session at that tip. **Ground pin provisional:** S0 re-pins current origin/master + a fresh green per-job CI run before any edit; no header claim is relied on before S0.
**Runtime posture:** additive, **scheduler SHADOW-ONLY** (no schedule-authority cutover this phase; the cutover pointer/adapter is a FUTURE amendment — no pointer file is created, and its absence is a verify-script tripwire, the COG-3 §7.4 idiom). The ONLY live-behavior deltas permitted are (a) behavior-preserving fleet COMPOSITION under §9's floor-conservation law and (b) evidence-gated retirement of superseded rows under foundry §3 L61 — both outside the scheduler's authority. Germline surface = EXACTLY the §4.5 extension gate pair (`framework/schemas/extension-manifest.schema.json` + `cabinet/scripts/validate-extension.sh`, schg-locked at `germline-lock.sh:128-129` — Captain-windowed amendment, named handback filed at contract landing); NO other germline touches. External comms: none.
**Provenance:** authored and self-ratified per the 2026-07-07 full-autonomy grant + the Captain 2026-07-20 cognitive-masterplan continuous grant; fleet composition/retirement acts additionally ride the 2026-07-21 ownership-on-GO. Contract authored on Fable 5 per the two-tier law.

---

## 1. Purpose and optimization target

Phase 4 proves, in shadow, that the Cabinet's periodic cognition can become **declared organs** — packaged extensions of the EXISTING extension/capability surface carrying a universal contract — whose namespaced domain operations resolve through declared capabilities into **ONE smaller constitutional effect/risk/undo descriptor** (legacy `ACTION_TYPES` demoted to a shadowed compatibility adapter, never removed this phase), and that a **deterministic pure-planner scheduler** can choose organ work inside launchd's fixed wakes from a versioned snapshot, with a separate dispatcher rechecking authority/budget/freshness against live state — while the permanent fleet SHRINKS (C4). The optimization target is the foundry's (:24): verified mission value per unit of Captain attention/time/cost/risk — never organ count, schedule volume, or code size.

**Vector evidence (gate instruments, none a score) — the eight ledger gate_cmd families + one charter exit clause (N9, SF4):**

| id | gate family (ledger gate_cmd) | exit target (mechanical) |
|---|---|---|
| N1 | deterministic scheduling | identical chained schedule hash after 3 subprocess rebuilds under 3 distinct `PYTHONHASHSEED` values from the SAME wake-snapshot (the C-F3 triple, kernel-supplied §6); delete→rebuild-from-snapshot reproduces the hash. Covers `schedule.jsonl` + `schedule-manifest.json` + the snapshot record |
| N2 | starvation | seeded high-urgency organ under adversarial competing load is chosen within its declared starvation bound (charter §4.5 L116 "starvation bounds"); the bound is a snapshot INPUT, never planner-invented. Exit L188 "no high-urgency starvation" |
| N3 | forged/stale decision | a tampered `schedule.jsonl` (rows-hash mismatch), a manifest **omitting** the rows-hash key (the objectives `query.py:214-215` skip-hole, closed here — absent key REFUSES §6.3), and a stale snapshot (live wake-input hash ≠ recorded, incl. the built-without-store null-hole analog `query.py:220-235`) all REFUSE at dispatch |
| N4 | budget | external hard ceilings bound the fold; cumulative would-dispatch cost exceeding remaining budget REFUSES **at dispatch** even when planning admitted it (exit L188 "budget overflow fails at dispatch") |
| N5 | authority recheck | a decision whose descriptor resolves to a gated/ceiling verdict NEVER would-dispatch; recheck reuses the EXISTING shadow joint read-only — `policy_engine.risk_of` (:1051) / `resolve_verdict` (:1081) / `read_cell_state` (:1121), undo-gap `_act_with_undo_gap` (:1177), `graduation.evaluate` (graduation.py:267). Exit L188 "unauthorized effects never dispatch" |
| N6 | latency/cost | the §10 measurement protocol: freshly measured pilot baseline at S0 on the landing tree, declared bound + tolerance, `COG4_ENFORCE_BOUND` armed with a REAL same-commit consumer (verify twin + `test_cog4_measurement.py`), declared measure-only skips; deterministic proxies (tool/MCP activation counts, budget units from the schedule artifact) in the always-on battery; wall-clock honestly a measured tripwire, never hash-deterministic (MR2) |
| N7 | service-retirement | census `services_total` observed **< 57** and `services_enabled` **< 44** at done-flip with maxima TIGHTENED to phase actuals (shrink-only law); ≥1 permanent service retired or composed per new enabled scheduler component (exit L188); the §9 fleet-truth conservation guard prevents paper-shrink (out-of-manifest organ count must not grow) |
| N8 | three heterogeneous non-software operation-to-effect fixtures | three genuinely non-software fixture Cabinets declare granular namespaced operations WITHOUT adding a central action type and resolve to the SAME single enforcement decision (exit L188); enum growth machine-blocked three ways (census max 30; `test_consequence.py:363` set-equality; `matrix.py:439-476` totality) — all three mutants KEPT green (§5.4). MR4: two NEW non-software cabinets + one extended |
| N9 | outcome/evidence parity (foundry exit L188 "outcome/evidence parity holds" — a charter exit clause, not a ledger gate_cmd family; named mechanical per SF4) | the §5.3 parity artifact `cog4-parity-record.json` extended over the ENTIRE pilot set + all three §12 fixture cabinets, gated by `test_cog4_parity.py`: zero divergent `(risk_class, ceiling, undo_contract, shadow verdict)` tuples between the descriptor path and the ACTION_TYPES path; any divergence = structural build failure (§5.3) |

**Refused metrics:** organ count, decisions-per-wake, schedule length, LOC, module count, "% of fleet composed".

## 2. Scope — slice and inputs

1. **Inputs to the scheduler snapshot (§7.1):** cortex fold-manifest `belief_store_hash` + fold epoch (via `load_beliefs_verified`-bound manifest read); objectives `graph-manifest.json` `graph_rows_hash` + epoch tuple (via the `framework/objectives/query.py` serve surface — never a direct `graph.jsonl` read); the organ registry hash (§4); the services manifest hash (`cabinet/services.yml` — NOTE the real path: there is NO `cabinet/config/services.yml`); budget ceilings + posture + trust_table_version + scheduler_policy_version as DECLARED versioned parameters (never env/clock reads — the A-M6 purity law); PLUS four input families added rev-1 (SF2), each a DECLARED parameter canonicalized + hashed into the snapshot: per-organ starvation bounds (organ-manifest field `starvation_bound` §4.2 — riding the §4.5 amendment — with a scheduler_policy default when absent), per-organ failure history and per-organ health outcomes (declared ledger paths under the runner state surface, CLI-injected; absence recorded honestly, never invented), and capability/MCP availability (a declared inventory input, CLI-injected) — so all 15 §12 sims (esp. 5/6/7/9) and N2 are expressible from snapshot data alone. Missing/corrupt snapshot or schedule state ⇒ the fixed safe schedule (launchd's existing wakes) and **never permission** (charter §4.5 L118).
2. **The four workstreams:** C2 boundary-manifest engine (kickoff, §8); C3 projection-kernel extraction with the scheduler as third instantiation (§6); the organ contract + ONE descriptor + trajectory v2 (§4–5); C4 composition/retirement + measurement (§9–10). Slate provenance: `~/cabinet-meta/BACKLOG.md:1569-1571` (C2 "Do at COG-4 kickoff"; C3 "WHILE BUILDING COG-4"; C4 "services.yml row count must SHRINK at COG-4 exit"), walls `:1567`, NOT-MERGE list `:1577`.
3. **Foundry required-sim coverage (15, L186):** §12 maps every sim to a suite with a negative control (§5 step-4 law, L137). The eight ledger gate_cmd families + the N9 charter exit clause (N1-N9) are §12's exit table.
4. **Phase-boundary (must NOT leak in):** league/arena/benchmarks/frozen-holdout/immutable trajectory ARCHIVE = COG-5 (ledger :3373-3382; trajectories observation-only per §3 L55); champion membrane/canary/promotion = COG-6 (:3383-3392); **ACTION_TYPES retirement + deep compaction below base maxima = Phase 7** (foundry L210); `CABINET_AUTHORITY_ENFORCING` flip = Captain-gated governance, OUT of scope (policy_engine.py:1046-1049 — the matrix verdict is shadow-consumed only, and stays so); veto-input machinery = the COG-3 read-pointer-flip amendment (COG-3 §6.7), not COG-4; C7 captain-law-ledgers-as-envelopes is a Captain-gated slate row, NOT scheduled here. COG-1 is PARKED in-flight (ledger :3341 note, "PARKED 2026-07-23... one-phase-at-a-time law") — COG-4 has **no dependency** on the un-landed outbox pilot; the version-dispatch precedent is cited by BYTES (§5.5, MR5), never via the parked row.

## 3. Component disposition table (foundry §5 L144 — reuse | extend | compose | retire | new)

| component | disposition | evidence |
|---|---|---|
| `framework/schemas/extension-manifest.schema.json` | **extend — THE organ packaging surface (MR1); GERMLINE (schg-locked, `germline-lock.sh:128`, "the extension gate pair" :120) — edited ONLY via the §4.5 Captain-windowed amendment** | draft-07 (STAYS draft-07 — §4.5 dialect law), `additionalProperties:false`, `required:[name,version,kind,action_types,risk_classes,undo_contract,entrypoints]` (:8); `kind` enum `[channel,source,skill,mcp]` gains `organ`; already binds the enforcement-joint vocabulary (13-member risk_classes enum inline) and already carries `version`+`sunset` (§4.5 L114 fields). **No parallel packaging format may exist** (charter L184 FIRST clause; §3 L56) |
| `cabinet/scripts/validate-extension.sh` + AX-5/AX-6 `framework/channels/tests/test_manifests.py` | **extend — the .sh is GERMLINE (`germline-lock.sh:129`), same §4.5 window; the AX suite is NOT germline** | the three fail-closed gates (manifest schema / realpath-contained entrypoints / axis lint) validate organ manifests too; its hand-rolled schema interpreter ("exactly the schema features used" — header :17-19) is extended in the SAME §4.5 Captain-windowed micro-unit as the schema edit, with a mutant proving an organ manifest missing a required organ field REDs (§4.3) |
| `framework/authority/classifier.py` `ACTION_TYPES` (30 incl. `ambiguous`, import-verified; `CEILING_ACTION_TYPES`=8) | **reuse as the SHADOWED COMPATIBILITY ADAPTER** — the classifier remains the constitutional enforcement join this phase (foundry §2 L33) | charter L184; §5.3 parity law (the adapter leg lives cabinet-side — MF-A1); retirement is Phase 7 (L210). Never a co-authority, never a second algebra (§3 L57) |
| `framework/authority/matrix.py` (RISK_CLASSES=13 :97; totality `_validate_risk_classes` :439-476; `ceiling_frozenset_map` totality :510) + `framework/policies/authority-matrix.yml` | **reuse — untouched** | the descriptor's risk vocabulary IS this closed set; the totality raise is one of the three kept enum-growth mutants (§5.4) |
| `framework/authority/policy_engine.py` `risk_of`/`resolve_verdict`/`read_cell_state`/`_act_with_undo_gap` (:1051/:1081/:1121/:1177) + `framework/fidelity/graduation.py` `evaluate` (:267) | **reuse, read-only, from exactly TWO cabinet-side CLIs — dispatch-recheck (§7.3) + parity (§5.3, MF-A1)** | the existing shadow enforcement joint; consumed via dedicated symbol-level AST pins (§8.4); `CABINET_AUTHORITY_ENFORCING` stays Captain-gated |
| `framework/schemas/cognitive-trajectory.schema.json` (v1 const :30; effect def :247-284: open `action_type` id, 7-status enum :266, receipt refs) + `framework/evolution/contracts.py` (`action_risk_map` :939-941; undo grammar `none\|delete_window(N)\|journal:<id>` :1012) | **extend via v2 version-dispatch (MR5)** | v1 is validator-only — NO live emitter exists (grep: `cognitive-trajectory/v1` appears in the schema + `framework/evolution/tests/test_contracts.py` only) ⇒ v2 is contract-level, zero data migration. Precedent BYTES: `framework/triggers/envelope.py` + `framework/triggers/tests/test_envelope_v2.py` ("version dispatch... decided BEFORE the v1 closed-set check") — cited by bytes, never via the parked COG-1 row |
| `framework/schemas/consequence-event.schema.json` (closed 30+null enum :40-57) + `test_consequence.py:363` set-equality | **untouched closed mirror** | the consequence ledger has no schema_version field; nullable `action_type` is its only seam — it stays behind the adapter, byte-untouched; the set-equality test is kept-green mutant #2 |
| `HUMAN_PHRASES` Python (`framework/frontdoor/action_language.py:129`) + TS (`cabinet/dashboard/src/components/receipts/action-language.ts:23`, parity-tested) | **untouched** | the four HARD mirrors stay presenting the closed 30-enum; no phrase entries change because the enum does not change |
| `cabinet/scripts/cog2-import-gate.py` (module-granular BY DESIGN, header :27-28; 9 rule ids; SWEEP_TREES :183-197; completeness invariant) | **compose → the C2 boundary-yml engine (§8)** | BACKLOG :1569; conversion is byte-compat (§8.2): engine output over the repo == old gate output; every existing mutant still REDs; rule ids preserved |
| `cabinet/scripts/tests/test_cog3_objectives_ast_pin.py` (seven-symbol pin + transitive closure + defaults-only as_of) | **reuse — stays a SIBLING gate, untouched (MR7)** | the gate header :27-28 pins module-granularity by design; symbol-level law lives in dedicated AST pins; boundary-yml rows carry a documentation-only `symbol_pin:` pointer, never symbol enforcement (§8.4). The pin never weakens |
| cortex kernel disciplines — `engine.py:434-442` `_atomic_write` (O_EXCL+fsync+os.replace); `query.py:300-335` `_verified_rows` single-read F4 | **extract → C3 kernel (§6)** | the scheduler adopts THESE, not objectives' two-read (`objectives/query.py:215`→`:236`) / plain `write_text` (`objectives/graph.py:612-615`) / skip-when-absent rows-hash (`objectives/query.py:214-215`) shapes |
| `framework/objectives/query.py` serve surface (`_load_bound` three REFUSE limbs :195-236, `serve_graph`/`serve_objective`/`recommend`) | **reuse — the scheduler's ONLY objectives read path** | fail-closed `ServeRefused`; the scheduler never reads `graph.jsonl` directly; `recommend()`'s non-raising `effective` bool is noted — the planner treats `effective` as data, never as evidence |
| `framework/cortex/query.py` `load_beliefs_verified`/`as_of` (defaults-only) | **reuse** | the scheduler's cortex reads inherit the COG-3 disciplines (canonical cutoff, scope mandatory, defaults-only — pinned by a cloned AST test over the scheduler tree) |
| `cabinet/services.yml` (57 rows / 44 enabled / 13 disabled; cron 30 · watchdog 15 · daemon 12 — parse-verified) | **compose + retire (C4, §9)** | THE census-pinned fleet measure; header: "adding a row here is what puts a service under no-silent-cron coverage" |
| `framework/watchdog/registry.py` `_parse_services_manifest` (:757, consumed :844 no-silent-cron + :1072 `_ev_manifest_entry`) | **extend (MR3)** | per-organ freshness floors derived from organ manifests (§9.2) so composing N rows never deletes N floors (charter §4.5 L114 freshness-needs / L118 floors stay) |
| `cabinet/scripts/tests/test_cog3_exit_fixtures.py` (3 cabinets; **only #2 is genuinely non-software** — community-garden+delivery, "ZERO software vocabulary") | **extend #2 + author TWO NEW non-software cabinets (MR4)** | COG-4's assertion surface is op→descriptor→single-enforcement-decision, NOT graph epistemic states — a new fixture battery, not 1:1 reuse |
| `cabinet/config/cognitive-architecture-contract.yml` (budgets + `temporary_allowances`) | **extend — COG-4 allowance rows only; thresholds never raised** (§11) | zero-headroom observed==max law; MR6 precision: allowances today = COG-0/COG-1/**relaunch-killswitch(+25)/relaunch-ci-tolerance(+7)**/COG-2/COG-3 — NO COG-4 rows exist |
| scheduler + organs + descriptor + kernel packages | **new** (§4–§7; file surface §14.1) | `new` evidence per L144: no existing joint holds pure-planner scheduling (grep: no organ/scheduler code exists); the kernel is EXTRACTION of two shipped instantiations, not invention |
| **retire** | **≥1 enabled permanent service per new enabled scheduler component + evidence-gated disabled-row retirements** (§9.4) | exit L188 + ledger rollback clause "retire or compose permanent services rather than add ceremony" |

## 4. The organ contract — extending the existing packaging (MR1)

### 4.1 One packaging surface
An organ is an **extension**: `manifest.yml|.yaml|.json` at its directory root validating against `framework/schemas/extension-manifest.schema.json` — the SAME file, extended additively; validated by the SAME `cabinet/scripts/validate-extension.sh`; covered by the SAME AX-5/AX-6 suite. **No parallel packaging format, loader, or second manifest schema may appear anywhere in the phase** (charter L184 first clause; anti-synonym-churn L144). A `new`-packaging disposition would require evidence the extension surface cannot hold the contract — none exists: the schema already carries name/version/kind/action_types/risk_classes/undo_contract/entrypoints/axis_compat/sunset. **Both files of this edit surface are GERMLINE** (schg-locked: `cabinet/scripts/germline-lock.sh` FILES[] :128-129, under the comment "the extension gate pair" :120) — the edit itself routes EXCLUSIVELY through the §4.5 Captain-windowed amendment; nothing in this phase edits or works around an schg path.

### 4.2 Schema extension (additive)
- `kind` enum gains `"organ"`.
- New OPTIONAL properties (schema stays `additionalProperties:false`; all are required-when-organ, enforced per §4.3): `inputs`, `outputs` (declared data surfaces, path/stream tokens); `domain_operations` (array of namespaced ids `^[a-z0-9_-]+/[a-z0-9._-]+$` — organ/domain-owned, NEVER extending any central enum, charter §4.5 L114); `descriptor` (the ONE resolved constitutional effect/risk/undo descriptor, §5.2 — one per operation or organ-level with per-op overrides); `permissions` (declared capability needs); `idempotency` (key discipline per operation); `state_ownership` (paths the organ owns transactionally — must be disjoint from every other organ's, collision = validation failure); `cost_model` (declared units per wake — budget-input, not billing); `starvation_bound` (the per-organ scheduling bound — max wakes/seconds an eligible high-urgency operation may wait; THE declared home for N2/sim-7's bound, a snapshot input; OPTIONAL with a scheduler_policy default when absent — SF2); `freshness_needs` (`max_staleness_seconds` + expected-output token — THE floor-derivation input, MR3 §9.2); `trigger_policy` (`periodic|event|on_demand` + parameters); `health_proof` (the probe command/expectation); `fallback` (behavior on failure: `skip|safe_noop|escalate`); `dependencies` (organ names + capability/MCP names). `version` + `sunset` already exist. Foundry §4.5 L114 field-for-field coverage is a schema test.
- `undo_contract` pattern **extended in place** from `^(none|delete_window\([0-9]+\))$` to the full grammar `^(none|delete_window\([0-9]+\)|journal:[A-Za-z0-9._:/-]+)$` — reconciled with the AUTHORITY grammar at `contracts.py:1012` (one grammar, two spellings today is a latent fork; a drift-tripwire test binds the schema pattern to the contracts.py regex language). Channel manifests (`framework/channels/manifests/{outlook,slack,teams}.json`) remain valid unchanged (superset pattern).

### 4.3 Validation mechanics
`validate-extension.sh`'s hand-rolled interpreter cannot express `if kind==organ then required [...]` — resolved WITHOUT jsonschema-in-framework (COG-3 schema idiom) and **dialect-consistent (MF-G1, dialect clause):** the schema STAYS draft-07, so NO `Draft202012Validator` ever runs against it. The organ-conditional requireds are enforced by (a) the extended hand-rolled interpreter inside `validate-extension.sh` — landing in the SAME §4.5 Captain-windowed micro-unit as the schema edit, per the validator's own header law "jsonschema when available, else a hand-rolled interpreter of exactly the schema features used" (:17-19; jsonschema's draft-07 path where available) — and (b) explicit organ-block checks in the AX-suite/callers. **Closed-set membership is enforced schema/AX-side, never inside `framework/organs` (MF-A1):** `descriptor.risk_class` via the schema's existing inline 13-member enum (drift-pinned by `test_axes_contract.py`); `descriptor.action_type` ∈ the 30 `ACTION_TYPES` and `descriptor.ceiling` ⊆ the 6 `HARD_CEILING_TOUCHES` asserted by AX tests, which legally import the constants (`action_types` is an OPEN string array in the schema today — no fifth inline mirror is minted). **AX consistency check (N-d):** a manifest-declared `risk_class` must equal the matrix-derived class for its declared compat `action_type` (and the declared `ceiling` must equal the `ceiling_frozenset_map` derivation) — any inconsistency REDs. Negative controls: an organ manifest missing `freshness_needs` REDs; missing `descriptor` REDs; a `domain_operations` id colliding with any `ACTION_TYPES` member REDs (namespacing means the flat 30 vocabulary is structurally un-collidable — the `/` separator is required); two organs claiming the same `state_ownership` path REDs — **a SUITE-level check by necessity (N-b): the per-file validator sees one manifest at a time, so cross-manifest collision detection lives in the AX-suite sweep over ALL organ manifests, never in `validate-extension.sh`**.

### 4.4 Organ registry
`framework/organs/` (new, small: `__init__.py` import-inert + `registry.py` loader + `descriptor.py` resolution) loads organ manifests from a declared directory parameter (CLI-injected — the COG-3 §7.6 layer law: NO `instance/` literal in framework code; `cabinet/scripts/cog4-*.py` own defaults). The registry hash (canonical bytes over sorted manifests) is a wake-snapshot input — an organ edit is an honest epoch bump, never silent drift. `descriptor.py` resolution READS manifest-declared closed-set values only — the package holds NO action-plane import (MF-A1: §5.3 owns the adapter leg; boundary row §8.3; AST pin §8.4).

### 4.5 Germline amendment + Captain handback (MF-G1 — the phase's ONLY germline touches)
Byte-verified at `e7f95d5a`: `cabinet/scripts/germline-lock.sh` lists BOTH edit targets in FILES[] — `framework/schemas/extension-manifest.schema.json` :128 and `cabinet/scripts/validate-extension.sh` :129, under the comment "the extension gate pair" :120. They are schg-locked. Rev-0's "no germline edits" self-certification was WRONG and is corrected across this document: **these two paths are the phase's only germline touches, and the edit is Captain-windowed.**
- **Route (the CG-4 manifest-sunset precedent, mirrored):** `docs/proposals/germline-amendment-manifest-sunset-2026-07-07.md` amended THIS SAME schema file under a Captain unlock window with a one-revert rollback — the exact shape reused: (1) a germline amendment proposal doc `docs/proposals/germline-amendment-extension-manifest-organ-2026-07-23.md` carrying the COMPLETE proposed schema + validator diff text (the `organ` kind, the §4.2 fields incl. `starvation_bound`, the undo-grammar extension, the interpreter organ-block); (2) a NEW `captain-gated` (CG-class) row in the operative ledger + its A13 plan-doc twin, landing in the same commit as the proposal doc; (3) a NAMED Captain sudo-unlock handback. All three are filed **AT CONTRACT-LANDING TIME** (maximum lead time — never deferred to W4); unlock window + SAME-DAY relock per doctrine, lock state re-verified fresh (`germline-lock.sh status`, `ls -lO`) immediately before the windowed edit.
- **Build sequencing:** W4+ organ-packaging units BUILD AGAINST the amendment's proposed schema text (corpus + fixtures target the proposed text); the ACTUAL edit of the two germline files is a Captain-windowed MICRO-UNIT (schema + interpreter together, §4.3a). If the window has not opened when W4 lands, every dependent organ-validation unit PARKS with a dated marker — schg is NEVER worked around (a recorded handback beats a workaround).
- **Dialect law (N-e, stated once):** the germline extension schema STAYS draft-07 (`$schema` line 2, byte-verified) — the organ extension is written draft-07-consistent and no `Draft202012Validator` runs against it (§4.3). NEW schema documents this phase (trajectory v2, §5.5) are FULL Draft-2020-12 per the COG-3 adjudication. One split, two dialects, each file self-declaring.
- **Rollback:** the §16 code inverse restores the two files from S0 — that restore is ITSELF a germline edit and rides a Captain window (the amendment doc carries the one-revert rollback, the CG-4 shape).

## 5. The ONE constitutional effect/risk/undo descriptor + the compatibility adapter

### 5.1 The three vocabularies being reconciled (byte-verified)
30 `ACTION_TYPES` (`classifier.py:90`, len=30 incl. `ambiguous`) → 13 `RISK_CLASSES` (`matrix.py:97`) → 6 ceiling categories (`framework/learning/capability_gaps.py:84` `HARD_CEILING_TOUCHES` = {secrets, spending, external_comms, production, network_write, credentials_grant}, joined by `ceiling_frozenset_map` whose totality `matrix.py:510` already raises on drift); plus the 7-status effect enum (trajectory schema :266) and the undo grammar (`contracts.py:1012`). Two ledgers disagree on `action_type` SHAPE: consequence = CLOSED 30+null enum; trajectory = OPEN id string validated against `action_risk_map`. The descriptor must serve BOTH.

### 5.2 The descriptor (smaller, references only existing closed vocabularies — zero new enums)
```
descriptor := {
  capability:      "<domain>/<operation>"          # open, organ-manifest-owned (observation identity, NO authority)
  action_type:     <one of the closed 30>          # the COMPATIBILITY member — manifest-DECLARED (AX-checked ∈ ACTION_TYPES, §4.3); presentation-only; the adapter leg that COMPUTES it lives cabinet-side (§5.3, MF-A1)
  risk_class:      <one of the closed 13>          # THE enforcement risk member — manifest-declared, matrix-consistency-checked (N-d, §4.3)
  ceiling:         [<subset of the 6>]             # manifest-declared; AX-checked equal to the ceiling_frozenset_map derivation — never a new spelling
  undo_contract:   none | delete_window(N) | journal:<id>
  status_vocab:    the 7-status effect enum        # proposed|denied|attempted|verified|failed|reversed|violation
  idempotency_key + classification/authority/effect/undo receipt refs   # the trajectory effect shape :247-284
}
```
"Smaller" is mechanical: the enforcement-relevant joint is `risk_class × ceiling × undo_contract` (13 × 6-subset × grammar) — the 30-member enum stops being enforcement vocabulary and becomes the adapter's presentation layer. **Operation names carry no authority** (ledger note-law 6): every predicate that gates anything reads `risk_class`/`ceiling`/`undo_contract`/verdict — never `capability`. A mutant proves a verdict function keying on `capability` REDs.

### 5.3 The shadowed compatibility adapter + parity law (exit L188 "demonstrably only a compatibility adapter") — the adapter leg lives CABINET-SIDE (MF-A1)
`framework/organs/descriptor.py` resolves `capability → descriptor` by READING manifest-declared closed-set values ONLY (validated schema/AX-side, §4.3) — the package never imports the action plane (§8.3 boundary row; §8.4 pin). The ACTION_TYPES shadow-adapter leg lives OUTSIDE `framework/organs`, in the cabinet-side parity unit — `cabinet/scripts/cog4-parity.py` + the AX-calibration suite — which LEGALLY imports the action plane: it independently computes the ACTION_TYPES-path tuple (`classify_action` — `classifier.py:215` — where a tool mapping exists, else the manifest's declared compat member; matrix-derived `risk_class`; `ceiling_frozenset_map`-derived ceiling) and the descriptor-path tuple through the organs PUBLIC resolution surface, then compares. **Parity law:** for every operation in the pilot + ALL THREE fixture cabinets, the descriptor-resolved tuple `(risk_class, ceiling, undo_contract, shadow verdict)` MUST equal the ACTION_TYPES-path tuple — recorded in the parity artifact `cog4-parity-record.json` (written by `cog4-parity.py`; a tracked copy lands in the phase record at exit) and gated by `test_cog4_parity.py` — the N9 exit gate (§1; SF4: foundry L188 "outcome/evidence parity holds", made mechanical). Any divergence is a structural build failure, never a warning. The classifier remains the constitutional join (foundry §2 L33); the descriptor SHADOWS it; retirement/compression is Phase 7 (L210) and requires the §8 deletion ledger there — this contract deliberately builds no retirement machinery.

### 5.4 Enum-growth mutants — KEPT, all three (N8)
(1) census `central_action_types` max 30 observed==max (`cognitive-architecture-contract.yml`); (2) `test_consequence.py:363` `schema_values == set(ACTION_TYPES)`; (3) `matrix.py:439-476` totality raise (+ `test_matrix.py:128` set-equality). All stay green through the whole phase: COG-4 adds ZERO members. The four hard mirrors (consequence schema literal, matrix totality, HUMAN_PHRASES Python+TS) are byte-untouched.

### 5.5 Trajectory v2 — version-dispatched (MR5, cited by bytes)
New schema document `framework/schemas/cognitive-trajectory.v2.schema.json` (full Draft-2020-12 per the COG-3 adjudication): the effect def keeps every v1 field INCLUDING `action_type` (compat, still validated against `action_risk_map`) and ADDS required `domain_operation` (`{organ, operation}` — the preserved granular identity) + `enforcement_descriptor` (the §5.2 block). `contracts.py` gains version dispatch **decided BEFORE the v1 checks** — the exact `framework/triggers/envelope.py` + `test_envelope_v2.py` precedent (v1 frozen; classify-then-dispatch; forged-version mutants cloned). v1 stays frozen and validator-only; NO data migration exists to perform (no live emitter — §3 row). The consequence ledger is untouched (its nullable `action_type` remains its only seam). **Never overload `action_type`** (charter L184): the granular id lives ONLY in `domain_operation`; a v2 instance carrying a namespaced id in `action_type` fails validation (mutant).

## 6. C3 — the projection kernel (third instantiation forces the right parameterization)

### 6.1 What the kernel is (extraction, not invention)
`framework/projection/` (new, 2 modules: `__init__.py` import-inert + `kernel.py`), extracted from the TWO shipped instantiations (cortex fold + objectives graph) whose ten true divergences are pinned in the premise-check corpus. Kernel contents (the already-law-identical parts): (a) canonical-bytes + digest (recorder dialect — currently duplicated `belief.py:31-32/67-75` vs `objectives/model.py:54-64` with parity tripwires); (b) the identity law (digest-of-identity-tuple, content-excluded, never a build-time ULID); (c) chained rows-hash over RE-PARSED rows (A-m11), **parameterized by seed + total-order** — NOT one forced algebra (cortex: sha256-chain + domain seed + id-order; objectives: digest-list + empty seed + canonical-bytes order — both remain expressible); (d) the manifest envelope shape (`{schema_version, epoch:{...}, store_hash, counts}`); (e) **atomic write** (O_EXCL + fsync + `os.replace`, `engine.py:434-442`); (f) **verified single-read serve** (F4 no-window, `query.py:300-335`) + a parameterized REFUSE-limb runner; (g) the canonical-cutoff validator (already replicated at `cortex/query.py:66` and `objectives/graph.py:43`); (h) the rollback grammar (cache-delete reversible-by-rebuild).

### 6.2 What stays domain-side (load-bearing differences are parameters, never kernel policy)
Epoch-tuple contents; limb sets; supersession algebra; derived-state timing (cortex query-time C-F7 vs objectives compile-time — the scheduler is compile-time, §7.2); staleness axes; adapters and ingest taxonomies.

### 6.3 The scheduler adopts the STRICT shapes — the known holes do not propagate
The scheduler's store discipline is cortex-grade via the kernel: single-read verified serve (never the objectives two-read TOCTOU `query.py:215`→`:236`); atomic writes (never `graph.py:608-615` plain `write_text`); and the **rows-hash limb is MANDATORY-PRESENT** — a schedule manifest that omits the rows-hash key REFUSES (closing the `query.py:214-215` `is not None and` skip-hole for the new surface; a forged/partial manifest cannot serve unbound rows). Each hole carries a mutant (§12 sim 11).

### 6.4 Adoption by cortex + objectives — byte-compat law
In-phase adoption units refactor `framework/cortex` + `framework/objectives` onto the kernel under a **byte-compat gate**: store hashes, manifests, and every existing determinism/serve suite byte-identical (the hash-algebra parameterization makes this a pure code move; `ENGINE_VERSION`/epoch untouched). If any divergence proves load-bearing mid-build, it becomes a kernel PARAMETER; if byte-compat is still unreachable, that adoption unit **PARKS with a dated marker** (LESSONS L1108 idiom) rather than forcing an epoch bump — the scheduler (third instantiation) still proves the kernel, and the parked adoption is recorded debt, never silence. Objectives' existing two-read/plain-write shapes may be upgraded to the kernel disciplines in the same unit ONLY as a behavior-tightening with zero hash change (stricter refuse = fail-closed widening is NOT byte-compat-breaking for stores; the F1-class serve suites re-run).

## 7. The deterministic shadow scheduler — pure fold over a versioned wake-snapshot

### 7.1 Snapshot (`framework/scheduler/snapshot.py`)
`build_snapshot(inputs...) -> snapshot.json` under `cabinet/cache/scheduler/` — every input a DECLARED parameter (§2.1 list), canonicalized + hashed. The snapshot records the **wake-input hashes**: `cortex_belief_store_hash`, `objectives_graph_rows_hash` + objectives epoch, `organ_registry_hash`, `services_manifest_hash`, `organ_health_hash` + `failure_history_hash` (the SF2 declared ledger inputs), `capability_availability_hash` (the SF2 declared inventory input), the per-organ starvation bounds (manifest fields riding `organ_registry_hash`, surfaced as explicit snapshot rows so N2 reads them from the snapshot), `budget_version`, `posture_version`, `trust_table_version`, `scheduler_policy_version`, `scope`, `cutoff` (canonical `YYYY-MM-DDTHH:MM:SSZ`, hard-error otherwise — the replicated validator). No env, no clock, no randomness (A-M6): mutants prove an env-reading or `datetime.now` fold REDs.

### 7.2 Fold (`framework/scheduler/fold.py`) — the pure planner
`build_schedule(snapshot_path, cache_dir) -> schedule.jsonl + schedule-manifest.json` — a pure function of the snapshot. Decision inputs per charter §4.5 L116: uncertainty, expected value of information, urgency, dependency readiness, marginal cost, failure history, starvation bounds — ALL from the snapshot; externally supplied hard ceilings bound compute/tool-use/spend/Captain-attention/exploration. Derived AT COMPILE, served epoch-labeled, NO serve-time re-derivation (the objectives §5.4 law lifted). Decision rows carry `(organ, operation, descriptor, reason, budget_units, deps, tie_break_key)` — tie-breaks are total-ordered by canonical bytes (determinism). Epoch = `{scheduler_version, snapshot_hash, wake_input_hashes, scope, cutoff}`; manifest carries the MANDATORY rows-hash (§6.3).

**Six forbidden powers (charter §4.5 L116), each mechanically enforced (§8.3 rows + pins):** cannot create objectives (no objectives-cache write path; boundary data-plane row; builder-writes-nothing-outside-its-cache assert); cannot grant capabilities (no learning/self-extension imports); cannot alter authority (no authority imports ANYWHERE in the planner tree, `framework/organs` included — read-only authority access lives in exactly TWO cabinet-side CLIs, each under its own AST pin: the dispatch recheck §7.3 and the parity CLI §5.3/§8.4); cannot execute effects (no acting/frontdoor imports; no subprocess/os.system in the planner tree — AST pin; tests use subprocess, the planner never does); cannot mutate its own weights (`scheduler_policy_version` is a snapshot INPUT; the fold writes only `cache_dir`; self-weight-update = sim-13 mutant); cannot declare its work successful (no write path to consequence/evidence/trajectory stores; promotion is the Gate's, COG-5/6).

### 7.3 Dispatch recheck — shadow comparator (`cabinet/scripts/cog4-dispatch-shadow.py`)
A SEPARATE component (charter L116 "a separate dispatcher"), a CLI outside the planner package. Per decision row, immediately before (hypothetical) dispatch: (1) serve the schedule through the kernel verified single-read + REFUSE limbs (forged rows, absent rows-hash key, counterfactual-style mismatches); (2) compare live wake-input hashes vs recorded — ANY mismatch (incl. recorded-null-but-live-exists) REFUSES (stale-snapshot, N3); (3) recompute the authority verdict read-only via `risk_of`/`resolve_verdict`/`read_cell_state` + `_act_with_undo_gap` + `graduation.evaluate` — a gated/ceiling/undo-gapped verdict REFUSES (N5); (4) check remaining budget vs cumulative would-dispatch cost — overflow REFUSES (N4); (5) check organ freshness_needs vs live staleness — stale REFUSES; (6) **IDEMPOTENCY recheck (SF1):** re-derive the decision row's idempotency key per the organ manifest's `idempotency` discipline (§4.2) and REFUSE any key already present in the shadow decision record — the parent charter's dispatcher QUADRUPLE "authority, idempotency, snapshot freshness, and remaining resource budget" (foundry §4.5 L116) binds over the ledger note's authority/budget/freshness triple (ledger :3370); the stricter parent wins. Output: a shadow decision record (`would_dispatch|refused(reason)`) appended to the shadow log. **It never executes anything** — shadow-only this phase; the future cutover is a pointer/adapter amendment (ledger rollback clause), and `~/.cabinet/state/cog4-dispatch-pointer` existing AT ALL this phase is a verify-script tripwire (COG-3 §7.4 idiom).

### 7.4 Fixed wakes + fallback law
launchd remains the OS supervisor; the scheduler NEVER loads/renders launchd jobs (`generate-plists.py` renders-only stays the sole renderer); it selects work inside observed wakes. Missing/corrupt snapshot or schedule ⇒ the fixed safe schedule (the services manifest's own cadence) and never permission (L118) — asserted by sim 15's corrupt-state arm.

### 7.5 Concurrency law (the scheduler owns it — the objectives no-locks gap does not propagate)
All writers to `cabinet/cache/scheduler/` are serialized by an O_EXCL lockfile (grounded in the kernel atomic-write idiom); every store write is atomic (tmp + fsync + `os.replace`); the verified single-read serve leaves no TOCTOU window, so serving during a rewrite is structurally safe (the replace is atomic; the read binds hash-to-rows in one read). Never serve a store mid-rewrite by a partial-write: impossible by construction with `os.replace`; the rows-hash limb is the backstop. A concurrent-writer sim (two builders racing one cache_dir) proves the lock: loser fails LOUD, store never corrupts.

## 8. C2 — the declarative boundary manifest (ONE engine + rows; phases add ROWS, not code)

### 8.1 Current rule inventory (byte-read this session — the conversion's ground truth)
`cabinet/scripts/cog2-import-gate.py` (655 lines) enforces NINE rules across five checks:

| rule id | source scope | forbidden target | direction | mechanism | allowlist | check |
|---|---|---|---|---|---|---|
| `FORBIDDEN_IMPORTS_CORTEX` | FORBIDDEN_TREES (`framework/frontdoor`,`framework/acting`,`framework/authority` :151-155) + `framework/fidelity/officer_runner.py` (:156-158) | `framework.cortex[.*]` | action→shadow | AST walk incl. relative + `from framework import cortex` alias (:324-379) | none (absolute) | 1 |
| `FORBIDDEN_CORTEX_TOKEN` | same | live-line regex `framework[./]cortex` \| `cabinet/cache/cortex` \| alias spelling (:210-213) | action→shadow | comment-safe grep backstop (`_live_lines` :382-391) | none | 1 |
| `FALSIFIER_IMPORTS_CORTEX` | `cabinet/scripts/cog2-parity-falsifier.py` (:159) | ANY cortex import, static/alias/dynamic (`_FALSIFIER_DYNAMIC` :221-224 incl. `'.cortex'` string) | falsifier→shadow (C-F17) | AST + `_IMPORT_LINE` + narrow dynamic regex | none | 2 |
| `UNALLOWLISTED_CORTEX_IMPORTER` | SWEEP_TREES (:183-197: framework, shared, instance, presets, cabinet/{scripts,admin-bot,mcp-server,adapters,channels,companion,evals,fixtures}, conftest.py) | `framework.cortex` | any→shadow | AST + `_DYNAMIC_CORTEX` (:230-232) | `framework/cortex/` internal (:162) + exact {cog2-rebuild, cog2-belief-hash, cog2-verifier} (:163-167) + globs test_cog2_*/lib_cog2_* (:168-171) + objectives-internal + cog3 allowlist (:565-566) | 3 |
| `FORBIDDEN_IMPORTS_OBJECTIVES` | FORBIDDEN_TREES + officer_runner | `framework.objectives[.*]` | action→shadow | AST (`_matching_imports` :454-475) | none | 1 |
| `FORBIDDEN_OBJECTIVES_TOKEN` | same | `_BACKSTOP_OBJ` (:289-291; cache path deliberately NOT here) | action→shadow | comment-safe grep | none | 1 |
| `FORBIDDEN_OBJECTIVES_IMPORTS_ACTION` | `framework/objectives/**` (:579) | ACTION_PLANE = framework.{frontdoor,acting,authority} (:261) | shadow→action (REVERSE) | AST + `_DYNAMIC_ACTION` (:307-309) | none | R |
| `UNALLOWLISTED_OBJECTIVES_IMPORTER` | SWEEP_TREES | `framework.objectives` | any→shadow | AST + `_DYNAMIC_OBJ` (:293-295) | objectives-internal (:269) + exact {cog3-rebuild, cog3-graph-hash, cog3-staleness} (:270-274; **cog3-ovi-parity DELIBERATELY absent** — the C-F17 analog ban, :265-268) + globs test_cog3_*/lib_cog3_* (:275-278) | O |
| `FORBIDDEN_OBJECTIVES_DATAPLANE` | SWEEP_TREES (independent of `flagged` — a file may import AND open) | live line containing `cabinet/cache/objectives` (assembled token :310-315, self-flag-safe) | any→store | comment-safe substring | objectives-internal + cog3 readers (:607) | D |

Plus the structural guarantees the conversion MUST preserve: the completeness invariant (`test_every_first_party_py_is_on_scan_surface` — every first-party `.py` is on the scan surface; non-code trees enumerated :92-102); the two accepted residuals (transitive attribution; fully covert string-assembly — C-F20, revisited at pointer-flip); check/report/json modes, exit 1 on breach; baseline-zero shrink-only (empty accepted-violation set, :31-33).

### 8.2 The conversion (kickoff wave, before any organ/scheduler code)
`cabinet/config/boundary-manifest.yml` (new, tracked) declares rows: `{token, kind: module|data_plane, forbidden_importers: [trees], internal_prefix, allowlist_exact: [], allowlist_globs: [], reverse_forbidden: [trees] | null, sweep: bool, rule_ids: {...}, symbol_pin: <test file>|null}`. The engine (`cog2-import-gate.py` refactored in place, name kept — the CI/docs surface is stable) loads the yml and executes the same five check shapes generically. **Byte-compat gate:** on the conversion commit, engine-over-repo output == pre-conversion output (both empty), every §8.1 rule id unchanged, and EVERY existing negative-control mutant (scratch-tree forbidden import per rule) still REDs — a per-row mutant harness (`test_cog4_boundary_rows.py`) generates the mutant from the row itself, so **every future row ships with its bite proven** (BACKLOG :1569 "per-row mutants"). The completeness invariant survives verbatim.

### 8.3 COG-4 rows (added as ROWS — zero engine code)
- `framework.scheduler` + `framework.organs` + `framework.projection` tokens: forbidden_importers = FORBIDDEN_TREES + officer_runner (action plane may not read the planner); reverse_forbidden for `framework/scheduler/` + `framework/organs/` = the action plane + `framework.{fidelity,missions,ovi,learning,evolution}` (the planner tree reads cortex/objectives serve surfaces + kernel + `framework.organs` registry/descriptor + stdlib, nothing else — matching the §8.4 pin; `framework/organs` itself: NO action-plane import, MF-A1).
- unallowlisted-importer sweeps with exact allowlists ENUMERATED — never a wholesale `cog4-*` glob for the scheduler token: **`framework.scheduler` allowlist** = {cog4-snapshot, cog4-schedule, cog4-dispatch-shadow, cog4-measure} + `test_cog4_*`/`lib_cog4_*` globs, with **`cog4-organ-runner.py` DELIBERATELY ABSENT** (the `cog2-import-gate.py:265-268` idiom — leaving it off the allowlist means any scheduler import from the runner REDs as UNALLOWLISTED; the composed wake vehicle is scheduler-blind by construction, §9.5, MF-AC2). **`framework.organs` allowlist** = the scheduler set + {cog4-parity, cog4-organ-runner} + the globs. **`cog4-parity.py` is the ONE sanctioned dual-plane importer** (organs public surface + action plane, §5.3) — explicitly allowlisted here, pinned §8.4.
- data_plane row: `cabinet/cache/scheduler` (assembled-token discipline), swept SWEEP_TREES-wide; allowlist = scheduler-internal + {cog4-snapshot, cog4-schedule, cog4-dispatch-shadow, cog4-measure} + test globs — **the runner DELIBERATELY ABSENT here too** (a runner touching the schedule store REDs; the §9.5 invariance battery is this static row's behavioral twin).
- `framework.projection` (kernel) is IMPORTABLE by cortex/objectives/scheduler internals only + their CLIs (rows) — the kernel never becomes a general utility grab-bag.

### 8.4 Symbol-level pins — sibling gates, never rows (MR7)
The engine stays module-granular BY DESIGN (`cog2-import-gate.py:27-28`); rows gain a documentation-only `symbol_pin:` pointer. The shipped seven-symbol pin `test_cog3_objectives_ast_pin.py` is **byte-untouched and never weakens**. NEW sibling pins, same shape: `test_cog4_scheduler_ast_pin.py` (planner tree: stdlib | `framework.projection` | `framework.scheduler.*` | `framework.organs.*` | the enumerated cortex query-surface seven | the enumerated objectives serve symbols `serve_graph, serve_objective, recommend, ServeRefused`; anything else RED — incl. `load_beliefs`, engine/adapters modules, any authority/acting/frontdoor/fidelity/missions/ovi module; + the defaults-only `as_of` pin cloned; + a no-subprocess/no-socket pin over the planner tree) and `test_cog4_dispatch_ast_pin.py` (the dispatch CLI may import EXACTLY `risk_of, resolve_verdict, read_cell_state, _act_with_undo_gap` from `framework.authority.policy_engine` + `evaluate` from `framework.fidelity.graduation` + the scheduler serve surface — so the dispatcher can never grow into an executor), plus `test_cog4_parity_ast_pin.py` (the parity CLI may import EXACTLY: `classify_action` from `framework.authority.classifier`, the matrix mapping surface (`RISK_CLASSES` / `ceiling_frozenset_map` accessors), the same four policy_engine read-only symbols + `graduation.evaluate` the dispatcher pins, and the organs PUBLIC registry/descriptor surface — the sanctioned dual-plane importer stays a comparator, never a resolver anything in `framework/` could grow to depend on). Transitive-closure subprocess tests cloned from the COG-3 shape with the exact consequence-import mutant.

## 9. C4 — composition semantics + the honest fleet measure

### 9.1 Baseline + the measure fix (attack refutation 1 of the fleet class; MR6 concern)
Census-pinned baseline at e7f95d5a: **`cabinet/services.yml` = 57 rows / 44 enabled / 13 disabled (cron 30 · watchdog 15 · daemon 12)** — parse-verified; census budgets `services_total` max 57 / `services_enabled` max 44 at observed==max. **The measure is services.yml rows** — BUT the manifest is not the whole fleet: **9 template-plist organs have NO manifest row** (`cabinet/launchd/`: chrome-profile, cost-summary, dashboard-kiosk, egress-proxy, heartbeat-watchdog, outbox-relay, ovi-weekly, role-evals-weekly, worktree-listener — ls-verified) **plus committed concrete-slug officer plists** (`com.cabinet.officer.cos.plist`, `com.cabinet.officer.cos-inbound.plist`, `com.cabinet.officer.comms-officer.plist` — comms-officer is not even in the 5-officer seed: instance leakage against the 2026-07-14 roster-derivation doctrine). **Fix, BEFORE any shrink claim:** W1 lands `test_cog4_fleet_truth.py` pinning the EXACT set of out-of-manifest template-plist organs (the 9 + the officer/dashboard/apoptosis/limit-reset/mission-supervisor/self-improvement templates that pair WITH manifest rows are distinguished from the 9 row-less ones) — any service moved OUT of the manifest, or any NEW row-less template plist, REDs. The exit shrink claim reports BOTH numbers: manifest rows (must fall) and row-less organ count (must not grow: ≤9, shrink welcome). The officer-plist instance leakage is a named cleanup unit (move to instance/deploy surface or delete; verify `deploy-mac.sh` renders from the template + roster) — scheduled, and if its deploy-script blast radius proves non-trivial it PARKS with a dated marker rather than riding the exit gate.

### 9.2 Floor-preserving composition (MR3 — the serious attack finding)
Watchdog freshness floors are derived PER services.yml row (`registry.py:757` `_parse_services_manifest`, consumed :844/:1072; services.yml header: a row IS what puts a service under no-silent-cron coverage). Composing N rows into one organ-runner row must NOT delete N floors. **Mechanism:** organ manifests carry `freshness_needs` (§4.2, charter L114); `framework/watchdog/registry.py` gains `_parse_organ_manifests` deriving ONE expectation PER enabled organ inside a composed runner row (expected-output token + max_staleness from the manifest), asserted by the same no-silent-cron probe beside the per-row expectations (charter L118 "mechanically derived freshness floors" stay). `_parse_organ_manifests` explicitly BINDS to the registry's stdlib-only narrow-parser SURVIVAL CONTRACT (N-c — the contract exists in the registry's own comments, byte-verified: "STDLIB ONLY … load-bearing" :13; "STDLIB-ONLY CONSTRAINT: the registry's survival contract forbids third-party" :663-665; `_parse_services_manifest`'s "stdlib only (see the survival-contract note above)" docstring :757-764): a narrow line-parser over exactly the manifest shapes this contract documents, degrade-safe, never yaml/jsonschema imports. What it parses is the runner row's EXPLICIT organ-manifest list (§9.5) — the row→manifest association is DECLARED, so the freshness probe resolves per-organ expected outputs. **Floor-conservation law — COUNT + TUPLE strength (SF5):** in any composition commit, (a) derived-floor COUNT after ≥ before for the composed set, AND (b) PER COMPOSED ORGAN the derived expectation tuple `(cadence, staleness threshold, probe)` is at-least-as-strict as the absorbed row's pre-compose expectations — per field: **cadence** = the runner row's wake interval ≤ the absorbed row's interval (the organ runs at least as often); **threshold** = the organ's `max_staleness_seconds` ≤ the absorbed row's derived floor seconds (the `_floor_for_entry` output, registry.py:855); **probe** = the per-organ expected-output token names a PER-ORGAN output artifact probed the way the row's log candidates were (`_service_log_candidates` shape, :856) — never only the shared runner log, so a silent organ inside a live runner still trips its floor. Proven by `test_cog4_floor_conservation.py`; the compose commit removes the N rows and adds the runner row + N organ manifests ATOMICALLY (same commit). Mutants: a composed organ whose manifest lacks `freshness_needs` REDs; a compose commit whose floor count drops REDs; **a LOOSENED threshold (organ `max_staleness` > the absorbed row's floor) REDs** (SF5).

### 9.3 Compose-vs-retire disposition over the 57 rows
The pilot targets the **~24 pure-projection/measurement rows** (rebuild-from-source idempotent; strongest organ candidates — undo-sweep, prediction-calibration, falsifier-daily, verifier, probe-github/vercel/sentry, regression-corpus, graduation-transitions, judge-calibration, retrieval-eval, memory-contradictions, preference-pairs, charter-shadow, exhaust-archive, dependency-radar, world-census, task-sync-drift, cabinet-doctor, ...). A bounded pilot set (≥3 rows → 1 organ-runner row) is selected at S0 by declared criteria: projection-class, no actor writes, disjoint state ownership, cadence compatibility with the runner's wakes. The **13 disabled rows** (incl. draft-lane "PARKED (superseded by act-not-draft ruling)") are legitimate retirement targets under foundry §3 L61 (reader inventory + parity + rehearsed rollback + quiet-fallback soak per row) — **but disabled-row retirement alone CANNOT satisfy the C4 spirit**: the exit requires `services_enabled` < 44 too (N7), which only enabled-row composition or evidence-gated enabled-row retirement can produce. Never composed/retired: stateful actors with distinct fault/security boundaries (cos-inbound, backup, memory-worker, dashboard, evidence-signing-broker's OS-user split), gate-apply (never a row, ships dark — services.yml doctrine), officers (roster-derived, not rows).

### 9.4 Exit measure (N7)
At done-flip: `services_total` < 57 AND `services_enabled` < 44 observed; census maxima TIGHTENED to the new actuals in the same commit (shrink-only); ≥1 permanent service retired or composed per new enabled scheduler component (the runner row itself is the accounting unit: runner +1 requires ≥2 composed-away rows for net shrink, satisfied by the ≥3→1 pilot); fleet-truth guard green (no paper-shrink); every retirement carries its L61 evidence bundle in the phase record.

### 9.5 The organ-runner (MF-AC2) — the composed wake vehicle, scheduler-blind by construction
`cabinet/scripts/cog4-organ-runner.py` (new CLI, §14.1) is the composed service the pilot's runner row wakes: launchd fires it on the row's fixed schedule; it loads EXACTLY the organ manifests its services.yml row names and runs each per its own manifest (`trigger_policy` / cadence, `health_proof`, `fallback`, `state_ownership` discipline), writing each organ's declared outputs. **Wake-row semantics:** the runner's services.yml row EXPLICITLY names its composed organ manifests — the row→manifest association is DECLARED, never discovered — which is (a) what `_parse_organ_manifests` (§9.2) reads to derive the per-organ `(expected-output, max_staleness)` floor pair, and (b) how the freshness probe resolves per-organ expected outputs instead of one shared runner log. **Boundary (§8.3):** the runner MAY import `framework.organs` (registry + descriptor — an allowlisted organs reader); it is FORBIDDEN `framework.scheduler` AND the schedule-store data plane, both enforced by DELIBERATE ABSENCE from those allowlists (the `cog2-import-gate.py:265-268` idiom). **Shadow-only made mechanical:** `test_cog4_organ_runner.py` runs the runner fixture twice — with and without a schedule artifact injected into the cache — and asserts byte-identical behavior (organs selected, outputs, exit status); the mutant runner that READS the schedule artifact REDs. The shadow scheduler's decisions influence NOTHING the runner does this phase — cutover remains the future pointer/adapter amendment (§7.3 tripwire).

## 10. Latency/cost gate — explicit measurement protocol (MR2; the phantom-M6 class must not recur)

Named lesson: `~/cabinet-meta/LESSONS.md:1102` — COG2_ENFORCE_P95 once shipped exported with NO consumer. Protocol here:
1. **S0 baseline, landing tree, pilot set:** `cabinet/scripts/cog4-measure.py` measures, on the S0-pinned tree, the pilot rows' current per-wake wall-clock, tool/MCP activation counts, and declared cost units; writes a dated baseline artifact (tracked in the phase record). No borrowed numbers; §5 L146 "freshly measured baseline".
2. **Declared bound:** max acceptable regression pinned IN THIS CONTRACT'S S0 addendum as measured-value × tolerance (tolerance declared per metric class before measurement: wall-clock p95 ×1.25; activation counts and budget units EXACT — they are deterministic from the schedule artifact).
3. **Armed consumer in the SAME commit as the flag:** `COG4_ENFORCE_BOUND=1` is consumed by `verify-cognitive-phase4.sh` AND `test_cog4_measurement.py` — both land in the commit that introduces the env var; a grep-style test asserts the flag name appears in ≥1 consumer (the anti-phantom tripwire, mechanical).
4. **Negative control:** a seeded regression fixture (an organ manifest with an inflated cost model / a fold admitting an over-activation plan) REDs when armed.
5. **Honesty clause:** wall-clock latency is a measured tripwire (env-flag-armed, declared skips in the always-on battery); the deterministic proxies (activation counts, budget units) ARE in the always-on battery. Exit L188 "total tool/MCP activation, latency, and cost do not regress beyond the measured bound" is claimed exactly at this strength, no more.

## 11. Census amendment + A13 plan (MR6)

- **Precision of record:** `cabinet/config/cognitive-architecture-contract.yml` `temporary_allowances` today = COG-0 (+2 mod / +1256 lines), COG-1 (+1 / +942), **relaunch-killswitch (+25 lines), relaunch-ci-tolerance (+7 lines)** — non-COG rows, named so the grounding map is exact — COG-2 (+5 / +1189), COG-3 (+12 / +1526). **NO COG-4 rows exist.** Effective maxima: modules 226, lines 65012, both observed==max; `services_total` 57, `services_enabled` 44, `central_action_types` 30 — **all observed==max, zero headroom**. Any net-add REDs the census immediately — that RED is the EXPECTED allowance path, never conflated with a STOP.
- **Allowance law (the COG-2/3 idiom, verbatim discipline):** every COG-4 unit that adds framework modules/lines bumps the COG-4 `temporary_allowances` rows to the EXACT measured running total (via `cognitive-architecture-census.py`) in the SAME commit — never a pre-allocated ceiling; tightened to phase actuals at done-flip (shrink-only); sunset 2027-01-19; deletion_gate = COG-7 compaction folds the scheduler/organ packages or the phase is superseded. Projected NEW modules (S0 measures the actuals): `framework/projection/` 2, `framework/scheduler/` ~5, `framework/organs/` ~3, plus watchdog/schema/contracts edits (line-only). `named_compiler_modules` stays 1: no new file matches `*compiler*.py` (permanent test + the trivial-half mutant, COG-3 §6.2 shape).
- **Services accounting:** any net-new service row (the organ-runner) lands in the SAME commit as a ≥2-row composition (net shrink) — no services allowance row is ever minted this phase (composition IS the budget mechanism); at done-flip the maxima tighten (§9.4). `central_action_types` stays 30 exactly (§5.4).
- **A13 parity:** the COG-4 ledger row keeps its matching `| COG-4 |` plan-doc table row; the A13 `gate_cmd` runs green BEFORE committing ledger changes and re-checked AFTER; re-read immediately before editing, retry ≤5x on conflict, never blind-overwrite; status moves flip `status` + `last_update` in the same commit; rows never deleted. On any deferral: PARK with a dated marker; grep for duplicate YAML keys (LESSONS L1108 — the COG-1 duplicate-key gate-rot precedent).

## 12. Gate simulations — the FIFTEEN required sims (foundry L186, byte-counted) + the N1-N9 exit gates

All suites `python3.12`, tests-first, file-seeded, no DSN, deterministic (declared cutoffs, no clock); every mutant must FAIL for the exact escape it names (a gate without a biting mutant is decoration). Sims run against the kernel-built schedule artifact.

| # | sim (L186 verbatim) | seeds | asserts | negative-control mutant (must FAIL) |
|---|---|---|---|---|
| 1 | burst load | many organs ready in one wake, budget-constrained | deterministic selection + ceilings respected; N1 triple holds on the burst fixture | dict-order tie-break (nondeterministic fold) |
| 2 | quiet period | zero eligible work | EMPTY schedule, zero ceremony, no invented work | idle-spin mutant scheduling a make-work no-op |
| 3 | stale organ | organ output older than its `freshness_needs` | staleness FLAGGED in the shadow record; never auto-permission to run outside ceilings; watchdog floor fires independently | staleness-implies-dispatch mutant |
| 4 | cost spike | organ cost model spikes above ceiling | planner defers under the ceiling with reason | cost-ignoring fold |
| 5 | organ crash | health_proof failing organ | manifest `fallback` honored (skip/safe_noop/escalate); floors still derive; other organs unaffected | crash-suppresses-floor mutant |
| 6 | dependency failure | organ dependency (organ/capability) unavailable | dependent work not chosen, explicit reason recorded | dispatch-anyway mutant |
| 7 | starvation | high-urgency organ + adversarial competing load across K wakes | chosen within its declared starvation bound (N2) | starvation-prone weighting (bound ignored) |
| 8 | contradictory organs | two organs proposing conflicting operations on one subject | BOTH surfaced, conflict recorded symmetric+sorted, never auto-resolved/LWW (the assemble-collision law analog) | LWW/auto-resolve mutant |
| 9 | unavailable MCP | declared capability's MCP absent | work skipped with reason; NEVER silently re-classified to a different capability/descriptor | silent-substitute mutant |
| 10 | unauthorized effect | operation whose descriptor resolves to always_gated/propose_only/ceiling verdict | never would-dispatch (N5); refusal recorded with the verdict | verdict-ignoring dispatcher |
| 11 | forged scheduler decision | hand-edited `schedule.jsonl`; PLUS a manifest with the rows-hash key REMOVED | dispatch REFUSES both — the absent-key case REFUSES (the mandatory limb, §6.3 — closing the objectives `query.py:214-215` hole for this surface) | skip-when-absent limb (the objectives shape) — must FAIL here |
| 12 | budget overflow | cumulative would-dispatch cost > remaining budget at dispatch time (planning admitted it) | REFUSE at dispatch (N4) | planner-said-yes-so-dispatch mutant |
| 13 | self-prioritization | an organ whose operations target the scheduler's own policy/registry; a fold variant writing `scheduler_policy_version` | structurally refused: fold writes only its cache; self-targeting ops carry no special weight; policy version is input-only | self-weight-update fold |
| 14 | stale-snapshot dispatch | live cortex/objectives/organ-registry hash ≠ recorded (incl. recorded-null-but-live-exists) | REFUSE (N3) | null-hole-skipping comparator |
| 15 | scheduler restart/replay | kill mid-fold; corrupt/missing snapshot & schedule | rebuild from the same snapshot reproduces the identical hash (N1); corrupt/missing state ⇒ fixed safe schedule, NEVER permission (§7.4) | fallback-implies-permission mutant |

**Exit-gate batteries beyond the sims:** N6 measurement protocol (§10); N7 census + fleet-truth + floor-conservation (§9); **N8 the three heterogeneous non-software fixture cabinets (MR4):** extend `test_cog3_exit_fixtures.py`'s #2 (community-garden + delivery) with an organ/op layer, and author TWO NEW genuinely non-software cabinets (candidate domains, final pick at corpus authoring: a physical-logistics/warehouse operation; a care-rota/community-operations cabinet — zero software vocabulary, token-grepped). Each declares granular namespaced operations in organ manifests, resolves through descriptors to the SAME single enforcement decision, adds ZERO central action types (the three kept mutants §5.4 assert during the fixture run), and exercises the REAL CLIs end-to-end (the COG-3 fixture idiom: subprocess, tmp roots, public serve surface only). Trajectory v2 records from the fixtures retain both `domain_operation` identity and the ONE descriptor, with the operation-name-authority mutant (§5.2) exercised per cabinet. **N9 parity battery (SF4):** `cog4-parity.py` runs over the ENTIRE pilot set + all three fixture cabinets; `test_cog4_parity.py` asserts zero divergent tuples in `cog4-parity-record.json` (§5.3) — foundry L188 "outcome/evidence parity holds" is exactly this gate, no more and no less. **Runner-invariance battery (MF-AC2, §9.5):** `test_cog4_organ_runner.py` runs the runner fixture with and without a schedule artifact injected into the cache and asserts byte-identical behavior; the mutant runner that reads the schedule artifact REDs — the shadow-only guarantee is mechanical, not doctrinal.

## 13. Corpus law + build discipline (tests-first; the 2026-07-23 lessons bind)

- **Tests-first, corpus immutable:** the §12 batteries + boundary suite + parity suites land FAILING-for-the-right-reason before implementation; builders NEVER edit tests — contradictions route to the integrator (the COG-3 prime law; three retired-guard precedents). **Every vacuity guard carries its own retirement condition in its docstring** ("retire when <artifact> lands") — COG-4's corpus authors get this in their brief verbatim (LESSONS L1106).
- **Reviews re-run everything:** a builder's suite-green claim is DATA, not evidence (L1105).
- **Integrator owns cross-unit footprint joins** (L1111); parallel units never maintain shared pinned constants; the committed-footprint ratchet stays the safety net.
- **Git hygiene:** after any post-`git add` edit, RE-ADD; verify HEAD bytes (`git archive HEAD | parse`) before pushing config/doc commits (L1107); PARK deferred rows with dated markers; grep duplicate YAML keys (L1108).
- **Dispatch hygiene:** every subagent prompt opens with a concrete first Bash command + the no-skill guard (L1109); every workflow script opens with `const A = typeof args==='string' ? JSON.parse(args) : args` (L1110); scratchpad clones fetch the canonical remote and assert the tip SHA before grounding (L1113); a twice-failed reader's scope folds into a larger agent's brief, never a third retry, and never a giant-single-table StructuredOutput schema (L1114) — the C2 grounding in §8.1 was produced under exactly this rule.

## 14. Waves, units, model routing, workflow protocol

### 14.1 File surface (proposed; germline surface = EXACTLY two files, the §4.5 extension gate pair — Captain-windowed; everything else non-germline, re-checked at S0 and before every commit)
| file | purpose |
|---|---|
| `framework/projection/{__init__,kernel}.py` | C3 kernel (§6.1) |
| `framework/scheduler/{__init__,snapshot,fold,serve}.py` (+ `model.py` if records warrant) | pure planner (§7.1-7.2) + kernel-bound serve |
| `framework/organs/{__init__,registry,descriptor}.py` | organ registry + ONE-descriptor resolution from manifest-declared values — NO action-plane import; the adapter leg lives cabinet-side (§4.4, §5.3, MF-A1) |
| `framework/schemas/extension-manifest.schema.json` | §4.2 additive extension (organ kind + fields + undo grammar) — **GERMLINE, §4.5 Captain-windowed micro-unit** |
| `framework/schemas/cognitive-trajectory.v2.schema.json` | §5.5 (v1 byte-untouched) |
| `framework/evolution/contracts.py` | version dispatch before v1 checks (§5.5) |
| `framework/watchdog/registry.py` | `_parse_organ_manifests` per-organ floors (§9.2) |
| `cabinet/config/boundary-manifest.yml` | C2 rows (§8.2-8.3) |
| `cabinet/scripts/cog2-import-gate.py` | the boundary ENGINE (converted in place, byte-compat §8.2) |
| `cabinet/scripts/{cog4-snapshot,cog4-schedule,cog4-dispatch-shadow,cog4-measure,cog4-parity}.py` | CLIs (own path defaults/injection — layer law §4.4); `cog4-parity.py` = the cabinet-side adapter/parity unit (§5.3, MF-A1) |
| `cabinet/scripts/cog4-organ-runner.py` | the composed wake vehicle (§9.5, MF-AC2) — scheduler-blind (deliberate-absence rows §8.3 + invariance battery) |
| `cabinet/scripts/validate-extension.sh` | organ-block interpreter extension (§4.3) — **GERMLINE, same §4.5 window** |
| `cabinet/scripts/tests/test_cog4_*.py` + `lib_cog4_*.py` | the §12 batteries, boundary-row mutant harness, AST pins (§8.4), fleet-truth, floor-conservation, parity, measurement |
| `cabinet/scripts/{verify-cognitive-phase4.sh,cognitive-phase4-review-scope.py,cognitive-phase4-rollback-rehearsal.py}` | phase twins (extraction to C6 stays recorded debt); verify carries the §7.3 pointer tripwire + the §10 armed consumer |
| `cabinet/scripts/egg-export-manifest.txt` + `test_egg_export.py` | delete/expect-absent lines for the phase twins (O-B3 precedent) |
| `cabinet/services.yml` + `cabinet/launchd/` | §9 composition commits only (atomic compose; fleet-truth guarded) |
| `cabinet/cache/scheduler/` | runtime artifacts (gitignored via `cabinet/cache/*`) |
| `docs/plans/cognitive-core-phase-4-rollback-manifest-2026-07-23.yml` | §16 |
| operative ledger + plan-doc parity rows | §11 |

### 14.2 Waves (each unit: dirty-guard per file, clean worktree off current origin/master, exact-path `git add`, FW-019 review artifact for >300-line commits — force-added, gitignored dir — per-JOB CI verification after every push)
- **W0 — S0 ground re-pin:** re-pin origin/master + fresh green per-job CI; verify every §-cited anchor at the new pin; census against a scratch tree-with-projected-COG-4 (two RED classes distinguished: expected-allowance vs STOP); measure the §10 baselines on the pinned pilot set; fleet-truth inventory snapshot; confirm no pointer files exist; `.gitignore` covers the new cache dir.
- **WR — riders lane (parallel, §18):** R1 verdict inbox + R3 shadow dividend on the shipped COG-3 serve surface.
- **W1 — C2 kickoff:** boundary-yml engine conversion (byte-compat + per-row mutant harness) + COG-4 rows + AST pins (§8.4) + fleet-truth test + officer-plist leakage cleanup unit (§9.1, parkable).
- **W2 — corpus:** the 15-sim batteries + the N1-N9 gate suites (incl. N9 parity, runner-invariance) + boundary/parity/floor-conservation/measurement tests, failing-for-the-right-reason; vacuity guards with retirement conditions (§13).
- **W3 — kernel + scheduler:** C3 extraction; snapshot/fold/serve on the kernel; schedule store (atomic, mandatory rows-hash, lockfile); cortex+objectives adoption units under the byte-compat gate (§6.4, parkable per-unit).
- **W4 — organs + descriptor:** the germline pair edit rides the §4.5 Captain-windowed MICRO-UNIT (handback + proposal doc + CG row already filed at contract landing; units build against the amendment's proposed schema text; window not open ⇒ dependent organ-validation units PARK with dated markers); organ registry; descriptor resolution (manifest-read-only, MF-A1) + the cabinet-side adapter/parity unit `cog4-parity.py`; trajectory v2 version-dispatch.
- **W5 — dispatch + fixtures:** cog4-dispatch-shadow (authority/budget/freshness/IDEMPOTENCY recheck §7.3, read-only pin); the three non-software fixture cabinets (MR4); N9 parity record over pilot + fixtures (§5.3).
- **W6 — composition + exit:** C4 pilot compose (≥3 projection rows → 1 runner row explicitly naming its organ manifests; `cog4-organ-runner.py` + invariance battery §9.5; per-organ floors under the COUNT+TUPLE law §9.2; atomic commit) + evidence-gated retirements; §10 measurement armed; N9 parity gate green; census tighten; frozen fresh-context panel (§15); rollback rehearsal; landing + A13 ledger flip.

### 14.3 Model routing (the two-tier law; pins EXPLICIT on every agent, both tiers — never inherit)
Judgment tier — `model: "fable"` pinned: this contract's plan-attack lenses (architecture + adversarial-correctness), the integrator, the frozen fresh-context panel, corpus-adjudication calls, synthesis/verifiers of claims. Execution tier — Opus 4.8 1M (`claude-opus-4-8[1m]`, single-quoted in shells) pinned as default: builders, test authors, mechanical verification, operations/governance lenses. **Fable-for-execution allowed** (Captain 2026-07-23 calibration) where the orchestrator judges it beneficial — named candidates: the C3 kernel extraction, the descriptor/adapter unit, the C2 engine conversion (complex builders where COG-3's Opus builders averaged 1-4 review-caught must-fixes). Record the model each agent actually ran on. Fable exhausted ⇒ STOP and tell Nate first line, never silent fallback.

### 14.4 Workflow protocol
Multi-phase builds ride the Workflow tool (journal-cache resume; per-phase adversarial review); every launch appends a RESUME-BOARD row same-turn; limit-death ⇒ ScheduleWakeup + resume handles. Every workflow script opens with the args shim; every subagent prompt opens with a concrete first Bash command + the no-skill guard; builder StructuredOutput schemas REQUIRE `census_delta` (exact measured integers) + `corpus_untouched_proof` (e.g. `git diff --stat` over the corpus paths) fields; worktree landings only (never the live tree); pushes verified per-JOB via `gh run view --json jobs`.

## 15. Review protocol

Clean-room review pre-merge (AUD-1 pattern) per wave; **the frozen §12.3-style fresh-context panel is the phase's LAST gate** (the F1 lesson, LESSONS L1112: per-unit reviews inherit the corpus's blind spots; the panel-subagent path is the vehicle — headless `bin/cabinet-review.sh` has no credentials). The panel reviews the CLAIM SURFACE (docstrings promising properties) against **EVERY public entry point of every new serve surface**: `framework/scheduler/serve.py` (all serve functions — each must route through the ONE kernel-bound loader; the F1 class is precisely a second entry point bypassing REFUSE), `framework/organs/registry.py` + `descriptor.py` public functions, the trajectory-v2 validator entry, `cog4-dispatch-shadow.py`'s decision path, `cog4-parity.py`'s two-leg comparison (the legs must stay independent — the ACTION_TYPES leg never derived FROM the descriptor leg), and the boundary ENGINE's scan entry. Standing panel questions: can any serve path return rows it did not hash in the same read? does any manifest-absent key skip a limb? can any non-declared input (env/clock) reach the fold? can the dispatcher execute anything? can `capability` reach any verdict predicate? does any path grow `ACTION_TYPES` or bypass the adapter parity? does composing rows drop a floor or LOOSEN its (cadence, threshold, probe) tuple (§9.2)? can the organ-runner observe the schedule store at all (§9.5)? Review-to-bytes bound by `cognitive-phase4-review-scope.py`.

## 16. Rollback manifest (`cognitive-core-phase-4-rollback-manifest-2026-07-23.yml`)

- **CLOSED range, by construction:** the footprint ratchet covers `S0_BASELINE_SHA..DONE_FLIP_SHA` — the done-flip SHA pinned into the manifest AT the ledger flip (the C1 fix shape). The **open-ended-HEAD ratchet defect is the named anti-pattern** (BACKLOG :1551-1555 — phase-1 already fails on `.mcp.json` on full clones); this phase does not inherit it. (Retrofitting phases 1-3 onto a shared closed-range engine is C1 — slate work, recommended alongside but not gating this phase.)
- **REAL footprint only:** every named artifact must exist in the tree/cache the manifest describes — the phase-3 manifest's phantom `objectives-index.sqlite3` (`cognitive-core-phase-3-rollback-manifest-2026-07-22.yml:25`, grep-verified: no code builds it) is the second named anti-pattern; the rehearsal script asserts existence of every named path class.
- **code inverse:** delete `framework/{projection,scheduler,organs}/`, `framework/schemas/cognitive-trajectory.v2.schema.json`, the `cog4-*` CLIs, phase twins, `test_cog4_*`/`lib_cog4_*`, `cabinet/config/boundary-manifest.yml`; restore from S0: `cog2-import-gate.py`, `extension-manifest.schema.json` + `validate-extension.sh` (the germline pair — the restore is ITSELF a germline edit riding a Captain window, §4.5), `framework/evolution/contracts.py`, `framework/watchdog/registry.py`, `cabinet/services.yml` + touched `cabinet/launchd/` plists, egg-export manifest + `test_egg_export.py`, **and `cabinet/config/cognitive-architecture-contract.yml`** with an `allowance_removal` block (restoring the yml removes the COG-4 allowance rows in the same commit — else observed<max headroom the mutant gate forbids) + `retain_append_only` (ledger/plan rows retained with dated rollback notes, never deleted — A13).
- **runtime inverse:** `rm -rf cabinet/cache/scheduler` — always safe by construction (schedule + snapshot are pure functions of declared inputs; the shadow decision log is the one honest loss, expendable in shadow by declaration, stated).
- **fleet inverse:** every composition commit is individually revertible (the atomic compose commit re-inverts to the N rows + floors); retirement rows re-enable from the manifest's per-row record; rehearsal proves one compose-revert round-trip.
- **`must_remain_unchanged` (G-F2, MF-AC1):** the Phase-0∪1∪2∪3 protected union **MINUS `cabinet/services.yml`** — the ONE deliberate, recorded SUPERSESSION: every prior manifest pins `services.yml` byte-unchanged (phase-0 :35, phase-1 :113, phase-2 :143, phase-3 :149 — byte-verified), but C4 composition EDITS it by charter; the superseding protections are NAMED — the §9.1 fleet-truth conservation test, the §9.2 floor-conservation law (COUNT + TUPLE), and the §9.4/§11 census maxima-tighten shrink-only gate — strictly stronger than a byte-freeze for what the freeze protected (no silent fleet growth, no lost floors). PLUS `framework/authority/{classifier,matrix,policy_engine}.py`, `framework/policies/authority-matrix.yml`, `framework/schemas/consequence-event.schema.json`, `framework/schemas/cognitive-trajectory.schema.json` (v1), `framework/frontdoor/action_language.py`, the dashboard `action-language.ts`, `framework/fidelity/graduation.py`, `test_cog3_objectives_ast_pin.py` — COG-4 touches none of the remaining union (the classifier/matrix/mirrors are READ, never edited). **Rehearsal per-path diff, updated to match:** `git diff --quiet` per protected path EXCEPT `services.yml`, whose rehearsal check is instead the compose-revert round-trip (fleet inverse) + the three superseding gates green.
- Rehearsal script green before landing.

## 17. Non-goals / phase boundary (restated as tripwires)

No scheduler-authority cutover and NO pointer file (existence = verify RED). No launchd job loading/rendering by the scheduler. No `CABINET_AUTHORITY_ENFORCING` flip (Captain-only). No ACTION_TYPES member added, removed, or renamed (Phase 7 owns retirement; the three §5.4 mutants stay green). No second effect algebra, no parallel packaging format, no new central enum (organ node kinds/statuses reference existing closed vocabularies only). No league/arena/benchmark/holdout (COG-5); no champion/canary/promotion (COG-6); no evidence-minting: the scheduler/organs never write consequence/evidence/trajectory stores in production paths (fixtures seed their own). No veto machinery (COG-3 flip-amendment obligation, untouched). No germline/schg action BEYOND the single §4.5 Captain-windowed amendment (two files — proposal doc + CG ledger row + named handback filed at contract landing; same-day relock); schg is never edited outside that window and never worked around. No external comms. No new Postgres roles or widened grants. No `instance/` literals in the new framework trees (S0 grep + landing layer-sep check; `check-layer-separation.sh` green over the working tree — its allowlist is Captain-gated AND censused max 19: no growth). Capacity law honored: the phase runs in isolated lanes, never pre-empting fresh-hatch/public-launch blockers or the Captain-label cadence.

## 18. R1/R3 rider scheduling (recommendation — an orchestrator capacity-law call, not a Captain fork)

R1 (verdict inbox, VOI-ranked top-3 on the ONE captain surface) + R3 (weekly read-only shadow-dividend report) are pinned "NEAR-TERM, right after COG-3 core ships" (BACKLOG :1559/:1561). They consume the SHIPPED COG-3 serve surface, have zero COG-4 dependency, and spend the scarcest resource (Captain judgment) by design. **Recommendation:** run them as **WR, a parallel narrow lane launched with W1** — small, read-only, high felt-value, and their verdict_human throughput directly feeds the evidence plane the COG-4 fixtures observe. They must not gate any COG-4 wave; if capacity forces a choice, WR lands before W3 (the riders discharge a Captain-visible pin; the scheduler is shadow and can wait a wave).

## 19. Register — MUST-RESOLVE + attack-refutation + synthesis-concern dispositions

| id | finding (source) | disposition |
|---|---|---|
| **MR1** (attack serious #1) | organ packaging must EXTEND the existing extension surface; the synthesis never grounded it | **RESOLVED via germline amendment + Captain handback (§4 + §4.5, rev-1 MF-G1):** `extension-manifest.schema.json` extended in place (`kind: organ` + required-when-organ fields + undo-grammar reconciliation) — BOTH files are GERMLINE (`germline-lock.sh:128-129`, "the extension gate pair" :120), so the edit rides a proposal doc + CG ledger row + NAMED Captain sudo-unlock handback filed at contract landing, a Captain-windowed micro-unit with same-day relock; `validate-extension.sh` + AX-5/AX-6 extended in the same window with mutants; W4+ builds against the proposed text and dependent units PARK if the window is closed; no parallel format anywhere (tripwire: any new manifest schema file = plan-attack RED) |
| **MR2** (attack serious #2) | latency/cost gate needs a real measurement protocol; phantom-M6 (LESSONS :1102) must not recur | **RESOLVED §10:** S0 landing-tree baselines on the pilot set; declared bounds/tolerances; `COG4_ENFORCE_BOUND` + consumer in the SAME commit + anti-phantom grep test; seeded-regression mutant; wall-clock honestly tripwire-only |
| **MR3** (attack serious #3) | composing N rows deletes N watchdog floors (`registry.py:757/:844/:1072`) | **RESOLVED §9.2:** organ `freshness_needs` → `_parse_organ_manifests` per-organ derived floors (stdlib-only survival contract, N-c); floor-conservation law strengthened rev-1 to COUNT + TUPLE (SF5 — per-field at-least-as-strict; loosened-threshold mutant REDs) + test; atomic compose commits |
| **MR4** (attack minor #4) | COG-3 shipped exactly ONE genuinely non-software cabinet; assertion surface differs | **RESOLVED §12 N8:** two NEW non-software cabinets + extend fixture #2; new op→descriptor→decision assertion surface; budgeted as corpus work, never claimed as 1:1 reuse |
| **MR5** (attack minor #5) | cite version-dispatch by BYTES, not the parked COG-1 row | **RESOLVED §5.5/§2.4:** `framework/triggers/envelope.py` + `framework/triggers/tests/test_envelope_v2.py` cited; COG-1 PARKED status stated (ledger :3341); no COG-4 dependency on the outbox pilot; un-park sequencing = capacity-law call recorded in §18's frame |
| **MR6** (attack minor #6) | census grounding imprecision: two non-COG allowance rows exist | **RESOLVED §11:** relaunch-killswitch (+25) + relaunch-ci-tolerance (+7) named; operative fact intact — zero COG-4 rows; 57/44/30 (and modules 226 / lines 65012 effective) at observed==max |
| **MR7** (attack minor #7) | C2 convergence must state the AST-pin fate | **RESOLVED §8.4:** engine stays module-granular (gate header :27-28 by design); `test_cog3_objectives_ast_pin.py` untouched; symbol law = sibling AST pins (+ two new COG-4 pins); boundary rows carry documentation-only `symbol_pin:` pointers; the pin never weakens |
| S-1 (synthesis concern) | fleet-measure integrity: 9 row-less template-plist organs + committed cos/comms-officer plists | **RESOLVED §9.1:** fleet-truth test pins the out-of-manifest set (conservation guard, no paper-shrink); dual-number exit report; officer-plist leakage cleanup unit (parkable) |
| S-2 | objectives serve-discipline holes must not propagate into the C3 kernel | **RESOLVED §6.3:** kernel adopts cortex single-read + atomic write; rows-hash limb MANDATORY-present for the schedule store; sim-11 mutant pins the absent-key case |
| S-3 | zero-headroom ordering: any net-add REDs the census | **RESOLVED §11:** per-unit exact-running-total allowance rows; same-commit compose for services; expected-RED vs STOP distinguished at S0 |
| S-4 | byte-true numbers: 15 sims (not 16); ACTION_TYPES=30 incl. ambiguous (not 31) | **ADOPTED throughout** (§header, §5.1, §12) |
| S-5 | rollback anti-patterns: phantom artifact + open-ended-HEAD ratchet | **RESOLVED §16:** closed range pinned at flip; existence-asserting rehearsal; both anti-patterns named |
| S-6 | eighth reader unrepresented | **CLOSED:** the missing coverage was the extension surface (= MR1, resolved §4) and the C2 file-level grounding — read IN FULL this session and inventoried at §8.1 |

**Genuine forks for the Captain:** none manufactured. The premise-check surfaced no fork (`fork_for_captain: null`); everything above is inside the standing grants. If the plan-attack surfaces a real one (e.g. a composition target the Captain operationally depends on), it goes to the premise-check-of-record before W6.

---

## VERDICT

rev 1 — PLAN-ATTACK ADJUDICATED; CONTRACT OF RECORD, CLEARED TO LAND (independent MF-verify verdict: LAND, 4/4 must-fixes byte-verified). Every ledger gate_cmd family, note-law, and rollback clause maps to a mechanical gate (§12/§16), plus the charter's L188 outcome/evidence-parity clause as the named N9 gate (§1/§5.3); all 15 foundry sims carry seeds + asserts + biting mutants; the three serious attack findings are resolved with named mechanisms (extension-surface packaging via the §4.5 germline amendment + Captain handback; measurement protocol §10; floor conservation §9.2 at COUNT + TUPLE strength); the scheduler is shadow-only with the six forbidden powers structurally enforced and the organ-runner scheduler-blind by construction (§9.5); ACTION_TYPES is shadowed-adapter-only — the adapter leg cabinet-side (§5.3) — with retirement explicitly reserved to Phase 7; the census walks the zero-headroom allowance path with exact running totals; C2/C3/C4 land as chartered (kickoff / during / exit). Remaining before code: S0 ground re-pin (§14.2 W0). The §4.5 window handback is filed at landing — Captain action at convenience, gating only the W4 germline micro-unit.

**Provenance:** authored + self-ratified per the 2026-07-07 full-autonomy grant + the Captain 2026-07-20 cognitive-masterplan continuous grant; fleet composition/retirement scope additionally per the 2026-07-21 ownership-on-GO. Premise-check corpus of record: `wf_8625da64-a2a` journal (7 readers + synthesis READY-TO-PLAN + attack UPHOLD), ground tip `e7f95d5a`.

---

## rev-1 2026-07-23 — panel dispositions of record

Four-lens plan-attack panel over rev 0; every finding orchestrator-adjudicated (ALL ACCEPTED) and applied in place by the REV-1 editor (Fable 5, judgment tier). Every byte-claim below was re-verified against ground tip `e7f95d5a` before being written into the contract. No existing obligation was weakened; each row names where the change landed. (The adjudications-of-record pattern per the COG-3 contract's "Build-time adjudications of record".)

| finding | disposition applied | landed |
|---|---|---|
| **MF-G1** (governance — gravest) | MR1 re-scoped: the edit pair is GERMLINE (`germline-lock.sh` "the extension gate pair" :120, FILES[] :128-129 — byte-verified). Route = germline amendment proposal doc `docs/proposals/germline-amendment-extension-manifest-organ-2026-07-23.md` + NEW CG ledger row + NAMED Captain sudo-unlock handback, ALL filed at contract landing (precedent: `docs/proposals/germline-amendment-manifest-sunset-2026-07-07.md`, CG-4 — the SAME schema file, Captain-windowed, one-revert rollback); W4+ builds against the proposed schema text, the actual edit is a Captain-windowed micro-unit, dependent units PARK on a closed window; the rev-0 zero-germline self-certification corrected everywhere; the schema STAYS draft-07 (`$schema` line 2) — no `Draft202012Validator` against it, validation = extended hand-rolled interpreter + AX tests | §4.5 (new), header posture line, §3 (two rows), §4.1, §4.3, §14.1, §14.2 W4, §16 code inverse, §17, §19 MR1 |
| **MF-A1** (architecture) | adapter-placement contradiction resolved as option (a): `framework/organs/descriptor.py` reads ONLY manifest-declared closed-set values (schema/AX-validated); the ACTION_TYPES shadow-adapter leg (`classify_action` classifier.py:215, matrix-derived risk_class, `ceiling_frozenset_map`) moved OUT of framework/organs into the cabinet-side parity unit `cabinet/scripts/cog4-parity.py` + AX-calibration suite (legal action-plane importer, own AST pin); framework/organs boundary rows unchanged (action plane FORBIDDEN); parity CLI explicitly allowlisted | §5.2, §5.3, §7.2, §8.3, §8.4, §14.1, §3 (classifier + policy_engine rows), §4.4, §15 |
| **MF-AC1** (adversarial) | §16 carve-out: COG-4 `must_remain_unchanged` = Phase-0∪1∪2∪3 protected union MINUS `cabinet/services.yml` (pinned in ALL four prior manifests — :35/:113/:143/:149, byte-verified); supersession recorded naming the superseding protections (fleet-truth conservation test §9.1, floor-conservation law §9.2, census maxima-tighten gate §9.4/§11); rehearsal per-path diff description updated to match | §16 |
| **MF-AC2** (adversarial) | organ-runner specified + guarded: `cabinet/scripts/cog4-organ-runner.py` named in the file surface; MAY import framework.organs (registry + descriptor), FORBIDDEN framework.scheduler AND the schedule-store data plane via DELIBERATE ABSENCE from the §8.3 allowlists (the `cog2-import-gate.py:265-268` idiom); invariance battery `test_cog4_organ_runner.py` (schedule-artifact injection; reader-mutant REDs) makes shadow-only mechanical; wake-row semantics: the runner services.yml row explicitly names its composed organ manifests — feeds `_parse_organ_manifests` per-organ expected-output resolution for the freshness probe | §9.5 (new), §8.3, §12 batteries, §14.1, §14.2 W6, §9.2, §15 |
| SF1 (idempotency) | dispatch recheck gains limb (6) IDEMPOTENCY: the parent charter's dispatcher quadruple "authority, idempotency, snapshot freshness, and remaining resource budget" (foundry §4.5 L116 — byte-verified) binds over the ledger note's authority/budget/freshness triple (ledger :3370) | §7.3, §14.2 W5 |
| SF2 (snapshot inputs) | four missing input families added as DECLARED, hashed snapshot inputs: per-organ starvation bounds (declared home = organ-manifest field `starvation_bound`, riding the §4.5 amendment, scheduler_policy default when absent), per-organ failure history, per-organ health outcomes, capability/MCP availability — all 15 sims (esp. 5/6/7/9) and N2 expressible from snapshot data alone | §2.1, §4.2, §7.1 |
| SF3 (organ-runner spec) | covered by MF-AC2 | §9.5 |
| SF4 (foundry L188 exit clause) | "outcome/evidence parity holds" (L188 — byte-verified) gets a NAMED mechanical gate: N9 in the §1 gate table — the §5.3 parity artifact `cog4-parity-record.json` extended over the pilot + ALL THREE fixture cabinets, gated by `test_cog4_parity.py` | §1 (N9 row), §5.3, §12 batteries, §14.2 W5/W6, VERDICT |
| SF5 (floor strength) | MR3 floor conservation strengthened COUNT → COUNT + TUPLE: per composed organ the derived `(cadence, threshold, probe)` tuple is at-least-as-strict, per-field definitions anchored to `_floor_for_entry` (registry.py:855) / `_service_log_candidates` (:856); loosened-threshold mutant REDs | §9.2, §19 MR3, §15 |
| N-a | `capability_gaps.py` citation fixed to its real tree: `framework/learning/capability_gaps.py:84` (byte-verified) | §5.1 |
| N-b | cross-manifest `state_ownership` collision detection is a SUITE-level check — the per-file validator cannot see it; stated where §4.3 mentions it | §4.3 |
| N-c | `_parse_organ_manifests` explicitly binds to the watchdog registry's stdlib-only narrow-parser survival contract — verified to exist in `framework/watchdog/registry.py` comments (:13 "STDLIB ONLY … load-bearing"; :663-665 "the registry's survival contract"; `_parse_services_manifest` docstring :757-764) | §9.2 |
| N-d | AX-suite consistency check added: manifest-declared `risk_class` must equal the matrix-derived class for the declared compat `action_type` (+ declared ceiling vs `ceiling_frozenset_map`); inconsistency REDs | §4.3, §5.2 |
| N-e | dialect split clarified ONCE (§4.5 canonical): NEW schema documents (trajectory v2) = Draft-2020-12 per the COG-3 idiom; the germline extension schema STAYS draft-07 | §4.5, §4.3 (§5.5 already correct, unchanged) |

**MUST-RESOLVE table status after rev-1:** MR1 = resolved via germline amendment + Captain handback (§4.5); MR2/MR4/MR5/MR6/MR7 unchanged-resolved as at rev 0; MR3 = resolved at COUNT + TUPLE strength (SF5). The §19 register rows were updated in place to match.

**Editorial repairs in the same pass (cross-refs only, no obligation changed):** two dangling `(§5.6)` cross-refs (a rev-0 typo — no §5.6 exists; the enum-growth mutants live in §5.4) corrected to `(§5.4)` at the §1 N8 row + the §3 matrix row; the §12 title and §14.2 W2 "eight-gate" phrasings updated to name the N1-N9 gate set after N9's addition.

**Orchestrator landing pass (2026-07-23):** independent MF-verify verdict **LAND** (4/4 must-fixes byte-verified, mutually consistent; three minor phrasing findings fixed in this pass — the §2.3 gate-count phrasing, the §8.3 planner-reads summary aligned to the §8.4 pin (organs included), the §4.3 "MF-G1d" tag normalized to "MF-G1, dialect clause"). Protocol-of-record correction (header + VERDICT): the drafted "Captain premise-check" next-step replaced by the governing protocol — the Captain's 2026-07-20 cognitive-masterplan grant delegates the premise-check to the orchestrator (run and COMPLETE: `wf_8625da64-a2a`, READY-TO-PLAN, attack UPHOLD, zero genuine forks; COG-3 landed under the same protocol). Captain involvement = the §4.5 unlock handback (filed at landing, action at convenience) + full visibility — not a gate. Per the 2026-07-07 full-autonomy grant + the 2026-07-20 masterplan grant.
