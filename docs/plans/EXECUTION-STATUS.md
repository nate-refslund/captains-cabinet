# Master-Plan Execution Status

> Live tracking for `plan-A-personal-clone-org-2026-07-02.md` + `plan-B-macmini-product-org-2026-07-02.md`.
> One line per item touched; evidence links mandatory. Update in the same change as the work (docs-track-code).

## Done

| Date | Item | What happened | Evidence |
|---|---|---|---|
| 2026-07-02 | (pre-F0) Plans + reports committed to repo | Both plans, the flavor-B blueprint, and the flavor-A report copied into `docs/plans/` so plan-state versions with the code it governs. Ride the F0.1 canonical commit. | this directory |
| 2026-07-02 | **F0.10** pipe-watchdog PATH fix + probe-error surfacing | Root cause: launchd's default PATH lacks `/opt/homebrew/bin`, so `_cabinet_redis()`'s bare `redis-cli` raised `FileNotFoundError`, swallowed by a blanket except — the dead-man's switch and cost-guard alerts were dark; log showed `outcome-watchdog: DOWN → Chair ping FAILED to enqueue` every 10min (a FALSE down: the read path was equally broken). Fix: (1) `check.py` resolves the binary via `shutil.which` + Homebrew fallback; transport failures now land in `PROBE_ERRORS`, reported every cycle and notified with a 6h cooldown — a probe error is an alarm, never a silent `''`; (2) plist gains an explicit PATH (belt-and-suspenders); agent booted out/bootstrapped and kickstarted. | `~/.screenpipe/state/pipe-watchdog.log` 14:20Z: `outcome-watchdog: alive (heartbeat 20min ago)` (was `DOWN/FAILED to enqueue` at 14:10Z). Files: `~/.screenpipe/pipes/pipe-watchdog/check.py`, `~/Library/LaunchAgents/com.screenpipe.pipe-watchdog.plist` |

## In flight

| Item | State | Evidence |
|---|---|---|
| **F0.1 prep** secret scan | **DONE — history is CLEAN.** gitleaks 8.30.1 over ALL history: 4 findings, all verified dummy test fixtures → allowlisted in `.gitleaks.toml` (rides the F0.1 commit). **No history rewrite needed; push unblocked** (after F0.2+F0.4 per union ordering below). Working-tree scan: real tokens ONLY in gitignored files — `cabinet/.env` (expected) and `memory/logs/2026-06-{23,26,30}.jsonl` (JWT ×6 + a Sentry user token echoed into local tool logs). NATE DECISION (sit-down Q5): precautionary rotation. NEW SUB-ITEM: secret-redaction filter on the post-tool-use logger — assigned to Plan A **A3.9** (secrets/debris class). | scan reports in session scratchpad; `.gitleaks.toml` |
| **F0.2 (core)** deploy-mac.sh wrong-fleet fix | Both hardcoded `cos cto cpo cro coo` sites replaced by `roster_officers()` — fleet DERIVED from `instance/config/roster.yml`; consultant guard applied per officer in `--all`; **no roster ⇒ refuse (exit 2), never a preset default**. Verified: `--all --dry-run` targets exactly the live 4; refusal exits 2 on a roster-less tree. REMAINING: germline residue purge (officer-capabilities.conf / platform.yml voices / mcp-scope.yml) — sit-down Q3. | `cabinet/scripts/deploy-mac.sh`; dry-run output in session log |
| **F0.4 prep** `cabinet/services.yml.draft` | Drafted from the 13 live LaunchAgents. Findings: (1) two live plists (`limit-reset-watchdog`, `cos-inbound`) are INVALID XML for strict parsers (`--` in comments) — regenerate, never hand-fix; (2) live draft-lane cadence **300s** vs documented 2h — decision = sit-down Q10; whichever wins, sync Plan B F0.3 spec block + Appendix E in the same change (review I4). Next: generate-plists.py + schema + verify-launchagents declared-vs-installed-vs-firing diff. | `cabinet/services.yml.draft` |

## Done (2026-07-02 evening — GATE 0 EXIT: canonical commits + push)

| Item | What happened | Evidence |
|---|---|---|
| **F0.1/F0.3** canonical commits + push + CI live | Tree taken from 114 dirty entries → **clean**. Two commits through the FW-019 checkpoint-review gate (fresh-context reviewer: slice 1 APPROVE, slice 2 APPROVE-WITH-NOTES, 0 blocking; L-1 label-validation applied; D-1 resolved drop-and-document — captain-decisions.md stays deployment-local by design): `d27751d2` (June autonomous-operation work, +13.3k LOC made visible to git) + `f57bfbc5` (F0 execution). Pushed `feat/fidelity-harness-design` to origin through the pre-push gate; **Cabinet CI fired on the live branch for the first time** (run 28618484301) incl. the new framework-tests job (known honest-red: 2 pre-existing graduation date-drift failures = debt 1b). | `git log` d27751d2/f57bfbc5; `gh run list --branch feat/fidelity-harness-design`; review artifact `shared/interfaces/reviews/feat-fidelity-harness-design-cp1.md` |

## Done (2026-07-02 night — F0.5 + F0.7: the labels moment)

| Item | What happened | Evidence |
|---|---|---|
| **F0.5** binder wire LIVE — mechanical verdict capture | `framework/frontdoor/binder_wire.py` (+11-test suite, fail-closed ordering asserted): Captain approve/edit:/skip: replies binding to a pending `·pid·` proposal are recorded as superseding ledger events IN-PROCESS, then delivered on approve/edit via `chair_drafts.deliver_draft` — Chair out of the recording path (skill Duty B amended; relay carries a `[⚙ binder: …]` prefix so it never double-delivers). Flag-gated `CABINET_BINDER_WIRED=1` in the cos-inbound plist (rollback = remove + redeploy). Deploy: generated plist REPLACED the invalid-XML hand-made cos-inbound plist (also fixed the missing officer arg found in the services row); poller verified: single instance, ESTABLISHED Telegram long-poll, offset preserved, 0 new timeouts/75s. SYNTHETIC E2E on the production ledger (synthetic lane): skip recorded (decision=rejected), tripwired delivery untouched, idempotent replay = no-pending-match. `decided_at` = decision time (reviewed_at threaded — caught in verification). Latent gap flagged: lane drafts store no `recipient_email` → email delivery fail-closes with "no email" and the Chair completes via person intel (Teams resolves by name); fix rides A1. | tests 284 passed/17 skipped (frontdoor+acting); synthetic transcript in session log; `/tmp/cos-inbound-plist-backup-*.plist` |
| **F0.7** ledger-liveness dead-man ARMED — and its first hour caught a TRUE POSITIVE | `cabinet/scripts/ledger-liveness-check.py` (hourly launchd via services.yml + generated plist; healthchecks check `ledger-liveness` 24h/6h grace, email channel assigned): starvation = pending>0 AND oldest>24h AND poller alive AND zero superseding decisions in 24h. Parser bug caught in first live run (fractional-ISO → 0.0h ages would have made starvation untriggerable) — fixed FAIL-LOUD (unparseable ⇒ ancient). Second run: **STARVING=True is REAL** — 4 pending drafts, oldest 33.7h, 0 decisions/24h → /fail pinged, DOWN email to Captain. The 47%-expiry disease, now visible the hour its instrument armed. Recovers automatically when decisions land through the new wire. | `~/Library/Logs/cabinet/ledger-liveness.log`; healthchecks `ledger-liveness` status=down (honest) |

## Remaining verification debt

- **F0.10:** fix landed + live-verified; remaining: (b) exception-path unit test for `_cabinet_redis`/PROBE_ERRORS (review I1). Debt (a) alarm-path drill: **CLEARED 2026-07-02** via the F0.13 kill drill (external path); the watched-pipe-kill detection drill joins the weekly-drill machinery.

## Done (2026-07-02 PM — F0.13 external dead-man LIVE)

| Item | What happened | Evidence |
|---|---|---|
| **F0.13/F0.13a** healthchecks.io external out-of-band dead-man | Captain created the account + provided keys (stored in `cabinet/.env` + `_shared/.env` + ping key in the watchdog plist env — send-only key, low sensitivity). Three checks created via API (`pipe-watchdog-alive` 10m/15m, `capture-fresh` 10m/30m, `outcome-watchdog-alive` 10m/80m — timeouts/grace matched to real cadences); pings wired into `pipe-watchdog/check.py` (stdlib urllib, in-process, best-effort): alive = every completed cycle, capture-fresh = all WATCH pipes healthy, outcome-alive = cabinet outcome-watchdog heartbeat <75min. **KILL DRILL RUN TWICE:** first drill proved down→up mechanics via API AND caught a real silent gap — API-created checks had EMPTY alert-channel lists (alerts would have gone nowhere); fixed (channels="*" on all three) and re-drilled: DOWN email 19:40:50 + UP email 19:41:49 landed in Gmail. Breaches now reach the Captain independent of Redis/Telegram/this Mac. Remaining: weekly automated drill job (small; rides F0.13 machinery item). | healthchecks.io project (3 checks up); `~/.screenpipe/state/pipe-watchdog.log` 19:38Z ping cycle; Gmail thread "DOWN/UP \| cabinet: pipe-watchdog-alive" |

## Review pass (2026-07-02 PM — adversarial reviewer over amended plans)

**Verdict: structurally coherent** — additions fit both dependency spines, no red-team-amendment or kill-list conflicts, board-ID sweep clean. Fixes applied same day: B1 work-model amendment authored + staged as F0.1 rider; B2 Plan A A3.T table corrected to the RE-POINT-TO-VAULT ruling (4 rows + enum + arithmetic); B3 union ordering declared (below); I1 verification-debt honesty (above); I3 this file restructured; M1 Plan B open-Q renumber; M4 redaction filter assigned to A3.9. **Build-time reconciliation notes (M2/M3/M5/M6):** B6.7's Stage-1 dossier and the Estate Mapper Source Map are ONE artifact (`instance/config/sources/<tool>.yml`) — amend B6.7 spec when its ID is minted; Plan B Appendix E parity assert needs a generated-rows pattern for compiler-made sync jobs; the PM CI ratchet gets its ID with the TaskAdapter item and B2.1's Monday-footer propagation implements adapter-side; Plan A A4.1's trinity page notes the canonicality shift when touched; B4.14 must port the context-4-reworked index.py if the A-side rework lands first. **I2 (context-4 re-baseline seam):** folded into the voyage entry below.

## F0 union ordering (binding — review B3)

Shared-phase ordering follows Plan B's chain: **F0.2 residue purge + F0.4 manifest/generator land BEFORE the canonical commit+push** (RT#13: never canonicalize a tree whose deploy tooling builds the wrong fleet). Concretely: sit-down answers → germline residue diff → work-model amendment staged → finish F0.4 (generate-plists.py) → commit → push → CI retarget → reply_binder wire.

## Captain-ratified additions (from Q&A 2026-07-02)

- **PM decoupling (Monday/Jira/Linear → optional adapters; foundation stores work locally).** Ruled 2026-07-02, EXTENDED same day: **the vault is the one synthesis destination; Monday keeps ONLY PM.** Work items: (1) canonical work store = the LOCAL SQLite task board (B4.11 — Neon optional for scale, never required); (2) formalize the `TaskAdapter` interface over `cabinet/adapters/` + `task_sync_runner.py` (pull backlog / push status / sync; local wins, adapter mirrors); (3) **RE-POINT-TO-VAULT (new disposition class, A3-phase, shadow-parity per pipe):** monday-halfhourly, monday-daily-{summary,insights,improvements}, monday-weekly-trends, monday-deep-research → synthesis born as vault markdown (`5-Reflections/Activity|Research/…`, indexed by embeddings/context-4); people-intel → vault dossiers only (People-board upsert retires; optional vault→Monday mirror later); meeting-intel Reflections-push retires (already vault-first; its TASK creation stays = PM); obsidian-sync's Monday→vault mirror → retirement after migration (kills the two-hop-lag class at the root); `context_lib._fetch_monday` → reads the vault daily-summary note (removes a third-party API from gather()'s hot path); (4) **Monday's ONLY remaining surface = PM:** dev-tasks plugin (PolAds/STEPhie), todos, commitment→task promotion, completion-tracker board closes, briefs reading due tasks — all via the adapter; (5) amend `docs/work-model.md` — STREAM canonical = local board, board 5091706356 = STEP collaboration mirror (rides the F0.1 commit); (6) CI ratchet: no `monday`/PM imports in `framework/`; (7) Mini v1 ships with zero PM dependency; (8) pipe renames (monday-* → activity/research names) land at re-point time, not before. Commercial upside: foundation fully independent of external apps; customers plug their own PM or none.
- **voyage-context-4 re-index (approved).** Contextualized chunk embeddings, matryoshka 1024d = drop-in for the existing sqlite-vec schema; ~39k-chunk vault re-embed fits the 200M-token free tier; wins on conversation + long-context — the vault's exact shape. Constraints: fidelity style/author-centroid stays PINNED to voyage-4-large until its A2 re-baseline (judge-contract rule); `embeddings/index.py` reworked to feed document-grouped chunks; DB backup before the pass. **Re-baseline seam (review I2, binding):** the swap silently step-changes every gather()-dependent number, so (a) the re-index run emits a `rebaseline` annotation consumed by A2.2's decision-match series and A2.8's CUSUM (analogue of B4.3's model-bump rule); (b) the centroid re-baseline is a named sub-item under A2.3's judge-contract discipline; (c) A4.2's UPDATE-only content_ts backfill is FOLDED INTO the unified pass, and the pass inherits A4.2's **≥80%** content_ts bar (not the weaker >64%).
- **Linking backfill (approved) — one unified vault pass:** re-embed (context-4) + `chunk_entities` build + content_ts re-derive (>64% target) + deterministic link backfill (materialize IDs already present into bidirectional links) in a single re-index run (template: `embeddings/backfill_content_ts.py` in-place pattern). Inferred links (time-window + topic reconstruction) carry `provenance: inferred` + confidence — never silent facts. Write-time ID stamping prevents new gaps from the same date.

- **Directions layer** (post-M1/M2, small): per-lane `directions.yml` (north-star, bets, NOT-goals, tradeoffs; Captain-authored quarterly) + renewal loop gains direction-gap + research-driven outcome generation, judged against direction-fit; retro gains a direction-drift check. Added to both plans' open-question/deferred sections. `system-self` lane gets a direction artifact too (autonomous self-improvement outcome proposals).
- **Brain linking program** (folds into Plan A A4): link-at-write-time ID stamping (bidirectional meeting↔task↔commitment↔thread), `chunk_entities` lite at index time (people_resolver-based tagging), frontmatter backlinks for the big-four relations; full typed-edge graph deferred until dossier-miss instrumentation shows which joins fail.
- **Dashboard direction:** prune + add (no rebuild): delete legacy governance editor + Docker officer controls; add post-B2: Scorecard, Evidence browser (per-proposal chain), Captain-debt queue, Fleet (services.yml auditor), Autonomy map.

- **Estate Mapper + sync compiler (Captain-ratified workstream, 2026-07-02).** Verdict: NOT built-in today (onboarding/research.py explores repos only; B6.7 SOP Stage-1 has the oracle-inventory concept; all existing syncs are hand-built pipes; "important boards" live as hardcoded IDs — the anti-pattern). New foundation module `framework/sources/`: (1) read-only DISCOVERY on tool connect (boards/channels/drives/folders); (2) CLASSIFICATION encoding memory-vs-SoT as data — `knowledge-sync` (→ vault markdown w/ provenance+content_ts) vs `live-adapter` (state stays in tool, queried at question time) vs `ignore`, + volatility + sensitivity; (3) SOURCE MAP artifact (`instance/config/sources/<tool>.yml` + vault note) = durable environment knowledge officers auto-use (replaces hardcoded board IDs forever); (4) SYNC-PLAN one-card proposal (mode/cadence/backfill-depth+cost/destination, per-step gates); (5) SYNC COMPILER generates parametrized jobs registered in services manifest WITH freshness floors + watchdog expectations (principle 7 by construction); (6) low-cadence RE-EXPLORATION diffing the estate, bi-temporal mappings; (7) safety: read-only discovery, ingestion-provenance quarantine [RT#5] applies, propose-first, reversible jobs. COMPLEMENT: **federated gather** — every adapter exposes `search` as an auto-registered tier-2 fetcher (context_lib architecture generalized), so un-synced ≠ invisible. Sequencing: foundation post-M1; first full exercise = B4 Mini bring-up (PolAds sources), B6.7 product #2 runs it end-to-end; flavor A uses it for every new tool (Slack/OneDrive etc.). Commercial: this IS the plug-and-play onboarding demo.
- **Gap sweep — RATIFIED by the Captain 2026-07-02 ("they make all perfect sense"), fold in at phase-planning as named items:** (1) generic inbound-event seam (webhook receiver: verify→normalize→route; pairs with the VPS catcher; reduces Make.com dependence); (2) **vault librarian** — curation over INGESTED knowledge (cross-source contradiction detection, dedup/supersede/flag) complementing the nightly self-memory consolidation; (3) org-knowledge elicitation (self-knowledge pattern generalized: budgeted gap-filling questions); (4) cross-tool identity resolution (folds into entity work, fed by Source Map); (5) source offboarding path (adapters disable, history keeps source-defunct provenance); (6) secondary-human surfaces (team members as permissioned users — commercialization-phase, reserve room in trust model).

## Ratification (2026-07-02 — 11-question batch ANSWERED; full record in `shared/interfaces/captain-decisions.md`)

Q1 spine ✅ · Q2 outcomes = presented for confirm (see reply) · Q3 residue purge ✅ APPLIED (capabilities/mcp-scope/platform voices; retrieval granted to live roster) · Q4 killswitch authority ✅ · Q5 tokens accept-as-is ✅ · Q6 push approved ✅ (flavor split via layer-separation + CI ratchets) · Q7 Console DEFERRED · Q8 **hard-line narrowed: autonomous account creation for low-risk services; human gate = legal/money only** · Q9 Tailscale not set up — deferred · Q10 draft-lane 300s ✅ (Plan B synced) · Q11 Mini = future main machine; **backups deferred post-testing/pre-commercialization — one-disk risk accepted and logged**. NEW RIDER: README + stale top-level docs refresh joins the F0.1 commit.

## Done (2026-07-02 evening — F0.4 + F0.3 partial + outcomes ratification)

| Item | What happened | Evidence |
|---|---|---|
| **F0.4** services manifest + plist generator | `cabinet/services.yml` promoted from draft (schema documented inline; real commands for limit-reset-watchdog + officer-supervisor-mac + intake-surface; draft-lane pinned 300s per Q10; draft deleted). `cabinet/scripts/generate-plists.py` (Corridor-reviewed contract: name regex before path use, plutil via arg-list subprocess, never calls launchctl, no secret values rendered) renders all 9 daemon/watchdog plists → `cabinet/launchd/generated/` (gitignored). **All 9 lint OK; `--check` = 9/9 MATCH vs installed** — the two invalid-XML installed plists (cos-inbound, limit-reset-watchdog) handled by regex fallback as designed. Officers stay with deploy-mac's roster-derived template path (F0.2). | generator run output in session log; `cabinet/services.yml`; `.gitignore` |
| **F0.3 (partial)** CI retarget + framework suite + pre-push fix | Triggers now `[master, feat/fidelity-harness-design]`; new `framework-tests` job (py3.12, pytest+pyyaml+jsonschema). Pre-push hook `date -d` GNU-only bug fixed with BSD `-j -f` fallback (only affected announced-force-pushes). **Local suite: 1,323 passed, 18 skipped, 2 FAILED — pre-existing date-drift fixtures in `framework/fidelity/tests/test_graduation.py`** (untouched since 06-19; wall-clock-relative day arithmetic rotted by 07-02). CI job ships honestly red; **fix is now a named debt and it sits in the trust path (graduation.py) — must be green before the B2.9 wire / B3.2 flip.** | `.github/workflows/cabinet-ci.yml`; pytest output |
| **Outcomes ratification APPLIED** | `instance/config/outcomes.yml`: polads-003 → achieved, stephie-001 → achieved, stepnetwork-001 → retired; polads-001-prod gains the billing-freeze acceptance line; NEW: polads-004 (active, 4 nodes, Paddle/live PROPOSE-ONLY), polads-005 (draft, queued), stephie-002 (active, 4 nodes, GAM-write/prod/comms PROPOSE-ONLY, spec gated on stephie not_goals TODO). Header amendment cross-refs directions.yml + work-model amendment. **Document validates against `outcome.schema.json` — 9 outcomes.** | `instance/config/outcomes.yml`; jsonschema validation in session log |

## Next up (union ordering — see binding section above)

1. ~~F0.4 finish~~ **DONE** (above)
1b. **NEW DEBT (blocks B2.9/B3.2, not the push):** fix the 2 date-drift graduation tests (freeze time via injectable clock or fixture-relative bar math)
2. README + top-level doc refresh (F0.1 rider, Captain-requested) — explicitly incl. **captains-cabinet-guide.md** ("the holy bible", Captain 2026-07-02: review + adjust to the ratified architecture) + work-model amendment staged ✅ (pre-push check: `grep -n canonical docs/work-model.md`)
3. F0.1/F0.3: commit → push to origin (Q6) → CI retarget + `pytest framework/` job
4. F0.5 reply_binder wire → labels flow (M1 clock starts)
5. F0.13a autonomous healthchecks.io account (Q8 first exercise: chrome plugin + gmail verification) → F0.13 dead-man checks + synthetic-kill drill (clears F0.10 debt a)
6. F0.8 killswitch fail-closed + DEL-whitelist removal + resume token (after F0.10/F0.13 alarms exist) + KILLSWITCH.md launchd rewrite
7. F0.9 backups: DEFERRED per Q9/Q11 (revisit post-testing; B4.1 pre-flight note stands — backups never live on the clean-room Mini)
