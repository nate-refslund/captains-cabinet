# Cognitive Core Phase 5 — Evolutionary Foundry E1/E2/E3 in Shadow: honest Gate arming, immutable trajectory archive, arenas, benchmarks, league, frozen holdout oracle — CONTRACT (rev 1)

**Status:** rev 1 (2026-07-24) — **CONTRACT OF RECORD, CLEARED FOR MF-VERIFY.** The four-lens plan-attack panel (architecture, adversarial-correctness, operations, governance) is COMPLETE: every finding orchestrator-adjudicated **ACCEPTED** and applied in place; dispositions of record in the rev-1 appendix; every byte-claim touched was re-verified against the ground clone at tip `70bca2ae` before being written. The **premise-check-of-record is COMPLETE** (workflow `wf_fc493c16-a6a`: 8 ground readers + synthesis verdict **READY-TO-PLAN** with 6 concerns + 14 plan_seeds + adversarial attack **UPHOLD** — the attack's own `final` field misfired as "FLIP" and self-corrects in its notes: "treat this output's final as UPHOLD; four serious refutations must land in the plan, zero fatal"). All 4 serious + 3 minor refutations and all 6 synthesis concerns carry named in-text resolutions (§19 register). Per the Captain's 2026-07-20 cognitive-masterplan grant the premise-check is orchestrator-run with ZERO genuine forks (`fork_for_captain: null`; the COG-3/COG-4 precedent) — no Captain gate; the §7.5 germline-window handback is FILED at contract landing for Captain action at convenience (it gates ONLY the holdout Ring-0 listing; the interim posture + PARK fallback stand). Next: independent MF-verify over this rev, then S0 ground re-pin, then tests-first build per §14. No COG-5 code exists at contract-landing time (`framework/evolution/` = `contracts.py` + tests only, ls-verified; ledger row `status: todo`).
**Ledger row:** `docs/plans/operative-egg-ledger-2026-07-07.yml:3373-3382` (COG-5, todo, last_update 2026-07-19); plan-doc twin `docs/plans/operative-egg-plan-2026-07-07.md:1052` — A13 parity holds. Every `gate_cmd` family, note-law (:3380: no-league-on-synthetic · trajectories observation-only · arenas no live credentials/writes · aggregate-only holdout), and the rollback clause (:3381 verbatim: "future Foundry remains unable to mutate live champions and its archive/pointers require independently rehearsed rollback" — requoted to bytes at rev-1; rev-0 presented a compression as a quote) is a binding obligation restated mechanically in §12/§16. The gate_cmd is PROSE (the todo-row convention, per the attack's minor #7) naming NINE families and omitting the ≥20-candidates sim; **this contract supplies the runnable battery: the foundry:196 ELEVEN, pinned verbatim (§12)** — "at least 20 candidates" is sim #1's PARAMETER, never the count (the recorded COG-4 count-trap, LESSONS-class).
**Parent:** `docs/cognitive-core-foundry.md` — Phase-5 charter **L190-198** (title L190 "Foundry E1/E2/E3 in shadow" — an EXECUTION mandate, §8; Gate-arming precondition + the EIGHT-piece package + aggregate-only holdout + candidate classes + "No live promotion" L192; the league-opening bar L194; the ELEVEN sims L196; the EIGHT exit clauses L198); §3 invariants L48-61 (esp. **L54 evidence-is-fuel-never-a-score**, **L55 trajectories-observation-only**, L58 text-genome-first, L60 shadow-before-authority, L61 deletion-requires-evidence); §4.6 L120-124 (Foundry shape: "Machine outcomes dominate LLM judgments. The live Cabinet is always a pinned champion"); §5 protocol L130-148 (L137 negative-control law, **L144 dispositions**, L146 fresh-baseline + no-borrowed-mitigation + allowance law, **L148 capacity law**); §7 scorecard L224-239 (**L226 "No scalar score may promote"**); §8 migration law L241-247; §9 stop conditions L249-262; **Phase-6 reservation L200-206** — champion pointer/canary/breaker/**thin promotion shim L202**/arch-search unlock L206 are COG-6; **Phase-7 reservation L208-214** — compaction below base maxima.
**Spec of record:** `docs/plans/evolution-engine-spec-2026-07-05.md` — ratified direction, adopted WITH the foundry superseding it where they diverge; **every supersession stated explicitly in §2.4** (the synthesis's own rule, applied symmetrically per attack refutations 2 and 5).
**Structural template:** `docs/plans/cognitive-core-phase-4-contract-2026-07-23.md` — section discipline mirrored (gate table, disposition table, corpus law, census/allowance law, born-closed rollback manifest + sibling force-pairing, frozen fresh-context panel, non-goals-as-tripwires, register + dispositions-of-record appendix). Its build-time adjudications BIND package style here: NEW schema documents = FULL Draft-2020-12 validated in callers/tests only; cache-dir/root-dir-first CLI-injected paths (no `instance/` literals in framework code); vacuity guards carry retirement conditions; builders never edit corpus.
**Grounding:** premise-check corpus at ground-clone tip **`70bca2ae`** (= master AFTER the COG-4 done-flip `c58d4a57`; COG-4 ledger row `status: done`, battery 690 armed green, census actuals 236/66548/52/40/30, frozen review PASS — ledger :3363-3372 byte-read). All `:line` cites below were byte-verified this session at that tip unless marked (corpus). **Ground pin provisional:** S0 re-pins current origin/master + a fresh green per-job CI run before any edit.
**Runtime posture:** additive, **EVERYTHING IN SHADOW**. No live promotion, no live champion mutation, no league opening for live-fitness claims (§6), no gate-apply arming (§4.6 — that specific `sudo launchctl load` is the Captain's own act, `cabinet/services.yml:79-84`). The only live-behavior deltas permitted: (a) behavior-preserving fleet COMPOSITION of new periodic foundry work into the existing `cog4-organ-runner` row under the COG-4 §9.2 floor-conservation law, and (b) the R2/R4 amendment DOCS authored in-phase (proposal-only). Germline surface = EXACTLY ONE Captain-windowed amendment: the `framework/policies/immutable-core.yml` holdout listing (§7.5) — NO other germline touches; `framework/learning/gate.py` + `framework/fidelity/graduation.py` are schg-locked (`germline-lock.sh` FILES :103/:70, corpus) and are NEVER edited: all arming lands at call-sites (§4). External comms: none.
**Provenance:** authored and self-ratified per the 2026-07-07 full-autonomy grant + the Captain 2026-07-20 cognitive-masterplan continuous grant; fleet-composition acts and the E1/E2/E3 shadow runs additionally ride the 2026-07-21 ownership-on-GO. Contract authored on Fable 5 per the two-tier law.

---

## 1. Purpose and optimization target

Phase 5 makes the existing Gate capable of an **honest PASS without weakening it** (sandbox verify harness + regression-corpus admission + credential-free isolation proof — foundry L192's precondition, "only then" the package), then implements the ratified evolution package — **exactly the eight chartered pieces** (L192): trajectory archive, candidates, generator, arena, scorers, league, benchmark factory, frozen holdout interface — and **RUNS E1/E2/E3 in shadow** on the sanctioned substrate (§8): E1 league MVP over ≥20 prompt/retrieval candidates vs the existing fidelity/scenario eval substrate; E2 mined public/private benchmark suites + the frozen holdout; E3 at least one skill and one memory-policy candidate cycle. The league ships **CLOSED for live-fitness claims** with a derived + recorded opening bar and an arming record (§6). The optimization target is the foundry's (:24): verified mission value per unit of Captain attention/time/cost/risk — never candidate count, league throughput, benchmark volume, or code size.

**Vector evidence (gate instruments, none a score) — the eleven foundry:196 sims are §12's battery; the EIGHT foundry:198 exit clauses are the exit table:**

| id | exit clause (L198 verbatim) | mechanical gate |
|---|---|---|
| X1 | "ranked archive preserves every lineage/failure" | after the E1 run (sim 1): every generated candidate — winners, losers, failures, crashed runs — has an archive lineage row; a seeded candidate-drop mutant REDs (`test_cog5_archive_lineage.py`); chain verification green over the sealed run segments (§5) |
| X2 | "known-bad loses" | sim 2: the seeded regression-inducing candidate ranks below the incumbent baseline on machine-outcome dimensions and is admission-ineligible; scorer-ignores-machine-outcomes mutant REDs |
| X3 | "judge-only cannot promote" | sim 4: a candidate winning ONLY on the LLM-judge dimension is promotion/admission-ineligible by the §9 vector-floor law; the judge-score-reaches-a-promotion-joint mutant REDs; the certainty-grammar tripwires (§9.3) stay green |
| X4 | "proxy-overfit is caught by private/holdout divergence" | sim 5: a candidate overfit to the public split diverges on private/holdout aggregates; divergence emits a demote/withhold signal, never a silent average; divergence-ignoring aggregator mutant REDs |
| X5 | "arena has no live credentials or writes" | sim 7 (escape): candidate code observes NO live credential env/files and can perform NO write outside its arena workdir; the harness holds model access (§8.4); env-passthrough mutant REDs; the boundary is also static (§10 rows) |
| X6 | "machine outcomes dominate" | the §9.1 fitness law: promotion/admission-relevant floors are machine-outcome floors (CI/probe/undo/regression); LLM-judge dimensions may rank, never satisfy a floor; asserted structurally by `test_cog5_fitness_vector.py` over the scorer output schema |
| X7 | "benchmark cases carry cutoff/leakage metadata" | every E2-mined + synthetic case validates against the case schema requiring `{source_trace_id, cutoff_ts (canonical), expected, allowed_tools, scorer_id, split, leakage_constraints, promotion_eligible}` (spec:37, adopted); a metadata-less case REFUSES ingestion (mutant) |
| X8 | "E1 produces no live mutation" | a THREE-PART no-mutation gate per E-run, scoped honestly (rev-1 SF-6): (i) BYTE-IDENTICAL before/after fingerprint over the quiescent live surfaces the run could plausibly touch — services.yml, champion-relevant configs, prompts/skills in the live tree, `shared/interfaces/gate-evidence/` (E-runs invoke no ratify), the live checkout + `git worktree list` (arena worktrees disposed, none stray); (ii) ADDITIVE-ONLY verification over the run's own sanctioned outputs (archive root, league run artifacts): every pre-run archive chain head remains a prefix of the post-run chain — nothing pre-existing modified or deleted; (iii) NON-INTERFERENCE over concurrently-written ledgers (consequence ledger tail, shadow/decision logs — the 40 enabled services write them continuously, so byte-identity is not honestly assertable there): ZERO foundry-attributed rows in the run window (the §6.2 provenance vocabulary + §17's no-evidence-minting law are the discriminators). Any pointer/apply artifact existing = verify-twin RED (the COG-3 §7.4 / COG-4 §7.3 tripwire idiom); gate-apply remains dark (services.yml :79-84) |

**Refused metrics:** candidates generated, league rounds, benchmark case count, archive size, LOC, module count.

## 2. Scope — slice, inputs, supersessions

### 2.1 In scope (the eight pieces + the arming precondition + the runs)
1. **Gate arming (§4):** `framework/evolution/sandbox.py` (unprivileged worktree-prep + exec — ALSO the arena substrate, one mechanism); the armed wrapper `cabinet/scripts/cog5-gate-arm.py` composing harness + `regression_gate.evaluate_gate` + `ceiling_probe` at the CALL-SITE (never a gate.py edit); the credential-free isolation proof (escape sim); the empty-corpus `no_verdict` decision (§4.5).
2. **The eight pieces (§3 dispositions; §5-§8):** archive (NEW, recorder-discipline), candidates (NEW), generator (NEW), arena (EXTEND the sim-harness/officer_runner substrate with a league driver), scorers (COMPOSE fidelity scorer + judge-calibration + machine-outcome collectors), league (NEW driver), benchmark factory (EXTEND `regression_corpus_lib` mining), frozen holdout interface (NEW-frozen + §7.5 listing ceremony).
3. **The runs (§8, MR3):** E1 executed (≥20 prompt/retrieval candidates, ranked archive, results explicitly non-fitness); E2 executed (real mined suites + frozen holdout_gen + the exit measurement of mined-corpus actuals vs the §6 recorded minimums); E3 executed (≥1 skill-procedure + ≥1 memory-policy candidate cycle on the sim substrate).
4. **League arming record (§6):** derived + recorded per-stratum minimums; league CLOSED at exit; opening = a post-phase event.
5. **Riders/slate (§11, §18):** R2/R4 foundry-era amendment doc authored in-phase (proposal-only); C5 registration interface (report-only law preserved); C6/C1 deferral decision with dated marker; C8 debt recording; R5 non-foreclosure shaping the archive schema (§5.4).

### 2.2 Inputs (all declared, injected — no env/clock reads in any pure surface)
The evidence substrate the factory mines and the league scores against (all exist, byte-verified): the consequence ledger (`framework/fidelity/consequence.py` — review.source vocabulary :110-126), the instance regression corpus (`instance/fidelity/regression_corpus`, non-empty on THIS instance; the framework `DEFAULT_CORPUS_DIR` ships empty by design — `regression_corpus_lib.py:58/:95`, `regression_gate.py:6-7`), fidelity case receipts + golden evals (**25 bodies** in `memory/golden-evals/` — eval-001..eval-026 with no eval-023; the directory's 27 entries include `README.md` + the `framework/` fw-*.sh shells, neither a body (recounted from bytes, rev-1) — germline-locked; runner report-only `run-golden-evals.sh:10-17`), probe outcomes (B2/B5 services.yml rows), the cog4 shadow decision log (SECONDARY source — §5.3: P1-raced, record_kind-colliding, currently unscheduled), and trajectory v2 records from the emitter this phase builds (go-forward source; every record clears `structural_issues` + `semantic_issues` with a trusted `ValidationContext` — `contracts.py:62-66/:247-268/:405`, fails closed without receipts/action_risk_map).

### 2.3 Phase boundary (must NOT leak in)
Champion membrane/canary/breaker/**promotion shim `promote.py`**/earned promotion = **COG-6** (foundry L200-206; ledger :3383-3392) — §17 tripwires. Architecture search stays locked; its unlock criterion is a COG-6 exit condition (L206; spec:18) — no COG-5 path reaches it. ACTION_TYPES retirement + compaction below base maxima = Phase 7 (L210). `CABINET_AUTHORITY_ENFORCING` = Captain-gated, untouched. Veto machinery = the COG-3 flip-amendment obligation, untouched. Gate-apply arming = the Captain's own `sudo launchctl load` (services.yml :79-84) — this phase delivers its named prerequisite (the harness) and STOPS. The COG-4 parked rows stay parked and un-inherited: organ-schema-validation (CG-33 window, `docs/plans/cog4-w4-u1-organ-schema-validation-PARKED-2026-07-24.md`) and cortex serve-adoption (kernel `read_rows`, `docs/plans/cog4-w3-u3-cortex-serve-adoption-park-2026-07-24.md`) — the archive is NOT a kernel adopter (§5.1) so neither park blocks anything here; COG-1 remains PARKED with a stale rationale ("COG-3 in-flight") — S0 refreshes its dated marker (parked-debt reader).

### 2.4 Spec↔foundry supersessions (each stated explicitly; foundry masterplan-of-record wins)
1. **Package membership:** spec:33 lists NINE modules incl. `promote.py`; foundry L192 charters EIGHT pieces and assigns "the thin promotion shim into the existing Gate" to Phase 6 (L202). **promote.py is COG-6** (attack serious #2). Spec §4 E3's "promote via Gate" step is scoped this phase to producing `gate.ratify` EVIDENCE PACKS only — ratify "APPLIES NOTHING" (gate.py:507-513 docstring, byte-read); the dark apply lane is untouched.
2. **Fitness:** spec:19 "Fitness = weighted sum…" is superseded by foundry §7 L226 "No scalar score may promote a candidate or phase" — stated ONCE, cleanly, per attack minor #5: the documents are closer than "contradicts" (spec §1 correction 3 already subordinates the judge scalar — "can rank candidates but can never promote one on its own" — and spec §3's promotion rule is floor-shaped). Binding form in §9.1: **the §7 vector with independent floors governs; a composite number may order a league table; no scalar reaches any promotion, admission, or graduation joint; missing evidence = `unknown`, never success.**
3. **E5 champion auto-promotion (spec:49)** = COG-6 scope entirely.
4. **E4 launcher-foundation (spec:48)** = substantially delivered since by the layer-separation program (`check-layer-separation.sh`, egg manifest, clean-room hatch gates); not re-commissioned; the known residual (product tokens) stays the recorded layer gap, not COG-5 scope.
5. **E0 reward-signal hygiene (spec:44)** = shipped piecemeal since 2026-07-05 (review.source + universal stamping live in `consequence.py:110-126`; B2 probes/verifier scheduled via services.yml; promotion sources matrix-keyed per CG-1) — reused, not rebuilt.
6. **§5 reconciliation pass** = historical, completed by the sovereign/guardian landings (CG-1 Option B `@e76706f3`, ledger :1515-1523).
7. **Sim count:** the canonical battery is foundry:196's ELEVEN (§12); the spec predates it and the ledger prose names nine families — both yield to the foundry list (synthesis concern 1).
8. **Archive lineage (spec:30 "extends SOV-8's variant archive"):** honored as COMPOSE-by-reference, never extension-in-place (rev-1 SF-5) — the variant store's sole writer is schg-locked gate.py (the S4 stage, gate.py:29/:594-597, `variants/<sha16>.patch` under gate-evidence); the trajectory archive references variant patches by content hash as §5.4 payload refs and never writes `shared/interfaces/gate-evidence/`; a literal extension would mint a second Gate-evidence writer (refused — the §5.2 WALL and the germline). Disposition row in §3.

## 3. Component disposition table (foundry §5 L144 — reuse | extend | compose | retire | new; attack serious #4 resolved)

| component | disposition | evidence |
|---|---|---|
| **arena substrate** — the fidelity ISOLATION stack, cited to its real mechanisms (rev-1 SF-1): `officer_runner.py` blind-drive (`run_case` :484; EVALUATION-MODE laws :65-73; "Every admitted source is then run through leakguard.filter_mcp_result" :133) + `framework/fidelity/leakguard.py` (the sacred boundary :1-12, both-ends fencing) + `oauth_llm.py` harness-holds-credentials (:1-9 — OAuth headless path, token held in the HARNESS env, "There is NO ANTHROPIC_API_KEY"; the driven session never receives it) + `sim_runner.py` SIE-7 quarantine (:1-24 — fail-closed sim-mode preflight; sim rows structurally invisible to live readers). Gate `_default_runner`'s unprivileged laws (euid≠0, no-shell arg-lists, hooksPath pinned both ways — gate.py:372-410) are the EXECUTION law-source `sandbox.py` replicates — an execution precedent, NOT the isolation mechanism | **EXTEND — a league driver over the EXISTING sim harness** | the spec's OWN plane table: Arena "exists; needs a league driver" (spec:29); its "worktree isolation" clause is the one piece with NO existing code (grep: zero worktree machinery in `framework/fidelity/`; gate.py refuses today for exactly this reason — :372-410 "no candidate worktree prepared (sandbox harness not built)") — §4.2 `sandbox.py` builds it. `framework/evolution/arena.py` drives candidate runs THROUGH these surfaces; `sandbox.py` (§4.2) supplies worktree prep + unprivileged exec for both the arena and the ratify wrapper — ONE mechanism, two consumers |
| **scorers** — `framework/fidelity/scorer.py` (shipped 3-pass verdict flow) + `judge_calibration.py` (JUDGE_HARD_BAR 0.80 / MIN_PAIRS 10, :91-95) + `run_f1.py` (BASELINE_MATCH_RATE 0.083) + probe/CI/undo machine-outcome collectors | **COMPOSE** — `framework/evolution/scorers.py` is an adapter layer producing the §9.1 VECTOR from existing scorers + machine outcomes; it invents no new judgment | attack #4's adjudication adopted verbatim; the calibration bars become derivation inputs to the §6 minimums |
| **benchmark factory** — `framework/fidelity/regression_corpus_lib.py` `extract_corrections` (:244 — harvests edit/skip/veto/undo/human_wrong from the ledger into a frozen, immutable, replayable corpus) | **EXTEND** — `framework/evolution/bench_factory.py` extends this exact mining pattern to spec E2's full source list (consequence events, undo reversals, human edits/rejects, probe outcomes, injection attempts) and stamps the §1 X7 metadata + splits | the E2 charter substantially duplicates the shipped miner (attack #4); `new` would need evidence that doesn't exist |
| **benchmarks (existing suites)** — golden evals (25, germline bodies — §2.2 recount) + phase gate sims + falsifier corpus | **REUSE as registered public-suite CITIZENS (C5, §11.3)** — a registration interface, NOT an execution migration; the eval-025/report-only law preserved verbatim (`run-golden-evals.sh:10-17`: "REPORT-ONLY: nothing reads the scalar as an autonomy GATE") | C5 (BACKLOG :1572); registration adds cutoff/leakage metadata rows referencing the suites; their runners are untouched |
| **the Gate** — `framework/learning/gate.py` `ratify(proposal, *, runner=, probe_fn=, …)` (:498-517, "APPLIES NOTHING"), `_default_runner` fail-closed (:372-410: "no candidate worktree prepared (sandbox harness not built)"), `touches_ring0` (:347) | **REUSE via the injection seam — arming at CALL-SITE/wrapper, ZERO gate.py edits** (schg FILES; the `regression_gate.py:38-41` precedent chose call-site composition precisely because graduation is germline) | the seam is injection-ready and was panel-probed in phase-4's review; grep: ZERO `regression` refs in gate.py today — §4.3 wires it outside |
| `framework/fidelity/regression_gate.py` `evaluate_gate` (:147; three-valued — `no_verdict` :27-35/:56/:81 "both 'fail' and 'no_verdict' are False") | **REUSE at the wrapper call-site (§4.3); the §4.5 `no_verdict` decision closes the open semantics** | deliberately kept out of germline graduation (:38-41, byte-read) — the same law keeps it out of gate.py |
| `framework/evolution/contracts.py` (v2 dispatch decided FIRST :247-268; `validate_holdout_receipt` :310; `semantic_issues` fails closed w/o trusted context :405; `action_risk_map` :62-66/:972; undo grammar :1045) + `framework/schemas/cognitive-trajectory{,.v2}.schema.json` + `holdout-evaluation-receipt.schema.json` | **REUSE — the archive/emitter validates THROUGH it; observation-only enforced at the contract layer** (header :1-7: "never carries promotion or fitness state") | foundry §3 L55; ledger note-law 2. The emitter this phase builds is a CALLER; contracts.py itself gains nothing (or at most additive helpers under its own tests) |
| **trajectory archive** + v2 emitter | **NEW — justified per L144:** no accumulate-forever, arrival-ordered, sealed SOURCE store exists anywhere in the estate (the kernel is a cache — §5.1; the recorder is per-trial capped evidence; the predictions store's mechanics are disqualifying §5.2) | §5; the physical discipline is ADOPTED BY REPLICATION from `framework/evidence/recorder.py` (the COG-4 organs-registry replica precedent — replication + parity tripwire, never a boundary-violating import) |
| **SOV-8 variant archive** — the Gate's S4 stage: content-hash-named diff patches (gate.py:29 "S4 archive — content-hash-named variant archive of the exact diff"; write site :594-597, `variants/<sha16>.patch` under `evidence_dir()` = `shared/interfaces/gate-evidence/` :84/:97-100) — the store spec:30 says the trajectory archive "extends" | **COMPOSE — reference-by-content-hash, never extend-in-place (rev-1 SF-5)** | the store's ONLY writer is schg-locked gate.py; extending it in place would mint a second Gate-evidence writer (refused: the §5.2 no-candidate-write-path WALL + the germline). Archive records REFERENCE S4 variant patches as content-addressed payload refs (§5.4); supersession of spec:30's "extends" stated in §2.4.8 |
| **candidates + generator** | **NEW** — no candidate-genome representation or patch/config generator exists (grep) | §8.2; text genome only (foundry L58): prompt / retrieval / skill / memory-policy classes, closed set this phase |
| **league** | **NEW** — the driver loop (spec:45); consumes arena+scorers+archive; NEVER a promotion path (§3 L48 "The Foundry has no alternate promotion path" — structurally: gate.py + graduation.py schg-locked) | §8.3 |
| **frozen holdout** — `framework/evolution/holdout_gen.py` + oracle CLI | **NEW-frozen** — spec:19: separately-versioned, league-invisible, Ring-0-listed; admission side EXISTS (`validate_holdout_receipt` + receipt schema) | §7; the LISTING is the §7.5 Captain-windowed amendment (MR1); interim posture honest and named |
| `framework/projection/kernel.py` | **ADOPT ITS LAWS BY STDLIB REPLICATION, parity-tripwired (rev-1 SF-4): canonical dialect, cutoff validator, atomic-write discipline — never an import; NOT the archive substrate** | §5.1: sorted rechain (:118-130) is arrival-order-INVARIANT, whole-file replace (:184-199), rollback=cache-DELETE (:325-344) — cache premises the archive must refuse; any shared code enters via the kernel's own extension seam (:59-63), never by bending limbs; boundary ROW 6 allowlists NO evolution importer (boundary-manifest.yml:316-339) — the replica path is the phase's pick (§5.1-§5.2), the seam-extension alternative is NOT taken |
| `framework/evidence/recorder.py` | **REUSE-BY-REPLICATION of the physical discipline** (O_APPEND+fsync+dir-fsync `_write_exact_event` :610-623; prev_hash/sequence; anchor attestation :599-608; pending.json exactly-once heal :625-670) | §5.2; the per-trial `trial_event_cap` (:69-75) is deliberately NOT cloned — R5 forbids a cap; sealed segments bound the O(n²) re-verify class instead |
| `framework/objectives/counterfactual.py` predictions store | **REUSE the WALL construction only** (disjoint namespace, own chained manifest, no candidate write path, excluded from projections — :5-14) | its mechanics are DISQUALIFYING as-is: append without fsync (:39-41), non-atomic manifest `write_text` (:45-57), O(n) full rechain per append — named so nobody copies them |
| `cabinet/scripts/cog4-dispatch-shadow.py` shadow log | **REUSE as a SECONDARY corpus source with the §5.3 duplicate-tolerance + field map** | P1 (review :218) + record_kind collision (`"run"`/`"decision"` :871-877 vs trajectory enum `["live","public_benchmark"]` schema :32) |
| `cabinet/services.yml` + `cabinet/config/organs/` + `cog4-organ-runner` | **COMPOSE — any new periodic foundry work lands as ORGAN MANIFESTS in the composed runner row, never new service rows** (the judge-calibration/prediction-calibration precedent, services.yml :73-76/:467-470) | §11.2; zero net service growth; floors per COG-4 §9.2 COUNT+TUPLE |
| `cabinet/config/cognitive-architecture-contract.yml` | **EXTEND — COG-5 allowance rows only; thresholds never raised; maxima tightened at done-flip** | §11.1; zero headroom observed==max: modules 236 / lines 66548 / services 52/40 / action_types 30 (yml :11-29 + COG-4 rows :113/:120, byte-read) |
| `cabinet/scripts/cog2-import-gate.py` + `cabinet/config/boundary-manifest.yml` | **EXTEND with ROWS only (the C2 engine, shipped in COG-4)** — holdout-invisibility rows, archive data-plane row, arena/league allowlists, per-row mutants auto-generated | §10 |
| phase twins (`verify-cognitive-phase5.sh`, review-scope, rollback-rehearsal) | **NEW (cloned)** — C6 parametric engine DEFERRED with C1 as a joint post-COG-5 compaction unit (§11.4, dated decision) | the twins-clone debt stays recorded, now 6 deep — the C8 argument FOR the deferral being short-lived |

## 4. Gate arming — the charter precondition (foundry L192 first sentence), all outside germline

### 4.1 The law
"First make the existing Gate capable of an honest PASS **without weakening it**." Arming = the three sub-obligations below. **Weakening tripwires:** gate.py and graduation.py byte-untouched (schg + §16 must_remain_unchanged); no stage removed, no bar lowered, no verdict vocabulary widened; `_default_runner`'s fail-closed laws (euid refusal, no-shell, hooksPath pinning, "evidence is never minted from nothing") are REPLICATED in the harness, provably (cloned mutants).

### 4.2 Sub-obligation 1 — the unprivileged sandbox verify harness
`framework/evolution/sandbox.py` (NEW): prepares a candidate worktree from a proposal diff (clean clone/worktree off the pinned base, diff applied, never the live tree), and executes verify-stage command sets unprivileged under the exact `_default_runner` laws (arg-lists no shell; `core.hooksPath=/dev/null` on argv AND env; euid==0 refuses; timeout; last-line evidence tails). It is BOTH the ratify `runner=`/workdir supplier and the arena's execution floor (one mechanism, §3). The armed wrapper injects it: `ratify(proposal, runner=harness_runner, probe_fn=…)` — the seam at gate.py:498-517 consumes it; gate.py never changes. **Honest-PASS demo (exit evidence):** a seeded IMPROVING candidate — fixes ≥1 frozen case, regresses none (the regression predicate DEMANDS demonstrated improvement: regression_gate.py:11 "PASS iff no frozen case regresses AND >= 1 frozen case improves"; a merely-flat candidate cannot honest-PASS this stage — §4.5's second arm) — ratifies end-to-end on THIS instance with every stage producing real evidence (worktree-run verify stages; regression stage over the real non-empty instance corpus; ceiling probe live) → verdict `pass`; AND a seeded known-bad candidate FAILS the same composition. "Honest" = no stage vacuous — asserted per-stage in the evidence pack.

### 4.3 Sub-obligation 2 — regression corpus wired into admission (call-site)
`cabinet/scripts/cog5-gate-arm.py` (NEW CLI): composes `regression_gate.evaluate_gate` (:147) as a mandatory admission stage AROUND ratify — the `regression_gate.py:38-41` precedent (deliberately not wired into germline graduation; the same composition law applies to gate.py). The stage replays the frozen corpus on both sides and folds its three-valued outcome into the evidence pack. The corpus path is CLI-injected (instance corpus on this deployment; `DEFAULT_CORPUS_DIR` semantics per §4.5).

### 4.4 Sub-obligation 3 — credential-free isolation, proven
Formalized as the §12 sim-7 escape battery over the harness + arena: candidate code runs with a SCRUBBED environment (no `~/.screenpipe`, no OAuth caches, no `.env`, no keychain reach — an explicit allowlist env, not a denylist), network egress blocked at the harness layer for candidate processes, and writes confined to the arena workdir (post-run diff of everything outside = empty). The HARNESS holds model access where scoring needs an LLM (the harness-holds-credentials pattern, cited to bytes rev-1: oauth_llm.py:1-9 — the OAuth token lives in the harness env, "There is NO ANTHROPIC_API_KEY" — driven through officer_runner blind-drive :484/:65-73; the candidate never sees the key). Enforcement is TWO-layer and the escape sim proves BOTH (rev-1 SF-1): the env-scrub above (behavioral) + the §10 static pin (`test_cog5_sandbox_ast_pin.py` — no credential-store import reachable from any candidate-visible surface). `bin/cabinet-review.sh` headless has NO credentials (LESSONS) — arena/panel runs that need a model ride the keyed panel-subagent path, never the clean-room headless path.

### 4.5 The empty-corpus `no_verdict` decision (synthesis concern 6 — DECIDED)
`regression_gate` is three-valued and `no_verdict` is never converted to pass (:27-35, :81). Decision: **for Foundry-candidate admission, `no_verdict` REFUSES admission** with the recorded reason `no_regression_evidence` — a distinct outcome in the pack, never spelled `fail`, never spelled `pass`. Rationale: foundry candidates are exactly the class where zero-regression-evidence admission would be dishonest; the fail-safe intent of the three-valued gate is preserved (nothing vacuously passes) and the fresh-hatch path is honest by construction: a hatched cabinet first MINES a corpus via the benchmark factory (E2 bootstraps admission evidence), then candidates become admissible. Negative controls: empty corpus → `no_verdict` → admission refused with the named reason (fresh-hatch arm); a mutant converting `no_verdict`→pass REDs; a mutant converting it→`fail` (which would misrecord honest absence as regression) also REDs. Non-foundry callers of `evaluate_gate` are untouched (their semantics unchanged).

**The OTHER honest-negative arm — benign non-improvement (rev-1 SF-7), pinned:** zero regressions + zero improvements → outcome `fail` with reason "no frozen case improved (predicate requires >=1)" (regression_gate.py decision table :159-168; code path :212-218) — byte-documented as "An honest negative" (:24-26). A no-improvement candidate therefore lands as an HONEST NEGATIVE in the evidence pack (distinguishable from a regression `fail` via the empty `regressed`/`improved` fields + reason string), is NOT admitted, and is NEVER surfaced as an error or a `no_verdict`. Negative controls (mutants, both directions): a flat candidate slipping through as `pass` REDs; a wrapper converting the flat-candidate `fail` into an error/`no_verdict` (misrecording an honest negative as an evaluation failure) also REDs. Sim 3 (known-good small improvement) is this arm's positive twin — sensitivity and honest-negative both pinned.

### 4.6 Arming ≠ applying
`gate.ratify` verdict `pass` only licenses the DARK apply lane / the Captain's manual window (gate.py docstring :507-513). `com.cabinet.gate-apply` ships dark, has no service row, and "Arming it is an explicit Captain `sudo launchctl load` AFTER the unprivileged sandbox verify harness exists" (services.yml :79-84, byte-read). This phase delivers that named prerequisite; the load itself is the Captain's, out of scope, and a §17 tripwire if any unit touches it.

## 5. The immutable trajectory archive (title deliverable; NEW — the substrate decision, plan_seed 1)

### 5.1 NOT a fourth kernel projection
Every kernel limb encodes the derive-at-compile CACHE premise: `chained_rows_hash` SORTS by order_key — deliberately arrival-order-invariant (kernel.py:118-130, C-F3); `atomic_write` is whole-file replace (:184-199) so every "append" would rewrite the store; the rollback grammar is cache-DELETE + rebuild (:325-344) — **delete forecloses R5** (BACKLOG :1563: the archive doubles as a training set; design so that option is never foreclosed). An archive's arrival order IS content (lineage, pre-registration honesty). The archive therefore adopts the kernel's premise-free laws ONLY — single canonical dialect (recorder-dialect `canonical_bytes`, kernel.py:70-77; NOTE the scheduler store's ensure_ascii=True FILE-ORDER dialect is a DIFFERENT dialect — never conflated), content-excluded identity, the canonical-cutoff hard-gate validator, mandatory-present/REFUSE manifest limbs — and takes its PHYSICS from the recorder. Any code sharing with the kernel enters via the kernel's own extension seam (:59-63 — "a third instantiation needing another algebra extends the kernel, never forks it"); if an arrival-order chain algebra is added there it is a kernel EXTENSION unit with its own tests; otherwise the archive replicates with a parity tripwire (the COG-4 organs-registry replica precedent). REV-1 PICK (SF-4): the REPLICA path, for every kernel-law adoption this phase (canonical dialect, cutoff validator, atomic-write discipline) — the seam path would ALSO require a ROW-6 allowlist addition for an evolution importer, which §10 deliberately refuses; the kernel row stays byte-untouched and each replica carries its parity tripwire.

### 5.2 Physical discipline (recorder-adopted, cap-refused)
`framework/evolution/archive.py` (NEW): append-only JSONL segments; every append `O_APPEND`+flush+fsync+dir-fsync (`recorder._write_exact_event` :610-623 discipline); `prev_hash`/`sequence` chain from ZERO_HASH; periodic anchor attestation (:599-608 shape); `pending.json` write-ahead exactly-once heal on crash (:625-670 shape); **sealed segments**: a segment closes at a declared row/byte bound, its chain head is sealed into the next segment's genesis + the manifest — bounding the recorder's O(n²) whole-trial re-verify class (:69-75) WITHOUT the recorder's mint cap, which R5 forbids (unbounded retention). Manifest-class artifacts (segment index, seals) are written with an archive-internal STDLIB REPLICA of the kernel atomic-write discipline (private O_EXCL tmp sibling + flush + fsync + `os.replace` — kernel.py:184-199), byte-parity-tripwired against the kernel source (rev-1 SF-4; the COG-4 organs precedent — the census record :113 names the identical choice: a "stdlib REPLICA with the kernel-parity test tripwire since boundary row 6 does not allowlist framework/organs as a projection importer"); NEVER a `framework.projection` import (ROW 6, boundary-manifest.yml:316-339 — §10's deliberate non-extension). WALLS (predictions-store construction, counterfactual.py:5-14): disjoint id namespace; NO candidate write path (candidate/arena code cannot import the archive writer — §10 rows); excluded from every projection (cortex/objectives/scheduler never read it); the predictions store's disqualified mechanics (no fsync :39-41, non-atomic manifest :45-57, O(n) rechain) are named anti-patterns with mutants. Rollback = **independently rehearsed RESTORE** (ledger :3381): seal + copy-out + verified re-read reproduces every chain head — NEVER cache-delete (§16).

### 5.3 Ingestion: sources, duplicate tolerance, the record_kind map (synthesis concern 4)
Sources in trust order (plan_seed 3): (a) the v2 EMITTER this phase builds — arena/league runs emit trajectory v2 records validated through `contracts.py` with a trusted `ValidationContext` (structural + semantic must be empty; fails closed without receipts/action_risk_map — :405); (b) the consequence ledger + fidelity receipts + instance corpus (already real, cutoff-disciplined, verdict_human-labeled — what E2 mines); (c) the cog4 shadow log, SECONDARY: **duplicate-tolerant by content fingerprint** (P1, `shared/interfaces/reviews/cognitive-core-phase-4-review.md:218` — two dispatchers racing one log can each record `would_dispatch` for the same idempotency key; the archive ingest dedupes on canonical-bytes fingerprint and records the duplication honestly), and **field-mapped**: shadow `record_kind ∈ {run, decision}` (cog4-dispatch-shadow.py:871-877) lands in an archive-native `shadow_record_kind` field — it NEVER populates trajectory `record_kind` (enum `["live","public_benchmark"]`, schema :32); the two vocabularies are disjoint and a conflation mutant REDs. **"read+check+append under one lock" is carried forward VERBATIM as named COG-6 cutover-amendment text** (the review's own must-fix routing :255); as a PRESENT fix (never a borrowed future mitigation, foundry L146) the shadow-log CLI gains the read-before-append lock fold in-phase as a small rider unit — it fixes today's raced reads for today's shadow log. CROSS-PHASE SCOPE NOTE (rev-1): the rider edits a COG-4 artifact (`cabinet/scripts/cog4-dispatch-shadow.py`) on a COG-5 wave — deliberate; the W4 FW-019 review artifact names it as a COG-4 artifact fix riding COG-5, and §16 restores it from S0 on rollback. If shadow dispatch is scheduled to accrue corpus, it becomes an ORGAN manifest in the composed runner row (§11.2), not a service row.

### 5.4 Archive schema — observation-only + R5-shaped from day one
Every archive record: content-addressed payload refs (receipts, not authority — foundry §3 L55: may NAME receipts, cannot assert authenticity/credit/eligibility/fitness); classification + allow/deny recorded separately; lineage (parent candidate ids, generation, operator); outcome refs resolved by the EXISTING evidence plane outside the candidate-controlled record. R5 non-foreclosure: training-pair reconstructability (input context + output + outcome labels queryable per record), identity-preserving redaction (secrets scrubbed, identities/lineage kept), unbounded retention (sealed segments, no cap). The archive root is a DECLARED parameter (CLI-injected); cabinet default `shared/interfaces/foundry/archive/`. DATA-PLANE EXCLUSION, stated honestly (rev-1 SF-2 — the rev-0 "gitignored runtime-surface class of gate-evidence/" premise was FALSE against bytes: NEITHER is gitignored today; .gitignore's only shared/interfaces rows are `shared/interfaces/**/*.md` (:173-174) + named `.jsonl`/runtime ledgers (:221-259), and gate-evidence/ (gate.py:84/:97-100, "runtime data") has no row): the phase ADDS the `.gitignore` row **`shared/interfaces/foundry/`** in W1, with a `git check-ignore` assertion in the phase twin, so archive segments/manifests are never tracked. Egg interaction: the export packages the git tree — untracked runtime data "never reach[es] the archive in the first place" (the manifest's own parenthetical), and the R116 transform FAIL-CLOSES the export on "any OTHER file under shared/interfaces/" that ever becomes tracked (`egg-export-manifest.txt:242-252`) — belt and braces; no expect-absent row is needed for untracked data. NEVER under `cabinet/cache/` (cache = deletable grammar; the archive is expressly not).

## 6. The league-opening bar — derived, recorded, CLOSED at exit (foundry L194; synthesis concern 3; attack #3 corollary)

### 6.1 The gate reading, pinned
Foundry L194 requires "a declared minimum corpus of real, cutoff-fenced trajectories and Captain labels for every promoted stratum, with the minimum **derived from the existing fidelity/evolution contract and recorded in the Phase-5 plan**." The ledger gate_cmd's "must pass real-corpus minimums" is READ as: **derive + record the minimums (below), measure the E2-mined real corpus against them at exit, and ship the league CLOSED with an arming record** — league opening is a separate post-phase event conditioned on labels. A reading requiring a MET corpus at exit would make the phase unfinishable and collide with the §5 capacity law (L148: never pre-empt live evidence generation or the Captain-label cadence). The charter itself sanctions this: "The league does not open merely because its code exists"; synthetic cases "can test plumbing and known mutants; they cannot establish live fitness."

### 6.2 The recorded minimums (derived from the existing constants — plan_seed 2)
Per promoted stratum (candidate class × risk stratum of the surfaces it touches):
- **Real trajectory floor:** ≥ **10** real, cutoff-fenced trajectories per stratum — MIN_PAIRS (judge_calibration.py:92-94: "below this the measurement is not evidence — 100% agreement on 3 pairs proves nothing") is the estate's own below-this-is-not-evidence constant, adopted as the per-stratum floor.
- **Captain-label floor:** ≥ **10** `verdict_human` labels per stratum inside the evaluation window (the same MIN_PAIRS logic applied to the human channel; `verdict_human` = any human's verdict per the phase-3 FORK-2 clarification), accrued via the R1 verdict inbox (SHIPPED, COG-4 WR merge `0d8a74d4`; `test_cog3_verdict_inbox.py` in-tree) — the label-cadence instrument exists; COG-5 must not disturb it.
- **Judge admissibility precondition:** before ANY LLM-judge dimension may even RANK on live data: agreement ≥ **0.80** over ≥ **10** pairs, status fresh (JUDGE_HARD_BAR/MIN_PAIRS/STATUS_MAX_AGE_DAYS, judge_calibration.py:91-95) — the existing hard bar, reused not re-derived.
- **Baseline floors:** candidate classes scored on fidelity tasks must beat the recorded generic baseline (run_f1.py BASELINE_MATCH_RATE 0.083) before "improvement" is claimable even as ranking; graduation-plane bars stay those read from `authority-matrix.yml` (graduation.py:92-130) — the league adds no second bar.
- **Certainty precondition:** live-fitness claims on mission edges require the §9.2 grammar — machine artifacts cap at P5; only `verdict_human` promotes (structurally zero-widening this phase).
- **The counting predicate (mechanical — rev-1 SF-8):** every archive/corpus row carries `provenance`, a CLOSED schema-internal enum `{real_live, real_mined, synthetic, sim_replay}` (validated in callers/tests per §11.1's no-new-central-enums law; disjoint from trajectory `record_kind`, schema :32, and from `shadow_record_kind`, §5.3). CHAIN-OF-CUSTODY: `provenance` is stamped by the archive INGESTER from the §5.3/§2.2 source class — consequence ledger, fidelity receipts, instance corpus, verdict-inbox labels, live emission ⇒ `real_*`; generator/arena/sim outputs ⇒ `synthetic`/`sim_replay` — and candidate/league/generator code can never set or rewrite it (the §5.2 no-candidate-write-path WALL covers the field). ONLY `real_live`/`real_mined` rows ingested from those NAMED real sources count toward the minimums above; the §6.3 measured actuals are computed by THIS predicate. Laundering mutant: a `synthetic`/`sim_replay`-marked row counted toward any §6.2 minimum → REDs (a `test_cog5_league_closed.py` arm); a row with missing/out-of-enum provenance REFUSES ingestion and never counts.

### 6.3 The arming record (ships at exit, league CLOSED)
A tracked artifact `docs/plans/cog5-league-arming-record-2026-07-24.yml` (+ `test_cog5_league_closed.py` asserting closure): the §6.2 minimums verbatim; the measured E2-mined actuals per stratum at exit (honest numbers, however short — computed by the §6.2 counting predicate: `real_live`/`real_mined` rows from named real sources ONLY, the laundering mutant guarding the count); the open conditions checklist (per-stratum floors met · judge calibration green · holdout freeze = Ring-0 listing LANDED §7.5 · Captain informed); `league_open: false`. Opening flips this record via a future amendment with its own review — never a code-existence event. Any league output while closed carries `fitness_claim: none` structurally (schema-required field; mutant REDs).

## 7. Benchmark factory + frozen holdout oracle

### 7.1 Factory (EXTEND — §3)
`framework/evolution/bench_factory.py` extends the `extract_corrections` mining pattern (:244) over spec E2's sources; every produced case carries the X7 metadata block (spec:37 adopted verbatim: source trace id, canonical cutoff_ts, expected behavior, allowed tools, scorer id, split `public|private|holdout`, leakage constraints, promotion eligibility); `graduation-credit:false`-derived cases are stamped promotion-ineligible (spec:39 honored — already excluded in `compute_ratios`). Split law (spec:38): public = league-visible cases; **private = league sees aggregate scores, never cases**; **holdout = league never sees cases OR per-case results — only the oracle-attested aggregate receipt**.

### 7.2 Oracle interface (NEW-frozen)
`framework/evolution/holdout_gen.py` (frozen, separately versioned) + `cabinet/scripts/cog5-holdout-oracle.py` — the ONLY sanctioned reader of holdout cases. The oracle evaluates a candidate against the holdout in isolation and emits an **aggregate receipt validating against `holdout-evaluation-receipt.schema.json` via `validate_holdout_receipt` (contracts.py:310)** — no cases, no reusable fingerprints, no per-case results (ledger note-law 4). Receipts are integrity artifacts; public↔holdout divergence is a demote/withhold signal (X4), never a tunable.

### 7.3 League invisibility (static + behavioral)
Boundary rows (§10): `framework.evolution.holdout_gen` importable by the oracle CLI + its own tests ONLY — league/generator/arena/candidate/scorers/bench_factory are structurally blind (unallowlisted-importer REDs); an AST pin (`test_cog5_holdout_ast_pin.py`) enforces the same at symbol level; the sim-6 leakage battery proves it behaviorally (a league-side attempt to read cases/fingerprints/per-case results REFUSES; a receipt carrying per-case data REFUSES validation — mutant each).

### 7.4 Freeze semantics — the STAGED truth (rev-1 SF-3/SF-9; no overclaim)
"Frozen" END-STATE = (i) separately versioned (its version in every receipt), (ii) league-invisible (§7.3), (iii) **Ring-0-listed in `framework/policies/immutable-core.yml` so gate S0 `touches_ring0` (gate.py:347) thereafter refuses any Foundry diff touching it** (spec:19 — the intended self-protection), (iv) thereafter mutable only via a Captain germline-unlock window. The end-state arrives in TWO STAGES, and this contract claims each piece only at its stage:
- **Stage A — in-phase (the COG-5 exit state):** (i) + (ii) + the §7.5.5 interim posture (content-pin CI tripwire, §7.3 rows + AST pin, egg-export exclusion, sole-reader oracle). Honestly NOT Ring-0: no schg, no hook guard, no gate-S0 refusal — a same-uid actor could edit file and pin in one commit (§7.5.5 honesty clause).
- **Stage B — the §7.5 Captain window:** the `immutable-core.yml` listing LANDS; `framework/policies` dir-cover schg (the dir is already whole-dir locked — §7.5, which is exactly why landing the listing needs the window) then covers the listing text, and gate-S0 refusal binds from the listing alone. (iii) and (iv) are TRUE from Stage B onward, never earlier. The amendment's `pending:` flags (§7.5.1) cover ONLY the four enforcement-list propagations (the SOV-C8 lockstep mechanism); they flip when a Captain unlock window — the same one or a later one — lands those list edits; gate-S0 refusal does NOT wait on them.
The §6.3 league-open checklist requires **Stage B LANDED**; the arming record carries `holdout_freeze: pending-captain-window` until then (§7.5.5).

### 7.5 The germline ceremony (MR1 — attack serious #1; the phase's ONLY germline touch)
**Byte-verified:** `framework/policies` is schg-locked as a WHOLE DIRECTORY (`germline-lock.sh` DIRS :143-151, entry :145, `-R` dir-cover: edits AND new files inside are blocked); `immutable-core.yml` self-documents it (header: "any path under one is Ring-0 by dir-cover, including this file itself"; EDIT DISCIPLINE: "Officers/loops propose changes to the Captain; only the Captain applies them in a germline-lock unlock window"; self-entry :172). The synthesis's "no sudo window needed" was grepped against FILES only and is **REFUTED — adopted**. Therefore the holdout listing is a Captain sudo ceremony, shaped exactly like CG-33 (the COG-4 MF-G1 precedent):
1. **Amendment proposal doc** `docs/proposals/germline-amendment-holdout-ring0-2026-07-24.md` carrying the COMPLETE proposed `immutable-core.yml` diff: one `files`-class entry `framework/evolution/holdout_gen.py` (+ `pending:` flags for the four enforcement lists per the SOV-C8 lockstep mechanism, the axes-amendment precedent — gate S0 refusal binds from the LISTING alone, "every class, not just the locked ones", header :8-11, so the schg four-list propagation may ride pending), + one-revert rollback.
2. **NEW CG-class ledger row** + A13 plan-doc twin, landing with this contract.
3. **NAMED Captain sudo-unlock handback** filed at contract landing — explicitly **batchable with the already-pending HANDBACKS item 19 window** (CG-33, unopened **as of 2026-07-24** — `~/cabinet-meta/HANDBACKS-perfect-cabinet-2026-07-10.md:319-327`; the batching offer is DATED and re-verified at WG — if item 19's window has meanwhile opened and closed, this handback stands alone): one window, two reviewed edits, same-day relock; lock state re-verified fresh (`germline-lock.sh status`, `ls -lO`) immediately before the edit.
4. **Build sequencing:** `holdout_gen.py` itself lands under `framework/evolution/` (NOT germline) — buildable now; units build against the proposed listing text; **freeze-verification units** (the arming-record `holdout_freeze` line; any sim arm premised on Ring-0 refusal) **PARK with dated markers if the window is unopened** — schg is NEVER edited or worked around.
5. **Interim posture (honest, evaluated per MR1):** until the listing lands — (a) a content-pin sibling test (sha256 of holdout_gen.py bytes; drift REDs CI), (b) the §7.3 invisibility rows + AST pin, (c) an egg-export EXCLUDE + expect-absent line for `holdout_gen.py` (O-B3 precedent) so no hatched cabinet ships an unprotected generator, retired when the listing lands, (d) the oracle CLI as sole reader. **HONESTY CLAUSE: none of this equals the Ring-0 listing** — no schg, no hook guard, no gate-S0 refusal (until listed, `touches_ring0` does NOT refuse Foundry diffs to holdout_gen — the attack's consequence 2, stated plainly); a same-uid actor could edit file and pin in one commit. Accordingly the arming record carries `holdout_freeze: pending-captain-window` and NO fitness/promotion claim may ever cite holdout results while interim — vacuously safe this phase (league closed), load-bearing at opening (the §6.3 checklist requires the listing LANDED).

## 8. Candidates, generator, scorers, league — and the E1/E2/E3 RUNS (MR3 — attack serious #3)

### 8.1 The execution mandate
The charter title (L190) commissions RUNS, not a delivery list; the L198 exit clauses are run-shaped and vacuously unprovable if E1 never runs. This contract commissions, in shadow, on the sanctioned substrate (synthetic/sim cases prove plumbing + known mutants; the existing REAL eval corpus supplies E1's tasks): **E1** — the league MVP RUN: archive + candidate + arena driver over the sim harness + league loop; **≥20 prompt/retrieval candidates** (sim #1's parameter) evaluated against the existing fidelity/scenario eval substrate (officer_runner cases, regression corpus replays, golden-eval-derived public cases); ranked archive produced; **results explicitly non-fitness** (`fitness_claim: none` on every row); no live mutation (X8 fingerprint). **E2** — the factory RUN: actually mine the real sources into public/private suites; freeze holdout_gen; **measure the mined per-stratum actuals against the §6.2 minimums and record them in the arming record** (never predeclare unreachability — the attack's corollary). **E3** — ≥1 skill-procedure candidate cycle (cluster → generate → replay/sim → mutate → archive; skills gain the candidate/tested/active/deprecated/failed lifecycle VOCABULARY as archive metadata only — activation stays with the existing skill machinery) and ≥1 memory-policy candidate cycle scored by downstream sim task performance. E-runs produce gate.ratify evidence-pack INPUTS at most (§2.4 supersession 1) — nothing applies.

### 8.2 Candidates + generator
`candidate.py`: the genome record — class ∈ {prompt, retrieval, skill, memory_policy} (CLOSED set this phase — foundry L192/L58), payload (patch/config), lineage, declared target surfaces, cost declaration. `generator.py`: seeded, deterministic-given-seed candidate production (mutation/crossover over text genomes); **refuses generation targeting Ring-0/germline surfaces** (reuses `touches_ring0` read-only over the candidate's declared diff paths — the Ring-0-out-of-search-space law, §17) and any non-closed-set class.

### 8.3 Scorers + league
`scorers.py` (COMPOSE, §3) emits the §9.1 VECTOR per candidate-run: machine-outcome dimensions (case pass/fail from the arena, regression replay, probe verdicts, cost/latency measured) + judge dimensions (via the calibration-gated judge path) + `unknown` for missing evidence (never imputed). `league.py`: rounds of generate → arena → score → archive → rank; ranking may use a composite FOR ORDERING THE TABLE ONLY (§9.1); admission-eligibility is floor-vector-only; budget ceilings are declared inputs — exceeding one halts/defers with reason (sim 11). The league writes ONLY to the archive + its own run artifacts; boundary rows deny it every other store (§10).

### 8.4 Arena credential boundary (ledger note-law 3; plan_seed 9 — re-grounded rev-1 SF-1)
Candidate code sees NO live credentials and performs NO writes outside its arena workdir. The isolation substrate, cited to its REAL mechanisms (gate `_default_runner` is the unprivileged-EXECUTION law-source, gate.py:372-410 — never the credential-isolation precedent): **blind-drive** — the arena drives candidates as `officer_runner.run_case` drives the officer-under-test (:484, EVALUATION-MODE laws :65-73); **credential custody** — the HARNESS holds model/API access and makes every scoring call itself, results passed in (the `oauth_llm.py` pattern :1-9: the OAuth token lives in the harness env — "There is NO ANTHROPIC_API_KEY" — the driven session/candidate never receives it); **context fencing** — leakguard both ends (leakguard.py:1-12; every admitted source through `filter_mcp_result`, officer_runner:133); **ledger quarantine** — any sim-side consequence emission rides the SIE-7 `verdict_sim` quarantine (sim_runner.py:1-24, fail-closed preflight; live readers structurally blind); **filesystem** — the §4.2 worktree/workdir isolation (`sandbox.py` — the spec:29 "worktree isolation" piece that exists nowhere yet). MECHANICAL no-network/no-credential enforcement for candidate code = TWO layers, one behavioral proof: (1) **env-scrub** — an explicit ALLOWLIST environment (§4.4), network egress blocked at the harness layer; (2) **static pin** — `test_cog5_sandbox_ast_pin.py` + the §10 rows (no credential-store import reachable from any candidate-visible surface); PROOF = the sim-7 escape battery exercises BOTH layers (env probe + egress probe + outside-workdir write each REFUSE; env-passthrough and hook-execution mutants RED). The clean-room headless path (no creds) is never used where a model is required (§4.4).

## 9. Fitness, certainty, and the composite law (the boundary section)

### 9.1 The fitness law (supersession of record — §2.4.2)
The §7 scorecard vector (foundry L224-239) with INDEPENDENT FLOORS governs every admission/promotion-adjacent judgment. A scalar/composite may order a league table for presentation. **No scalar reaches any promotion, admission, or graduation joint** — mechanically: the scorer output schema separates `vector` (floors-checkable) from `table_order` (presentation); every eligibility predicate reads the vector only; a predicate keying on `table_order` is a mutant that REDs; missing evidence = `unknown` and `unknown` never satisfies a floor (L239). Machine outcomes dominate (X6): judge dimensions can never be the satisfying evidence for a floor.

### 9.2 THE COMPOSITE CERTAINTY LAW — written once, here (synthesis concern 5; assembled from its three records)
> **Machine-gate promotion fuel is confined to the internal graduation plane, and there only in label-floor-met periods — structurally ZERO today; mission/world edges promote on human verdicts ONLY; every widening is a separate amendment.**
Its three records, joined: (1) **CG-1 Option B** (ledger :1515-1523, done `@e76706f3`; consequence.py :110-126 + :899-911): `confirmed` fuels promotion only when `review.source == "verdict_human"`; `verdict_gate` confirms fuel the internal plane only under sovereign posture AND deterministic-inverse action types AND label-floor-met periods — and `_label_floor_met` returns **False unconditionally** (consequence.py:800-816: "The A3 sensor DOES NOT EXIST YET… NO period is label-floor-met"; absence narrows); `verdict_judge` stays promotion-inert until CG-10; `wrong` demotes from ANY source. (2) **Phase-3 FORK-2** (contract :354, Captain-resolved 2026-07-22, option A): mission/world ("tested") edges promote on HUMAN judgment only — any human's verdict via any comms, never a machine/LLM; `states.py` enforces it as code (HUMAN_VERDICT_SOURCE :34-39; derive_edge_state :149-242; the P5 cap :237-240). (3) **Amendment-each widening** (FORK-2 option A; phase-3 §6.4/:289): any future widening — A3-era gate-confirm fuel, judge calibration unlock — is a fresh contract amendment with its own adversarial pass, never an automatic inheritance. Until now this law existed only as an assembly across those records plus a META-ONLY design doc (`~/cabinet-meta/docs/whole-cabinet-evidence-self-improvement-2026-07-16.md`) the repo and egg omit; **this section is its citable in-repo record** — the Foundry builds against it, and the egg now carries it.

### 9.3 Certainty grammar for scores (plan_seed 7)
League/benchmark/holdout artifacts are MACHINE-CLASS: on mission/world edges they cap at **P5 `observationally_supported`** (assumptions-gated, ruling R-A — states.py:237-240) and may demote/withhold, never mint `tested`/`falsified`. League surfaces speak `scored | ranked | observed` — never the Captain vocabulary (`tested`, `it worked`, `falsified`). The only bridge to `tested` is the human-verdict channel (the shipped R1 verdict inbox raising a ranked hypothesis). Mechanics: clone `test_cog3_verdict_vocab_tripwire.py` for every foundry source (a foundry artifact carrying a Captain-vocabulary claim REDs); extend the SIM-2 mutant family with two arms — **league-writes-verdict_human** (any foundry code path minting `review.source: verdict_human` REDs) and **promotes-on-league-score** (any edge/graduation predicate consuming a league score as promotion fuel REDs).

## 10. Boundary rows + AST pins (C2 engine — rows only, zero engine code)

Rows added to `cabinet/config/boundary-manifest.yml` (per-row mutants auto-generated by the shipped harness):
- `framework.evolution.holdout_gen` token: forbidden to ALL importers except allowlist_exact {cog5-holdout-oracle} + `test_cog5_holdout_*` globs (§7.3). The league/generator/arena/candidate/scorers/bench_factory modules are NOT allowlisted — deliberate absence (the `cog2-import-gate.py:53-54 (doctrine) + :342/:368-375 (the deliberately_absent mechanics)` idiom).
- **archive data-plane row:** the archive root token (`shared/interfaces/foundry/archive` assembled-token discipline), swept SWEEP_TREES-wide; allowlist = archive-internal + {cog5-archive-restore, cog5-league (append via the emitter only)} + test globs; **candidate/generator/arena tokens DELIBERATELY ABSENT** (no candidate write path — §5.2 WALL made static).
- `framework.evolution` reverse row: the evolution package may not import the action plane (`framework.{frontdoor,acting,authority}`) except the TWO sanctioned read-only joints: `touches_ring0` (generator refusal, §8.2) and the graduation/policy read surface already sanctioned for parity-class CLIs — each under its own symbol-level AST pin (sibling gates, never rows — the COG-4 §8.4 law; `test_cog3_objectives_ast_pin.py` and the COG-4 pins stay byte-untouched).
- projection-store denies: archive/league/arena tokens forbidden from cortex/objectives/scheduler cache paths (observation-only both directions).
- **deliberate ROW-6 non-extension (rev-1 SF-4):** NO allowlist addition puts any `framework/evolution/*` module on the `framework.projection` row (boundary-manifest.yml:316-339 — importable by cortex/objectives/scheduler internals + the cog2/3/4 CLIs only); the archive's kernel-law adoption is stdlib-replica + parity tripwire (§5.2), so the kernel row stays byte-untouched and an evolution import of `framework.projection` REDs as UNALLOWLISTED under the shipped engine.
New sibling pins: `test_cog5_holdout_ast_pin.py`, `test_cog5_league_ast_pin.py` (league tree imports = stdlib | evolution-internal | the fidelity scorer/leakguard/officer_runner PUBLIC surfaces | nothing else — no subprocess in the pure ranking fold; the arena/sandbox layer is the sanctioned subprocess site), `test_cog5_sandbox_ast_pin.py` (the harness may import git/subprocess machinery; it may NOT import credential stores or the archive writer).

## 11. Census, fleet, riders, slate (zero headroom — the allowance path)

### 11.1 Census law
Baseline actuals at 70bca2ae, ALL observed==max (yml byte-read): modules **236**, lines **66548**, services **52/40**, action_types **30**, coupling ≤24/allowlist ≤19, named compilers 1. Every COG-5 unit adding framework modules/lines bumps COG-5 `temporary_allowances` rows to the EXACT measured running total in the SAME commit (the COG-2/3/4 idiom verbatim; census RED on net-add is the EXPECTED allowance path, distinguished from STOP at S0); tightened to phase actuals at done-flip (shrink-only); sunset 2027-01-24; deletion_gate = Phase-7 compaction folds/retires the evolution additions or the phase is superseded. Projected NEW modules (S0 measures actuals): `framework/evolution/{sandbox,archive,candidate,generator,arena,scorers,bench_factory,holdout_gen,league}.py` ≈ 9 (+ an `emitter.py` only if archive.py warrants the split), plus cabinet CLIs {cog5-gate-arm, cog5-league, cog5-bench-factory, cog5-holdout-oracle, cog5-archive-restore} + phase twins + tests (non-census or line-only). `named_compiler_modules` stays 1; `central_action_types` stays 30; no new enum anywhere (candidate classes/splits/statuses are schema-internal closed sets validated in callers/tests, never central vocabularies).

### 11.2 Fleet law (no new permanent services)
Any periodic foundry work (bench mining cadence, archive verify sweep, shadow-log accrual if scheduled) lands as ORGAN MANIFESTS under `cabinet/config/organs/` composed into the existing `cog4-organ-runner` services.yml row — the judge-calibration/prediction-calibration precedent (:73-76/:467-470) — under the COG-4 §9.2 COUNT+TUPLE floor-conservation law. `services_total`/`services_enabled` do not grow; if any row is touched, maxima re-tighten same-commit. E-runs themselves are workflow-driven build acts, not services.

### 11.3 C5 — evals as foundry citizens (include-or-defer: **INCLUDE the interface, DEFER the migration** — dated 2026-07-24)
The registration INTERFACE ships: benchmark-registry rows referencing golden evals + phase gate sims + falsifier corpus as public-suite sources with X7 metadata. Their RUNNERS are untouched; the league READS their published results as machine-outcome evidence. The eval-025/report-only law is preserved verbatim and tripwired: `run-golden-evals.sh`'s scalar stays REPORT-ONLY (:10-17); a registry row or league path consuming the golden-eval scalar as a gate/floor REDs (cloned mutant). Full execution-migration = post-phase slate work.

### 11.4 C6 + C1 (include-or-defer: **DEFER, jointly** — dated 2026-07-24, orchestrator capacity call)
The parametric phase engine (C6, BACKLOG :1573) and the closed-range rollback-ratchet engine (C1, :1568) are ONE mechanism-compaction unit scheduled immediately AFTER COG-5 lands: refactoring 6 phases' shipped twins + 5 manifests inside an already-large additive phase couples unrelated risk; the phase-4 born-closed manifest shape already contains the C1 fix for THIS phase (§16). The twins-clone + open-range debts stay recorded (this marker is the dated record); C8 counts them.

### 11.5 C8 — mechanism-count budget (record, don't build)
No mechanism census exists yet (C8, :1575 — anchor corrected at rev-1; :1574 is C7 captain-law ledgers). Each new bespoke engine this phase — archive append-chain, league loop, bench miner, holdout oracle, sandbox harness — is NAMED as C8-countable in its allowance-row reason string, so the future C8 row inherits an exact inventory. Recorded debt, not silence (attack minor #6).

## 12. Gate simulations — the ELEVEN foundry:196 sims, pinned VERBATIM (synthesis concern 1) + the X1-X8 exit battery

All suites `python3.12`, tests-first, file-seeded, no DSN, deterministic (declared cutoffs/seeds, no clock); every mutant must FAIL for the exact escape it names. "At least 20 candidates" is sim #1's PARAMETER (the count-trap, recorded). Synthetic corpora are SANCTIONED for all eleven (plumbing + known mutants — foundry L194); what synthetic evidence may never do is open the league or ground a live-fitness claim.

| # | sim (L196 verbatim) | seeds | asserts | negative-control mutant (must FAIL) |
|---|---|---|---|---|
| 1 | at least 20 candidates | the E1 run: ≥20 seeded prompt/retrieval candidates vs the existing eval substrate | full ranked archive; every lineage/failure preserved (X1); deterministic re-rank from the same archive + seeds | archive drops a failed candidate; rank order varies under PYTHONHASHSEED |
| 2 | known-bad candidate | a candidate seeding a known frozen-case regression | ranks below incumbent; admission-ineligible; the regression stage catches it (X2) | scorer ignores machine outcomes (judge-only rank) |
| 3 | known-good small improvement | a candidate fixing one frozen failure, regressing nothing | detected + ranked above incumbent on the machine dimension (sensitivity) | aggregate that buries the improvement (insensitive fold) |
| 4 | judge-only winner | candidate winning ONLY the judge dimension | ineligible for admission/promotion (X3); judge dimension recorded as ranking-only | judge score satisfies a floor / reaches a promotion joint |
| 5 | proxy-overfit winner | candidate tuned to the public split | private/holdout aggregate divergence flags demote/withhold (X4) | divergence silently averaged away |
| 6 | holdout leakage attempt | league/generator code paths attempting case/fingerprint/per-case reads; a receipt carrying per-case data | every attempt REFUSES (boundary + oracle + `validate_holdout_receipt`); aggregate-only receipt validates | per-case field tolerated in a receipt; an allowlisted league import of holdout_gen |
| 7 | credential/network escape | candidate code probing env/credential files/network/out-of-workdir writes | scrubbed env observed; egress blocked; outside-workdir diff empty (X5); euid/hooksPath laws hold | harness passes live env through; hook execution not disabled |
| 8 | nondeterministic scorer | a scorer varying on identical input (seeded) | flagged by the triple-run discipline (3 subprocess runs, distinct PYTHONHASHSEED); its scores quarantined `unknown`, never averaged | nondeterminism silently averaged into the vector |
| 9 | corrupt archive | truncated tail; bit-flipped row; forged prev_hash; broken seal | chain verification detects each; serve refuses beyond the last good seal; pending.json heal completes exactly-once | skip-verify reader serves past corruption |
| 10 | lineage rollback | seal + restore drill on a populated archive | the independently rehearsed RESTORE (ledger :3381) reproduces every chain head + row count; no lineage row lost | restore drops/reorders a lineage row undetected |
| 11 | cost explosion | a candidate/league round exceeding declared ceilings | halt/defer with recorded reason; per-candidate cost in the archive; ceilings are snapshot inputs, never league-tunable | cost-ignoring league loop keeps spending |

**Exit-gate batteries beyond the sims:** the X1-X8 table (§1); the arming battery (§4: honest-PASS demo + known-bad FAIL + the §4.5 no_verdict AND flat-candidate honest-negative arms + escape); the §9.3 vocabulary tripwires + SIM-2 extension arms; the §5.3 duplicate/field-map tests + P1 lock-fold rider test; `test_cog5_league_closed.py` incl. the §6.2 provenance laundering arm (§6.3); the §10 per-row boundary mutants + AST pins; the §7.5 interim content-pin + egg expect-absent; the X8 three-part no-mutation gate on every E-run (§1); the §12.1 declared-bound armed twin + seeded-regression mutant.

### 12.1 The declared regression bound — foundry L146's first sentence, DISCHARGED (rev-1 MF-1; the COG-4 §10 shape)

L146 ("Each detailed plan must define a maximum acceptable regression relative to a freshly measured baseline") is cited as binding throughout this contract (§5.3's no-borrowed-mitigation reading; §11.1's allowance law) — this clause discharges its first sentence mechanically:
1. **S0 fresh baselines (measured on the S0-pinned tree, never borrowed):** W0 measures and records in a dated tracked baseline artifact: (a) the armed COG-4 battery pass state (the 690-green state, ledger :3370, re-RUN fresh at S0 — never quoted forward), (b) golden-eval pass state over the 25 bodies (the runner stays REPORT-ONLY, `run-golden-evals.sh:10-17` — the baseline is the pass/fail vector, never the scalar-as-gate), (c) `verify-cognitive-phase4.sh` wall-clock p95 over ≥5 runs, (d) the boundary-engine full-sweep counts (`cog2-import-gate.py` SWEEP_TREES files-swept + violations; violations baseline = 0).
2. **Declared per-metric tolerances — declared HERE, BEFORE measurement:** pass-state metrics carry **ZERO regression tolerance** — the COG-4 battery and the golden evals stay green through every COG-5 wave (the §11.1 expected-allowance census RED is a census signal, never a battery RED, and is not exempted by this clause); sweep violations stay **0** and the files-swept count moves only by the phase's own added files (exact-integer accounting per §13's `census_delta` discipline); wall-clock bound = **max(p95×1.25, p95+5s)** — the COG-4 ×1.25 tolerance made floor-aware, so a fast baseline cannot manufacture spurious REDs (a sub-second p95 ×1.25 is noise-width).
3. **Armed via the phase verify twin with a REAL consumer in the SAME commit (the anti-phantom law, COG-4 §10.3 cloned):** the bound artifact + its arming flag are consumed by `verify-cognitive-phase5.sh` in the commit that introduces them; a grep-style test asserts ≥1 real consumer — an exported bound nobody reads is the named phantom class (COG-4 §10: COG2_ENFORCE_P95 once shipped exported with NO consumer).
4. **Seeded-regression mutant (negative control):** a fixture regressing past the bound (a seeded battery RED / an inflated wall-clock stub) REDs the armed twin; the mutant must fail for the exact escape it names (the §12 law).

## 13. Corpus law + build discipline (the 2026-07-23/24 lessons bind)

Tests-first; the §12 batteries land FAILING-for-the-right-reason before implementation; builders NEVER edit corpus (contradictions route to the integrator); every vacuity guard carries its retirement condition in its docstring; a re-keyed vacuity arm is an HONEST re-key at surgery, never a deletion (LESSONS :1117 rule 4). Reviews re-RUN batteries — a builder's green claim is DATA (claim-surface-vs-bytes lens standing, rule 1: docstrings are claims, run them). Integrator owns cross-unit footprint joins; the committed-footprint ratchet is the net. Git hygiene: re-add after post-add edits; verify HEAD bytes before config/doc pushes; PARK with dated markers; grep duplicate YAML keys. Dispatch hygiene: every subagent prompt opens with a concrete first Bash command + the no-skill guard; every workflow script opens with the args shim `const A = typeof args==='string' ? JSON.parse(args) : args`; builder StructuredOutput schemas REQUIRE `census_delta` (exact integers) + `corpus_untouched_proof`; scratchpad clones fetch the canonical remote + assert tip SHA before grounding; a twice-failed reader folds into a larger brief, never a third retry.

## 14. Waves, model routing, workflow protocol

### 14.1 File surface (proposed; germline surface = the ONE §7.5 amendment; everything else re-checked at S0)
`framework/evolution/{sandbox,archive,candidate,generator,arena,scorers,bench_factory,holdout_gen,league}.py` · `cabinet/scripts/{cog5-gate-arm,cog5-league,cog5-bench-factory,cog5-holdout-oracle,cog5-archive-restore}.py` · `cabinet/scripts/{verify-cognitive-phase5.sh,cognitive-phase5-review-scope.py,cognitive-phase5-rollback-rehearsal.py}` · `cabinet/scripts/tests/test_cog5_*.py` + `lib_cog5_*.py` · `cabinet/config/boundary-manifest.yml` rows · `cabinet/config/organs/*.yml` (periodic foundry organs) · `docs/proposals/germline-amendment-holdout-ring0-2026-07-24.md` · `docs/plans/cog5-league-arming-record-2026-07-24.yml` · `docs/plans/cognitive-core-phase-5-rollback-manifest-2026-07-24.yml` · egg-export manifest + `test_egg_export.py` lines (twins delete/expect-absent + the §7.5 interim holdout exclusion) · census allowance rows · operative ledger + plan-doc A13 rows · the R2/R4 amendment doc (§18). Shadow-log CLI lock-fold rider: `cabinet/scripts/cog4-dispatch-shadow.py` (small; CROSS-PHASE — a COG-4 artifact fixed on a COG-5 wave, §5.3 scope note) · `.gitignore` (ONE row: the §5.4 foundry data-plane ignore).

### 14.2 Waves (each unit: dirty-guard per file; clean worktree off current origin/master; exact-path `git add`; FW-019 review artifact for >300-line commits; per-JOB CI verification after every push)
- **W0 — S0 ground re-pin:** re-pin origin/master + fresh green per-job CI; re-verify every §-cited anchor; census scratch run distinguishing expected-allowance RED vs STOP; fleet-truth snapshot; refresh COG-1's stale park marker; verify the archive-root ignore STATE (the `.gitignore` row itself LANDS in W1 — §5.4, rev-1 SF-2); measure the §12.1 fresh baselines (armed COG-4 battery pass state, golden-eval pass state over the 25 bodies, verify-twin wall-clock p95, boundary full-sweep counts) + the E1-substrate baseline (case counts, corpus size).
- **WG — germline/amendment lane (filed AT LANDING, parallel):** the §7.5 proposal doc + CG row + named handback (batched offer with item 19 — window state as of 2026-07-24, re-checked here); the R2/R4 amendment doc skeleton (§18).
- **W1 — boundaries + interim freeze:** §10 rows + AST pins + per-row mutants; §7.5 interim content-pin + egg exclusion lines; the §5.4 `.gitignore` row (`shared/interfaces/foundry/`) + `git check-ignore` assertion; A13 parity green.
- **W2 — corpus:** the eleven-sim batteries + X1-X8 + arming battery + vocabulary tripwires + duplicate/field-map + league-closed + measurement tests, failing-for-the-right-reason; vacuity guards with retirement conditions.
- **W3 — Gate arming:** `sandbox.py` + `cog5-gate-arm.py` (harness injection + regression call-site + probe + the §4.5 laws — no_verdict + flat-candidate honest-negative arms); escape sim green; honest-PASS demo (improving candidate) + known-bad FAIL recorded as evidence packs.
- **W4 — archive + emitter:** §5 discipline; sealed segments; v2 emitter through trusted-context validation; shadow-log ingest (duplicate tolerance + field map) + the dispatch-shadow lock-fold rider (cross-phase COG-4 artifact — §5.3 scope note; the W4 FW-019 artifact names it); sims 9/10 green incl. the restore rehearsal.
- **W5 — factory + holdout:** `bench_factory.py` (E2 mining, X7 metadata, splits); `holdout_gen.py` (frozen; content-pinned; league-invisible) + oracle CLI + receipt path; C5 registration interface; sim 6 green; freeze-verification arms PARK if the window is unopened.
- **W6 — candidates/generator/scorers/league + THE RUNS:** §8.2/8.3 builds; **E1 run** (≥20, ranked archive, `fitness_claim: none`, X8 fingerprint); **E3 cycles** (≥1 skill + ≥1 memory-policy); **E2 measurement** of mined actuals vs the §6.2 minimums → the arming record; sims 1-5, 8, 11 green.
- **W7 — exit:** full battery armed green; arming record final (league CLOSED); census tighten (shrink-only); **frozen fresh-context panel (§15) — scheduled EARLY in a usage window** (LESSONS :1117 rule 3: COG-4's panel died to the Fable cap at t=0; front-load or checkpoint before it); rollback rehearsal green; landing + A13 ledger flip; handback status recorded honestly (listing landed OR pending).

### 14.3 Model routing (two-tier law; pins EXPLICIT on every agent, both tiers — never inherit)
Judgment tier — `model: "fable"` pinned: this contract, the plan-attack lenses (architecture + adversarial-correctness), the integrator, the frozen panel, corpus adjudications, the E-run result readings (deciding what a run showed is judgment). Execution tier — Opus 4.8 1M (`claude-opus-4-8[1m]`, single-quoted in shells) default: builders, test authors, mechanical verification, operations/governance lenses. **Fable-for-execution named candidates** (Captain 2026-07-23 calibration): the archive engine (append-chain correctness), `holdout_gen` + oracle (adversarial surface), the league driver + scorers vector (subtle correctness), the gate-arm wrapper. Record the model each agent actually ran on. Fable exhausted ⇒ STOP and tell Nate first line, never silent fallback.

### 14.4 Workflow protocol
Multi-phase builds ride the Workflow tool (journal-cache resume; per-phase adversarial review); every launch appends a RESUME-BOARD row same-turn; limit-death ⇒ ScheduleWakeup + resume handles carrying `Workflow({scriptPath, resumeFromRunId})`; §13's dispatch-hygiene rules verbatim; worktree landings only; pushes verified per-JOB via `gh run view --json jobs`.

## 15. Review protocol

Clean-room review pre-merge (AUD-1 pattern) per wave; the **frozen fresh-context panel is the phase's LAST gate**, reviewing the CLAIM SURFACE against **EVERY public entry point**: archive append/serve/verify + restore CLI; the emitter's validation path (can any record land without trusted-context semantic validation?); `bench_factory` mining entry (can a case land without X7 metadata?); the oracle receipt path (can anything beyond the aggregate leave?); generator (can a candidate target Ring-0 or a non-closed class?); arena/sandbox exec entry (can candidate code see a credential or write outside?); league ranking + eligibility (can `table_order` or a judge score reach a floor? can any output claim fitness while closed?); `cog5-gate-arm` decision path (can `no_verdict` become pass? can a stage go vacuous?); the boundary engine rows. Standing questions additionally: does any foundry artifact speak the Captain vocabulary? does any path write `verdict_human`? does anything mutate a live surface during a run (X8)? is the interim holdout posture ever described as Ring-0? Review-to-bytes bound by `cognitive-phase5-review-scope.py`. The panel rides the keyed panel-subagent path (headless clean-room has no credentials).

## 16. Rollback manifest (`cognitive-core-phase-5-rollback-manifest-2026-07-24.yml`)

- **Born-closed range (the C1 fix shape, phase-4 precedent :12-29):** footprint ratchet covers `S0_BASELINE_SHA..DONE_FLIP_SHA`; done_flip pinned at the ledger flip; the open-ended-HEAD ratchet is the named never-inherited anti-pattern (BACKLOG :1551-1555). REAL footprint only: the rehearsal asserts existence of every named path class (the phase-3 phantom-artifact anti-pattern named).
- **code inverse:** delete `framework/evolution/{sandbox,archive,candidate,generator,arena,scorers,bench_factory,holdout_gen,league}.py`, the cog5 CLIs, phase twins, `test_cog5_*`/`lib_cog5_*`, the boundary rows, the organs manifests added, the arming record; restore from S0: `boundary-manifest.yml`, `cabinet/scripts/cog4-dispatch-shadow.py` (the lock-fold rider — cross-phase, §5.3), `.gitignore` (the §5.4 row), egg manifest + `test_egg_export.py`, `cabinet/services.yml` if touched, and `cognitive-architecture-contract.yml` **with an `allowance_removal` block** (COG-5 rows removed same-commit — else observed<max headroom the mutant gate forbids) + `retain_append_only` (ledger/plan rows keep dated rollback notes — A13). If the §7.5 amendment LANDED, its inverse is the proposal doc's own one-revert **riding a Captain window** (a germline edit both ways).
- **runtime inverse — the ONE deliberate divergence from the cache grammar, stated:** shadow-run artifacts under any cache dir are deletable; **the ARCHIVE is not deleted** — rollback = seal + verified RESTORE-capable copy-out (the ledger :3381 clause, verbatim "its archive/pointers require independently rehearsed rollback", rehearsed as sim 10); deleting it would forge the observation-only history and foreclose R5. The manifest records the archive as `retained_sealed`.
- **must_remain_unchanged:** the Phase-0∪1∪2∪3∪4 protected union (phase-4 manifest §16 shape) PLUS `framework/learning/gate.py`, `framework/fidelity/{graduation,regression_gate,officer_runner,leakguard,scorer,judge_calibration,consequence}.py`, `framework/objectives/states.py`, `framework/policies/immutable-core.yml` (EXCEPT via the §7.5 Captain-windowed amendment, which carries its own rollback), `memory/golden-evals/`, the v1/v2 trajectory schemas, `holdout-evaluation-receipt.schema.json` — this phase READS them all, edits none. `contracts.py` is protected-except-additive-helpers (diff-scoped check).
- **Sibling force-pairing (LESSONS :1117 rule 5):** every paired surface — ledger row + §9 twin, arming record + league-closed test, manifest.remove + EXPECTED_SCOPE, allowance row + census actual, content-pin + holdout bytes — ships with the `sibling_landing_note` mechanical pairing so a one-sided edit fails closed.
- Rehearsal script green before landing (incl. one archive seal/restore round-trip + one compose-revert if any organ row landed).

## 17. Non-goals / phase boundary (restated as tripwires)

**No `promote.py`, no promotion entry point, no thin shim** (COG-6, foundry L202): a grep tripwire REDs on any `framework/evolution/promote*` path or any new caller composing ratify with an APPLY step; ratify evidence-packs are the phase's terminal output. **No league opening for live-fitness claims:** `league_open: false` + `fitness_claim: none` structural + the X8 fingerprint; opening is a post-phase amendment. **No live credentials or writes in arenas** (sim 7 + static rows). **Ring-0 permanently out of the search space:** generator refusal via `touches_ring0` + a seeded Ring-0-targeting candidate REDs. **No architecture search** (closed candidate-class set; unlock = COG-6 exit L206). **`CABINET_AUTHORITY_ENFORCING` untouched** (Captain-gated). **No gate-apply arming** (the Captain's `sudo launchctl load`, services.yml :79-84 — any unit touching the plist/row REDs). **No germline/schg action beyond the single §7.5 Captain-windowed amendment**; schg never worked around; freeze-verification units PARK on an unopened window. **No evidence-minting:** foundry code never writes consequence/evidence stores or `verdict_human` (§9.3 arms); trajectories observation-only (contracts enforced). **No new central enums, no second packaging format, no scalar at any promotion joint.** **No new permanent services** (§11.2). **External comms: none.** **Capacity law honored** (foundry L148): isolated lanes; the R1/R3 label cadence (shipped, live) is never pre-empted — it is the very channel the league bar waits on.

## 18. Rider + slate scheduling (orchestrator capacity calls, recorded — not Captain forks)

- **R1/R3 — SHIPPED** (COG-4 WR lane, merge `0d8a74d4`; `test_cog3_verdict_inbox.py` in-tree): their verdict_human throughput is the label cadence §6.2 consumes. COG-5 consumes, never disturbs.
- **R2 + R4 (BACKLOG :1560/:1562) — IN-PHASE amendment slots:** the foundry-era machine-evidence amendment doc is AUTHORED during the phase (skeleton at WG, completed after W4 when the archive/pre-registration substrate exists to cite): pre-registered experiment records (R2) as archive-native record class + epistemic canaries (R4) as permanently seeded known-false candidates/beliefs that must NEVER promote, alarmed if they do (the mutant discipline extended into runtime — a standing sim-4/sim-5-class arm). Proposal-only this phase; its widening content rides the §9.2 amendment-each law.
- **R5 (:1563)** — binds the §5.4 archive schema NOW (done in-contract).
- **C5 (:1572)** — interface INCLUDED / migration deferred (§11.3). **C6+C1 (:1573/:1568)** — jointly DEFERRED post-COG-5 (§11.4). **C8 (:1575 — anchor corrected rev-1)** — inventory recorded in allowance reasons (§11.5).
- **HORIZON (:1564)** — the red-cabinet falsification organ becomes schedulable once the arena exists; NOT scheduled here (a post-phase organ proposal riding §11.2 mechanics).

## 19. Register — MUST-RESOLVE + synthesis-concern dispositions

| id | finding (source) | disposition |
|---|---|---|
| **MR1** (attack serious #1) | holdout Ring-0 listing requires a Captain sudo window — `framework/policies` is DIRS-locked whole-dir (`germline-lock.sh:145`, dir-cover self-documented in immutable-core.yml); the synthesis's FILES-only grep was wrong | **RESOLVED §7.5:** amendment proposal doc + CG ledger row + NAMED handback filed at contract landing, batchable with item 19's window; `holdout_gen.py` builds non-germline; freeze-verification units PARK on an unopened window; interim posture (content-pin + invisibility rows + egg exclusion + sole-reader oracle) named HONESTLY as not-Ring-0; gate-S0 refusal absent until listed — stated; arming record carries `holdout_freeze: pending` and the league-open checklist requires the listing LANDED. Rev-1 (SF-3/SF-9/N-4): freeze semantics restated as the §7.4 STAGED truth — Stage A in-phase ≠ Ring-0, (iii)/(iv) true only from Stage B (listing lands at the window; pending: flags = the four-list propagation only); the item-19 batching offer dated as of 2026-07-24 |
| **MR2** (attack serious #2) | `promote.py` is Phase 6; foundry L192 lists exactly EIGHT pieces | **RESOLVED §2.4.1 + §17:** the contract scopes the eight; promote/apply anything is a grep-tripwired non-goal; E3's "promote via Gate" = ratify evidence-packs only ("APPLIES NOTHING", gate.py:507-513) |
| **MR3** (attack serious #3) | the charter title commissions E1/E2/E3 RUNS; machinery-only fails the run-shaped L198 exit; conversely a met-corpus reading is unfinishable | **RESOLVED §6.1 + §8:** E1/E2/E3 RUN on the sanctioned substrate (E1 ≥20 vs the existing real eval corpus; E2 mines + MEASURES actuals vs recorded minimums; E3 ≥1+1 cycles); league CLOSED for live-fitness claims with the derived+recorded bar + arming record; both failure edges pinned (X1-X8 run-shaped; `fitness_claim: none` structural) |
| **MR4** (attack serious #4) | no disposition table in the synthesis; major pieces exist in substance | **RESOLVED §3:** full reuse/extend/compose/retire/new table — arena=EXTEND (spec's own "exists; needs a league driver"), scorers=COMPOSE, bench_factory=EXTEND `extract_corrections`, archive/candidate/generator/league=NEW with L144 evidence, holdout=NEW-frozen, Gate=REUSE-via-seam at call-site (regression_gate.py:38-41 precedent — no germline touch on the arming path) |
| **MR5** (attack minor #5) | fitness-scalar tension real but the spec already subordinates the scalar | **RESOLVED §2.4.2 + §9.1:** supersession stated ONCE — §7 vector + independent floors governs; a scalar may order a league table; no scalar reaches any promotion/admission/graduation joint; missing evidence = `unknown` |
| **MR6** (attack minor #6) | R3 and C8 omitted from the meta map | **RESOLVED §18/§11.5:** R1/R3 verified SHIPPED (COG-4 WR `0d8a74d4`) — consumed, not re-scheduled; R2/R4 in-phase amendment slots; C5 include-interface/defer-migration; C6+C1 jointly deferred with dated marker; C8 inventory recorded in allowance reasons |
| **MR7** (attack minor #7) | the COG-5 gate_cmd is prose (todo-row convention) | **RESOLVED §12:** the runnable battery = the foundry:196 ELEVEN pinned verbatim, each with seeds/asserts/a biting mutant; ≥20 = sim #1's parameter, never the count; + the L198 EIGHT exit clauses as X1-X8 incl. "E1 produces no live mutation" |
| S-1 (synthesis concern 1) | sim-list drift (ledger 9 families vs foundry 11 vs twin "11") | **RESOLVED:** foundry:196 is canonical, pinned verbatim (§12); the drift is named in the header |
| S-2 | spec-vs-foundry fitness supersession must be explicit | **RESOLVED** = MR5 |
| S-3 | league bar unreachable in-phase — pin the DERIVE+RECORD reading | **RESOLVED** = MR3/§6; numeric minimums derived from JUDGE_HARD_BAR/MIN_PAIRS/F1-baseline/matrix bars and RECORDED (§6.2); league-open a post-phase event; capacity law honored |
| S-4 | archive↔shadow-log: P1 duplicate race + record_kind collision | **RESOLVED §5.3:** content-fingerprint duplicate tolerance; `shadow_record_kind` field map (never the trajectory enum); "read+check+append under one lock" carried verbatim as named COG-6 cutover text; the CLI lock-fold shipped in-phase as a present fix |
| S-5 | the composite certainty law exists nowhere as one record | **RESOLVED §9.2:** written once in the boundary section, assembled from CG-1 Option B (+ `_label_floor_met` False) + phase-3 FORK-2 + amendment-each, with refs — now citable in-repo and egg-carried |
| S-6 | empty-corpus admission semantics undefined | **RESOLVED §4.5:** `no_verdict` refuses foundry-candidate admission with the named reason; never converted to pass OR fail; fresh hatch mines first (E2 bootstraps); fail-safe intent preserved with mutants both directions |

**Genuine forks for the Captain:** none manufactured (`fork_for_captain: null`; everything above sits inside the standing grants). Captain-facing items are the §7.5 unlock handback (action at convenience, batchable with item 19) and full visibility of the arming record — neither gates planning or the build.

---

## VERDICT

rev 1 — **CONTRACT OF RECORD, CLEARED FOR MF-VERIFY.** Every ledger gate_cmd family, note-law, and the rollback clause maps to a mechanical gate (§12/§16); the eleven foundry:196 sims carry seeds + asserts + biting mutants and the eight L198 exit clauses are the X1-X8 battery; foundry L146's declared regression bound is discharged mechanically (§12.1); the four serious attack refutations land as §7.5 (germline ceremony + the §7.4 STAGED freeze truth), §2.4.1/§17 (promote.py = COG-6), §8 (the E1/E2/E3 execution mandate with the league closed for fitness), and §3 (the full disposition table incl. the SOV-8 variant-archive composition); the six synthesis concerns each carry a named resolution (§19) and every four-lens panel finding is applied with a disposition of record (appendix); the Gate is armed entirely at call-sites with gate.py/graduation.py byte-untouched; the archive is recorder-disciplined, R5-shaped, kernel-law-adopting by parity-tripwired replica (never an import), and expressly not a kernel cache; the league-open bar counts only provenance-real rows (§6.2); the certainty law is written once and tripwired; the census walks the zero-headroom allowance path with no new permanent services. Remaining before code: the independent MF-verify pass over this rev, then S0 ground re-pin (§14.2 W0). Panel dispositions of record: the appendix below.

**Provenance:** authored + self-ratified per the 2026-07-07 full-autonomy grant + the Captain 2026-07-20 cognitive-masterplan continuous grant; fleet-composition and shadow-run scope additionally per the 2026-07-21 ownership-on-GO. Premise-check corpus of record: `wf_fc493c16-a6a` journal (8 readers + synthesis READY-TO-PLAN + attack UPHOLD), ground tip `70bca2ae`.

---

## rev-1 2026-07-24 — panel dispositions of record

Four-lens plan-attack panel over rev 0 (architecture, adversarial-correctness, operations, governance); every finding orchestrator-adjudicated **ALL ACCEPTED** and applied in place by the REV-1 editor (Fable 5, judgment tier). Every byte-claim below was re-verified against the ground clone at tip `70bca2ae` before being written into the contract. No obligation was weakened — rev-1 only ADDED gates or removed overclaims; each row names where the change landed. (The dispositions-of-record pattern per the COG-4 contract appendix.)

| finding | disposition applied | landed |
|---|---|---|
| **MF-1** (adversarial + architecture, same finding) | foundry L146's "maximum acceptable regression relative to a freshly measured baseline" was cited as binding but never discharged → the declared-bound clause added in the COG-4 §10 shape: S0-measured fresh baselines (armed COG-4 battery pass state · golden-eval pass state · verify-twin wall-clock p95 · boundary full-sweep counts), per-metric tolerances declared BEFORE measurement (pass-state = ZERO regression tolerance; wall-clock = the floor-aware **max(p95×1.25, p95+5s)**), armed via `verify-cognitive-phase5.sh` with a real consumer same-commit (anti-phantom), seeded-regression mutant | §12.1 (new), §12 battery list, §14.2 W0, VERDICT |
| SF-1 (ops) | arena isolation re-grounded on the REAL substrate, cited to bytes: officer_runner blind-drive (:484/:65-73/:133) + leakguard (:1-12) + oauth_llm harness-holds-credentials (:1-9, "There is NO ANTHROPIC_API_KEY") + sim_runner SIE-7 quarantine (:1-24) + §4.2 worktree isolation (the spec:29 piece with NO existing code — gate.py:372-410 refuses today for that reason); `_default_runner` re-attributed as the unprivileged-EXECUTION law-source only; mechanical no-network/no-credential enforcement = env-scrub (allowlist env) + static AST/import pin, the sim-7 escape battery proving BOTH | §3 (arena row), §4.4, §8.4 |
| SF-2 (gov) | the archive data-plane exclusion was mis-specified: `shared/interfaces/foundry/archive/` is NOT gitignored today and neither is gate-evidence/ — the "gitignored runtime-surface class" premise REFUTED against bytes (.gitignore :173-174/:221-259; gate.py:84/:97-100) → the phase ADDS the `.gitignore` row `shared/interfaces/foundry/` (W1) + `git check-ignore` twin assertion; egg interaction stated: untracked runtime data "never reach[es] the archive in the first place" and R116 FAIL-CLOSES on any tracked stray under shared/interfaces/ (egg-export-manifest.txt:242-252) | §5.4, §14.1, §14.2 W0/W1, §16 |
| SF-3 (gov) + SF-9 (adv) | §7.4 claimed Ring-0 + Captain-unlock mutability while §7.5 staged the listing behind a pending window — internally inconsistent → freeze semantics restated as the STAGED truth: Stage A in-phase (content-pin + rows + egg exclusion; honestly NOT Ring-0), Stage B at the Captain window (listing lands → dir-cover schg + gate-S0 refusal; (iii)/(iv) TRUE only from Stage B); `pending:` flags scoped to the four enforcement-list propagations with their flip condition named; the §6.3 league-open checklist requires Stage B LANDED | §7.4, §7.5.3, §19 MR1 |
| SF-4 (arch) | §5.2's "archive manifests use kernel `atomic_write`" violated boundary ROW 6 (`framework.projection` allowlists only cortex/objectives/scheduler internals + cog2/3/4 CLIs — boundary-manifest.yml:316-339) → resolved per the COG-4 organs precedent, the REPLICA path PICKED and stated: archive-internal STDLIB REPLICA of the kernel atomic-write discipline (O_EXCL tmp + flush + fsync + os.replace, kernel.py:184-199) with a byte-parity tripwire (census :113 records the identical organs choice); ROW 6 stays byte-untouched (deliberate non-extension recorded); the §5.1 seam-vs-replicate fork closed for all kernel-law adoptions | §5.1, §5.2, §3 (kernel row), §10 |
| SF-5 (arch) | the disposition table omitted the SOV store spec:30 says the archive "extends" — found in-tree: the Gate's S4 content-hash-named variant archive (gate.py:29; write site :594-597, `variants/<sha16>.patch` under `evidence_dir()` = shared/interfaces/gate-evidence/ :84/:97-100) → explicit disposition **COMPOSE** (reference-by-content-hash, never extend-in-place — the sole writer is schg-locked gate.py); supersession stated as §2.4.8 | §3 (new row), §2.4.8 |
| SF-6 (arch) | X8's byte-identical fingerprint raced the live fleet (consequence ledger tail fingerprinted while 40 enabled services write) → X8 re-scoped as a THREE-PART gate: byte-identity over the quiescent live surfaces the run could plausibly touch (services.yml, champion configs, prompts/skills, gate-evidence/, checkout + worktree list); ADDITIVE-ONLY verification over the run's own outputs (pre-run archive chain heads remain a prefix); NON-INTERFERENCE over concurrently-written ledgers (ZERO foundry-attributed rows — the §6.2 provenance vocabulary + §17 no-evidence-minting as discriminators), byte-identity deliberately NOT claimed there | §1 X8 |
| SF-7 (arch) | the regression stage's second honest-negative arm pinned from bytes: PASS requires ≥1 frozen case improves (regression_gate.py:11); a benign non-improving candidate lands as `fail` "no frozen case improved (predicate requires >=1)" (:159-168/:212-218) — "An honest negative" (:24-26), never an error, never admission; mutants both directions (flat→pass REDs; flat→error/no_verdict REDs); the §4.2 honest-PASS demo corrected to seed an IMPROVING candidate | §4.2, §4.5, §12 battery list, §14.2 W3 |
| SF-8 (adv) | the league-open counting predicate was gameable (synthetic rows laundered as real) → mechanical discriminator added: CLOSED `provenance` enum `{real_live, real_mined, synthetic, sim_replay}` stamped by the INGESTER from the named source class (chain-of-custody; candidate/league/generator can never set it — the §5.2 WALL); ONLY provenance-real rows from named REAL sources count toward the §6.2 minimums and the §6.3 actuals; laundering mutant (synthetic-marked row counted → REDs; out-of-enum provenance refuses ingestion) | §6.2, §6.3, §12 battery list, §1 X8(iii) |
| N-1 (notes) | golden-eval body count corrected 27 → **25** (eval-001..eval-026, no eval-023; the directory's 27 entries include README.md + the framework/ fw-*.sh shells, neither a body — recounted from `ls` bytes) | §2.2, §3, §12.1 |
| N-2 (notes) | C8 BACKLOG anchor corrected :1574 → **:1575** (:1574 is C7 captain-law ledgers — recounted from bytes) | §11.5, §18 |
| N-3 (notes) | the header's ledger-rollback "quote" was a compression presented as verbatim → requoted to bytes: :3381 "future Foundry remains unable to mutate live champions and its archive/pointers require independently rehearsed rollback"; §16's paraphrase requoted likewise | header (Ledger row), §16 |
| N-4 (notes) | the item-19 batchability claim DATED "as of 2026-07-24" and made re-verifiable at WG (a dated offer, never a standing fact — the window may open/close before WG) | §7.5.3, §14.2 WG, §19 MR1 |
| N-5 (notes) | the cog4-dispatch-shadow lock-fold rider carries an explicit CROSS-PHASE scope note (a COG-4 artifact fixed on a COG-5 wave; the W4 FW-019 review artifact names it; §16 restores it from S0) | §5.3, §14.1, §14.2 W4, §16 |
| editorial (exactness, same pass) | `STATUS_MAX_AGE` → `STATUS_MAX_AGE_DAYS` (the real constant name, :95); the MIN_PAIRS comment-quote range :92-95 → :92-94 (em-dash restored to match bytes); the P5-cap cite :238-240 → :237-240 (the cap is named at :237) — no obligation changed | §6.2, §9.2, §9.3 |

**MUST-RESOLVE register status after rev-1:** MR1 restated at STAGED strength (§7.4 Stage A/B — resolution unchanged, overclaim removed); MR2-MR7 and S-1..S-6 unchanged-RESOLVED as at rev 0. All eight L198 exit clauses, all eleven sims, and every ledger note-law remain mechanically gated; rev-1 ADDED gates (the §12.1 declared bound + seeded-regression mutant, the §6.2 provenance/laundering arm, the §4.5 flat-candidate arm, the §10 ROW-6 non-extension) and weakened none.

**Orchestrator landing pass (2026-07-24):** independent MF-verify verdict **LAND** (all five MF/SF resolutions present + wired + byte-supported at 70bca2ae; 3/3 appendix spot-checks true; sample-claim table TRUE except one wrong line anchor). The three minor findings fixed in this pass: F-1 deliberate-absence cite re-anchored :265-268 -> :53-54/:342/:368-375 per the verifier's own byte-read; F-2 header ls-claim gains __init__.py; F-3 egg-manifest cite gains its path. Zero obligation bytes changed. Per the 2026-07-07 full-autonomy grant + the 2026-07-20 masterplan grant.
