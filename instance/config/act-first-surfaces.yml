# =============================================================================
# act-first surfaces — ACCESS INVERSION (Captain-ruled 2026-07-04)
# =============================================================================
#
# DEFAULT-ALLOW. The Captain: "you have autonomous access to ALL boards in
# Monday — we should not have an allowlist, rather a denylist... for now there
# is no denylist, keep it empty." Access is granted first and narrowed only by
# explicit exclusion — the access-axis mirror of earn-demotion. Safe because
# default-allow ≠ default-act: every act-first step still needs a registered
# inverse (48h undo), and the six hard ceilings (incl. external comms — the
# Captain owns ALL outbound) are unchanged on every board.
#
# Two sections gate a board, both Captain-owned, mechanically identical:
#   denylist:      — explicit Captain exclusions. EMPTY by the 2026-07-04
#                    ruling. Connect-time interviews of new critical-info tools
#                    ("what may this NOT touch?") append here.
#   cascade_gated: — NOT an access denial. Audit-proven boards where a write
#                    mechanically fires outbound mail/Teams to a human (TI-0
#                    cascade audit, docs/cascade-audit-2026-07-04.md) — acting
#                    there = sending, so the write stays propose-first under
#                    the UNCHANGED external-comms ceiling. The Captain lifts
#                    any entry by deleting it (one line).
#
# Corruption honesty: this file ABSENT ⇒ empty denylist (the ruled posture).
# This file PRESENT but unparseable ⇒ every board gated until repaired (an
# unreadable Captain exclusion list is never ignored).
#
# The previous default-deny allowlist draft (TI-0, ratification pending) is
# SUPERSEDED by the inversion ruling; its audit evidence lives on below as the
# cascade_gated whys. Boards the audit called "unverified" (People &
# Relationships 5096013693, Reflections & Research 5096013783, Products
# 5091839409, Activities 5091999365, …) are now ALLOWED — the Make.com
# blind-spot risk on private AI-Workspace boards is accepted by the ruling.
# =============================================================================

version: 1
status: ruled                     # Captain ruling 2026-07-04 (access inversion)
audit_ref: docs/cascade-audit-2026-07-04.md
ruled_at: "2026-07-04"
# cascade_gated enumeration: full-estate read-only webhook sweep 2026-07-04
# (lane L5-cascade-enum) — 535 active boards paged, webhooks(board_id:) queried
# for every one; 36 webhook-bearing boards found, all rowed below.
enumerated_at: "2026-07-04"
enumerated_boards_scanned: 535
enumerated_webhook_bearing: 36

# --- acting identity (Captain-ruled 2026-07-04) -------------------------------
# "Use my existing Monday API key with full privilege as the agent token —
# I want the cabinet to be able to act like me." MONDAY_API_TOKEN (fallback
# MONDAY_API_KEY) from ~/.screenpipe/pipes/_shared/.env is the acting
# credential — full-privilege user 48307552, org-wide. The earlier scoped-
# agent-user recommendation is superseded by this ruling. Names only — never
# values — in any file or log.
acting_identity:
  monday_user_id: "48307552"
  credential_env: MONDAY_API_TOKEN   # fallback: MONDAY_API_KEY
  scope: full-org (by Captain ruling)

# --- instance act-first knobs --------------------------------------------------
confidence_floor: 0.65            # proposer confidence below this ⇒ propose-only

caps:                             # honest, conservative starters — tighten never loosen
  estate:
    max_acts_per_run: 5           # across all boards+kinds in one lane run
    max_acts_per_day: 40
  per_kind:
    max_acts_per_day: 20          # per action_type (e.g. task_create)
  per_board_default:
    max_acts_per_run: 3
    max_acts_per_day: 15

# --- executor obligations (hard, deterministic; enforced in action_exec) ------
# Invariants on EVERY act-first write regardless of board — these, not board
# lists, are what keep default-allow safe. Code home: SEC-3 in action_exec.py.
executor_obligations:
  - id: no-at-mention
    rule: >
      NEVER emit Monday @-mention syntax (@name, @team, or a raw Monday user-id
      mention token) in any create_update / note body. A mention notifies AND
      EMAILS the mentioned user — a real outbound cascade the board gate does
      not capture. Strip @-tokens from all lane-written Monday bodies. [RT-A8]
  - id: provenance-banner
    rule: >
      Every lane-created artifact carries a loud human-legible "🤖 cabinet:"
      prefix + the cid footer, so a cascade that DOES fire is unmistakably
      machine-authored.
  - id: create-not-mention-people
    rule: >
      Reject any payload/set key matching people|person|assignee|subscriber|
      owner — assigning/subscribing a human on a Monday item emails them.

# =============================================================================
# DENYLIST — explicit Captain exclusions. EMPTY per the 2026-07-04 ruling.
# (Connect-time interviews of new critical-info tools append rows here:
#  {board_id: "...", kinds: [...optional...], why: "..."} — no kinds = whole board.)
# =============================================================================
denylist: []

# =============================================================================
# CASCADE-GATED — writing here mechanically SENDS to a human (audit-proven).
# Gated under the unchanged external-comms ceiling; Captain-liftable per entry.
# =============================================================================
cascade_gated:
  - board_id: "1549621337"
    name: "Bookings (STEP Network 3.0)"
    why: >
      Status-change webhooks (126701248, 166272135) drive Make scenario 1952299
      "Booking Changed", which EMAILS Jannie. Acting here = sending.
  - board_id: "1623368485"
    name: "Deals (AdSales)"
    why: 4 webhooks incl. button-column Make/automation launch points. Live CRM revenue board.
  - board_id: "1402911034"
    name: "Contacts (AdSales)"
    why: create_item + contact_email-change webhooks — CRM contact creation fans out (enrichment/email).
  - board_id: "1402911042"
    name: "Sales Activities (AdSales)"
    why: create_item + board-relation webhooks on the live AdSales CRM.
  - board_id: "5092199368"
    name: "PolAds Tickets (Support & Service)"
    why: >
      Ticket boards route complainant/requester EMAIL via native automation
      and/or Make (the PolAds complaint-conduit flow). Outbound near-certain.
  - board_id: "5091003553"
    name: "Prices (AdSales)"
    why: commercial AdSales surface (policy class from the audit's blocked set).
  - board_id: "5091706356"
    name: "Tasks (AI Workspace) — UPDATE PATH ONLY"
    kinds: [monday_task_update]
    why: >
      change_column_value webhook 157255553 (+ subitem 157257050) with an
      UNIDENTIFIED destination — an update triggers an unknown subscriber.
      Creates are clear (no create_item subscriber) and act freely; updates
      stay propose-first here until that webhook's destination is identified.
  # ---------------------------------------------------------------------------
  # ENUMERATED 2026-07-04 (lane L5-cascade-enum, closes checkpoint condition #4
  # / KILLED #3 enumeration half): full-estate READ-ONLY webhook sweep over ALL
  # 535 active boards (not just the audit's name-matched focus set) — 36 boards
  # carry API-visible webhooks; every one not already rowed above is gated
  # below, citing the observed webhook ids/events. Boards with ZERO API
  # webhooks stay default-allow EXCEPT the commercial policy class (the
  # webhooks API cannot see native Monday automations or Make-side triggers —
  # the audit's acknowledged blind spot). Over-inclusion is cheap here: this
  # gate is propose-first, not access denial, and the Captain lifts any row by
  # deleting one line.
  # ---------------------------------------------------------------------------

  # --- (a) link_to_teams team-task boards: a create/board-relation write fans
  # --- out to whole Microsoft Teams channels. Audit prose row, now concrete.
  - board_id: "1717613454"
    name: "Tasks - AdOps (STEP Network 3.0)"
    why: >
      create_item webhook 112133147 + link_to_teams_Mjj8r8Eo board-relation
      webhook 113416702 (+ change_name 110316095) — Teams-channel fan-out.
  - board_id: "1693359113"
    name: "Tasks - Marketing (STEP Network 3.0)"
    why: >
      create_item 112133452 + link_to_teams_Mjj8FZuw board-relation webhook
      113415995, plus budget/status/subitem webhooks (117443641, 117449795,
      117629133, 118240117, 118241052, 122964859 — two watch subitem board
      1743738324) — Teams fan-out + unknown Make destinations.
  - board_id: "1635510115"
    name: "Tasks - Video (STEP Network 3.0)"
    why: >
      create_item x3 (105424034, 105424042, 110123411) + link_to_teams_Mjj8R4OI
      board-relation webhook 113415722 (+ 105388897, 107323503) — Teams fan-out.
  - board_id: "1635251745"
    name: "Tasks - AdTech (STEP Network 3.0)"
    why: >
      create_item 110119945 + link_to_teams_Mjj8tJFz board-relation webhook
      112145892 (+ 108103385, 108105285) — Teams fan-out.
  - board_id: "1762038452"
    name: "Tasks - Yield & Growth (STEP Network 3.0)"
    why: >
      create_item 113417166 + link_to_teams_Mjj8r8Eo board-relation webhook
      113417159 (+ change_name 113417164) — Teams fan-out.
  - board_id: "5091535905"
    name: "Automations (STEP Network 3.0)"
    why: >
      NOT in the audit prose — found by the full sweep: create_item 155382436 +
      link_to_teams_Mjj8r8Eo board-relation webhook 155382437 (+ change_name
      155382435) — same Teams fan-out class as the team-task boards.

  # --- (a') Data & Intelligence task boards: audit named them in the
  # --- link_to_teams class; live sweep shows no link_to_teams TODAY but dense
  # --- write-triggered webhooks with UNIDENTIFIED destinations (class c).
  - board_id: "1711642084"
    name: "Tasks BI-Engineering (Data & Intelligence)"
    why: >
      board_relation webhook 129786886 + task_status webhook 139863375 + six
      item-lifecycle webhooks (153622707-153622712, incl. item_deleted /
      change_name) — destinations unidentified.
  - board_id: "1711637245"
    name: "Tasks Email-Automation (Data & Intelligence)"
    why: >
      column webhook 141544721 + six item-lifecycle webhooks
      (153623310-153623315) — destinations unidentified.
  - board_id: "2028600766"
    name: "Tasks Audience (Data & Intelligence)"
    why: >
      column webhook 137515376 + six item-lifecycle webhooks (153622266,
      153622267, 153622269, 153622272, 153622274, 153622276) — destinations
      unidentified.

  # --- (b) commercial CRM / Support ticket boards observed webhook-bearing ---
  - board_id: "1402911049"
    name: "Opportunities (AdSales)"
    why: >
      19 webhooks — the densest board in the estate: deal_stage status webhooks
      104602165 + 104712545, catch-all change_column_value 121335717, and 16
      per-column watchers. Live CRM pipeline; any column write triggers
      unidentified Make scenarios.
  - board_id: "1647372207"
    name: "Tickets (Support & Service)"
    why: >
      create_item 106719564 + 108848941, email-column webhook 108623278,
      people-column webhooks 106720039/151021911, status 108773141/123040886,
      button 164567833 — ticket flows route requester EMAIL.
  - board_id: "1734103411"
    name: "Zendesk Tickets v2 (Support & Service)"
    why: >
      status webhook 111465440 + email-column webhook 111465453 + people-column
      111465487 — Zendesk-bridged; writes fan to requester email.
  - board_id: "1868329933"
    name: "Tickets test (Support & Service)"
    why: >
      status 120366307 + email-column 120366327 + people-column 120366325 —
      same ticket-flow shape as the live Tickets board.

  # --- (b') commercial policy class: ZERO API-visible webhooks, gated anyway —
  # --- money/customer boards where the Make/native-automation blind spot is
  # --- least acceptable (webhooks API cannot enumerate those triggers).
  - board_id: "5098809499"
    name: "Invoice management (STEP Network 3.0)"
    why: >
      0 API webhooks observed (sweep 2026-07-04) — gated as commercial policy
      class: invoicing board, Make/native-automation blind spot.
  - board_id: "5098758685"
    name: "Win Tracker (CRM)"
    why: >
      0 API webhooks observed — gated as commercial policy class (CRM
      workspace), Make/native-automation blind spot.
  - board_id: "5098758504"
    name: "Opportunities (CRM)"
    why: >
      0 API webhooks observed — gated as commercial policy class (CRM
      workspace), Make/native-automation blind spot.

  # --- (c) onboarding / publisher-ops boards with write-triggered webhooks
  # --- whose destinations are unidentified (create_item / status / button) ---
  - board_id: "1562037034"
    name: "On/Off-Boarding Overview (SN - On/Off-boardings)"
    why: create_item webhook 110699212 — destination unidentified.
  - board_id: "1510894391"
    name: "Onboarding fra Typeform (SN - On/Off-boardings)"
    why: create_item webhook 108132462 — Typeform-bridged, destination unidentified.
  - board_id: "5094806643"
    name: "Publisher Onboarding (SN - On/Off-boardings)"
    why: status webhook 164880143 — destination unidentified.
  - board_id: "5089801442"
    name: "Publisher Onboarding v2 (SN - On/Off-boardings)"
    why: button-column webhook 164884051 — button columns are Make launch points.
  - board_id: "5096317725"
    name: "DK4 - D'kultur (SN - On/Off-boardings)"
    why: status webhook 168409056 — live publisher onboarding, destination unidentified.
  - board_id: "5091488428"
    name: "Presse-fotos.dk (SW) (SN - On/Off-boardings)"
    why: status webhook 155247610 — live publisher onboarding, destination unidentified.
  - board_id: "1615264261"
    name: "Publisher Onboarding (Web OnBOARDing template)"
    why: status webhook 143280671 — onboarding template, destination unidentified.
  - board_id: "1222800432"
    name: "Publisher total overview (STEP Network 3.0)"
    why: >
      9 webhooks: catch-all change_column_value 158695050, file/board-relation
      watchers (149184658, 151442530, 151442989), five button-column Make
      launch points (159522331, 160688738, 160689201, 167640590, 168599885) —
      central publisher-ops board, destinations unidentified.
  - board_id: "1631918659"
    name: "Objectives & Key Results (STEP Network 3.0)"
    why: >
      create_item 109336756 + create_subitem/subitem-lifecycle webhooks
      (107163270, 107190115, 107190550, 107195187, 107195513, 109320458,
      109379138, 122037559, 124738512) — destinations unidentified.
  - board_id: "1677240056"
    name: "Budgets (STEP Network 3.0)"
    why: numeric-column webhook 117445048 — finance board, destination unidentified.
  - board_id: "1658073379"
    name: "Expenses (STEP Network 3.0)"
    why: numeric-column webhook 117454604 — finance board, destination unidentified.
  - board_id: "1558597958"
    name: "Størrelser (STEP Network 3.0)"
    why: button-column webhook 100653114 — Make launch point, ad-ops config board.
  - board_id: "1558578956"
    name: "Placements (STEP Network 3.0)"
    why: button-column webhook 130155441 — Make launch point, ad-ops config board.
  - board_id: "1558569789"
    name: "Ad Units (STEP Network 3.0)"
    why: button-column webhook 125736923 — Make launch point, ad-ops config board.
  - board_id: "1810210072"
    name: "Adform deal update(New publishers) (SN - Ad Manager)"
    why: button-column webhook 118291563 — Adform-touching Make launch point.
  - board_id: "1775404963"
    name: "Publishers (AdUnit) (OMGz) (SN - Ad Manager)"
    why: button-column webhooks 114201376 + 114219097 — Make launch points.
  - board_id: "1781515860"
    name: "AI test - Alle kampagner (Media & Insights)"
    why: status webhook 116738709 — destination unidentified.
  - board_id: "1163227592"
    name: "Marketingopgave (overblik) (Projekt)"
    why: >
      people-column webhook 95293434 — a people-column write EMAILS the
      assigned human (same cascade the no-people executor obligation guards).
