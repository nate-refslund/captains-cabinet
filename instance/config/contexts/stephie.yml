# DECLARATION NOTE (not activation): committing this file makes the slug
# immediately valid in pre-tool-use.sh's context_slug cache — the cache is
# built from instance/config/contexts/*.yml filenames and does NOT filter on
# active: false. No warroom rows, no officer routing, no activity start from
# this file alone; those require explicit R2 activation by the Captain.
slug: stephie
name: STEPhie
capacity: work
description: |
  STEPhie — the deployment's tool-suite + AI assistant/creator product
  lane. Repo, hosting and tool-inventory identity are deployment-personal
  and live in the untracked instance overlay (public-tree scrub
  2026-07-12). Declared for the HQ (MacBook) instance; inactive until R2
  activation.
active: false
