slug: system-self
name: System Self-Maintenance
capacity: work
description: |
  Cabinet + screenpipe self-maintenance lane: the system improving the
  system. Examples: promoting the typed policy engine from shadow to
  enforcing (with CI parity proof), inbound-mail quarantine before LLM
  exposure (P4), pipe-health follow-ups. screenpipe edits follow the house
  convention (timestamped backups under ~/.screenpipe/state/, not git).
  Declared for the HQ (MacBook) instance; inactive until P2 activation.
active: false
