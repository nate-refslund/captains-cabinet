# =============================================================
# Project: PolAds
# =============================================================
# R2 prep draft for the HQ (MacBook) instance. status: pending —
# nothing activates until switch-project/activate-project is run by the
# Captain. PUBLIC-TREE SCRUB 2026-07-12: repo/tracker/database identity
# values below are neutralized placeholders — the real product identity is
# deployment-personal and lives in the untracked instance overlay.
# =============================================================

product:
  name: PolAds
  description: "Regulatory-transparency platform (the polads lane's product)"
  repo: https://github.com/example-org/polads
  repo_branch: main                # staging branch also active
  mount_path: /workspace/product   # mac-native local checkout path decided at activation

activation:
  status: pending
  mode: existing_repo_url
  activated_at: ""
  activation_mission_id: ""
  notes: "HQ instance R2 prep — the Captain activates; outcomes live in instance/config/outcomes.yml (gated to CABINET_ID=hq-macbook)"

# =============================================================
# Notion — not used for PolAds (knowledge lives in the brain vault
# 9-Codebases/PolAds/ + Monday docs). IDs stay empty intentionally.
# =============================================================
notion:
  cabinet_hq_id: ""

  dashboard:
    page_id: ""
    decision_queue_db: ""
    daily_briefings_db: ""
    weekly_reports_db: ""

  business_brain:
    page_id: ""
    vision_id: ""
    strategy_brief_id: ""
    brand_guidelines_id: ""
    messaging_pillars_id: ""
    growth_guardrails_id: ""
    pricing_id: ""

  research_hub:
    page_id: ""
    research_briefs_db: ""
    competitive_intel_db: ""
    market_trends_db: ""

  product_hub:
    page_id: ""
    product_roadmap_db: ""
    feature_specs_db: ""
    user_feedback_db: ""

  engineering_hub:
    page_id: ""
    architecture_decisions_db: ""
    tech_debt_db: ""

  cabinet_ops:
    page_id: ""
    decision_journal_db: ""
    improvement_proposals_db: ""

  reference:
    page_id: ""

  archive:
    page_id: ""

# =============================================================
# Linear — NOT used. PolAds tracking lives on Monday.com.
# =============================================================
linear:
  team_key: ""
  workspace_url: ""

# =============================================================
# Neon — lane production database (placeholder; real project name is
# deployment-personal)
# =============================================================
neon:
  project: example-polads-db

# =============================================================
# Vercel — lane hosting (placeholder; real app name is deployment-personal)
# =============================================================
vercel:
  project: example-polads-app

# =============================================================
# Tracker — product pulse lives on the deployment's SHARED task board
# (each task → epic → product). Routing is via the dev-tasks plugin in the
# repo, NOT a cabinet adapter (see Tasks note below).
# =============================================================

# =============================================================
# Tasks — DELIBERATELY ABSENT. No tasks: block / task-sync adapter for
# this project: the dev-tasks plugin (Monday.com) is the sanctioned task
# route per the clone-convergence plan (R2: "leave task-sync disabled").
# Do not add a Monday adapter here — that duplication is by-design avoided.
# =============================================================

# =============================================================
# Telegram — single_ceo (HQ default); bot via hq-instance.yml.draft
# =============================================================
telegram:
  bot_mode: single_ceo
  ceo_officer: cos
  ceo_bot: ""                      # TOKEN-TBD — see instance/config/hq-instance.yml.draft
  officers:
    cos: ""
    cto: ""
    cro: ""
    cpo: ""
    coo: ""
