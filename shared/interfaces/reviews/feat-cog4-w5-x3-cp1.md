# Review — feat/cog4-w5-x3 cp1 (COG-4 W5 x3: the N9 parity record)

**Batch:** five S0 pilot organ manifests
`cabinet/scripts/tests/fixtures/cog4/pilot/{charter-shadow,preference-pairs,
judge-calibration,world-census,prediction-calibration}.yml` + the tracked N9
artifact `cabinet/scripts/tests/fixtures/cog4/cog4-parity-record.json` (the
REAL `cog4-parity.py` output over the entire pilot set + all three W5-x2
fixture cabinets, zero divergent tuples) + the parity-set assembler
`cabinet/scripts/tests/lib_cog4_parity_set.py` + the pre-prove-out-of-band gate
`cabinet/scripts/tests/test_cog4_parity_record.py` + this artifact. Contract:
`docs/plans/cognitive-core-phase-4-contract-2026-07-23.md` §1 N9 + §5.3 (the
parity law / the ONE sanctioned dual-plane comparator) + §12 N9 battery + §9.3
(pilot set) + §13 (corpus immutable — ZERO edits under existing tests;
`git status` scope = adds only).

## Self-review findings (checked before commit)

1. **The record is the REAL CLI's two-independent-leg output** — generated by
   `cabinet/scripts/cog4-parity.py` (subprocess, hermetic) over a flat
   assembly of the 11 committed manifests; 33 operations, `record_errors == []`
   and `divergent_rows == []` under the W2 corpus reference checkers (imported
   from `test_cog4_parity.py`, never re-implemented — the gate law stays
   single-sourced). Leg b (ACTION_TYPES: matrix-derived `risk_class`,
   `ceiling_frozenset_map`-derived ceiling) never reads leg a (the descriptor
   path's manifest-declared members) — parity holds because the pilot
   manifests declare matrix-CONSISTENT members (the §4.3 N-d law), proven by
   zero divergence, not by derivation.

2. **Parity-clean pilot manifests by construction** — each pilot operation's
   declared `risk_class`/`ceiling` equals the matrix derivation for its
   declared `action_type` (`investigation_run → read_only_dispatch → []`;
   `local_edit → reversible → []`). `judge/status.rewrite` carries a
   per-operation OVERRIDE, so BOTH legs' organ-block+override merge path is
   exercised (leg a `_merged_members`, leg b `_leg_b_declared`) and still
   agrees. The full record spans six distinct enforcement tuples including
   three hard-ceiling `external_comms → always_gated (ceiling
   [external_comms])` cases (from the cabinets) — a heterogeneous N9 artifact,
   not a single repeated tuple.

3. **Determinism** — the record is byte-identical across three
   `PYTHONHASHSEED` values (0/1/2) and equal to the tracked bytes (the N1
   determinism discipline applied to the parity artifact); hermetic mode with
   an empty ledger yields `graduation.evaluate → None → "unmeasured"` on any
   machine, so the shadow verdict is reproducible. `test_cog4_parity_record.py`
   regenerates from the committed manifests and byte-compares, so a manifest
   edit without a record regen REDs.

4. **N9 coverage law** — the record's organ set equals exactly the five pilot
   organs + the six cabinet organs (11); its operation set equals the union of
   `domain_operations` the manifests declare (single-sourced via the SAME
   registry loader the CLI uses); no duplicate rows. `lib_cog4_parity_set.
   assert_source_shape()` pins the organ set so a manifest rename cannot
   silently shrink coverage.

5. **Corpus immutable (§13) — the two RED arms are DESIGNED and route to the
   integrator** — landing the record trips exactly two existing corpus arms,
   both by design/oversight, both requiring integrator surgery (a builder
   cannot edit the corpus):
   * `test_cog4_parity.py::TestParityGateRealArtifact::test_real_record_arm`
     (SKIP→FAIL) — the record-keyed vacuity arm's companion assertion fires on
     record existence, exactly its documented RETIREMENT CONDITION. Surgery:
     convert to load THE tracked record and assert clean+coverage.
   * `test_cog4_parity_cli.py::TestBoundaryLawsLive::
     test_no_record_ever_written_inside_the_repo_by_this_battery` (PASS→FAIL) —
     a W2 self-discipline whole-repo rglob that forbade ANY record; it did not
     foresee the legitimate W5/W6 record and now catches it "for the wrong
     reason" (its own docstring's phrase). Surgery: exclude the ONE sanctioned
     record at the fixtures path (preserving the stray-write discipline).
   Both are pre-proved GREEN out-of-band by `test_cog4_parity_record.py`; both
   are surfaced in the unit's `contradictions[]` with drop-in surgery text.

6. **Sweep reconciliation (zero unexplained)** — base (HEAD 4e8e3cec) cog4
   surface = 11 failed / 633 passed / 8 skipped; branch = 13 failed / 638
   passed / 7 skipped. Delta accounts exactly: +6 new PASS (this unit's gate),
   `test_real_record_arm` SKIP→FAIL, `test_no_record...battery` PASS→FAIL. The
   11 inherited REDs (10 `test_cog4_sim_dispatch` RealDispatchCliArms + 1
   `test_cog4_dispatch_ast_pin` armed-and-absent) are the x1/x2 dispatch-CLI
   landing tripwires — identical base and branch, integrator-owned, NOT this
   unit. FULL `cabinet/scripts/tests` sweep: 3268 passed / 13 failed / 17
   skipped — all 13 failures are the cog4 set above; ZERO non-cog4 breakage.

7. **Census / boundary / layer** — all additions are cabinet-side (budget-
   exempt): census PASS with ZERO framework delta (236 modules / 66409 lines
   unchanged; `central_action_types` 30 — zero new enums); layer-sep new=0;
   `cog2-import-gate` exit 0 (the `lib_cog4_*`/`test_cog4_*` globs allowlist
   this unit's framework.organs reads); the import-gate completeness invariant
   passes with the two new first-party `.py` files.

8. **Placement + egg/gitleaks** — the pilot dir and the record are SIBLINGS of
   `fixtures/cog4/cabinets/` so the §12 N8 token sweep (scoped to `cabinets/`,
   asserts exactly six files) never sees them; the pilot rows are REAL software
   services, so software vocabulary is honest for them. The record ships as
   enduring test surface (no egg-export manifest entry needed — the x2
   fixture-add precedent); it carries no secrets or digests (only risk-class /
   ceiling / undo / verdict strings + operation/organ names), so no
   `.gitleaks.toml` allowlist entry is required.

Model: authored on Opus 4.8 1M (execution tier — N9 parity record, mechanical
verification against bytes). Per the 2026-07-07 full-autonomy grant + the
2026-07-20 cognitive-masterplan continuous grant.
