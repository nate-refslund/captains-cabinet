# Checkpoint review — feat/cog3-wave4, cp3 (the phase-complete wave)

**Scope:** the wave-4 branch off master `61e1f87a` — W4A (adapters package +
fold evidence-binding, 5 commits incl. fix), W4B (phase apparatus: rollback
manifest/rehearsal/closure, verify + review-scope twins, OVI parity falsifier,
measurement gate, egg-manifest landing; 5 commits incl. fix), W4C (exit-gate
fixture battery, 1 commit + integrator nit), integration commits (footprint
reconciliation, adapters egg-row activation), the frozen-review F1 fix, and
the frozen §12.3 review itself. Several commits exceed FW-019's threshold →
this artifact (pairs with cp1/cp2 of wave 3).

## The wave in one paragraph

W4A closed the declared P6 deferral: adapters emit adapter-pinned
`join_spec`/`admissible_subjects`/`expected_effect`/evidence subjects, the
fold binds them through the one `as_of` read path, and the KEYSTONE test
proves a `verdict_human` consequence promotes its edge to
`intervention_supported` through the REAL pipeline (judge-confirm caps;
assumptions-less lands hypothesized per R-A). W4B built the phase-complete
apparatus mirroring the phase-2 twins (incl. the contract-named pre-bump
refold assert the reviewer refused to let drop, and the enduring-gate
composition). W4C delivered the three heterogeneous end-to-end fixture
cabinets (software / genuinely non-software operations / missing-product-
adapter with declared absence) + the §9 r14 token sweep — the first unit of
the phase to pass adversarial review with zero must-fixes.

## Review rounds (all adjudicated, all closed)

- W4A FIX_FIRST(1): the CLI merge boundary bypassed the assembly collision
  law — proven repro (same node_id, conflicting content, exit 0) now exits
  non-zero with no artifact written; roots + adapters route through ONE
  `assemble()` law.
- W4B FIX_FIRST(2): the contract-named pre-bump refold assert was absent
  (now discharged by running the RESTORED baseline determinism suite on the
  inverse tree — 42 green in the rehearsal) and the verify twin had dropped
  the enduring-architecture-gate composition (now composed, matching the
  phase-2 twin exactly).
- W4C SHIP (nits only; the r14 sweep extended to the schema surface at
  integration).
- FROZEN §12.3 REVIEW (fresh-context Fable panel, clean-room clone):
  FIX-FIRST on F1 — `serve_objective` + `recommend` bypassed all three serve
  REFUSE limbs (direct `graph.jsonl` reads; probe-proven answering on a
  counterfactual branch and a tampered cache). Fixed: one `_load_bound`
  loader binds the WHOLE serve surface; a 12-cell serve-surface-uniformity
  class (3 functions × 3 limbs + positive controls) proven discriminating
  (6 fail on the unfixed bytes). A fresh re-verify pass confirmed 9/9
  refusals + controls and flipped the artifact to **Verdict: PASS**. F2
  (fold-auth limbs fixture-reachable only) recorded as a medium,
  non-blocking finding in the artifact.

## Verification on the landing tree (`python3.12`, PG17)

- Full battery **361 passed / 2 declared measure-only skips** (green under
  `COG3_ENFORCE_P95=1` in the verify gate); cog2 280 green; import-gate
  rc=0; layer-sep green; census PASS at exact zero headroom (modules
  226==226 / +12 running total; lines 65012==65012 / +1526 running total).
- `verify-cognitive-phase3.sh` FULL green with the review binding ACTIVE:
  frozen digest `6c800e12ee83…` verified against the committed tree.
- Rollback rehearsal PASS end-to-end (incl. the pre-bump refold assert and
  the allowance-removal inverse).
- P8 handback row written on the HANDBACKS surface (root files exist
  unlocked; Captain schg-lock required before any future pointer flip).

Provenance: per the 2026-07-07 full-autonomy grant + the Captain 2026-07-20
cognitive-masterplan grant; contract + appendix adjudications; the
2026-07-23 Fable-for-execution calibration recorded in doctrine.
