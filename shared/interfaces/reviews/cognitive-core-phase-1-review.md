# Cognitive Core Phase 1 (COG-1) — implementation review checkpoint

Verdict: PENDING

This placeholder is deliberately non-passing (Phase-0 placeholder pattern). It
exists so the phase-1 rollback manifest is a closed, exact inventory before the
candidate bytes freeze. It is replaced only after the fresh-context Fable
whole-diff review (plan §12.3) inspects the frozen candidate against the COG-1
contract + masterplan and every blocker is resolved; the PASS body must carry
the Reviewed-Scope-Digest that verify-cognitive-phase1.sh recomputes and
matches.
