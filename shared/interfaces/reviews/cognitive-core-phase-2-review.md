# Cognitive Core Phase 2 (COG-2) — frozen post-implementation review (contract §12.3)

Verdict: PASS

Reviewed-Scope-Digest: a6a101e49ed8318655eb30faf4b995a53131936697c6835f858fc88a0337542f
Reviewed-Commit: <PLACEHOLDER — the unit-5 landing commit; orchestrator records post-commit (advisory, phase-1 precedent)>

Reviewed 2026-07-22 against docs/plans/cognitive-core-phase-2-contract-2026-07-22.md rev 2 (THE standard — the post-attack revision whose A-*/C-*/O-*/G-* dispositions §14 pins) from rollback baseline b393f41b579c5ff4723f4502b006df58e9664cf1. Review construction: units 1–4 landed tests-first behind per-unit adversarial review checkpoints (the FW-019 artifacts for units 1–3 are themselves scope members, so this digest binds them); the unit-5 integration landing then took a fresh-context adversarial panel over the assembled phase surface, whose findings — including one real shadow-boundary escape — are the §4 register, ALL fixed tests-first inside this same landing. Every code verdict below was verified against the candidate's bytes (file:line cited); every measured number is quoted verbatim from the recorded transcripts of the verified M6 measurement run. No implementer self-report was consumed unverified.

## 1. Binding and exclusion boundary (this artifact + the two operative ledgers)

The digest above is SHA-256 over sorted `<mode> <sha> <path>` git ls-tree lines of the 41-path COG-2 scope = (rollback-manifest `remove` MINUS this artifact) UNION `restore_from_baseline`, computed over the committed tree at HEAD (cabinet/scripts/cognitive-phase2-review-scope.py — EXPECTED_SCOPE pin :32-77, resolve_scope() :98-114, ls-tree digest :117-139). Excluded by construction: (a) THIS artifact — REVIEW_ARTIFACT is subtracted from the scope (:104) and guard-refused if present (:109-110), so recording the digest is non-self-referential and the flip of this file to the verdict line above cannot break the binding (the manifest's `remove` block still lists it at :96 as the one remove path allowed absent pre-landing, so a rollback deletes it — subtraction from the DIGEST, not from the rollback); (b) the two append-only operative ledgers, docs/plans/operative-egg-ledger-2026-07-07.yml and docs/plans/operative-egg-plan-2026-07-07.md — not scope members, so the later COG-2 status flip (in-flight to done, status-only) lands without touching the digest. Everything else that can change COG-2 behavior — the rollback manifest, the scope tool itself, the phase verify twin, and every EXTENDED file (framework/fidelity/consequence.py, cabinet/scripts/egg-export-manifest.txt, tests/test_egg_export.py, task-sync-drift-falsifier.py + its test, cabinet-doctor.sh, evidence-coverage.py, the census contract yml) — is bound: any post-review edit to a scope file changes the digest and verify-cognitive-phase2.sh BLOCKs (:41 runs `--verify` against this file) until a re-review re-freezes it. Manifest↔tool scope drift is itself a BLOCK (resolve_scope :111-113), and the binding's teeth are machine-tested (test_cognitive_phase2_rollback.py:260 scope-tool parity; :300 deterministic-digest + tamper-detection teeth; :211/:238 footprint-coverage ratchets against the ACTUAL committed footprint).

## 2. Named interpretation — shadow-only, audited (confirmed consistent)

The foundry stop condition ("changes live behavior before its shadow gate") is trivially satisfied this phase and the contract's Runtime posture is read STRICTLY: Cortex is a disposable read-side projection with zero live seams. Confirmed against bytes, not doctrine:

- Writes NO authoritative store: the only write paths in framework/cortex are engine._atomic_write/write_projection (:434-456) under an explicit cache_dir (production target cabinet/cache/cortex/, gitignored), plus two flocked append-only runtime ledgers under cabinet/logs/* (the parity verdict JSONL, cog2-parity-falsifier.py:329-336; quarantine receipts, cog2-verifier.py:193-212). adapters.read_outbox_rows connects read-only REPEATABLE READ and rolls back (:63-69); the §7.2 `cortex_ro` role cannot write by construction (below).
- Emits NO events: no cortex module imports the emitter or relay dispatch — the reverse allowlist (§7.1) admits only the relay's pure `build_dispatch_fields` (adapters.py:37), and the import gate's Check-3 sweep would flag any new importer; grep of framework/cortex for emit/XADD surfaces zero.
- Adds NO services.yml row: the nightly parity cycle rides the EXISTING task-sync-drift-falsifier fleet row as an external subprocess (run_cog2_parity, task-sync-drift-falsifier.py:972 — the COG-1 falsifier-composition precedent). services.yml is byte-identical to master: it is a `must_remain_unchanged` manifest row (:141) whose byte-identity the rollback suite asserts over the Phase-0∪1 protected union (test_cognitive_phase2_rollback.py:176-193) and the rehearsal re-checks per-path.
- Edits NO germline path: scope ∩ germline-lock list = empty; consequence.py (the one extended framework file) is non-germline, re-verified at S0; the recorder coupling is IMPORT-only of a frozen module's pure canonicalization (belief.py:31-32), byte-parity-tripwired (test_cog2_rebuild_determinism.py:119-137), never an edit.
- Is imported by NO authority/action code: mechanically gated by the NET-NEW AST import gate + grep/data-plane backstop (cabinet/scripts/cog2-import-gate.py — forbidden surface :97-104, allowlist :108-117, three checks :287-322), rc=0 over the real tree, with the boundary proven end-to-end by injected-mutant tests (27 tests, incl. the hardened spellings of §4 F1). The shadow-parity falsifier is additionally banned from importing ANY cortex module (C-F17, Check 2 :303-310) so parity ground truth can never route through the projection's own fold.

Read pointer is `none` for the whole phase (§9 risk 8): ~/.cabinet/state/cog2-read-pointer never leaves `none`, and the manifest's read_pointer inverse (:19-30) documents that no forward cutover exists to invert. Projection deletion is safe by construction at a frozen source state (§7.3): `rm -rf cabinet/cache/cortex` is a manifest one-command inverse (:31-39) and the purge→delete→rebuild sim plus the M1 evidence below prove a rebuild-from-zero reproduces the exact belief set and hash. The contract's honesty clauses are carried, not laundered: both production streams are axis-degenerate this phase (observation_time := occurred_at, adapters.py:130-131 — declared, per A-B1), the two-axis and contradiction proofs ride the production envelope-file adapter which has NO production feed (§2/§5.2a, declared), and the first contradiction-capable source pairing stays a named next-phase obligation (C-F10).

## 3. Review dimensions

### Belief plane
- Bitemporal record: Belief carries both axes as first-class fields (belief.py:100-101) with `source_time` = valid time and `observation_time` = transaction time, pinned per adapter — outbox both := occurred_at (adapters.py:130-131, degenerate, declared); envelope-file source := occurred_at / observation := recorded_at (the real second axis, adapters.py:252-253); consequence both := ts parse-or-absent (adapters.py:402-403), where `_parse_ts_or_absent` (:349-364) canonicalizes to the single UTC-second spelling or yields honest absence — NEVER passes garbage through (C-F6; garbage fixtures pinned in the consequence-seam suite).
- Deterministic identity: `compute_belief_id` digests exactly `(kind, subject_key, dimension, provenance.event_id, adapter_ordinal)` in the recorder dialect (belief.py:75-86). `claim_digest` is REMOVED from identity — a stored field only (fold nulls both claim and claim_digest on purge, engine.py:280-281) — so identity survives a source-content purge (A-B3/C-F12; test_belief_id_independent_of_claim, test_cog2_rebuild_determinism.py:145). The build-time-ULID mutant seam exists solely to fail the gate (engine._mint_identity :116-123).
- Dimension discipline: adapter-pinned mechanically, never heuristic — tasks adapter emits the entity/status + observation/occurrence pair with adapter_ordinal 0/1 (adapters.py:139-143); envelope-file carries the payload_schema-declared `<domain>/<name>` (:249); consequence pins `consequence` (:345). Supersession and contradiction key on (subject_key, dimension) only (engine.resolve_relationships :181-183).
- Supersession/contradiction/status: resolve_relationships (engine.py:167-207) partitions each (subject_key, dimension) group into producer lineages (§5.5 — `_lineage_key` :138-151 with the merged/correlation mutant seams), chains each lineage in the SOURCE's own total order, and cross-links multiple current heads symmetrically SORTED (:204-206). Status is one derived function with the pinned priority source_purged > superseded > contradicted > asserted (derive_status :154-164). Confidence is fixed-point ppm derived statically from the versioned trust table, always paired with source_trust, absent-producer ⇒ absent (unknown) never 0, and a malformed ppm on a PRESENT producer fails LOUD rather than laundering a broken table into honest ignorance (derive_confidence :89-109). The G-F4 producer-identity invariant is schema-borne and suite-pinned (test_officer_producer_key_is_structurally_invalid, test_cog2_rebuild_determinism.py:339).
- Dedupe is content-checked, not first-arrival-wins: two protos sharing the identity tuple must be byte-identical — exact physical duplicates collapse (dup-invariance), a divergent-content collision raises (engine.fold :234-254; order-independence tested).

### Derivation plane
- Pure env-free fold: fold() reads no clock, no randomness, no environment (engine.py:210-295); the consequence side's sim-row policy is UNCONDITIONAL inside `iter_ledger_rows` itself — the iterator never reads CABINET_SIM_MODE (consequence.py:612ff docstring + body), deliberately diverging from read_ledger's env-sensitive drop so no env var is ever a fold input (A-M6/O-B2).
- Canonical hash = re-derived rows, never file bytes (A-m11): chained_hash re-canonicalizes each belief in belief_id order under a domain-separation seed (belief.py:145-156, seed :58); hash_canonical_rows is the JSONL-reader twin that re-sorts derived collections before hashing (:159-175); every derived collection entering canonical bytes is SORTED (to_canonical_dict :130-132 — the C-F3 defense that makes bytes invariant under PYTHONHASHSEED). The writer emits exactly the canonical bytes + `\n` (engine.write_projection :452), so writer bytes and re-derived bytes agree — proven by test_reread_hash_matches_manifest and broken-detectably by test_byte_flip_is_detected (test_cog2_rebuild_determinism.py:354/:362).
- Hash-epoch law (A-m13): the manifest records the full epoch tuple (engine_version, trust_table_version, rank_table, source_set, frontier) beside the hash, frontier lag, blockers, and the seen-set intervals (build_manifest :399-431); RANK_TABLE is schema-frozen per engine version (:45-49).
- Frontier law (A-B2/C-F1): F = min(id WHERE event_id IS NULL) − 1 if any NULL exists in-snapshot else max(id); eligible = event_id NOT NULL AND id ≤ F (frontier_of :56-73, eligible_rows :76-82) — the frontier NEVER advances past a NULL, and the `past_null` mutant seam exists only to prove the negative control bites (test_mutant_frontier_past_null_folds_stranded_rows, test_cog2_rebuild_determinism.py:304). The read is one REPEATABLE READ read-only snapshot with the SELECT a fixed literal carrying ORDER BY id (adapters.py:45-50, string-pinned by test_select_sql_pins_order_by_id :379), and idempotency_key is fetched so it flows into provenance as the capture-stable secondary key (:386/:392 tests).
- Supersession authority is source order, never K's observation_time (A-B4): `_supersession_key` default is `(stream_rank, intra_stream_seq)` (engine.py:126-135); the long-transaction seed (late id, early occurred_at) pins that the current head follows id (TestSupersessionSourceOrder, test_cog2_rebuild_determinism.py:240-279), and the observation_time-keyed mutant picks the wrong head detectably.

### Query/fence plane
- As-of fencing: inclusive-≤ on both axes with the leak predicate `ts > cutoff` (query._fence :159-174, C-F8); an ABSENT observation_time is EXCLUDED from an observation-fenced answer and included only unfenced (:167-169, A-M5). A non-canonical cutoff (offset spelling, fractional seconds, garbage) would fence OPEN against canonical stored timestamps, so it is a HARD ERROR, never a silent mis-fence (_CANON_TS_RE :66, as_of input gate :209-217).
- Derived state is re-derived per cutoff (C-F7): as_of re-runs the SAME engine.resolve_relationships over the fenced sub-history (:257-262) — status/superseded_by/conflict links are never read back from stored full-history state (rederive=False is the stored-final-status mutant, :186-188), and closure holds by construction: links only ever resolve within the fenced set, with evidence_ids the fenced sub-history (:263). The asof-fence suite (25 tests) carries the same-second boundary, out-of-order-arrival, and rowid-fence mutants.
- Serve-time hash binding (C-F15): `_verified_rows` reads beliefs.jsonl EXACTLY ONCE, re-derives the chained hash from the re-parsed rows, binds it to the manifest's belief_store_hash, and returns the verified rows themselves — the serve path builds Beliefs from THOSE rows, never a second filesystem read, closing the TOCTOU window in which a rebuild's os.replace could slip unhashed bytes past the check (:300-335); ANY mismatch or unreadable/malformed store/manifest raises StoreCorruptError = REFUSE to serve (:78-84), recovery = rebuild-from-zero. verify_store and load_beliefs_verified share the one read (:338-362); the unverified loader is documented as tooling-only (:291-297).
- Scope fences HARD-ERROR, never empty-success (§7.4/C-F11): missing/unresolvable scope raises ScopeError before any read (:219-225); a subject that exists ONLY foreign raises (cross-cabinet fail-closed, :238-245); the unknown/purged/denied triad is explicit — zero evidence ⇒ explicit `unknown` (:247-249), purged-only ⇒ a status-bearing view never unknown, denial ⇒ ScopeError (scope_mode="lenient" and unknown_mode="default" are the pinned denial-as-ignorance / fabricated-answer mutants). A serve-side belt additionally refuses cross_cabinet-tainted beliefs even under a matching local cabinet_id — the invariant enforced at BOTH boundaries (:229-237). Result-level status is contradiction-driven, not head-count-driven, with the multi-facet aggregation rule pinned (:269-286). Never-a-score shape guard: the API returns only full BeliefView tuples (value?, source_trust, provenance, status, conflict_set — :87-108); no bare-scalar confidence accessor exists, and the standing ratchet in the contradiction suite fails the build if one ever appears (G-F3).

### Boundary/security plane
- The AST import gate (net-new, O-B6): `_cortex_imports` walks real import nodes only — `import framework.cortex`, `from framework.cortex import x`, the ordinary `from framework import cortex` via alias-name resolution, AND relative imports resolved against the importing file's package path (`_import_from_targets`, cog2-import-gate.py:165-193) — so `from ..cortex import engine` and `from .. import cortex` inside framework/authority resolve to the real framework.cortex and bite (hardened in THIS landing, §4 F1). The deliberate non-over-fence is verified: `from . import cortex` inside framework/authority resolves to framework.authority.cortex — a DIFFERENT module — and is correctly not flagged (docstring :173-175; test_single_dot_relative_to_sibling_module_not_overfenced, test_cog2_import_gate.py:157). Dynamic reach is covered by the narrow importlib/__import__ patterns including the two-arg relative form (:154-156) swept across SWEEP_TREES = framework, cabinet/scripts, cabinet/admin-bot, cabinet/mcp-server (:121 — the runtime surfaces added this landing); the grep backstop over forbidden trees additionally flags the `from framework import cortex` spelling and the literal cabinet/cache/cortex data-plane path on live lines (:134-137, C-F20). The stated residual (a fully covert string-assembled import) is accepted per §7.1 and re-opened at read-pointer flip. 27 tests prove each rule bites on injected mutants and folds clean on the real allowlist.
- §7.2 `cortex_ro` read-only-by-construction (G-F6): provision_ro runs idempotent SQL in one atomic transaction — existence-guarded CREATE ROLE cortex_ro LOGIN NOINHERIT (never a bare CREATE), a converge-ALTER re-asserting NOCREATEDB/NOCREATEROLE, REVOKE ALL on all tables+sequences, GRANT USAGE on the schema, then SELECT on EXACTLY officer_tasks_outbox + officer_tasks and nothing else (cog2-rebuild.py:61-123, tables pinned :58) — so a re-run converges to the same minimal catalog. TestCortexRoReadOnly (test_cog2_fencing.py:416-510, real ephemeral PG17) proves SELECT succeeds on the two tables, UPDATE and INSERT are refused, the grant catalog matches the enumerated list EXACTLY (:467), no privilege exists on any other table (:488), the role is a plain login (:498), and provisioning is idempotent (:510).
- §7.4 cross-cabinet fail-closed: the ingest fence owns three refusals checked most-precise-first — classification=cross_cabinet (a VALID taint tier that passes validate_any, refused regardless of cabinet_id — the pinned leak), absent cabinet_id (absent is never local), foreign cabinet_id (adapters._envelope_scope_reason :198-221); plus validate_any-failing lines, malformed JSON, payload_ref (no inline subject — the fold never derefs), and subjectless payloads — EVERY refusal writes a quarantine receipt, silent skip is the pinned mutant (build_envelope_protos :259-304; receipts persisted flocked+scrubbed by cog2-verifier.write_quarantine_receipts :193-212; TestIngestQuarantine, test_cog2_fencing.py:103-165).

### Parity/shadow plane
- Authoritative-side sampling frame (C-F16): the universe is drawn from the authoritative side — distinct task_ids via one constant read-only json_agg SELECT over the outbox (cog2-parity-falsifier.py:476-479, conn in psql argv only) union consequence identity chains via the DOMAIN's own read_ledger (:482-512) — never from the belief store, so a store-deleted subject is sampled and found unanswerable = breach; the "store" frame is the documented mutant that structurally cannot fail that seed (parity_scan :184-188). Ground truth is domain-computed (:466-479), the projection is queried as an external black box (COG2_PARITY_CORTEX_CMD, :534-556 — the C-F17 import ban makes f(x)==f(x) impossible in-process), and the falsifier's module top is stdlib-only so --probe runs anywhere (:74-85; cabinet-doctor row :1156-1160).
- Sticky re-sampling + escalation (C-F18): prior open breaches are force-included every cycle until clean (:191-196); the sticky carry is read from the last REAL verdict line, skipping error/unconfigured skeletons so a reader outage cannot wipe it (:339-351); the second-consecutive-breach-DATE ladder files ONE captain card through the attention gateway with fixed-shape text, and an outage date neither escalates nor resets (:354-380, :383-410).
- Per-day floor: each complete UTC day must carry its effective floor of samples, absent days breach (a dead falsifier cannot pass vacuously), the floor is capped by the day's recorded frame (a small honest universe is not a shortfall), and run() judges the MOST-RECENT complete day so one old dark day cannot permanently poison later clean days (per_day_floor :241-306; latest_breach judgment :641-645). The F7 exemption — a day whose last line is `unconfigured` (an honest no-op) is NOT a floor breach — lands in this review's fix set (§4 F4); absent and `error` days still breach (:290-301).
- Honest no-op until configured: no seam + no outbox conn + no cortex reader ⇒ `unconfigured`, exit 0 (:624-630); a CONFIGURED reader that breaks (psql/cortex-cmd/consequence import) is a LOUD `error`, exit 1 — credential rot never parks the watchdog green (:129-134, :487-504). Nightly wiring: run_cog2_parity invokes the sibling falsifier as an external subprocess from the existing drift-falsifier row (task-sync-drift-falsifier.py:972; wiring proven by test_cog2_parity_wiring.py — main() invokes it on the default path, the subprocess seam keeps the import ban intact), with the §4 F6 timeout semantics: launch failure ⇒ honest no-op exit 0; a configured child HUNG past the ceiling ⇒ exit 1 loud (timeout arm :993).
- Doctor-probed: --probe is pure file inspection printing one token line (NOFILE/BADLINE/STALE/NOSYNC/OK/BREACH/ERROR, :681-722), consumed by cabinet-doctor's cog2-parity row.

### Corruption/gap plane
- Byte-flip ⇒ detect ⇒ REFUSE ⇒ rebuild restores the hash: the rows-not-bytes re-derivation catches any content flip (test_byte_flip_is_detected); the serve path refuses via StoreCorruptError including the corrupt-JSONL-with-INTACT-index seed (the C-F15 window the single-read binding closes); `cog2-verifier verify` is the operator instrument (:223-235) and the corruption suite (22 tests) walks byte-flip → REFUSE → rebuild → hash-restored end-to-end on real PG17.
- Gap taxonomy (A-M9/C-F14): classify_gaps regresses the manifest's durable seen-set — an id folded before and absent now = BREACH (deleted committed history), a genesis shift = BREACH (truncation), a hole never seen in either fold = benign rollback-burned BIGSERIAL id — and NEVER fabricates a bridging belief (cog2-verifier.py:112-171); the hole-as-breach and forward-window mutants fail their respective seeds (:139-140, :154-155). The consequence stream is regressed on stable (day-file basename, physical line) coordinates — a deleted day-file or row is a detected breach, and the ids-only "seq" mutant (engine.consequence_seen :347-396) demonstrably MISSES a count-preserving day-file deletion, which is exactly why the seen-set keys on (file, line) (_classify_consequence :72-109). This is the §7.3 retention-contract tripwire in code.

## 4. Findings register (unit-5 adversarial panel; ranked; ALL resolved in this landing)

F1 (P0 — was a real shadow-boundary escape) — import-gate boundary holes: `from framework import cortex` and the relative `from ..cortex import X` passed CLEAN through the gate. Empirically confirmed against injected mutants in a scratch tree: the pre-fix AST walk resolved neither the alias-name form (it checked only node.module) nor relative imports (level>0 was skipped), so an authority-tree module could reach the projection through either spelling with the gate green — M5's "zero imports" claim was unenforced for those spellings. FIXED tests-first in this landing: `_import_from_targets` now returns the base module AND each `base.<alias>` candidate and resolves level>0 against the importing file's package path (cog2-import-gate.py:165-193); dynamic importlib/__import__ reach including the two-arg relative form is caught (:145-156); SWEEP_TREES extended to the live runtime surfaces cabinet/admin-bot + cabinet/mcp-server (:121); mutant-bite proofs recorded (test_cog2_import_gate.py:128 all import spellings, :138/:149 relative forms, :240/:332 dynamic, :228 data-plane open). The correct NON-over-fence is pinned too: `from . import cortex` inside framework/authority resolves to framework.authority.cortex — a different module — and is deliberately not flagged (:157); the dangerous double-dot forms bite.

F2 (P1) — contract-mandated phase-2 landing tooling was MISSING: no cognitive-phase2-review-scope.py, no cognitive-phase2-rollback-rehearsal.py, no rollback-manifest yml, no rollback-closure test — i.e. no §12.3 review-to-bytes binder and no §12.4 rehearsed inverse, despite both being named contract deliverables (§6). BUILT in this landing mirroring the phase-1 twins: the 41-path scope binder with drift-refusal and digest teeth, the manifest with one-command runtime inverses + code inverse + allowance removal + append-only retain, the rehearsal script, and test_cognitive_phase2_rollback.py (10 tests) including the G-F2 `must_remain_unchanged` block over the Phase-0∪1 protected-surface union (test :176-193) and the committed-footprint coverage ratchets (:211/:238).

F3 (P1) — the §7.2 `cortex_ro` read-only role (G-F6) was UNIMPLEMENTED: the contract's named "read-only-by-construction" security control did not exist — no --provision-ro mode, no CREATE ROLE SQL anywhere in the tree, no role tests; §7.2's "test_cog2_fencing.py attempts UPDATE and INSERT under the role" described tests that had never been written. Recorded candidly: this was a contract-visible gap the units 1–4 reviews missed — four review checkpoints read §7.2 and none diffed it against the bytes. BUILT in this landing: idempotent atomic provisioning (cog2-rebuild.py:61-123) + the seven-test TestCortexRoReadOnly battery on real PG17 (refusal, exact grant catalog, no-other-table, plain-login, idempotency — test_cog2_fencing.py:416-510).

F4 (P2) — per_day_floor false-BREACH on the first configured day: a stretch of honest `unconfigured` days (cycle ran, nothing to measure — the pre-soak steady state on any box without a cortex reader) counted as sampled=0 days, so the FIRST configured run would judge the prior unconfigured day a floor breach and open the soak red. FIXED: a day whose last verdict line is `unconfigured` is exempt from the floor; ABSENT days (no line — dead falsifier) and `error` days (configured reader broke) still breach (cog2-parity-falsifier.py:290-301, docstring :257-263).

F5 (P2) — verify-cognitive-phase2.sh was a stripped gate: the pre-landing draft lacked the clean-tree guard, the frozen-review presence/tracked/PASS binding, the review-scope --verify step, the rollback-closure suite, and one whole suite (test_cog2_parity_wiring.py) — a phase gate that could pass with the review unbound and dirty bytes. REWRITTEN as the full phase-1 twin: clean-tree guard (:27-30), review present+tracked+`Verdict: PASS` (:31-39), scope binding (:41), composed architecture gate (:43), rollback closure (:45), ALL 10 cog2 suites (:47-59), census --check (:61), evidence-coverage (:63), the §7.1 import gate (:65), golden evals + docs sweep + ledger parity (:66-68), the A13 heredoc byte-identical to phase-1's (verified by diff during this review), the O-B3 egg-export battery (:82), null-hatch (:83), the rollback rehearsal (:85), ending honestly at READY_FOR_CI (:86). COG2_ENFORCE_P95=1 is exported by the gate itself (:22) — the COG-1 addendum-2 precedent, enforcing ceilings on the phase gate's controlled hardware.

F6 (P3) — run_cog2_parity timeout semantics reconciled: the draft treated TimeoutExpired like a launch failure (exit 0 no-op), which would let a WEDGED configured reader mute the nightly watchdog behind a momentary green. Reconciled to LOUD: a configured child hung past the ceiling exits 1 (a stuck watchdog is not a launch failure); only a subprocess that cannot be LAUNCHED (sibling absent / interpreter missing) is the honest no-op exit 0 (task-sync-drift-falsifier.py:972, timeout arm :993; honesty-split tests test_task_sync_drift_falsifier.py:498-519, which call the REAL function).

## 5. Commands and results (the §11 exit-gate evidence)

M6 measurement gate — FULL pinned history (50,000 outbox rows + 5,000 consequence rows, ephemeral PostgreSQL 17.10, python3.12, quiet host, ceilings ENFORCED). Measured numbers quoted verbatim beside their ceilings per §8/§11:

- Full rebuild wall time: **19.81 s** (best of 3: 19.81 / 19.97 / 20.00; post-heap-shuffle rebuild 20.19 s) — ceiling ≤ 60 s → PASS (~3× headroom).
- As-of query p95: **4.20 ms** (p50 3.74 ms, max 4.33 ms, min 2.17 ms; 500 as-of queries over the 100,000-belief projection; cold verified load `load_beliefs_verified` 1,493 ms ONE-TIME — the load-once-serve-many pattern, after which every query is in-memory) — ceiling ≤ 250 ms → PASS (~60× headroom).
- Store ratio: **3.95×** source bytes (logical full source row, payload INCLUDED) / 4.63× by pg_total_relation_size (on-disk, TOAST+index-bearing) — ceiling ≤ 5× → PASS on both bases. The source-bytes definition is hereby PINNED to the production-faithful whole-row basis: a captured outbox row always carries its payload jsonb, so the payload-included logical row is what the source actually stores. Recorded honestly: on a payload-EXCLUDED basis the ratio is 5.86× (logical) / 6.06× (on-disk) — EXCEEDS the ceiling. Per the §11 gate law (fix or roll back, never lower a threshold), if a future review re-pins the lean basis that is a FIX-ITEM against the projection's row weight, not a threshold relaxation.
- Scale detail: belief count 100,000 (2 per eligible outbox row); frontier 50,000; store 107,930,284 bytes beliefs.jsonl + 735 bytes fold-manifest.json; consequence stream 5,000 rows → 5,000 beliefs, fold 0.165 s.
- Bounded CI-path measurement (5,000 outbox / 500 consequence, measurement-only per O-B4): rebuild 2.0 s, as-of p95 0.18 ms, store ratio 3.96×.

Determinism (§8 sim 1 / §11 "identical canonical hash after three rebuilds", one hash epoch):

- belief_store_hash **2dadcfb5ea4a0b8c867e6fbc039c26b79a9712c48f8a620f949beaa9ba493d5b** — IDENTICAL across 4 independent subprocess rebuilds: PYTHONHASHSEED 0 / 1 / 2 from zero, plus a rebuild after a physical heap shuffle (seed 7; scattered in-place UPDATEs + delete/re-insert prefix per C-F4).
- Epoch tuple (recorded in every manifest): engine_version "cortex-engine/1", trust_table_version 1, rank_table {officer_tasks_outbox: 0, consequence: 1, envelope_file: 2}, source_set ["officer_tasks_outbox"], frontier 50000.
- Repo instrument: `python3.12 -m pytest cabinet/scripts/tests/test_cog2_rebuild_determinism.py` → 42 passed (subprocess triple, heap shuffle, frontier pins, source-order supersession, sim-1 mutants, recorder byte-parity, byte-flip detection).

Committed battery and gates:

- Full COG-2 battery: **270 passed, 0 failed** — all 10 test_cog2_* suites (asof_fence 25, consequence_seam 18, contradiction 18, corruption 22, fencing 24 incl. the §7.2 role tests on real PG17, import_gate 27, parity 37, parity_wiring 13, provenance 11, rebuild_determinism 42 test functions; parametrization expands to the 270 run-level items) + test_cognitive_phase2_rollback.py (10) — on PostgreSQL 17.10 under python3.12.
- cabinet/scripts/cognitive-architecture-census.py --check → PASS with observed==max on the moving budgets (zero headroom — the structural-compaction mutant gate's requirement); the two COG-2 temporary_allowances rows carry the EXACT measured deltas framework_production_modules +5 and framework_production_noncomment_lines +1189 (cabinet/config/cognitive-architecture-contract.yml:82-92), both under the §10 planning ceilings (6 modules / 1,800 lines), sunset 2027-01-19, to be tightened to phase actual at the done-flip (shrink-only).
- cabinet/scripts/cog2-import-gate.py → rc=0 on the real tree ("no authority/action code imports framework.cortex — shadow boundary intact"); the gate is proven not-a-no-op (the forbidden surface exists and mutants bite — test_cog2_import_gate.py:86).
- §7.2 provisioning runbook line (the ONE production invocation, per §7.2): `python3.12 cabinet/scripts/cog2-rebuild.py --provision-ro --dsn <outbox-admin-dsn>` — idempotent CREATE ROLE cortex_ro LOGIN NOINHERIT + GRANT SELECT on exactly officer_tasks_outbox and officer_tasks, nothing else; inverse: `DROP ROLE IF EXISTS cortex_ro` (manifest :40-47).

Still owed by the landing/done-flip protocol (§12.5, §11) — not by this artifact: the landing commit itself (this review + the unit-5 files + the O-B3 egg-manifest lines land together), a green `verify-cognitive-phase2.sh` run over that committed tree (it re-runs everything above plus the rehearsal and binds this file's digest), master CI green PER-JOB after push, the ledger/plan COG-2 status flip (A13-checked, status-only, digest-untouched), and the ≥7-day authoritative-frame parity soak with sticky re-sampling before the `done` flip — judged on POSITIVE evidence (present clean verdict lines), which the F4 fix makes honestly startable and the F6 fix makes honestly loud.

## 6. Deferred / forward notes (all per contract §13 — recorded, not hidden)

1. Store-ratio margin is the TIGHTEST of the three M6 ceilings (3.95×/4.63× against 5×, and above it on the payload-excluded basis) — re-measure as live history grows, and treat any basis re-pin as a fix-item per §11. Rebuild-time headroom SHRINKS with history: the measured profile is ~2.0 s fixed + ~0.36 ms/row, so the 60 s ceiling crosses at roughly 150–160k outbox rows — later reviews must re-measure before that horizon and fix (fold cost or contract-sanctioned epoch change), never relax.
2. No production source feeds the envelope-file adapter this phase (§2 — declared): the two-axis and contradiction proofs are fixture-proofs through production fold+query code. The first contradiction-capable source pairing (e.g. org_events × tasks on a shared subject) is a NAMED next-adapter-phase obligation and must land with a production-contradiction sim (C-F10).
3. The read pointer stays `none`; no consumer reads Cortex. The later reader-wiring phase inherits the standing no-bare-scalar ratchet and the import gate, and must amend both consciously; the accepted C-F20 covert-bypass residual is re-opened at that flip.
4. Consequence purge is out of scope (the domain has no purge mechanism today); day-file deletion is classified a GAP and detected by the (file, line) seen-set regression — a future consequence retention/compaction change re-opens §7.3 via that tripwire.
5. Recorded debt unchanged: the phase-local review/verify twins (phase-0/1/2 now three instances) still await shared-routine extraction (owner cognitive-core-program); the recorder private-symbol coupling (G-F5) stands with its byte-parity tripwire; the Phase-1 schema.sql debt stays re-deferred (§9 r9).
